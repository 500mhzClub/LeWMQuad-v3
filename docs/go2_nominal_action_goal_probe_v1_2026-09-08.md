# Nominal action constraint native probe V1

Run four fresh known-layout cases with the existing final full-RGB direct/JEPA
snapshots on layouts 052/039, the complete roster shuffled with seed 2026091102.
Bind the completed continuous-connector native cohort and its readout, plus the
retained geometry diagnostic and all six original fit records. No old attempt
is retried, replaced, resumed or modified.

After the unchanged waypoint/scan utility and articulated surface checks, reject
any candidate whose first-half-second predicted base-displacement straight chord
touches or comes within 0.45 m of ANY current observed occupied 5-cm square.
Use the same continuous segment geometry and 1e-12-m numerical allowance as the
route connector. Retain the phase action set and every original surface veto.
Choose highest utility among the remaining candidates with original stable tie
order. If none remains, stop with explicit joint nominal/surface infeasibility;
do not pretend another view clears an already occupied start or waive the
radius to escape. Keep every original forecast and before/after action receipt.

This consistency constraint is nominal: no model-error bound, curved-motion
bound, gait envelope, unknown-space or safety certification is added. Previous
endpoint errors up to 62 mm make that limitation material. It is not expected
to solve inaccurate predictions by itself; prospective failure is retained.
Everything else stays fixed: mapper, observer, model weights, scoring, command
vocabulary/commitment, scenes, sensing, actuator, goal, budget, zero drain,
physical stops and native evaluator. Floor-return classifications waive no
foot/calf veto. Five new and 22 inherited focused tests must pass before launch.

Use four fresh single-thread CPU processes, as supported by the existing exact
native scaling benchmark. Each collects then independently replays the complete
sensor-to-command chain with a fresh evaluation-only snapshot. Original goal
and actuator audit functions stay identical. Inspect hardware first; require
32 GiB available RAM and 8 GiB planned output plus 40 GiB reserve. Monitor CPU,
RAM, GPU and storage about every 15 seconds. All sibling attempts finish and
retain evidence even if one fails. No automatic infrastructure replacement.

Report verified goals, contacts, terminal/minimum distances, visual failures,
constraint decisions, raw/measurement audit results and concurrent timing.
Simulation pauses during computation. Four known-layout tests cannot establish
novel-maze navigation, backtracking, JEPA advantage, real-time or hardware use.
