# Explicit 45-degree native goal readout V1

Authenticate both complete native missions and raw audits with the fixed corrected
seed-2026091001 JEPA/direct pair and the exact 30-degree bounded-reobservation
predecessor. Preserve both goal, measurement, contact, stop and timing outcomes.
Report active zero waits and actual feasible recoveries, distinguishing hold/turn
from translation. Require the original goal gate and all measurement gates for
any counted success.

Compare each method to its predecessor only through the observation before the
first different requested command or terminal state. Compare the new pair at
the same boundary including auxiliary public depth/masks. Keep actual physics,
policy, gyro, primary RGB, observer/map and forecasts without assuming equality.
The calibration intervention changes auxiliary sensing; common native motion
does not imply identical auxiliary maps or chosen actions. Never infer outcomes
of an unexecuted proposal.

For each recorded forecast, retain a compact per-candidate trace of phase,
surface, first-step nominal and eight-step nominal checks, with primary and
auxiliary intersecting shapes and foot coverage eligibility. An uncovered foot
without an intersecting measured return is not itself a recorded surface veto.
These are the controller's original checks, not posthoc reranking or proof of
unexecuted clearance.

Create exclusive go2_auxiliary_downward45_goal_readout_v1_attempt_001 after full
source and input verification. Reverify afterward. No fitting, controller change
or native execution. One reused development layout provides no independent-maze,
backtracking, matched-baseline, real-time or hardware qualification.
