# Training-only translation correction native probe V1

Run the same preselected seed-2026091001 full-JEPA and full-direct models with
their complete, frozen training-only XY intercept records. Require full
eighteen-model/thirty-head coefficient admission and completed corrected
readout. No native data, target-layout residual or new evaluation result may
change the coefficients. Original snapshots remain unchanged.

Use the preceding eight-step planner exactly: waypoint cost at 800 ms, scan
utility at 100 ms, all eight nominal path segments checked against all observed
occupied squares with radius 0.45 m, and the original first-step articulated
surface veto. Apply the float32 XY correction before any cost or geometry
check, retaining all yaw/contact values. This intervention changes prediction
translation only; add no error bound or probability-calibration claim.

Preserve route/view state, public sensing, overlap-retention observer, persistent
geometry, one 100-ms executed command per observation, 240 navigation ticks,
ten-command drain, observed 0.04-m arrival and the original native 0.06-m quiet/
contact gate. Preserve physical/sensor stops and no-feasible-candidate termination.
No mapping optimization, coefficient adjustment or fallback predictor is added.

Freeze all sources, coefficient/readout identities and predecessor outcomes
before the exclusive `go2_training_bias_goal_probe_v1_attempt_001` root. Run both
fresh CPU scenes on the reused `family_episode_039` layout, in the unchanged
seed-2026091501 order. Use two fresh processes and one numerical thread each,
within the passing four-process native benchmark and completed two-scene resource
envelope. Recheck 32 GiB available RAM and 8 GiB output allowance above the
40-GiB reserve, and monitor actual resources and complete-iteration timing.

Verify exact original warmup public/physical prefixes. Replay every decision
with a fresh assigned corrected wrapper and unchanged base snapshot. Retain the
original actuator and native-goal auditor functions. Require unchanged complete
wrapper state, including correction buffers, and reverify source/fit/correction/
readout artifacts afterward. Preserve all outcomes and failures; no retry/resume.

This is a development integration experiment, with zero independent novel mazes.
Physics remains paused during computation. Even success leaves independent
layout missions, physical backtracking, matched reactive/nonpredictive and
planning/memory baselines, realistic sensing/timing and bounded hardware evidence
outstanding.
