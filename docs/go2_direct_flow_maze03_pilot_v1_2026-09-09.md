# Fresh physical maze3 tracking fallback pilot V1

Run the same DirectFlowFloorTransportController already evaluated in the maze1
and maze3 full-controller prefixes on development maze3. Preserve the assigned
full-JEPA model, training-only correction, paired cameras, gyro, gait, dynamics,
mission, memory, original rigid and temporal checks and ten-frame bridge limit.
Only the frozen missing-pose association fallback differs from original maze3.
No additional tracking tuning, residual-feasibility change or copy optimization.
This repeats a development layout and is not a new generalization trial.

Require completed learned cohort result
a0ac72330a6c34fbae7812a360b22189086f16bd83f586d71e769539ddc2e720
and completed maze3 tracking prefix result
104edd70824e7d4e7b909be924eb68a918542efd3464ff3247f9570ddc28669e.
Authenticate all source/input/environment/model/artifact bindings and inherited
tracking-prefix evidence, including the preserved failed maze1 prefix V1.
Admit all265 saved candidate decisions,264 prior actual requests,261 raw
forecast banks, exact preceding original decisions, accepted current recovery
at264 and unchanged weights/inputs. No observation265 was used in that replay.

Collect from the original initial physical state. Use the same3000-navigation
tick budget shared outbound/return, three warmup observations, ten terminal
zero commands and paused physics during compute. One spawned CPU scene, one
OpenCV/PyTorch/BLAS thread and fresh model/controller per collection. Collection
calculations are identical to the maze1 tracking collector with only output
status labels changed. Directly reuse its layout-parameterized raw audit with
a fresh identical model; do not fork or weaken physical evaluation.

Compare13,950 physics samples before the changed command,265 paired public
observations and all265 complete candidate decisions against the prospective
prefix. Require all264 prior actual commands and261 raw forecast banks exact.
At264 the original terminal zero changes to left turn[0,0,0.45]. Do not borrow
following physical outcomes from the original run. If the new command is only
partially dispatched because of a physical stop, preserve that actual outcome.

Raw audit reconstructs all public sensors, full model/controller decisions and
actual commands, then independently evaluates settled arrivals, reverse-route
return, terminal quiet/contact, strict visibility and hard measurement failures.
Retain scientific and infrastructure failures, including raw files and completed
audits if a later prefix or identity check fails. Native truth is evaluator-only.

Exclusive output:go2_direct_flow_maze03_pilot_v1_attempt_001. Runner:
scripts/run_go2_direct_flow_maze03_pilot_v1.py --learned-cohort-result-sha256
with the cohort hash above. First use --preflight-only; it returns before any
output creation, model load or native scene. Require32GiB available RAM and
40GiB artifact reserve plus10GiB collection and1GiB persistence allowance.
Refresh CPU topology/affinity/load, RAM, GPU/VRAM, storage and competing jobs.
These admission allowances are not enforced OS quotas.

The existing native queue is current residual maze2, tracking maze1, then the
fixed supervised layouts1–3 when their frozen storage gate passes. This maze3
tracking test follows that queue; preparing/preflighting it does not launch it
or reorder cases. One native scene at a time. No restart, deletion, source export,
sealed access, training, real-robot movement or promotion. Full-goal evidence
still requires independent successes, physical return, useful prediction/JEPA/
memory comparisons, practical timing and separately justified hardware claims.
