# Six-model executed commitment error readout V1

Use the single completed overlap-retention direct-039 trajectory. Reconstruct
the exact causal four-packet RGB/body/control tensors at all 46 model selection
contexts. Freshly admit all six fixed family fits and their complete ledger/raw
score identities. Load each fixed snapshot in evaluation mode and generate all
six eight-horizon forecasts at each context, using its predeclared primary head
and full/no-RGB input treatment. Require all 276 direct-model candidate forecasts
to reproduce the original direct controller exactly. Preserve model states.

Persist all six models' predictions and input fingerprints before opening native
motion labels. Evaluate only the actually selected action's first 500 ms where
all five requested commands completed exactly. The 45 complete commitments are
the shared population; retain the interrupted tick-228 commitment as missing.
Never label unexecuted candidates or substitute an interrupted executed tape for
the requested plan. Use the same current-body XY and relative body-yaw convention
as family training labels, with exactly 250 physics intervals per commitment.

Report all per-commitment predictions/errors, uncalibrated contact scores and
six fixed descriptive scopes: all, waypoint, near-goal (the existing 0.35-m
alignment-fade distance), action switch, repeat, and initial selection. Switch
means the selected action differs from the preceding selected commitment; it is
not a causal attribution of error. Report means and maxima without statistical
independence, model selection or contact-calibration claims.

This is common-context prediction evaluation on one direct-policy-owned,
reused development trajectory and one optimization seed. It is not a matched
control experiment, a new optimization run, independent-maze evidence or proof
that any unexecuted model policy reaches the goal. The full/no-RGB treatment
ablates predictor RGB; shared visual localization remains in the recorded policy.

Bind sources, focused coordinate tests, prior negative correspondence report,
fit identities and native/readout inputs before and after. Use an exclusive root
with terminal failure on error. Assess hardware before admission and record it
after completion; require 8 GiB RAM, 40 GiB storage reserve and 256 MiB allowance.
Run the 276 small model/context calls sequentially with one Torch/OpenCV/BLAS
thread, loading one model at a time. There is no native or training workload to
parallelize. No old-result changes, retry, training, model promotion, new physics,
navigation, calibrated uncertainty, real-time or hardware qualification.
