# Prospective later-floor-evidence contact resolution prefix V1

The completed eighth native attempt failed navigation despite an admitted
current plane. Its original unknown-return partitions retained nominal-foot
conflicts after later actual views covered four terminal enclosures. The
diagnosis result3d9a5d3109eff7fb9f8e38acf347ac13a631ef0e643679883eeed3db468b9912
is evidence for this general memory revision, not a whitelist of cells.

Candidate LaterFloorResolutionRoundTripController uses the independently
verified frame-cache implementation and records the actual complete floor-cell
sets from each primary and auxiliary observation. Camera arrays, calibration,
RGB/depth hashes, timestamp and reference pose remain explicit. The same-image
auxiliary reference transform is checked. Publish a pair only after both
original map observations succeed; no partial current pair can support a query.
Bound the ledger to4096 frames,40000 cells and two records per frame.

For every nominal22mm foot sphere, reconstruct the original primary and
confirmed-auxiliary unknown queries and enumerate every intersecting enclosure.
Use outward-rounded arithmetic to transform its full measured bounds into the
fixed map. Require the whole height interval to lie inside the original10mm
floor band and one strictly later single-camera observation to cover every
5cm floor square touched by the enclosure. Do not combine different views or
frames into one witness. Latest-sample time invalidates earlier resolution
evidence when new ambiguous returns enter the cell. Out-of-domain, larger than
16-square, off-plane and uncovered enclosures remain unresolved. Keep every
original index and classification and the complete original contact check;
report resolved and remaining hits separately. Non-foot shapes are unchanged.
This is conditional contact classification under existing static-floor and pose
hypotheses, not calibrated uncertainty, support or free-space certification.

The learned checkpoint, raw forecasts, pose estimator, map, mission, original
action bank, nominal radius and full800ms horizon, physical/acquisition stops
remain unchanged. Only the contact interpretation uses the new recorded later
evidence. No receipt-copy optimization is installed.17 focused synthetic tests
passed, including exact mapping, conservative arithmetic, single-view scope,
new-sample invalidation, every-hit accounting and unchanged non-foot conflicts.

Exclusive output go2_later_floor_resolution_maze_prefix_v1_attempt_001. Reuse
the completed eighth raw audit and reconstruct a fresh candidate from its public
packets. Before intervention require exact original pose/visual evidence, map
and mission receipts; require exact raw predictions and original contact checks
where both policies plan, plus unchanged full nominal path checks when present.
Stop at the first requested-command or terminal difference, before consuming
an outcome of a command the candidate did not issue. Preserve every failure.
This is a development intervention on reused maze0, not independent navigation.

Bind completed native/readout, cache-equivalence and diagnosis results and
artifacts, model admission and complete source closure before/after. No source
edits after launch. Run --preflight-only and inspect resources before execution.
One CPU candidate process with one numerical thread, minimum8GiB available RAM,
1GiB output allowance above the40GiB reserve. No competing scene is needed;
record topology, affinity, utilization, RAM/GPU, competition and storage. No
native execution, training, checkpoint selection or qualification by this replay.
