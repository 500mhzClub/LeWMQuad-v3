# Complete measured-return floor partition diagnostic V1

Authenticate the completed sample-bound native cohort and readout, all source
and input identities, and reconstruct the direct-039 recording. Reproduce every
available full floor-map receipt and every original six-candidate surface check.
Keep unavailable updates unavailable; no command or native result changes.

Classify EVERY stride-four valid depth return at every available observation.
Measured floor requires the existing four adjacent ground-normal/planarity
quads and nine valid pixel heights within 10 mm of the fixed measured plane.
Retain separate monotone floor and other/unknown within-voxel bounds with counts
and first witnesses. A later unclassified return in a formerly floor-only key
remains in the other index; never classify a whole key from its first witness.

For the four exact URDF foot spheres only, compare original all-return queries
with floor and other/unknown queries. For each predicted foot also require every
5-cm floor-grid square touched by its closed nominal projected disk to belong
to the existing measured-floor set. Record missing cells. Other body parts keep
all-return collision checks. Describe the candidate result IF observed floor
contact were permitted only for those fully covered feet, retaining all unknown,
non-floor and other-body conflicts. This is a diagnostic, not a contact waiver,
support certificate, scene-label lookup, or prospective control change.

Four partition/projection tests plus the five bound tests and nine original
geometry/floor-patch tests must pass. Inspect hardware and require 8 GiB RAM
available, 41 GiB artifact free space and 1 GiB output capacity. This single
chronological memory has no independent frame units: use one CPU thread and
record workload time rather than parallelizing dependent updates. Failures are
terminal in this exclusive attempt. No simulator, fitting, retry or promotion.
