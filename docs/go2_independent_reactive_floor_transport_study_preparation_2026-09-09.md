# Independent reactive cohort preparation

Prepared, not executed. The fixed learned layouts1–3 and current reactive
maze0 outputs are still absent. Their completed raw-audited results are required
before this new reactive layouts1–3 cohort can be admitted.

The new runner fixes layouts1,2,3, their order and the current reactive policy
before any of these independent executions. It uses the existing reactive
collector and full raw audit unchanged, with a fresh process/controller/memory
per case. No high-level world model, learned residual or predicted candidate
outcomes are loaded or used by reactive execution. The low-level gait remains.

Admission binds both completed predecessor result/artifact/source sets, checks
their shared learned maze0 predecessor and the reactive maze0 intervention
prefix, and compares exact scene/mission/runtime/URDF/renderer/thread/budget
settings. Scientific negatives are admitted unchanged. All three learned
records and their saved raw audits must agree. Conflicting frozen source
bindings fail before execution. No independent-layout trajectory equality or
isolated ranking/training/memory effect is claimed.

The new cohort retains all three negative results, pairs their native outcomes
with the learned cohort without replacing absent return evidence, and writes
exclusive progress after every case. An infrastructure or resource failure
stops and retains the partial cohort without retry, skip or deletion. Resource
admission covers32GiBavailableRAM and40GiBreserve plus11GiBper remaining case;
refresh assessment before any real launch. One native scene/process at a time.

Validation:

- Session27103 closed:27focused tests passed in2.69seconds. Coverage includes
  incomplete/reordered cohorts, changed model/controller/runtime, invalid raw
  replay or prefix, predecessor disagreement, source conflicts, preserved null
  return/failures, all-negative execution, exclusive progress, process settings,
  no retry after infrastructure/resource failures, nonexecuting preflight,
  retained raw/audit files after later failures, and scene/mission matching.
- CLI59064 closed successfully.
- Source preparation23527 closed successfully:1658prepared learned cohort
  sources,1675prepared reactive pilot sources,1682prepared independent reactive
  sources. All1654live native and1666completed reactive-prefix frozen sources
  were checked and unchanged. All three future native output roots were absent.

New source SHA-256 identities:

- lewm/independent_reactive_floor_transport_study_development.py:
  816d06adaa94ad1311aaf42102f8c00fff0fe35398f6ba16910271c84dd0c7e4
- scripts/run_go2_independent_reactive_floor_transport_mazes_v1.py:
  2cf04527ed64af42141fff0af9c2b6921e3097f8bc85daf6e1a0c85b26edf7b0
- lewm/tests/test_independent_reactive_floor_transport_study_development.py:
  ad051dfaeabcb7a65d06aa09f526ca9610ee1f54a0f4dd606d147a77572fd4e3
- docs/go2_independent_reactive_floor_transport_mazes_v1_2026-09-09.md:
  c83338fa9939fc358370d73e02fa38f5f5f83bd0a545c3a69d2959190b0f4587

Order remains: finish current learned maze0 audit/readout, execute the fixed
learned independent cohort, execute the reactive maze0 pilot and paired readout,
then preflight and execute this fixed independent reactive cohort with actual
--learned-cohort-result-sha256 and --reactive-pilot-result-sha256. Each launch
requires current hardware assessment and no competing native scene. No source,
model, controller or outcome from the running/frozen attempts was altered.

At the latest process check, native19976worker2447506was still active98.7%CPU
after72m52s,CPU71m57s,RSS10,646,680KiB. Collection is terminal at the mission
budget; persistence/fullrawaudit remain pending. No root result/failure, saved
case audit or worker terminal existed. Goal active,zero verified round trips.
