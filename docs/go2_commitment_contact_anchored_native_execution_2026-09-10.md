# Native ordinary commitment-contact trial queued

The native waiter is running as PID 2703476, creation epoch
1789048977.88, tool session 66117. Its launch SHA-256 is
`0c57eabf201e1eaba5f19e4f4fba55ed5948d490c8e5dc20d3bf4098f92e4cb2`. It binds 1,988 source paths,
including every one of the child launcher's 1,985 source bindings. The combined
source-only preflight passed before this exclusive launch; 54 source, prefix,
launcher, input-chain and waiter tests passed during preparation.

The fresh-model contact-score replay must first complete after the original
supervised worker audit. Native execution additionally waits for the whole
six-case adapter batch, reached-frontier pilot and hold-reorientation pilot,
through the exact already-running prior waiter. Both immediate predecessors
were observed live after launch. The new waiter creates no native scene while
waiting, polls their exact owners every 30 seconds and retains failure without
retry if an owner ends without authenticated completion. It permits one child
within the frozen 48-hour wait, preserving the existing queue order.

The paired prospective boundary remains four original public observations and
900 initial physics samples. The first changed command at observation 3 is
left-turn to forward, and its first physical command interval must complete
through sample 949. This candidate retains the original ordinary action gates
and raw 800-ms forecasts while matching the contact-score horizon to the
executed 100-ms command. Contact scores remain uncalibrated. The assigned
expanded supervised model is unchanged. Any later behavior requires the new
native collection and fresh-model full raw audit; saved rescoring does not
establish that behavior.

At admission the machine had 16 physical CPUs, 74,714,157,056 available RAM
bytes and 644,731,236,352 free artifact bytes. The child requires 32 GiB RAM
and 51 GiB free storage and rechecks resources before native execution.
Storage is not the current blocker. The original supervised audit owns the
current native batch stage; queued trials have not produced new outcomes.

This record establishes scheduling and preparation only. Navigation success,
independent-layout reliability, real-time execution and hardware qualification
remain unproven. Machine-readable owner, resource and event evidence is in
`go2_commitment_contact_anchored_native_scheduling_2026-09-10.json` with SHA-256
`5d5361726d64642272b11083bd78f95b9bd80694a78cfc52d62654f5c0b929ba`.
