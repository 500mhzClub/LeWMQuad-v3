# Identical partial-height native pilot with scoped input verification

This separately named fresh launcher implements the same scientific experiment
as go2_partial_floor_height_maze01_pilot_v1_2026-09-09.md. That prepared original
launcher has not executed. Its source remains frozen and no attempt is retried.
Keep exactly its controller, model/training bias, scene, sensor acquisition,
physics, mission budget, actual command selection, raw audit and physical-prefix
comparison. Reuse its existing collector, audit and comparator without edits.
The root/worker status labels identify this distinct verification launcher.

Only input-verification execution differs. Require completed benchmark
137867773bfe6c6eb05a125a288012ff6017aa3134f4687d7bccdc7f99c02071,
authenticate its source/artifact bindings, and admit both completed original and
scoped stages on the exact original tracking native input. Invoke the prepared
pilot's original verify_inputs in a fresh isolated digest scope. All its original
conditions execute. Each unique file first uses the original digest; guarded
repeats may reuse it only within that call. Every cached file receives a fresh
SHA-256 and metadata check before success, followed by a whole-population
metadata recheck. No cache survives or modifies imported globals. Preserve the
200,000-file limit, fail-closed behavior and unchanged launch-input check.
Every parent/preflight/worker/completion verification uses a new scope.

Require the same completed positive height-prefix replay and its explicit result
SHA-256. The full admission still re-compares all 505 saved decisions with the
tracking predecessor, demands exact preintervention requests/model banks and a
current recovered controller with a changed command at frame 504. Do not launch
from an interim stream or bypass a negative/failed replay. The resulting native
episode must still reproduce 25,950 physics samples and 505 paired observations
before its changed command, and pass the independent full raw command audit.
Physical outcomes after that command must be newly executed and evaluated.

Output is exclusive
go2_partial_floor_height_maze01_scoped_verification_pilot_v1_attempt_001.
Runner: scripts/run_go2_partial_floor_height_maze01_scoped_verification_pilot_v1.py
with --prefix-result-sha256 set to the completed positive result. First run
--preflight-only before output/model/scene creation; then refresh hardware and
run without that option. This is the selected first native height-pilot launcher
once its checks pass; do not also run the original prepared launcher.

Retain the original 32 GiB RAM admission, 40 GiB artifact reserve, 10 GiB
collection allowance and 1 GiB persistence headroom, one CPU scene/task,
one numerical/OpenCV thread, and paused physics during computation. Refresh
CPU topology/affinity/load, RAM, GPU/VRAM, storage and competing jobs before
substantial execution. Bounded CPU verification may overlap active CPU analyses
within measured headroom. Never start a second native scene.

Keep the original pilot's scheduling rule: an idle native slot may be used after
positive replay and resource admission. Do not alter the fixed supervised cohort,
its storage gate, or the prepared tracking maze 3 dependency. No cleanup,
deletion, sealed access, source export, model refit, real-robot action, automatic
retry/resume, qualification or deployment claim. Preserve every failure. A
repeated-layout success does not establish independent-maze reliability,
JEPA/memory advantage, 100 ms operation or hardware readiness.
