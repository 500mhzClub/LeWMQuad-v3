# Observed floor-contact native readout V1

Authenticate both completed native contact-policy missions and raw audits,
alongside their 45-degree predecessor. Preserve both goal, measurement, contact,
stop, timing and incomplete outcomes. Report active waits, actual recovered
actions, selected commands and compact original candidate checks. Distinguish
the new observed-floor collision policy from the predecessor complete-projection
veto in each check. Incomplete support stays unknown even if floor-only contacts
are permitted. Never count a goal without the original native quiet/contact
and all primary/auxiliary sensor gates.

Compare each method to its predecessor only through the observation before its
first different command or terminal state. Compare the new pair similarly,
including raw physics, policy/gyro histories, RGB, auxiliary depth/masks,
observer/map and model forecasts. Do not infer unexecuted outcomes or ascribe
any improvement from changed contact semantics to JEPA training.

Exclusive go2_observed_floor_contact_goal_readout_v1_attempt_001 root after
complete source and artifact checks, with reverification afterward. No fitting,
controller change or native execution. This reused flat-floor development
layout gives no independent-maze, backtracking, matched-baseline, real-time,
general terrain/support or hardware qualification.
