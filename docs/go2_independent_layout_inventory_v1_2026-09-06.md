# Prospective connected-layout/context development inventory V1

Freeze construction metadata before any new collection or fitting. This stage
creates no RGB, physics trajectory, model, navigation result or final benchmark.
The completed one-layout pulse pilot remains training-only and is not relabelled.

## Independent development units and prospective roles

Construct twelve connected4x4/16-cell layouts on a1.2m grid. Randomized Kruskal
spanning trees plus1,2or3extra passages give four layouts per cycle-rank stratum.
This is a declared procedural distribution, not uniform sampling of all mazes.
Require a straight corridor, corner, branch and dead end in every layout. Source
construction seed2026090803 and separate role-shuffle seed2026090804 are fixed.
Structural rejection uses only missing context types or exact topology duplicates,
never physical outcomes, model scores or held-out information. Cap generation at
10,000candidates and exact graph searches at100,000states; unresolved identity is
a failure, not permission to claim independence.

Canonicalize abstract adjacency by exact bounded individualization/refinement,
including the complete leaf adjacency code. A degree sequence or WL/refinement
hash alone is insufficient. Group graph-isomorphic embeddings conservatively:
the same abstract path with different bends is still one topology group. Independently
cross-check all66layout pairs by degree-constrained adjacency-bijection backtracking,
without using canonical codes. Also canonicalize metric grid geometry under
translation, reflection and right-angle rotation. Preserve one role for every
layout's appearance, context, support, history and action variant.

Within each cycle-rank stratum, prospectively assign two layouts to train, one to
selection and one to development_eval:6/3/3layouts total. Development evaluation
has only three topology units and is not final scientific qualification; report
layout-level results and their limited precision. Final evaluation requires
separate custodian-owned material and future independent evidence. No sealed
material is read, created or exported here.

## Five contexts, factorial histories/support/actions

For each layout, declare an open straight passage, corner turn, junction, dead-end
approach and near-wall context. Select eligible cells/directions from fixed
layout-index seeds. The two dead-end contexts face the blocked direction opposite
the sole passage. The near-wall spawn is0.16m forward of the cell centre; the
other contexts use the centre. Spawn height0.375m; yaw faces the declared cardinal
direction. Walls are0.08m thick and1.4m high. Materialized wall boxes must correspond
exactly to closed cell boundaries; later native audits must check actual physics.

The completed pilot's nominal long-forward endpoint had approximately6mm of
front-envelope clearance, versus100mm at departure. Its spawn was0.08m forward
of centre. The distinct0.16m near-wall offset is therefore a prospective hazard
hypothesis, not a collision label or a claim that warm-up will pass. Keep the full
articulated4cm-padded setup check. Any setup/common-warm-up failure remains a
failed context, never a fabricated candidate target. A shifted historical trace
cannot establish a new physical outcome. No positive-contact quota is presumed.

Cross each context with quiet and recent-forward histories, nominal1.0 and lower
0.15friction, and all six existing pulse cells: forward0.2m/s or yaw±0.45rad/s,
each for2or5command ticks. After750settling physics samples, execute eight common
zero or0.12m/s forward ticks according to history. Candidate departure is2.3s,
tick8; every pulse is followed by20zero ticks. This yields240six-action groups and
1,440planned episodes (720train,360selection,360development_eval).

Physics/appearance seeds and spawn are identical across action siblings and also
held fixed across support/history factors within a declared context. Friction is
construction/evaluator metadata only, never an inference input. Recent excitation
must be present in actual causal histories; at rest, support may be unidentifiable.
Only a future raw audit can verify physical/sensor prefix equivalence. The current
inventory check establishes planned equivalence, not actual counterfactual data.

## Scope, checks and next execution work

The writer exclusively creates
`.generated/go2_independent_layout_inventory_v1_attempt_001` with launch, inventory,
independent structural audit and result bindings. Preserve10GiB workspace space
plus100MiB metadata allowance. Bind the completed pilot/corrected-audit/failure
identity witnesses and all new source/tests before and after construction. No
whole-tree materialization, predecessor modification, GPU, physics or training.

Tests cover all720label permutations of each of two six-node regular graphs,
non-isomorphic regular-graph separation, an independent small-graph permutation
oracle, rigid metric copies, differently bent isomorphic paths, all action/context
cells, every wall boundary, strict scalar types and role/schema corruption.
Synthetic nominal-joint envelopes must fit all60planned context spawns; this is
source geometry feasibility, not settled native support or future safety.

Next implement the distinct scene-pack/session/collector adapter and raw auditor
for this immutable inventory. Keep tracker-independent bounded simulation
excitation and native stops. Plan collection/storage in explicit bounded batches;
the previous pilot averaged about40MB per episode, suggesting roughly58GB before
headroom for the1,440episodes, not a guaranteed bound. Preserve40GiB external free
space, measure actual consumption, and do not delete older evidence. Freeze any
batch membership and execution/resource limits before launch. Audit all attempted
contexts and hazard coverage before fitting; never hide failures or select only
successful prefixes. Then run matched sensor/history/action utility and
JEPA/direct/supervised-rollout comparisons on independent development layouts.
Reliable execution, online rollout/memory/backtracking, complete novel-maze tasks,
realistic sensors/deadlines and bounded hardware evidence remain required.
