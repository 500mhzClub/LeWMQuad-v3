# Prospective expanded-training matched maze2 experiment

Freeze this experiment before reading expanded fitted losses, transfer scores,
correction coefficients or any expanded-model native outcome. The primary and
controls are those already fixed before fitting in
docs/go2_all_phase_matched_fits_v1_2026-09-10.md. Run the complete fixed order:

1. seed_2026091001_full_jepa
2. seed_2026091001_full_supervised_rollout
3. seed_2026091001_full_direct
4. seed_2026091001_no_rgb_jepa
5. seed_2026091001_no_rgb_supervised_rollout
6. seed_2026091001_no_rgb_direct

Every case uses development layout2, a fresh corrected evaluation-only model,
fresh controller/memory/residual state and a fresh native scene. No alternative
seed, checkpoint, condition, layout or shortened case substitutes for a failure.
Scientific failure, including failed visibility, remains in the cohort and does
not cancel later cases. An incomplete collection/raw replay or changed binding
stops execution and preserves all evidence. Execute at most one native scene at
a time. Do not overlap the current original recent-qualified direct-flow maze3
pilot. Authenticate that pilot's completion and original owner before admission.

Use the unchanged ResidualAnchoredContinuationController and its existing raw
collector/auditor. Their current exact source identities are:

- lewm/residual_anchored_continuation_controller_development.py:
  f35ff18c78b4db81c0c3c766eed1015823bf972d484600907954941ef7fb946a
- scripts/residual_anchored_continuation_maze_episode_development.py:
  4e98a79ea5ad16c1d2021deae52abd2aec0a38d4ace1df80d2ca3994d990e949
- scripts/residual_anchored_continuation_maze_audit_development.py:
  6ae658b91a0859b8d4b7ae99ba9da22dc2541183c4a52409301bd857a24675f3

Bind their full recursive source closure and the existing physical environment,
URDF, scene specification, public mission and sensor acquisition at launch.
Keep the same eight candidate forecasts,100ms command interval,800ms forecast,
3000-tick shared outbound/return budget, three warmup and ten drain commands,
geometry/clearance checks, visual pose gates and arrival/return criteria. Do not
add recent-reference retention, partial-height registration, contact-horizon
changes, new actions or controller tuning to this experiment.

The intervention against the completed old maze2 residual-anchored pilot is the
expanded training recipe and its corresponding freshly fitted correction. The
old result SHA is818a598ca6336866cf5f4768c11edaf67c8c1ca60896c305f93f69fd0ed5230c.
It produced zero round trips and stopped after measured drift during zero
commands; it is a development diagnostic, not a new independent layout. More
training contexts also change sample draws and fitted coefficients, so any
difference cannot be attributed solely to zero-command examples. The recording
collection still has the original fixed command-switch phases.

Require complete admission of all18 expanded fits and all30 training-only
corrected heads before loading any native model. Bind the exact full-fit and
correction results. Use the new all-phase correction admission/load functions;
never reuse the old408-context correction. All model selection remains fixed.
Direct conditions use direct_outcomes; supervised/JEPA use rollout_outcomes.
All retain the same candidate planner and map. The no_rgb treatment removes RGB
from the learned model input only: the controller's RGBD mapping/localization
remains active and unchanged. Thus the direct comparison is not a reactive or
non-predictive control, and this cohort does not isolate online planning or
persistent memory. Those controls and independent-layout execution remain open
requirements of the full goal.

Before any learned command, verify a common physical/public startup against the
completed old maze2 episode:900 physics samples, observations0 through3, and the
three completed zero warmup commands. Do not require old/new model predictions
or subsequent controller states to agree. Each complete new physical trajectory
must be raw replayed using a fresh exact assigned model and the unchanged
auditor. Require unchanged model state and all original raw sensor, command,
renderer, physical-stop and visibility accounting. Report failed visibility
without converting it to a verified navigation success.

Report all six case outcomes, observed and native arrivals/return, terminal
reason, distinct traversed edges separately from repeated crossings, contact,
strict visibility, full timing and resource evidence. Count a verified round
trip only under the existing joint observed/native/visibility criteria. Compare
the primary with the old development pilot and each matched treatment with the
new primary; do not select a winner and silently promote another model. A
positive development result motivates a separately frozen independent-layout
study, with no intervening outcome-driven controller changes in that study.

Launch only after a current hardware assessment. Reserve32GiB available RAM and
40GiB artifact storage plus the existing10GiB collection and1GiB persistence
allowance for each remaining case (106GiB free for all six). These are admission
allowances, not OS reservations. Monitor actual resources and use one fresh
worker process per case. CPU-only fitting/admission can coexist only within
current available capacity. Physics remains paused during controller compute;
no real-time, hardware, deployment or full-goal qualification follows from this
experiment. Exclusive root:
go2_all_phase_residual_maze02_matched_native_v1_attempt_001.

This protocol fixes the prospective science. A separately checked launcher,
startup comparison and exact completed input/queue admissions remain required
before native execution. No new native scene has been launched by this document.
