# Continuous connector native readout V1

Authenticate the exact completed four-case continuous-connector native result,
all artifacts and source closure before and after. Produce per-case and per-
model verified goals, stops, terminal/minimum distances, visual failures, pose
errors, phase/action traces, floor proposal statuses, raw audit outcomes and all
recorded timing summaries. Bind the unchanged commitment-pose predecessor and
compare requested command prefixes. Before the first changed command executes,
compare exact RGB, observer/map evidence, model forecasts, native physics and
public body/gyro array identities. Report disagreement if present. Do not infer
unexecuted outcomes, compare timings as uncontended, or modify old results.

For each selected forecast also check its first-half-second displacement chord
against the already observed nearest occupied square recorded in the original
proposal. A conflict with this one square proves a nominal disk conflict; a
miss does not establish clearance from other squares. Where five unchanged
requested commands completed and the subsequent visual pose is available,
describe its measured endpoint chord and prediction error against the same
past square. This is retrospective executed-outcome analysis, not a new planner
input, intermediate-path certificate or proof of articulated collision.

This is an exclusive read-only diagnostic attempt; failure is terminal. Four
known-layout native outcomes do not establish novel-maze navigation or the full
goal. No new model training, native execution, hardware motion or promotion.
