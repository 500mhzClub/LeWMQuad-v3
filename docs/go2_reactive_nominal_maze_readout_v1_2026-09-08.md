# Completed reactive nominal-route native readout V1

After full native collection, independent raw audit and both physical/public
prefix comparisons complete, run
`scripts/read_go2_reactive_nominal_maze_pilot_v1.py --native-result-sha256 HASH`
once at exclusive `go2_reactive_nominal_maze_readout_v1_attempt_001`. Authenticate
the completed result, all bound native artifacts and frozen source/native
dependencies before/after readout. Preserve every supplied native outcome.

Report actual native path length, minimum/terminal goal distance, open-edge
traversal, arrival/return evidence and all measurement/visibility gates. Preserve
first and last valid reactive infeasibility separately, describing current
nominal clearance, current footprint conflict, measured connector and unknown
cells, and heading/view status. No learned prediction is invented for this
baseline. Retain the actual decision and native observation-clock traces and all
acquisition/control/command/receipt timing.

Bind completed waypoint readout
`1bb9ae412452fd68a99fa827ad304d9201f5dfdc2b7bb24f57e003390ba7799c` for a compact
same-scene comparison of those actual outcomes. The two native prefix checks
establish the expected common physical and public starting trajectory. Scene,
sensors, locomotion policy, mission and shared budget match; reactive current-
geometry gates and learned predicted future gates differ explicitly. Persistent
observed memory is present in both. A single reused-layout comparison establishes
neither independent-layout reliability nor causal ranking-only, JEPA, RGB or
memory advantage. A descriptive native near-goal endpoint never substitutes for
the observed mission and native arrival/backtracking criteria.

One ordered CPU readout; require 8 GiB RAM and 128 MiB above the unchanged 40 GiB
reserve. No native scene, model training, hardware execution or source selection
occurs. Preserve paused-physics/ideal-sensor assumptions and any readout failure.
Keep the full navigation/comparison goal active regardless of component tests.
