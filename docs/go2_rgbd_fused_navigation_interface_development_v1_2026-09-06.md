# Fused full-task consumer interface diagnostic V1

Purpose: test sensor-to-navigation integration on the already acquired fresh
shadow-motion tape, not a new navigation outcome. No physics, rendering,
hardware, model training, calibration or old-result rescore. No proposed
command will be executed. Recorded applied commands remain unmodified.

Input is exactly the launch/result/audit and 582 artifact-bound files of
`.generated/go2_rgbd_shadow_motion_development_v1_attempt_001`; verify its entire
source/input/native/OpenCV closure before and after. New source seeds are this
protocol, the new diagnostic script and the new fused-navigation tests, with
their recursively discovered source dependencies. No sealed access or export.
Output is exclusively a new directory:
`.generated/go2_rgbd_fused_navigation_interface_development_v1_attempt_001`.

For neutral, repeated and distinctive arms, instantiate one RGBDFusedNavigation
with local-only route hypotheses, the original 1.5 s supplied velocity prior
and unchanged explicit 2 mm/10 mm point hypotheses. Process frames in causal
order, beginning at the first recorded anchor, until controller termination,
sensor refusal or end of tape. Never reinvoke a terminal/faulted controller.
Compare admitted fusion and raw depth motion with the frozen shadow output.
Log every proposed command, frame index/time, mission stage, raw depth/point
provenance, uncertainty and failure cause. Also log the unchanged RGB floor
detector's support counts to distinguish segmentation from fusion failures.

The controller uses one fused position/ray memory and a read-only shared global
attitude. A scan uses a local rotation view of that same history without global
reset. Braking reads labelled fused endpoint velocity in current body axes,
not a fabricated full-depth translation. Existing local speed/timeout/turn
volume/arrival and global discovery/return rules remain development hypotheses.
Current floor-plane projection is observed depth; exit proposals still require
the predecessor's palette-specific RGB floor mask. Retain any failure of that
appearance assumption; do not recolor data or substitute a new detector here.

The tape was not generated by these proposals. Even if it reaches a home or
arrival candidate, it is not successful closed-loop exploration, return,
physical safety, real-time performance, calibrated uncertainty or JEPA benefit.
Save all new source/input identities before the diagnostic and preserve them
thereafter. A separate fresh full-mission protocol is still required.
