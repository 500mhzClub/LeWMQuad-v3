# Automatic fixed six-case native handoff

Register one waiter while the original recent-qualified direct-flow maze3 parent
2636286 is still alive. Bind its exact creation time, command, boot and launch,
and the exact correction waiter2639661 and its launch. Freeze the complete source
union, all six assignments and the already fixed experiment protocol. Never edit
the running native, fitting, correction or waiter closure.

Poll the original process identities every30 seconds. A still-live process is
not terminal on observation timeout. A missing/dead owner requires its exact
completed result and all bound artifacts, with no failure file. Preserve and
stop on missing terminal evidence, failed identity, source change or48-hour wait
expiry. No retry/resume, new model selection or substitute predecessor. Waiting
launches no scene and does not interrupt either original owner.

After both original owners have finished, record their observed completed result
SHA-256 identities. Recheck current hardware and the full six-case106GiB storage
and32GiB available-RAM admission; require native idle. Start the frozen six-case
launcher once, passing both exact identities. Its complete correction, original
native and old development-reference admissions precede any scene. Each worker
rechecks bound inputs and loads a fresh exact assigned model. Complete input
admission is executed again after the cohort, before its final result. Do not
modify any assigned model or the controller while executing the fixed cases.

The parent preserves the existing three-warmup/3000-navigation/ten-drain budget,
raw collector/auditor, complete physical/public startup check, all negative
outcomes, renderer/visibility evidence, distinct-edge accounting and timing.
It uses one fresh worker process per case and monitors hardware. Direct remains
a predictive model control; no_rgb retains controller RGBD. This remains a
development-layout experiment with physics paused during computation.

After the child exits successfully, verify all six terminal/audit/startup/readout
receipts and bound artifacts, reconstruct the complete fixed cohort accounting,
and save a waiter completion receipt. This final receipt check does not repeat
the expensive raw controller replay; the immutable original audits remain bound.
Retain every failure/partial output and never relaunch the child. Exclusive waiter
root go2_all_phase_residual_maze02_native_wait_v1_attempt_001. This handoff grants
no real-time, hardware, deployment, independent-maze or full-goal qualification.
