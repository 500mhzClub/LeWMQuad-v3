# Prospective geometry-progress ordered-near-field pilot V1

One fresh24episode CPU development collection and one raw audit in
`go2_geometry_progress_near_field_v1_attempt_001`, under the existing external
navigation-development artifact base. Freeze this document, source and focused
tests in the launch. No retry/resume, predecessor edits, fitting, evaluation-set
access, deployment or hardware execution. Retain all physical/sensor failures.

Preserve the prior pilot's two mirrored panels, two appearance seeds, six actions,
seeded opaque episode assignments, nominal friction, initial pose, frozen gait,
3 quiet history ticks and40candidate ticks including10terminal zeros. Preserve
the0.15m native goal-distance-reduction requirement toward departure-body(1.2,0)m,
complete-horizon/no-contact/no-other-stop rule, and all24denominators. Require
the same two mirrored arc-success reversals, failed nonprogress controls, and
absence of a constant successful action. This remains local progress, not passage
traversal, maze arrival, an evaluated learned policy or independent-maze evidence.

The declared intervention is the existing acquisition stack used by the later
independent RGB/body collection:5mm camera near plane, union wall visuals,
floor-first deterministic raster ordering, native raster-precision witnesses,
and `NearFieldCapture`'s separate same-epoch RGB and single-sample depth calls.
Actual physical collision boxes and gait commands remain the same. This is an
acquisition-stack comparison, not an isolated near-plane-only intervention.
The old5cm collection and all failed/audited readouts remain unchanged.

Record all existing raw physics/contact/body/control/gyro/camera/actuator/setup
artifacts plus every raster-order/precision witness and the two visual mesh files.
The audit uses the existing5mm physical-first-surface sensor reconstruction,
exact float64 request identity and independently reconstructed float32 actuator
clipping, native stops and the unchanged native progress evaluator. Reconstruct
all packets, including failed candidates; never infer motion from commanded time.

Predeclared measurement roles:

- Physical first opaque surfaces are compared even below public depth range.
  Public depth stays invalid outside0.2--5m. An unavailable public depth is not
  interpreted as free space, and a clipped foreground must not expose background.
- Preserve every original strict visibility/depth score. Separately apply the
  existing raster-footprint evaluator with its fixed half-pixel-diagonal boundary
  band. No band or numerical threshold is chosen from this pilot's errors.
- A hard measurement failure is any frame with fewer than1,000 valid stable
  interior rays, stable-interior error above1mm/nonfinite error, a clipped opaque
  ray, or falsely valid near-surface depth. Use the existing evaluator fields
  `stable_interior_metric_pass` and `near_occlusion_failure` unchanged.
- The prospective RGB/body prediction gate requires no hard measurement failure
  in any recorded frame, all24admitted departures and complete candidate
  acquisition (full schedule or recorded contact terminal), and the native action
  design gate. No outcome-dependent episode exclusion. Strict boundary failures
  remain reported and grant no depth-navigation qualification.
- Keep native motion, contact and future-image masks independent. A contact
  event remains a valid label after its endpoint image becomes unavailable.
  No post-stop image/motion target is fabricated. No training is part of this run.

Use serial CPU scenes, one OpenCV/BLAS thread, at least24GiB available RAM,
3GiB planned storage and40GiB reserve. Record actual machine inventory before
launch and stop on reserve breach. Physics pauses during processing; wall times
do not establish real-time control. No competing heavy job during collection.
Verify complete source/native/input bindings before and after execution.

Only if the complete prediction gate passes should a separately frozen larger
independent geometry/layout collection be prepared for matched RGB/no-RGB,
action/time, direct/rollout/JEPA studies and subsequent online planning. This
pilot grants no automatic training or navigation qualification.
