# Bounded reobservation native readout V1

Authenticate both completed native reobservation missions and their raw audits,
along with the original auxiliary-depth predecessor. Preserve both goal, contact,
sensor, timing and terminal outcomes. Report active zero-wait ticks, changes in
the feasible-action recovery counter, their selected actions and actual requested
commands. A recovered feasible hold is not resumed translation. Count arrivals
only with the original native quiet/contact and both-camera measurement gates.

Compare each new mission with its same-model predecessor only through the
observation before the first differing command or terminal policy. Terminal
intervention can precede command divergence because an active zero wait and a
terminal drain both request zero. Later phase bookkeeping and active mapping
are different; do not extend matched-decision claims beyond that boundary.
Compare the new JEPA/direct pair at the same causal boundary and include auxiliary
public depth and valid-mask equality. Retain all raw physics, policy, gyro,
primary RGB, observer/map and forecast comparisons without assuming agreement.

Create the exclusive `go2_auxiliary_depth_reobserve_goal_readout_v1_attempt_001`
root after complete source and input admission and reverify afterward. Perform
no fitting, controller change or native execution. These reused-layout results
do not establish independent navigation, physical backtracking, matched baseline
advantages, real-time performance, hardware readiness or completion of the goal.
