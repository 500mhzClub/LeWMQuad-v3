# Prospective chained tracking native experiment on development maze 02

Execute at most one fresh native episode using
`scripts/run_go2_measured_plane_chained_maze02_v1.py` at artifact root
`go2_measured_plane_chained_maze02_v1_attempt_001`. Source preparation and
source preflight do not start an episode. Execution requires the exact original
timing replay and queued chained-controller replay to complete and their
original owners to end. The completed chained waiter result SHA is a required
runtime input; it cannot be replaced by a pair-probe result or fabricated
boundary. The admission helper reauthenticates both completed waiter chains,
the consumed controller rows/public packets, and original native results.

The prospective candidate is `MeasuredPlaneChainedAnchorController` with the
same corrected no-RGB direct world model as the original learned pilot. Its
measured-plane wrapper encloses original, direct-flow and chained image fits;
original missing-floor handling, qualified measurement conflicts, reference
rules and bridge allowance remain unchanged. No model is trained or refit.
The separate single-pass timing optimization is not adopted by this run.

Match the completed learned pilot's robot, scene, seeds, initial state, paired
public sensors, map and mission rules, command limits, predictive planner,
4,000 navigation ticks, maximum 4,013 commands and 4,014 observations, and
deterministic CPU/renderer environment. This is a reused development layout.
The controller receives no native robot pose or hidden maze graph.

The first intervention is whatever the completed full-history controller
comparison actually establishes, between observation 3 and 3123. Admission
requires a nonterminal, failure-free candidate at the first changed request
or terminal decision. No particular frame, command or navigation outcome is
assumed here. A terminal-only change with an unchanged command remains distinct
from a changed command. A negative or failed replay grants no execution through
this launcher; preserve its evidence.

Collect the new episode and perform the complete sensor/controller/model/
command/renderer/native-outcome raw audit with the same candidate controller
and fresh unchanged assigned model. Authenticate all resulting artifact hashes.
Reconstruct the entire preintervention physics trajectory, every public packet,
and both complete controller decisions through the actual boundary. Require
the actual boundary command's 50 physical steps. Following outcomes belong to
the new episode and are not inferred from the original recorded path. The
native-result validator repeats this reconstruction when accepting the saved
worker receipt. Preserve an early scientific negative without claiming an
intervention or navigation success.

Before output creation require completed predecessor admission, the current
source closure including this protocol and focused tests, model identity,
at least 32 GiB available RAM and 55 GiB artifact space, and an idle native
execution slot. Recheck inputs and resources after waiting. Freeze the exact
source hashes in the launch before starting one fresh spawned scene worker,
with one OpenCV and BLAS thread and one task per process. Do not retry, resume,
overwrite or bypass a failed attempt. A source preflight creates no model,
output root, worker or scene.

Success requires the unchanged physical round-trip and settling checks jointly
with strict physical visibility and no hard measurement failures. Retain
negative outcomes and full raw audits. Physics still pauses during computation;
this experiment supplies no independent-maze, JEPA/memory advantage, real-time,
hardware or deployment qualification. Even a successful round trip does not
complete the full thread goal.
