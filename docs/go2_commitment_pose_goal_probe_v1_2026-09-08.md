# Commitment-horizon waypoint pose native probe V1

After the twelve-case matched-model cohort is complete and read out, run four
fresh known-layout integration cases: the existing final full-RGB direct and
full-RGB JEPA models, each on family layouts 052 and 039. Full-RGB direct is a
development reference selected for its lower executed-transition XY error; JEPA
retains the original method for comparison. This is declared method selection
using development evidence, not an independent evaluation or a search across
checkpoints. Both final snapshots stay fixed. Shuffle the complete case roster
once with seed 2026091102, before launch.

Use the separately specified commitment-pose waypoint utility: first-half-second
predicted distance and bearing potential reduction, minus first-half-second
contact cost. Preserve every original model forecast and full-horizon utility.
Reapply the existing persistent articulated-surface vetoes after reranking; all
unknown-space limitations remain. Scan targets and scan scoring are unchanged.
This changes both waypoint value and scoring horizon, so it is not an isolated
heading ablation. It does not repair prediction error or visual continuity.

Keep the same native scenes/sensing, model input treatment, six candidate plans,
five-tick command commitment, observed floor mapper, visual observer, 1.2-m mission
goal, 240-tick navigation budget, quiet arrival, zero drain, physical stops and
native evaluator. A new small controller subclass changes only waypoint ranking
and its descriptive controller label. Each case has a fresh process, scene,
model reload, observer and memory. None resumes or replaces an earlier attempt.

Authenticate the exact completed twelve-case predecessor, its complete artifact
and source bindings, and the existing full six-fit ledger/schedule/input/raw-score
admission. Bind all six final snapshots, though only the two declared methods
run. Each worker loads only its assigned snapshot, verifies source/native/input
identities, records complete raw execution, and replays every sensor-to-command
decision with a fresh evaluation-only reload. Original goal and actuator audits
remain identical. Eight focused component/integration tests must pass first.

Use four fresh independent processes with one OpenCV/BLAS/PyTorch CPU thread each.
Authenticate the prior native scaling benchmark result
`ed64dfb610062caea529f96db01d3ce5a3be2c621a5ca8bcd04822f5c1523969`,
which found 3.34-times throughput and exact numeric/pixel records for four
concurrent versus serial cases in this native scene pipeline. That benchmark
used fixed command tapes, so it is supporting scheduling evidence, not a proof
of current online-controller equivalence. The separate six-model inference
benchmark also found identical serial/concurrent predictions. Current loop
timings are recorded under concurrent load and must not be presented as
uncontended timing or as comparable timing improvements over the serial cohort.
Physics pauses during computation; no real-time or hardware claim is made.

Inspect current CPU topology/activity, RAM, GPU/VRAM, competing processes and
storage. Require 32 GiB available RAM, plan 8 GiB output plus the existing 40 GiB
reserve, retain each episode's storage stop and monitor every approximately
fifteen seconds. These are capacity checks rather than OS quotas. After launch
all four sibling workers finish and retain evidence even if one infrastructure
audit fails; no automatic replacement. Scientific failures are retained normally.

Primary outcomes are native verified goals, contacts, terminal/minimum goal
distance, visual failures and measurement checks. Retain complete phase/action
traces and all timing. These known-layout development cases do not establish
novel-maze success, backtracking, general RGB/JEPA benefit, calibrated safety or
deployment readiness. The full navigation objective remains unchanged.
