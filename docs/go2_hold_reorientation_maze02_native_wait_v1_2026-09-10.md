# Automatic native handoff after raw replay and scheduled frontier

Own one subsequent native development case using the already specified
`run_go2_hold_reorientation_maze02_pilot_v1.py`. This separate registration
follows the source preparation; it does not alter an existing experiment.

Bind the exact original raw-replay waiter PID 2669739, created 1789035532.54,
and frontier waiter PID 2663938, created 1789032169.81, including boot identity,
full command lines and launch hashes. Poll those owners every 30 seconds for up
to 48 hours. Require each owner to end and its original complete result to pass
source/artifact identity and status checks. Pin each first completed result
while the other remains live. A timeout, missing completion, changed identity
or failed prerequisite does not permit a replacement run or restart.

After both complete, reverify all frozen sources, the existing one-case resource
envelope and an idle native slot. Launch exactly one child with the two explicit
completed waiter result hashes. The child performs full input admission before
creating its native output, reconstructing the original completed six-case
batch, frontier experiment, raw replay and original first-case identity. This
preserves the frontier waiter's earlier native priority.

The child uses one fresh native subprocess, 32 GiB available RAM and 51 GiB
artifact envelope. Audit and verify its complete model/command/sensor evidence,
paired physical boundary and newly measured later outcomes. Successful terminal
processing is required; scientific navigation failure is retained. No additional
run, seed, checkpoint, layout, retry or resume is admitted.

Exclusive waiter output `go2_hold_reorientation_maze02_native_wait_v1_attempt_001`.
No native scene is launched while waiting. Bind logs, events, both input
identities, child completion and all inherited/new source hashes. No independent
layout execution, navigation advantage, real-time or hardware qualification is
claimed by queuing or completing this experiment.
