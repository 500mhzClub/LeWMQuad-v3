# Chained measured-plane controller replay waiter V1

Execute one paired controller replay under the separately defined
`go2_measured_plane_chained_controller_prefix_v1_2026-09-12.md` protocol only
after the exact existing full-history timing waiter and its CPU replay finish.

Observe timing waiter PID 2924370, creation 1789167167.42, its fixed command,
boot and launch SHA-256
`c63f9bccb0cdfc273b1d3fd721310da5fac384f1350dcfc4625098fd103184a4`.
Transient process/filesystem observation errors retry the same owner. Poll
every 30 seconds without model loading or controller replay while waiting.
An ended process alone is insufficient: require its successful completion
receipt, exact child artifact bindings and the completed original native audit.
Preserve failures; no automatic restart, alternate predecessor or bypass.

Observe 64 GiB available RAM and 43 GiB free artifact space immediately before
dispatch. If unavailable, keep waiting and remeasure. Start one child with the
actual native-result and timing-waiter-result SHA-256 arguments. The child owns
the original-versus-candidate replay, source/packet/model checks and first
changed-command boundary. Use a `replay_go2_` executable name so the existing
native-scene guard does not misclassify it as another physics worker.

After the child ends successfully, verify the full output artifact set,
reauthenticate original raw inputs, and reconstruct all consumed output rows,
public packets and the boundary report without rerunning controller inference.
Record negative candidate outcomes as valid scientific results. There is no
native scene launch, changed-command physical execution, navigation recovery
claim or automatic retry in this waiter.

Exclusive waiter root:
`go2_measured_plane_chained_controller_wait_v1_attempt_001` under the owned
development artifact volume. The child root is the separately fixed
`go2_measured_plane_chained_controller_prefix_v1_attempt_001`. Both must be
absent before registration. Freeze the complete discovered source union at
waiter launch and preserve it throughout waiting, execution and verification.
