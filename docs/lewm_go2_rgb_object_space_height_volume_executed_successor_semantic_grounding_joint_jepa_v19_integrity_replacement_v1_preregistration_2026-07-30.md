# V19 executed-successor semantic-grounding integrity replacement V1 preregistration

Date: 2026-07-30

Status: preregistered science-identical zero-update integrity replacement only.
No replacement reservation, generated-input access, checkpoint or tensor
access, GPU work, training, calibration, G2, navigation, or held-out access has
occurred.

## Trigger and terminal predecessor

- The original V19 one-shot attempt is consumed and terminal. It may not be
  retried or resumed.
- Its complete failure audit is at
  `docs/lewm_go2_rgb_object_space_height_volume_executed_successor_semantic_grounding_joint_jepa_v19_terminal_failure_result_2026-07-30.json`,
  file SHA-256
  `1f1708d615cbf375c99fa49efd11699882c302ad92ae436e925af152e18da36d`,
  byte count `9292`, canonical content SHA-256
  `e3d7cee42656bcf12deda496d2e77f64f847f1dfaa72c8e0c27dac53d35596e3`,
  and failure-result commit `37a87ac49ebcdebe57263476c20b1476877e36c2`.
- The attempt reserved successfully, revalidated all certified source and
  authorized runtime inputs, initialized the exact V19 model, hard-synced the
  target, passed all twelve initial structural checks, and wrote its one
  initialized update-0 trace row.
- During update-0 observation, the unchanged inherited evaluator emitted its
  factual eight-family `family_deltas` mapping ending in
  `visual_sensor_stress`. The new receipt-only V19 adapter incorrectly expected
  `structured_corridor_rooms` as its last registered family and raised
  `V19 inherited paired-comparison grouping changed`, whose exact SHA-256 is
  `5587b70a14a4bf1ec0d0ade06bdfb1d7298b3616b4729eb851f59aa11e325bc9`.
- No metric or checkpoint was published. The attempt completed zero training
  updates, presentations, microbatches, predictor training forwards, factual
  successor objectives, backward/gradient calls, optimizer steps, or EMA
  steps. It reached no scientific gate and supplies no evidence for or against
  executed-successor semantic grounding.

Replacement V1 is a distinct fresh one-shot root, not a retry or resume of the
consumed attempt.

## Sole permitted correction

- Correct only the numeric-comparison receipt adapter's registered family
  tuple: replace its obsolete final value `structured_corridor_rooms` with the
  frozen inherited evaluator and checkpoint-selection value
  `visual_sensor_stress`.
- Preserve the other seven family names and their order exactly. The corrected
  adapter tuple must equal the frozen `REGISTERED_FAMILIES` consumed by
  `paired_control_comparison_v1` and the frozen checkpoint-selection scene
  family registry.
- Keep `_sanitize_comparison_v19` numeric validation and retention behavior
  otherwise byte-identical. The adapter remains observation-only and may not
  rescore, reopen, or feed a comparison into training.
- Add focused CPU-only source tests proving exact registry identity, acceptance
  of the inherited eight-family mapping, rejection of a genuinely changed
  family mapping, and unchanged four-control capture/restore behavior.
- Change only the lifecycle identities and bindings required for the fresh
  replacement: preregistration/failure witnesses, schema/evidence prefix,
  source manifest/review/certification/authority paths, experiment-arm
  identity, source root, output root, and corresponding focused tests.

No model source, model parameter, tensor operation, architecture, input, role,
row, label, RGB, camera metadata, seed, schedule element, optimizer setting,
learning rate, AdamW setting, EMA operation, loss, coefficient, Q route,
gradient cap, observation, evaluator, metric definition, threshold, stopping
rule, or accounting multiplier may change.

## Frozen scientific identity

Preserve V19 preregistration commit
`6255a9a2cccffde4e777169eacf95105a828cf7e`, frozen source-and-review
commit `0aa8b5670c0f7de22020d4cb290b2013d57bede0`, and controlling V18 result
commit `f2e290ce42f7b0cd142131f3272d1119b7b5d3d1` exactly, including:

- the unchanged V18 learned RGB encoder, eight-height object-space volume,
  semantic head, survival head, action-conditioned predictor, and exact model
  initialization;
- joint online encoder/representation/predictor learning against one
  stop-gradient EMA target in every completed update;
- the factual executed-action next-semantic objective `Q_succ`, routed only to
  the registered 13-tensor, 259008-parameter latent-transition predictor
  subset, with its independent unit-norm cap and no semantic-head,
  representation, encoder, evidence-head, survival-head, or target gradient;
- all inherited `S/P/U/R/O/C` losses, route clipping, parameter groups,
  float32 AdamW settings, one optimizer step and one EMA step per update;
- the exact N320 initialization, train/checkpoint-selection roles and files,
  4262-pair schedule, four microbatches of four, all seeds, and observations at
  updates `0`, `100`, `400`, and conditionally `1000`;
- accounting of four training predictor forwards, eight total predictor
  objectives, four factual-successor objectives, twelve total gradient calls,
  one optimizer step, and one EMA step per completed update; and
- maximum `1000` updates and `16000` ordered presentations.

No predecessor runtime tensor, checkpoint, optimizer state, trace, metric, or
output may be reused. There is no seed change, coefficient search, architecture
variant, automatic resume, longer run, or gate reinterpretation.

## Unchanged gates

- Update 100 remains informational science plus mandatory structural,
  accounting, finite-gradient, target-isolation, access, and custody checks.
- Update 400 remains the decisive falsification gate: structural and target
  integrity; all twelve unchanged causal-control checks; passed physical
  margin count strictly greater than `72`; total physical shortfall strictly
  less than `68.96954700805838`; and rough depth p95 strictly less than
  `1.8582415819168085` m.
- Any update-400 failure is terminal with no retry, resume, or checkpoint.
  Passing continues in the same process to update 1000.
- The update-1000 final gate remains: inherited V12 full arm `24/24`, at least
  `112/189` physical margins, total shortfall strictly below
  `33.05143763708337`, at least one complete physical scope, rough pixel
  balanced accuracy strictly above `0.8198594673963917`, rough ground balanced
  accuracy strictly above `0.647134926562893`, rough depth p95 strictly below
  `0.9777327477931971` m, and structural integrity.
- Only a complete passing update-1000 result may publish a development
  checkpoint. It does not directly authorize calibration, G2, navigation,
  held-out, sealed, promotion, production, or deployment access.

## One-shot replacement identity and authority boundary

- Schema/evidence prefix:
  `lewm_go2_rgb_object_space_height_volume_executed_successor_semantic_grounding_joint_jepa_v19_integrity_replacement_v1`.
- Fresh output root:
  `.generated/go2_rgb_object_space_height_volume_executed_successor_semantic_grounding_joint_jepa_v19_integrity_replacement_v1/attempt_v1`.
- Fresh clean source root:
  `/home/andrewknowles/Workspace/LeWMQuad-v3-v19-executed-successor-semantic-grounding-integrity-replacement-v1-source`.
- There is exactly one replacement attempt. Both fresh roots must initially be
  absent. Retry and resume are false.
- The terminal V19 failure result and original preregistration may enter only
  as committed source-identity witnesses. Original V19 authority,
  certification, reservation, runtime artifacts, model state, tensors, and
  checkpoint remain closed.
- Any replacement source, authority, reservation, custody, exception, or gate
  failure is terminal and publishes no failed checkpoint. No second integrity
  replacement is preregistered.

Implementation, focused regression, recursive source closure, independent
source review, narrow clean-export certification, and separate one-shot
authority must be committed before reservation. Until a complete passing
update-1000 result, probability calibration, G2, navigation, held-out, sealed,
production, promotion, deployment, retry, resume, recovery, and extension
remain forbidden.
