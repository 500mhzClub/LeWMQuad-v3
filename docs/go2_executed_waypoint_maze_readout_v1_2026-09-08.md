# Completed executed-waypoint maze readout V1

After the executed-waypoint native pilot completes collection, raw audit and exact
physical/public prefix comparison, run its dedicated script
`scripts/read_go2_executed_waypoint_maze_pilot_v1.py --native-result-sha256 HASH`
in exclusive `go2_executed_waypoint_maze_readout_v1_attempt_001`. Authenticate
the completed native result, all artifact bindings and frozen recursive source/
native dependencies before and after readout. Preserve the existing outcome.

Retain the recovery readout's decision trace, actual native goal distance/path,
mission arrivals and native backtracking evaluation, first/last infeasibility,
full raw measurement gates and original acquisition/control/receipt timing.
Retain actual recovery interval errors and later ordinary nominal-gate evidence.
For every nonterminal actually selected intermediate-waypoint action, compare
its original corrected-model first 100 ms XY and causal scoring XY with native
body-frame displacement over exactly its 50 executed physics steps. Bind the
selected action to the command tape. Censor incomplete intervals, and report
means only over complete intervals. Include the original action from local
reranking, waypoint distance and available residual sample count.

These are descriptive errors for executed actions. A changed local ranking is
not an inferred original-controller trajectory, an unexecuted action outcome,
or causal planning/memory benefit. Native poses remain evaluator-only. This is
reuse of development maze 0, with zero additional independent layouts. No model
training, native scene, hardware execution or outcome promotion is performed.

One ordered CPU readout; require 8 GiB RAM and 128 MiB above the unchanged 40 GiB
reserve. Record hardware admission and preserve failures separately. Paused
physics and ideal sensor timing remain limitations; no real-time or deployment
qualification follows from this analysis.
