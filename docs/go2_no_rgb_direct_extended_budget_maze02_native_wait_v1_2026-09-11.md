# Append the budget diagnostic after the original native queue

This waiter schedules the prepared no-RGB direct 4,000-tick development
diagnostic after the original six-case batch and the frontier, hold,
commitment-contact and tracking experiments. It changes none of those runs.
It uses their exact existing launch hashes and PID/create-time/command owners.
No simulator worker is created while any prerequisite owner is live.

The existing bounded waiter loop is reused with a frozen five-item prerequisite
roster and the same 48-hour wait limit and 30-second polling interval. Lightweight
completion checks authenticate source/launch/result identities, complete batch
population and no-retry status; completed identities must remain unchanged
across polls. A missing or failed result after an owner ends, changed process
identity, changed source, or wait expiry stops this waiter without restarting
any predecessor. The inherited event label mentions a replay, but this roster
contains only the batch and four native waiters, not another controller replay.

Once all five identities are available, recheck owners, source bindings,
55 GiB free artifact space, 32 GiB available RAM, competing native runners and
the absence of a candidate output root. Start exactly one subprocess of the
prepared launcher, forwarding the exact five completed result hashes. That
launcher performs full original model/input/queue admission before collecting
the fresh episode. No queued controller change is adopted by the budget test.

Wait for the actual child exit status; never retry a failed child. Authenticate
its complete artifact roster, persisted worker/audit/readout/prefix records,
model/input admission and scientific outcome accounting. Reconstruct the
physical/contact/timing readout and the actual prefix finding. Early termination
and earlier prefix divergence remain negative evidence, not reasons to replace
the attempt. Original scientific success is not a prerequisite.

The waiter and child both use exclusive attempt roots. No source change, retry,
resume, alternate child or independent-layout policy promotion is permitted by
this scheduling record. Completion of this diagnostic remains separate from
reliable unseen-maze navigation, causal comparisons, real-time performance and
bounded real-platform evidence required by the active goal.
