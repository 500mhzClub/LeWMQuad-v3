# Observed foot/ground contact native probe V1

Run the same fixed corrected seed-2026091001 JEPA/direct pair on reused
family_episode_039, unchanged seed-2026091501 order. Require complete correction
admission/readout, the 45-degree native predecessor/readout and two exact fresh
contact-policy prefix replays per model. Require at least one changed command;
the replay predicts no native outcome.

The sole intervention is the explicit observed_floor_contact_v1 policy:
individually classified measured floor returns may contact the four exact
22-mm foot spheres even when the full projected support area is unseen.
This deliberately changes the preceding complete-projection eligibility rule.
Unseen support remains UNKNOWN and is not certified. Keep every point, mask,
classification, footprint and coverage witness. Foot other/unknown contacts
from either stream and all non-foot contacts remain blocking. Preserve the
original classifier and all nominal first-step/eight-step wall checks.

Keep the two robot-visible cameras/calibrations, primary observer, model
inputs/weights/corrections, candidate bank, waypoint/view logic, bounded waits,
240-tick navigation budget, goal dwell, ten-command terminal drain, gait,
actuator slew/gains, setup and physical stops. Native pose/segmentation stay
renderer/evaluator-only. Use original exact raw sensor, command, controller
replay and native 0.06-m quiet/contact arrival gates for both outcomes.

Freeze source and input bindings before exclusive root
go2_observed_floor_contact_goal_probe_v1_attempt_001. Two independent one-thread
CPU scenes, prior scaling evidence and a fresh hardware check requiring 32 GiB
RAM and 8 GiB output allowance above the 40-GiB reserve. Monitor resources and
whole-iteration time. Match the original first-four 45-degree paired public
frames and first-900 native samples. Reverify all bindings afterward. Preserve
every failure without retry/resume.

The development operating domain remains the reused nominal-friction flat-floor
integration layout. This contact-policy change is not evidence of support on
unknown terrain, obstacle-free motion or hardware safety. Count no goal without
all original native and sensor gates. No independent-maze, backtracking,
matched-baseline, real-time or hardware qualification is established here.
