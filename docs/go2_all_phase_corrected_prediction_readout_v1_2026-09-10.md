# Complete expanded-model corrected prediction readout

Use the completed18-model fit result
44c4cd65812b021b29cfb0aff33e2058cfaaa71dcc627d36eced30dfac17ea35 and completed
training-only correction1b36dc77ca51d342e45d73da027ebdbdbd1263ab5be4142766948fd8258dd460.
Execute complete correction admission, including all18 fit/ledger/raw-score/
snapshot checks and reconstruction of all30 trained-head coefficients. No
coefficient refitting, model training, checkpoint selection or new native run.

Apply each fixed float32 XY intercept to both recorded roles for all18 models
and all30 trained heads. Preserve the raw fitted arrays. Write all36 corrected
model/role files and a global completion receipt before any corrected scoring.
Score all60 head/role combinations with the unchanged observation-horizon score
function. Reproduce every original primary-head score exactly. Verify identical
indices, known clocks/masks, zero padding, raw yaw/contact components and all
non-position scores/counts. Keep all motion/contact censoring and undefined-yaw
accounting; do not remove the36 motionless training contexts. Both roles retain
the original4010 training and420 geometry-transfer available-context populations.

Retain complete before/after scores for every stratum and head. Use the existing
target-count-weighted metric aggregation. Report before/after per-model metrics,
descriptive three-optimization-seed means/sample standard deviations, and fixed
corrected comparisons: full supervised/direct minus full JEPA within seed, plus
no_rgb minus full within each condition and seed. Compare each role/source/stratum
on unchanged denominators. Positive position-error differences mean worse
prediction error. Keep every result, not only improvements. Auxiliary direct
heads in JEPA/supervised models are reported but do not replace their rollout
primary head. No new significance test or independent-maze confidence claim.

The six queued native assignments and unchanged controller were fixed in
docs/go2_all_phase_residual_maze02_matched_native_v1_2026-09-10.md before this
readout. Its full/no_rgb JEPA, supervised and direct cases remain unchanged
regardless of these scores. Training scores are resubstitution. Geometry transfer
is existing development prediction evidence, not sealed evaluation or successful
navigation. Optimizer seeds are not independent mazes. Contact probabilities
remain uncalibrated; no real-time or hardware qualification follows.

Use one CPU process and one numerical thread,8GiB currently available RAM and
512MiB output allowance above the40GiB artifact reserve. Check hardware before
admission and again before creating the exclusive output. No new RGB/future
tensor materialization or neural forward pass is needed: score the admitted saved
predictions using stored targets. Full admission may inspect bound predecessor
artifacts; it does not change their permitted roles. Check current source and
input bindings before/after work, authenticate original input collections again
after scoring, reproduce saved corrected arrays/scores, and preserve any failure.
No retry/resume. Exclusive root:
go2_all_phase_corrected_prediction_readout_v1_attempt_001.
