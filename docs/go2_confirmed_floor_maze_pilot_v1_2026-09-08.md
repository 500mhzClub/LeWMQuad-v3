# Prospective current-primary floor-confirmation maze pilot V1

Test the separately named ConfirmedFloorRoundTripController on reused development
maze 0, after its completed causal prefix identifies the first changed command.
Require a hash-bound successful prefix result and recorded result document;
partial or failed replay is not launch evidence. The prefix must reconstruct
the complete frozen predecessor selection and original contact checks, preserving
all public observations, raw maps, mission, forecasts and nominal path checks.
The new controller uses an additional auxiliary floor partition confirmed from
contemporary measured primary floor patches. Only the four nominal auxiliary
foot queries change; other/unknown and non-foot contacts still block.

The collector and fresh-controller raw audit preserve the completed view-reentry
pipeline except controller and result identities. Retain the same public RGB-D,
auxiliary depth and body sensors, observer, original raw maps and partitions,
corrected JEPA checkpoint, PPO locomotion, six actions, 100 ms replanning,
continuous 2 ms physics, slew limits, shared 3000-tick outbound/return budget,
arrival and backtracking evaluation, strict visibility gates, physical stops,
artifact persistence and raw command reconstruction. No training or native
state input to the policy is allowed by this intervention.

The first changed command must differ from its recorded predecessor. A terminal
policy difference may occur at that same frame, provided the new controller is
nonterminal. Compare fresh native/public evidence through this observation,
including all earlier requests. Normalize only the declared added receipt for
the original map comparison; require each complete original surface witness.
No observation after the changed command is attributed to the old trajectory.

The predecessor fails strict primary visibility at frame 909. If the exact
physical/public prefix includes that frame, the unchanged gate is expected to
fail again. This experiment can establish the physical outcome of the floor
intervention, but cannot qualify navigation while any strict gate fails.
The separate raster-edge diagnosis does not repair pixels or relax this gate.

Exclusive output: `go2_confirmed_floor_maze_pilot_v1_attempt_001`. One CPU native
worker, 32 GiB available RAM admission, 10 GiB collection plus 1 GiB persistence
headroom above the unchanged 40 GiB reserve. Assess hardware immediately before
launch; no parallel native scenes. Sources and completed input/artifact bindings
are verified before and after execution. Preserve all partial failures.
`--preflight-only` checks admission without creating output. Actual execution
requires `--prefix-result-sha256` with the completed result identity.

Report arrivals, returns, route edges, terminal constraints, physical stops,
strict visibility and timing even on navigation failure. This is a repeated
development layout, contributes zero new independent layouts, and pauses physics
during computation. Real-time, hardware, matched attribution and independent
maze success remain unestablished.
