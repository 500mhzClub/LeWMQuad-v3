# Go2 scene-diversity recurrent replication integrity replacement V2

Date frozen: 2026-08-04

Status before execution: `PREREGISTERED_NOT_RUN`

Scope: one development-only, science-identical infrastructure replacement for
the two consumed scene-diversity collection failures. This document grants no
sealed, held-out, navigation, rollout, planner, promotion, deployment, retry,
resume, refill, overwrite, partial-reuse, adaptive-fallback, or further-
replacement authority. A separate independent source review and exact one-shot
execution authority are required before the fresh attempt root may be created.

## Consumed predecessors and absence of science

The original V1 attempt and integrity replacement V1 attempt are consumed and
immutable. Their partial generated artifacts are ineligible for reuse.

The original V1 terminal is SHA-256
`df4cecb5edc45f25a98f4753e82e95334a6b8c4e9e0d719bb13150b9be690bfa`
and 451 bytes. Its terminal review is SHA-256
`7f2ac7eb3f9fa16fd91a5311009cdbcd7d4777e8d9c3f7746666ce1afbc6da59`
and 15,809 bytes, with status
`PASS_FAIL_CLOSED_INFRASTRUCTURE_TERMINAL_REVIEW`. That attempt persisted 32
train scenes and one evaluation scene before the selected-device monitor
observed `17,808,412,672` bytes against the exact `16,977,405,952`-byte cap.

Integrity replacement V1's terminal is SHA-256
`c2650529ff6b1aa1322738e7d4f748fbab03ea9ef33a623ff3e182a98e1cb77d`
and 473 bytes. Its terminal review is SHA-256
`e25f9281449147b6937edfafa022b5fbeb18ca3e844438c70a5babb84c2eb0cb`
and 21,621 bytes, with schema
`lewm_go2_scene_diversity_recurrent_replication_integrity_replacement_v1_terminal_review_v1`
and status `PASS_FAIL_CLOSED_INFRASTRUCTURE_TERMINAL_REVIEW`. That replacement
persisted two complete train scene units before the active monitor observed
`17,259,581,440` bytes against the unchanged `16,977,405,952`-byte cap.

Both terminals are `FAIL_INFRASTRUCTURE_NO_SCIENTIFIC_DECISION`, bind no
result, and authorize no retry or resume. Neither attempt published a role
result, combined `physics_result.json`, scientific checkpoint, metric, gate,
or result. No predecessor scene unit, receipt, RGB frame, process result,
reservation, or other runtime artifact may be copied, opened by the model,
joined, resumed, refilled, or admitted to V2.

## Single frozen infrastructure intervention

The only intervention is a finer process-lifetime boundary. A fresh parent
must reserve the absent root
`.generated/dev/go2_scene_diversity_recurrent_replication_integrity_replacement_v2/attempt_v1`
and launch exactly 64 sequential clean scene workers, one for each scene in
the frozen exact-plan order. Worker index `i` may generate only frozen scene
index `i` and must exit before worker `i + 1` can start. No worker batch size,
ordering, retry, replacement worker, family-dependent route, or concurrency is
permitted.

Immediately before each `Popen`, the parent must read and bind that worker's
fresh selected-device baseline. While the child is live, the parent must
measure both process-group identifiers, require the child identifier to equal
the parent's identifier, and preserve those observed values in the worker
evidence; inheritance may not be asserted from launch arguments alone. The
matching release barrier must bind exactly that worker's pre-launch baseline.

Every worker must explicitly initialize Genesis from the full plan's first
scene physics seed `14102849992353107924`, with effective signed-31 seed
`315871188`, before building its assigned scene. This is the same full-plan
process-global seed required by the predecessor replacement and does not
derive a new initialization seed from the assigned scene. All per-scene
physics and visual seeds remain exactly plan-bound.

The claim that this reset is science-identical is conditional, not assumed.
Before authority issuance, independent source review must establish that after
the plan-first Genesis initialization there are no outcome-affecting
uncontrolled stochastic draws in scene construction, exact-clone creation,
control application, physics/dynamics stepping, or rendering, and that every
declared scene variation is derived only from its frozen plan fields and bound
per-scene seeds. The source review must report each of those five route checks
explicitly. If any route is stochastic, ambiguous, or unbound, source review
must fail and V2 receives no execution authority; resetting the initialization
seed per process may not be called science-identical on plan equality alone.

The required audit covers the exact installed dependency bytes as well as the
repository source. It must verify that process-global Genesis initialization
is idempotent; scene construction consumes the bound `ScenePack`; exact clones
overwrite dynamic state and disable spawn randomization; lockstep execution
uses only caller-provided action blocks; texture selection uses its private
scene-keyed RNG; and replay surfaces are explicit. Any reachable global draw
may be accepted only if it is proven outcome-irrelevant and unobserved, such
as collision-debug RGBA that affects neither rigid-body state nor a stored
replay surface. The authority source closure must rehash the minimal reviewed
Genesis/RSL dependency files on every validation.

After every worker exits, including worker 63 after the final scene, the parent
must complete a bound read-only selected-device release barrier before any
next stage. Each barrier requires observed device use no greater than that
worker's pre-launch baseline plus `536,870,912` bytes within 60 seconds,
sampled at least every 100 ms. A failed worker, nonzero exit, malformed or
misbound scene result, cap breach, or failed release barrier consumes the
attempt and forbids the combined physics result.

The order is fixed even on error: collect the child wait receipt (including a
nonzero return code), run the mandatory matching release barrier, then reject
a nonzero exit. A scene result may be loaded and validated only after a zero
exit and its successful barrier. Thus an unsuccessful worker cannot bypass
release accounting, and a malformed result cannot be inspected before the
worker's resource boundary is complete.

Only after all 64 distinct bound scene results and all 64 release barriers pass
may the parent perform a metadata-only join in frozen plan order and publish
the standard combined `physics_result.json`. Missing, duplicate, reordered,
cross-role, escaping, or predecessor-derived inputs fail closed. There is no
partial reuse, adaptive batching, cap increase, alternate split, refill,
screening, or fallback.

After loading that combined index and before any ledger, DINO, model, or
scientific access, the runner must rehash all 64 scene-result bindings, all 256
state-receipt bindings, all 64 live-render-receipt bindings, and every derived
mesh binding. Each mesh must remain within its scene-local cache and agree with
the bound cache summary. The pre-science closure report must explicitly state
`all_scene_result_receipt_mesh_bindings_rehashed: true`; otherwise execution
fails before science. At the same boundary the runner must call the frozen
plan-binding rehasher for every runtime, texture, scene-manifest, and
scene-Genesis binding and must rerun the existing bound-scene check that scene
inputs are absolute and non-generated; the report must state
`all_plan_input_bindings_rehashed: true`.
Installed Genesis/RSL dependencies remain covered by the authority source
rehash. Independent review must demonstrate each part explicitly.

## Exact scientific identity

The V2 exact plan differs from the original frozen V1 exact plan only in
`attempt_id` and `output_root`. It retains byte-canonical equality for every
other field: 256 ordered states, 64 scene identities and bindings, role
allocation, targets, history actions, nine candidate actions, runtime/render
contracts, textures, counts, seeds, caps, model declarations, metrics, and
gates.

The frozen recurrent custody route remains unchanged. Train receipts and
context RGB are the only pre-checkpoint inputs; no train successor RGB may be
opened. The runner must durably write and reopen the checkpoint before any
evaluation receipt or context PNG is opened, must open zero evaluation
successor PNGs, and must evaluate the disk-reopened checkpoint twice with
exact result equality.

The DINO repository and checkpoint, three model seeds, sampler seed,
train-only PCA/statistics, 800 updates, optimizer, losses, live task/action
control, scoring, 10,000-resample family/scene bootstrap, and five frozen gates
remain unchanged. No successor RGB, semantic/depth target, reward, planner,
rollout, encoder update, or JEPA target is admitted.

## Unchanged decision rule and bounds

The fixed gates remain: integrity and custody pass; visual normalized rank
regret is at most `0.13`; visual minus live task/action regret is at most
`-0.02` with paired 95% upper bound below zero; visual minus matched no-vision
regret is at most `-0.01` with paired 95% upper bound below zero; and visual
beats uniform-random expectation. The pass and stop meanings remain those of
the original preregistration. Infrastructure failure yields no scientific
decision, and no threshold or model choice may change after data or metrics
are observed.

The global selected-device ceiling remains `16,977,405,952` bytes, total wall
ceiling remains 7,200 seconds, stored RGB remains capped at 512 MiB and 3,072
frames, and every simulation/render call cap remains unchanged. The parent
continues 20 ms selected-device sampling across every child. Process exit plus
the mandatory per-scene release barrier is the sole infrastructure change; a
higher cap is not authorized.

Completion of 64 clean processes within 7,200 seconds is not established
before execution. Unbound operator observations of roughly 100 seconds for a
first scene and 70--80 seconds for a second scene are diagnostic only and may
not be treated as a timing qualification, authority input, cap change, or
adaptive-stop rule. Exceeding the unchanged wall cap fails closed with no
scientific decision and no retry.
