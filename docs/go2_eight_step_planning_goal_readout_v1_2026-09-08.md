# Eight-step planning native readout V1

Read both complete audited cases without selection from the exclusive
`go2_eight_step_planning_goal_probe_v1_attempt_001` attempt. Verify its complete
source/artifact bindings and the completed short-horizon predecessor. Freeze
this reader, its focused tests and protocol before the exclusive
`go2_eight_step_planning_goal_readout_v1_attempt_001` output.

Retain both arrival and terminal outcomes, all sensor/raw replay and visibility
gates, action counts, observed pose errors and full-iteration timing. Count
first-step nominal changes separately from the combined eight-step scoring/path
changes. Count candidate forecasts that passed the first nominal segment but
failed the full predicted path. Do not infer a hypothetical executed outcome.

For each model, compare the new and previous cases through the observation
preceding the first differing command or terminal event. Verify raw physical,
policy, fast-gyro, RGB and observer/memory prefixes. Because the assigned models
are unchanged, require exact forecast banks throughout those prefixes. Compare
the new JEPA/direct cases at the same causal boundary without requiring their
different models to predict identically. Never include affected future frames.

Reverify all bindings after analysis and preserve failures. This readout trains
no model and launches no native scene. It cannot establish independent-maze,
real-time, hardware or overall-goal completion.
