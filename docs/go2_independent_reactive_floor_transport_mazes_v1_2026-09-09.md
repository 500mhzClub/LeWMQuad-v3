# Fixed reactive comparison on development layouts1–3

Define this cohort before either method executes these three layouts. Execute
the existing ReactiveFloorTransportController on layouts1,2,3, in that order,
with fresh observed state and one fresh CPU process per case. Retain the exact
collector and raw audit prepared for reactive maze0. No high-level learned
model, learned residual, predicted candidate outcomes, training, selection,
case omission, policy change, retry or outcome-dependent adaptation.

Run scripts/run_go2_independent_reactive_floor_transport_mazes_v1.py after the
fixed learned layouts1–3 cohort and current reactive maze0 pilot have completed
their raw audits. Supply actual --learned-cohort-result-sha256 and
--reactive-pilot-result-sha256, initially with --preflight-only. Both prior
experiments may have negative physical/visibility results; incomplete or failed
raw audits prevent admission. Require the reactive pilot's exact prospective
and physical/public prefix, and require both experiments to bind the same
current learned maze0 predecessor. Preserve all predecessor artifacts.

Exclusive output:go2_independent_reactive_floor_transport_mazes_v1_attempt_001.
Compare exact input/native/runtime/URDF/renderer identities, thread settings,
navigation budget and paused-physics semantics across the predecessor launches.
Require learned cohort scene specifications and public missions to equal the
fixed current layouts1–3 definitions. Reuse the same3000tick shared outbound/
return budget, three warmup observations, ten terminal zero commands, physics,
gait, measured sensing, floor transport, map/contact memory and settled mission.
No maze topology enters the controller; only the coordinate mission and actual
sensor packets do. The low-level gait policy remains present in both methods.

The separate method executions are not claimed to have identical trajectories
or command prefixes on layouts1–3. Each receives its own actual observations
and must independently replay its entire raw sensor-to-command history. The
maze0 prefix is predecessor method-integration evidence, not borrowed evidence
for the independent cases. No later learned trajectory is supplied to reactive
execution. This comparison includes different candidate feasibility logic;
it does not isolate prediction ranking, JEPA training or persistent memory.

Assess CPU topology/affinity, utilization and competition, RAM, GPU and output
space before execution. Require32GiBavailableRAM and40GiBfree reserve plus
10GiBcollection and1GiBpersistence per remaining case. Recheck after input
verification and before each case. One native scene, one OpenCV/PyTorch/BLAS
thread, one task per spawned process. These are admission checks, not OS quotas.

Persist every case collection, raw audit, outcome, resource admission, logs and
artifact hashes. Write exclusive progress after each completed case. Scientific
failure counts in the denominator and execution proceeds to the next fixed
case. Infrastructure/resource failure stops the partial cohort with no retry,
skip, overwrite or deletion. Only all three raw-audited cases yield a completed
aggregate; retain each method's native arrival/retracing/quiet/visibility result
without converting a physical candidate into verified success.

This is a predeclared paired method comparison on three development layouts,
with fresh controller memory each time. It is not a sealed evaluation,
statistical reliability proof, isolated planning/memory/training ablation,
100ms timing qualification or hardware validation. The full goal stays active.
Preparation does not launch this cohort or any predecessor.
