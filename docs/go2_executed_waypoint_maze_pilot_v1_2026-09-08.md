# Prospective executed-waypoint maze pilot V1

After the bound execution-horizon prefix completes with a first changed command,
run `scripts/run_go2_executed_waypoint_maze_pilot_v1.py --prefix-result-sha256 HASH`
once in exclusive `go2_executed_waypoint_maze_pilot_v1_attempt_001`. Authenticate
the completed prefix, predecessor recovery native/readout, unchanged model and
training-only correction, and all frozen source/native dependencies before and
after execution. Require the pure waypoint-score transformation and unchanged
causal observation, map, mission, model forecasts and original constraints through
the prospective intervention. All prior failed outcomes remain immutable.

Run one fresh CPU scene on the same development maze 0, JEPA seed 2026091001.
The only policy change is the source-defined intermediate waypoint utility:
100 ms corrected scoring XY and heading, using strictly past public-pose residuals,
with the same 800 ms contact penalty and all eight original nominal path gates.
View, reentry, exact final-goal scoring and surface vetoes remain unchanged.
No unseen maze route or native position enters the controller. Preserve paired
primary/auxiliary RGB-D, real visible robot geometry, acquisition checks, same
PPO actuator and 2 ms physics, 100 ms actual commitments, and full command tapes.
Keep the original shared 3,000-tick outbound/return mission and arrival criteria.

The collector and raw audit are separate narrow copies of the completed recovery
sources, changing controller identity, output labels and declared collection
allowance only. The independent fresh-controller raw audit retains all original
sensor/model/command/physical visibility and native arrival/backtracking checks.
The existing tested physical/public prefix comparator must match original raw
physics and packets through the new first changed command. This is reuse of one
development layout, with zero additional independent layouts; a passing raw audit
does not turn failed navigation into success or establish a model/memory advantage.

Assess hardware immediately before launch. Require 32 GiB available RAM and
10 GiB collection plus 1 GiB persistence headroom above the unchanged 40 GiB
artifact reserve. The prior allowance was 11+1 GiB; this explicit resource-only
change can censor collection and must be preserved as such. Do not delete bound
artifacts or lower the reserve. The collector checks available space and consumed
volume each decision, retains its final evidence and audit, and reports any
storage stop. RAM admission is a capacity check, not an enforced OS limit.
Only one fresh scene is admitted: the available volume does not fit parallel
collection envelopes. One ordered causal controller cannot split decisions among
workers. No GPU or model training is requested; monitor actual CPU/RAM/storage.

Keep physics-paused computation and ideal sensor timing explicit. This pilot
does not establish real-time control, calibrated model uncertainty, articulated
future-motion certification, production or hardware qualification. Preserve
partial artifacts and any infrastructure/audit/prefix failure at the exclusive
attempt root. Follow completion with a separately bound outcome readout.
