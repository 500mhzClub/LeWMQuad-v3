# Density-routed complete controller replay

Use the original 1,428-observation full-RGB JEPA late-history development prefix,
including the original strict sensing failure at observation 1173. Compare two
fresh independently stored copies of the original assigned model and fresh
VisibilityBatchedFootprintController and DensityRoutedFloorController instances.
Preserve the completed predecessor and all its negative evidence.

The candidate substitutes the exact density-routed floor kernel in registration
and the existing per-observation map cache. The mapper keeps the original memory,
packed indices, patch stores, update bodies, paired camera witnesses and cache
lifetime. No safety threshold, model, model condition, selector, history, command,
mission, observer or sensing rule changes. Existing visibility skipping, receipt
copying and packed/fused/scoped footprint behavior is common to both controllers.

The 854-observation registration replay was completed with every registration
receipt and complete state identical, reducing paired registration time by
18.370435105055247%. Its completion audit reconstructed every actual original
public input fingerprint and every timing total, but did not independently rerun
registration. That evidence does not establish complete-controller performance.

Privately bind the original paired replay function to the two controllers. Keep
its complete 1,428-frame input order, alternating execution order, original raw
public packet and command-tape checks, full normalized decision equality,
1,425 model forecasts, independent tensor storage, unchanged model-state digest
and gradients, and seven retained memory/map/residual/history state checks at
frames 3, 12, 395, 404, 1173, 1418 and 1427. These seven state checks use the same
predecessor hashes and ten existing patch/index type normalization paths; no new
state type normalization is introduced. The state checks do not separately cover
every internal mapper or registration field at every frame.

Normalize only the candidate controller name and added implementation flag before
applying the predecessor's original normalization. Require the baseline decision
hash to equal the preceding completed visibility-batched candidate hash at every
frame. Timings cover complete controller.observe calls, including registration,
map building, learned inference and selection; they exclude reconstruction,
comparisons and physical sensor acquisition. Report all original timing windows
and counts over 100 ms. Shared-host timing is not real-time qualification.

Verify sources and complete original raw/model input bindings before and after.
Reconstruct the whole preceding result through its frozen completion helpers.
Require previous full replay and registration owners ended. Admit resources of
at least 64 GiB RAM, 41 GiB artifact space and four physical CPUs. Create one
exclusive attempt and preserve failure without automatic retry or resume.
Consume no observation 1428, train no model, and run no physics or hardware.
Queued navigation experiments remain unchanged; prospective execution and
independent-maze evidence remain necessary for navigation claims.
