# Reactive nearer-route connector native pilot V1

Run one fresh CPU native scene on reused development maze 0, after the separately
owned view-recovery native job and its audit finish. This tests whether the
prospectively verified nearer observed-route target resolves the original
reactive baseline's blocked waypoint connector during actual execution.

Required completed prefix result SHA-256 is
`99386e0bdc125cf250b70c0925d5d38b5bb01a4cdc7949b646fecc139b35d236`.
Bind its launch, complete input artifacts and frozen recursive sources. The
prefix contains 91 observations with its first changed command at tick 90:
forward `[0.2, 0, 0]` instead of zero. No changed-command outcome was replayed.

Only the high-level reactive controller is replaced in the frozen predecessor
collector and independent raw auditor. The new controller selects an earlier
point on the same observed route when the original intermediate target connector
fails, current clearance and surface checks pass, and an earlier connector
satisfies the unchanged 0.45 m radius. Original feasible actions, exact mission
targets, arrival tolerances, observer, persistent memory, view budget, command
dispatch, infeasibility stops and native guards remain unchanged. The controller
uses no high-level world model or candidate future outcome. Existing low-level
PPO locomotion and public primary/auxiliary RGB-D and body sensing are retained.

Keep the 3,000-command shared outbound/return navigation budget, warmup and final
zero drain. Require 32 GiB available RAM and a full 10 GiB collection allowance
plus 1 GiB persistence headroom above the 40 GiB artifact reserve. Preflight
checks and reports resources without creating output or a scene. Actual launch
uses one worker with one CPU/OpenCV/BLAS thread; no second native scene runs
concurrently. No training or checkpoint selection is performed.

Independently reconstruct all raw sensor, complete controller, command slew,
phase, visibility, timing and mission checks. Compare original and new raw
physical traces and public packets exactly through the observation at tick 90,
and require all earlier commands and the bound first intervention to match.
Actual arrivals and physical return are judged by the original evaluator.
Preserve every failure and partial artifact without retry or outcome relabeling.

Exclusive output: `go2_reactive_connector_maze_pilot_v1_attempt_001`.
This is same-layout development, not independent-maze generalization. Both
reactive policies use persistent memory and lack learned future geometry gates;
this experiment alone establishes no memory or predictive-planning advantage.
Physics pauses during computation; report observed timing without a real-time
or hardware qualification claim. Full navigation remains unachieved until the
complete goal requirements have independent evidence.
