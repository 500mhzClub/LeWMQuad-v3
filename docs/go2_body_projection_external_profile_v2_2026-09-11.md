# Prospective full-history nonblocking external profile V2

This is a new tooling diagnostic after the preserved V1 tracing-lock failure.
Its root is `go2_body_projected_external_sampling_v2_attempt_001`. It neither
resumes nor overwrites the failed V1 attempt. The controller, model, original
1428-observation history, 1425 forecasts, seven state witnesses, costs, actions,
normalization and all before/after original-input admissions remain unchanged.

The V1 prospective protocol remains the scope reference:
`docs/go2_body_projection_external_profile_v1_2026-09-11.md`. The three literal
launcher/checker/watcher derivatives and every declared substitution are bound
in `docs/go2_body_projection_external_profile_v2_source_derivatives_2026-09-11.json`.
The original replay and admission function code is reused with private output
bindings. No original module global is mutated.

## Changed tooling and prerequisite evidence

Add only `--nonblocking` to the pinned py-spy 0.4.2 sampling command, alongside
the new output and execution identities. The parent continues to own a fresh
Python child and a fresh profiler and to authenticate their exact identities.
No pre-existing navigation, training or timing job is attached to a profiler.
The parent reaps both children. The original 100-Hz, idle-thread, full-filename,
30-marker scope is unchanged; no native-stack, GIL-only or subprocess sampling
option is added.

Before creating V2, authenticate the V1 failure audit
`c0ec1fd70fec1ea2e2848e5bb6eeab407c664e4e607e16234036af3d982c20cb`,
all eleven original artifacts and five recorded documents, and every original
source binding. Require the V1 parent, child, profiler and watcher to be ended
on their original boot with original creation times and commands. Preserve the
absence of a successful V1 replay result and complete profile.

The nonblocking owned-child marker smoke is fixed at
`341eb89e317ba7d99ea530c9933df1b8e158ed70a9173f60c6062c64c92c7369`.
The original successful actual-input admission probe remains a prerequisite.
Resource gates remain 64 GiB available RAM, 41 GiB artifact free space and four
physical CPUs. One full CPU replay may run beside the original native queue.

## Completion and interpretation

All original exit-status, source, input, model, complete-row, state-witness,
sample-accounting and zero-reported-sampling-error requirements remain. All
30 markers require valid samples below the marker on the original main thread.
The entire profile, including unmarked rows, must pass the unchanged bounded
parser. Missing or inconsistent data rejects the attempt; no successful subset
is selected. There is no automatic retry, resume or partial-profile completion.

Nonblocking reads do not guarantee an internally consistent instantaneous
stack snapshot. Launch, result and completion receipts explicitly retain this
limitation. Format checks and marker coverage do not remove it. Nominal sample
weights are not measured durations or CPU self times. No sampling bias or
profiler overhead is declared removed. Do not interpret profiled elapsed time
as an unprofiled speed comparison.

The independent checker executes once after the parent has ended, under actual
result/launch hashes. It reauthenticates original raw/model inputs and all
completed artifacts and reconstructs the sampled summary. Descriptive state-size
snapshots remain outside independent reconstruction. The original sensing
failure 1173, negative navigation scope, paused-physics limitations and all
failures remain. The goal cannot be completed from this profiling diagnostic.
