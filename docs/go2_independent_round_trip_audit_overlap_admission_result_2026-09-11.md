# Overlap verifier passed real evidence admission

The source-bound overlap verifier is implemented and passed against the actual
completed four-arm CPU startup and complete old short raw-audit probe. Both
full and quick admission returned normally without changing the supplied
launch component. This check executed no model, raw auditor or native scene.

Full admission rechecked the original bounded raw and corrected-model input
bindings in 25.469299433054402 seconds. The quick check took
0.4897761018946767 seconds. These are observed admission durations on the
shared host, not controller latency or a collection/audit speedup benchmark.

The verifier binds the exact monitored worker/driver preparation, monitor
policy, one-collector/one-auditor limits, probe identities and monitored driver
entrypoint. It checks all four monitor reports and 16 factory decisions against
their original reference, and the complete short raw-audit report against its
original failed-navigation reference. Actual future independent-study audits
must still produce valid monitor evidence for parent acceptance.

Validation: 29 tests passed in 10.08 seconds, session 10747, exit 0. Tests
cover altered evidence, incomplete arms, changed decisions/physics, expanded
qualification claims, changed concurrency or entrypoint, source substitution,
propagated evidence failures and the separate final-verifier requirement in
the real driver. Source preflight and actual full/quick verification ran in
session 39865, exit 0, with 2,054 frozen source bindings.

Verification record:
`docs/go2_independent_round_trip_audit_overlap_admission_verification_2026-09-11.json`,
SHA-256 `7bdcf7fe48efd4e6756bb0d362bf88426950897b8232a615e85186ad01920911`.

This completes the overlap-admission component, not the complete population
launch. The final population verifier, completed extended native queue,
full population input admission and navigation-policy review remain required.
The validated object was explicitly a launch component; no complete native
launch or final approval record was constructed. The queued navigation fixes
must inform policy review before scaling the independent-layout experiment.
No independent navigation, real-time, hardware or measured speedup claim is
made, and the current native queue remains unchanged.

At closing observation, the separate combined optimization replay had written
all 1,428 comparison rows, with exact normalized decisions at its last row.
Its original process was still live and had not published a terminal result.
Final model/state checks and complete input re-admission remain authoritative;
the last row alone is not completion. Case 5 of the six-case native comparison
also remained in its original raw audit.
