# Full moving-prefix composite tensor check

Run only after the complete384 physical trials pass the independent raw audit.
Use the already implemented policy-only composite loader on every available
cell of its fixed600 planned population. Preserve unavailable-cell counts and
all original24 layout roles; no example, checkpoint or outcome selection occurs.

Verify actual four-frame RGB/body/control tensor shapes, finite current inputs,
prospective40-tick initial or30-tick moving plans and zero/invalid unknown tails.
Verify contact/motion/future-image masks, finite known targets, absent censored
targets and strictly noncontact motion. Compare complete current tensor hashes
across every action sibling in each of120 layout/past-action conditioning groups.
No group may cross roles or duplicate an action. The existing diagonal cells
ensure every conditioning group is represented even if a new suffix is unavailable.

During loader construction and item access, instrument both ordinary and Path
file opening. Reject raw physics, camera-world audit, native-contact/topology/
event and protected paths before opening them. The allowed supervision labels
and result/audit identity metadata remain separate from model input tensors.
This checks the actual policy-only I/O boundary; it does not forbid a separately
declared privileged raw auditor from doing its evaluation work.

Fixed fresh output:
`.generated/go2_moving_prefix_tensor_check_development_v1_attempt_001`.
Bind physical/full-audit/source/test/loader/protocol/input identities at launch
and verify afterward. No fitting, new physics, retries, source substitution or
final evaluation. This600-cell view does not silently include the other698 old
temporal windows; the next training protocol must state its population and
sampling comparison explicitly.
