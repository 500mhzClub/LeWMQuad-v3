# Paired native planning-map persistence outcome readout

Run scripts/read_go2_current_observation_planning_maze_pilot_v1.py only after
the current-observation planning native pilot and baseline learned native/
readout have completed. Supply actual --native-result-sha256 and
--learned-readout-sha256. Exclusive output:
go2_current_observation_planning_maze_readout_v1_attempt_001.

Bind both completed native result/artifact/launch sets and the exact baseline
readout result/launch. Reject mismatched baseline result identities, runtime,
scene, sensor/actuator/renderer, mission, numerical thread and budget settings.
Require the same assigned model, complete1250sample/11packet native prefix,
all eleven prospective decisions and eight identical forecast banks at the
intervention. Require retained contact, localization, prediction/residual,
mission/settling and scan state to be declared. This is a planning-cell
persistence comparison, not a fully memoryless comparator.

Preserve the baseline readout's actual-motion and outcome calculations for the
candidate. Include original full arrival windows, outbound/return traversals,
physical retracing, terminal quietness, physical candidate result, strict
visibility, hard measurement failures and verified-round-trip result. Retain
absent return evidence as null. Include actual path length, terminal position,
outbound distances, duration, actions, controller/acquisition/physical terminals,
registered/transport pose accuracy, paired-camera use and receipt-inclusive
timing distributions. Add the candidate's current planning-cell counts along
its decision trace; do not rewrite any original outcome or reselect a controller.

Assess hardware before execution and require8GiBavailableRAM plus128MiBabove
the40GiBartifact reserve. This readout loads completed files only, no model or
native scene. Verify bound sources and all input artifacts before and after.

One reused-layout pair cannot establish statistical reliability, independent
maze generalization, JEPA training benefit, a memory advantage across layouts,
100ms execution or hardware qualification. The report preserves physical
candidates and strict failures separately. The full goal remains active.
