# Four-arm adapter factory startup compatibility check

The original independent-study factory loads the expanded wrapper rejected by
the planner's evaluation-interface guard. Add separate adapter factory and
multiarm collector/audit files. Keep the fixed four arms, 32-case/eight-layout
assignment, selected model states, controller classes, acquisition, command,
audit and outcome calculations unchanged. The new factory loads the tested
byte-identical AllPhasePlannerModel and explicitly rejects incompatible types
or training mode. This change does not add the frontier intervention.

Exercise the actual old and adapted factories, including their real controller
`observe` methods and model inference, for all four arms using cases 0–3 of the
fixed assignment (layout index 0). The factory instantiates that study's fixed
public mission. Supply only four raw public sensor packets from the already
completed original development maze2 full-JEPA startup; do not render, observe
or navigate any independent layout. The study mission differs from the original
physical scene's mission, so do not claim reconstruction of original native
decisions or a valid independent-layout episode. This is factory compatibility
evidence with explicitly configured public goals.

Both factories receive the same arm, mission, original raw packets and fresh
originally assigned models. Require three actual completed zero warmup commands
and identical paired decisions through them. At observation 3 require the old
learned wrapper failure to reproduce and the adapted learned selector to receive
valid forecasts. Compare every forward-output tensor and the selected forecast
bank against direct original expanded-model inference. Require the reactive arm
to remain model-free with all four complete paired decisions unchanged. Compare
observed, map and retained contact state. Consume no packet after the candidate's
first planning request. Execute no command and infer no counterfactual outcome.

Bind the completed original six-case failure result
`a08496e1d62ec6e00ffae3d85729cc7cf069c2fddcb60d421910c83d30556e80`, earlier
multiarm verification, all inherited/new sources, and the currently queued
frontier source identities. Preserve all those files. Verify original completed
artifact/model/correction bindings before and after; do not claim another full
coefficient/training reconstruction. Use one inference/BLAS/OpenCV thread, 32 GiB
available RAM and 40 GiB disk reserve plus one GiB allowance. Exclusive output
`go2_independent_adapter_factory_startup_v1_attempt_001`; no retry/resume.

The independent population remains unexecuted and requires complete development
review, full input admission and a prospective population launcher. Compatibility
success cannot establish navigation, model-training, predictive-planning or
persistent-memory advantage.
