# Readout of every completed supervised maze1 action score

Use only the completed first worker of the original supervised cohort, pinned
by its original launch, completed worker terminal and first cohort-progress
SHA-256 identities in the runner. Authenticate all original1672 source bindings,
the worker's18119 artifact bindings, saved raw audit, exact physical prefix and
unchanged assigned supervised model. Independent14939 has already verified these
bindings before/after and the complete first-case evidence. The raw audit passed
with no arrivals/crossings,1501 right turns,1498 left turns and1 hold. The original
cohort continues layouts2 and3; this readout cannot alter or skip those cases.

Population is all3014 observations and3013 completed commands, including all3000
actual selections. For each, restore the saved input candidates/action/contract
and800ms scoring-horizon field, then call the original score_waypoint_execution
with the same saved strictly causal residual receipt. Require its complete output
to equal the saved selection exactly. This checks the actual100ms pose potential
and800ms contact score, existing phase/surface/path feasibility and original
tie handling. No model, observer or controller reexecution is claimed.

Tabulate every candidate's feasibility, utility, distance/alignment progress,
contact score and utility margin to the selected action. For each translation
candidate record all existing phase, surface and segment vetoes. Check each saved
path summary against all eight saved segment flags. Do not recompute geometry
or infer an unexecuted physical outcome. For selected turns, distinguish no
feasible translation, feasible translation with greater geometric potential
progress but lower total utility after contact cost, and the remaining cases.
Count consecutive actual turn-direction changes and retain the first example
of each reason. No contact coefficient, horizon, command or controller changes.
These are retrospective score decompositions, not alternative rollout outcomes
or calibrated contact probabilities. Do not infer full-cohort JEPA advantage.

Before/after verify all original source/output bindings; record the original
worker's completed transitive input verification honestly. This readout does
not independently rerun that transitive verifier or raw-sensor reconstruction.
Its numerical check directly reuses the frozen original scorer. Six initial
saved selections reconstructed exactly in read-only41024 before source freezing;
the reported analysis must cover all3000 and match the original audit counts.

Use one CPU process,8GiB RAM admission and16MiB output allowance above the
original40GiB storage reserve. Inspect CPU, memory, GPUs, competing processes and
both volumes before work; one readout fits alongside one native scene. These are
capacity admissions, not OS limits. Write only to exclusive
go2_supervised_maze01_turn_scores_v1_attempt_001. Preserve any failure and all
original outputs. No training, new native scene, scientific retry or deletion.
