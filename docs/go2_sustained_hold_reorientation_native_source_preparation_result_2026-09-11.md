# Native collector and auditor prepared

The sustained-reorientation native collector and raw auditor are implemented as
separate source files. Whole-file syntax-tree checks prove equality to the frozen
original hold-run implementations except for the prospective controller import
and name, experiment status labels, and the new enabled receipt/assertion.
Original sensing, renderer witnesses, model replay, physical command audit,
navigation budget, terminal drain, timing measurements, contacts, visibility and
round-trip success checks remain in the comparison.

All nine source-equivalence and mutation tests passed in 2.58 seconds, session
14268, exit zero. Mutations to budget, sensor arguments, terminal drain, tracker
requirements, visibility failures and complete raw decision matching were rejected.
An initial test run had eight passes and one test-fixture failure: the budget
mutation string matched both the constructor and result receipt. Narrowing that
mutation to controller construction corrected the test. No runtime implementation
changed during this correction; the initial result is retained in the record.

The source preparation verified 1,976 bindings, including the complete running
raw-replay source closure. The preparation record is
`docs/go2_sustained_hold_reorientation_native_source_preparation_2026-09-11.json`,
SHA-256 `5672c15cca06fe6978af91b6b19043d4a3519a47aa6e8da721cc8aefc6c67f26`.

The original raw replay remains process 2813368, creation 1789115323.91, session
87754. It reported observation 50 reconstructed and matched, so 51 observations
have been verified by the running paired loop. This is not a completed prefix.
Its source remains frozen at launch SHA-256
`6f68017be0f68198ab08af97fb4d90f33ebbdc2b1b56f8da123e8b34b4dd89df`.

The original contact experiment has advanced from admission to native execution.
Its launch SHA-256 is
`8d6a736c2f1f6de1f461fa6952341c1a7f3d321ccdd37881ac27a6da35191b8c`,
with 1,985 source bindings and case
`full_supervised_commitment_contact_anchored_maze_02`. Its original parent is
2808232; tracking and extended budget remain queued behind it. The sustained
recovery collector has not been launched and no original queue source changed.

Next implement and check the raw-prefix completion/native-prefix comparator,
requiring all 407 original/public observations, all 404 model forecasts, the exact
first changed request at observation 406, and the complete physical trajectory
before that intervention. A fresh native launcher must require the completed
replay, completion of the original queue, and available resources. Source tests
alone do not establish navigation, real-time or hardware qualification.
