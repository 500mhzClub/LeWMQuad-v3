# Automatic CPU replay handoff after the original JEPA worker

Own one raw replay launch, without launching a native scene. The exact original
adapter JEPA worker is PID 2662101, created 1789031235.91, owned by adapter batch
PID 2659758, created 1789030196.29. Bind boot identity, complete command lines,
parent relationship at registration, original launch identity and all inherited
and new source hashes. Poll the same worker every 30 seconds for up to 48 hours.
Quiet output does not authorize a restart or a different worker.

After the worker ends, require its complete exact case terminal and bound raw
audit, retaining scientific failure. Pin that terminal SHA-256 and launch
`replay_go2_hold_reorientation_maze02_prefix_v1.py` once with the explicit hash.
The child independently authenticates all original artifacts and reruns full
original input admission before replay and at completion. It requires 32 GiB
available RAM and a 40 GiB reserve plus one GiB artifact allowance, and uses one
CPU thread per compute library. A separate native batch worker may continue.

Authenticate the child's original-worker identity, source/output bindings and
complete 406-observation, 403-forecast, first-change-at-405 result. Preserve all
failures; no automatic retry, replacement, source mutation or candidate native
execution. Exclusive waiter output
`go2_hold_reorientation_raw_prefix_wait_v1_attempt_001`. The existing frontier
waiter retains priority for the next native experiment after the six-case batch.
