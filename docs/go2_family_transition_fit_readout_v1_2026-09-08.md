# Complete paired transition-fit readout V1

After the fixed six-fit study completes, authenticate its exact result, all
artifacts, all7200 ledger rows and final model identities, and reconstruct every
role score from saved raw predictions. No optimization, native commands or
checkpoint selection. Bind the exact study result SHA-256 before the readout.

Retain every method, role, all/initial/moving scope and both parameter clusters.
Report unweighted two-cluster macro position error, yaw error and contact Brier
only when both constituent values exist. Preserve counts and undefined yaw.
Compare JEPA with supervised rollout for each input variant; supervised rollout
with direct for each variant; and full with no_rgb for each condition. Report
all left-minus-right differences with their constituent cells. No selective
head, horizon, cluster, role or checkpoint reporting.

There is one optimization seed and only two parameter clusters per role;
mirrored layouts and overlapping windows are dependent. These are descriptive
development comparisons with no significance, calibrated probability, learned
benefit or navigation qualification claim. Training scores are resubstitution.
Full-JEPA seed2026091001 remains the fixed native bootstrap candidate regardless
of this readout. Native integration must retain its own prospective scope.

Freeze new readout source and focused reduction tests before execution. Verify
study/source bytes after reduction. Write only to exclusive
`go2_family_transition_fit_readout_v1_attempt_001` under the established
navigation artifact base. Retain all original failures and fitted models.
