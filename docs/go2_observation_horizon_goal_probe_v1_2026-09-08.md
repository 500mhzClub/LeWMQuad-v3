# Trained observation-horizon native goal probe V1

After full eighteen-fit admission and descriptive readout, run exactly the
fixed first-seed full-JEPA and full-direct short-horizon models on the existing
`family_episode_039` integration layout. This assignment precedes results.
Use both final seed-2026091001 models, without checkpoint or seed selection.

The new native selector must consume the trained eight-step 100–800-ms
forecast bank. Score waypoint or scan progress, test measured surfaces and
test the nominal predicted chord at the actual first 100-ms horizon. Execute
one 100-ms command and replan on the next public observation. Require exact
forecast clock metadata before every timed scoring/filtering helper; old
500-ms outputs are incompatible. Do not interpolate forecasts or label a
planned interval as completed if the native command was interrupted.

Keep the existing six command choices, potential coefficients and contact
cost, public sensing, overlap-retention RGB-D observer, floor and surface
memory, route/view logic, 0.45-m nominal radius, 240 navigation ticks and
0.04-m observed-arrival rule. Use the original actuator auditor and native
0.06-m goal/quiet/contact gate functions unchanged. The ten-command drain,
physical stops, sensor gates and failure accounting remain intact.

Freeze the new source closure, full fit/readout receipts and both completed
cadence-case outcomes/readout before the exclusive
`go2_observation_horizon_goal_probe_v1_attempt_001` root. Use the same two-case
order seed 2026091501. Check exact original physical/public four-frame warmup
prefixes and replay every actual decision with a fresh copy of its assigned
unchanged final model. Preserve both cases and all failures, and verify every
bound source and input after execution. No retry, resume or replacement.

Run two independent fresh processes with one numerical thread each, within
the measured four-process native benchmark capacity and the completed
two-scene cadence probe's resource envelope. Recheck 32 GiB available RAM
and 8 GiB output allowance above the unchanged 40-GiB reserve, and monitor
resources. This does not claim optimal concurrency. Native physics remains
paused during compute; report complete-iteration timing explicitly.

This reused layout supplies zero independent novel mazes. Report both goal
outcomes, all measurement gates and terminal reasons, regardless of success.
Even successful integration would leave matched reactive/nonpredictive and
memory/planning baselines, independent layouts, physical backtracking,
realistic sensing/timing and bounded hardware evidence outstanding.
