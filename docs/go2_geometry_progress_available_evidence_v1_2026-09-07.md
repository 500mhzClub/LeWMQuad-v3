# Geometry-progress available-evidence readout V1

Both prior raw-audit attempts remain terminal failures. Original audit: command
representation mismatch, zero completed episode reports. Separate exact-command
readout: completed episodes 000--007, then failed in episode 008 with
`sufficient finite interior depth rays`. No prior attempt is resumed or edited.

This separately named posthoc readout uses only the same frozen 24-episode
collection and the exclusive root
`go2_geometry_progress_available_evidence_v1_attempt_001`. No simulation,
recollection, fitting, threshold selection or new runtime control. Bind original
collection, both failed launches/terminals, completed predecessor reports, new
source, protocol and focused tests before processing.

One diagnostic control-flow change: the unchanged native depth comparison mask
still requires at least 1,000 finite interior rays for a passing frame. A frame
with fewer rays is now recorded as `INSUFFICIENT_NATIVE_DEPTH_RAYS` and
`within1mm=false`, allowing the remaining episodes to be described. An empty ray
population has null maximum error, never zero error or an inferred pass.
Nonfinite errors still fail terminally. Every other raw sensor/contact, native
geometry, command/actuator, setup, stop and outcome check remains intact.

This changes early termination into explicit failed-measurement accounting; it
does not make an insufficient frame valid. Keep separate fields for exact raw
packet reconstruction and native depth-verification success. Report all failed
frames, ray counts and actual capture clocks. All episodes, contacts and missing
futures remain in the denominator.

The original predeclared native progress and mirrored-action gate is unchanged.
The combined design-and-measurement gate is false if any frame fails the original
depth evidence criterion, even if native action outcomes show informative
reversals. Such a result does not authorize scaling the unchanged collection
design into a learned comparison. The sensing failure needs a separately frozen
successor with valid acquisition/censoring semantics.

The tensor adapter may verify the schema of actual recorded packets and masks
without fitting a model; tensor materialization is not measurement eligibility.
No image, motion or post-stop future is fabricated. Source-level tests identify
the isolated diagnostic change. Publish original failures and limitations beside
the final available-evidence result. No independent-maze, RGB/JEPA benefit,
real-time, navigation, deployment or overall-goal success claim.
