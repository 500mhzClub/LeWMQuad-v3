# Exact pulse observation pairing: recorded development diagnostic V1

Use only the completed coupled-room-return attempt's three fixed conditions,
bound launch/result/raw-audit identities and exact RGB frame metadata, command
tape and decision logs. No current running successor data, native pose labels,
image payload indexing, fitting, training, GPU, new physics, validation split
or generalization claim. Preserve the original0/3 mission failures.

Enumerate every logged new-pulse event without selection by return success,
visual tracking, state estimation error or future availability. Retain windows
with missing history or future targets and report them explicitly. The local
logged command and declared2/5 pulse ticks define the prospective requested
prefix followed by20 guaranteed zero-command ticks. They are requested
commands, not claimed executed base velocities: the downstream gait/slew
dynamics remain part of the prediction problem.

Use actual four-frame causal history at offsets-.3,-.2,-.1,0 s. Targets occur
at+.5,+1,+1.5,+2 and exactly+2.2/+2.5 s; the final three slots are unknown-plan
padding. A target at departure+k consumes tape commands [departure,departure+k),
never the command selected at the target observation. Validate every tape
interval and completed flag. A changed, incomplete or absent commanded prefix
censors its affected targets; a missing RGB frame is separately recorded.
Do not extend a short pulse's final target to2.5 s or fabricate future RGB.

Raw target-image availability is deliberately separate from visual-estimator
availability and future native motion/contact labels. A tracking failure is
not evidence that a recorded image does not exist. The tensor materializer's
inputs and future-target dictionaries are separate; inference accepts only
inputs. This diagnostic materializes metadata only; its focused tests check
one actual recorded RGB/body pairing and target timestamp corruption.

Exclusive output `.generated/go2_pulse_timed_pairing_diagnostic_v1_attempt_001`.
Freeze protocol/module/checker/test sources at launch; bind old input metadata
before and after enumeration. Report all windows, history-ready counts, action
duration populations, target statuses and exact minimum-brake endpoint coverage.
Source tests and this index are not a trained JEPA or a useful-navigation result.

Next: independently reconstruct target-only native displacement/contact labels
with explicit physical-stop censoring; adapt the matched loss to partial timing
and separate image/outcome masks. Then collect independent scene/action/state
coverage and freeze layout splits before fitting. Three scripted room streams
cannot supply the required independent-maze predictive-training evidence.
