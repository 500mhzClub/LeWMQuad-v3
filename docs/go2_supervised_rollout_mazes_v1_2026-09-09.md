# Matched supervised rollout development cohort V1

Execute layouts 1, 2 and 3 once each, in that fixed order, using the original
MeasuredFloorTransportController, collector, raw audit and native evaluator.
These are repeated development layouts, not new independent generalization
evidence. Preserve every outcome, including tracking failures, infeasible
actions, visibility failures and exhausted budgets. A scientific negative does
not stop the cohort; an integrity, raw-audit or prefix mismatch stops it and
retains partial evidence. No automatic retry or outcome-based case selection.

The assigned model is seed_2026091001_full_supervised_rollout, with corrected
state SHA-256 171c8576d2c3fcfd0ce698351acf86a05f2ea29bdf829041e98641cdac778a73.
Use full inputs, the rollout_outcomes head and its own authenticated training-only
translation correction. The paired JEPA objective, common training schedule,
initialization, input data and correction procedure are admitted from the
completed matched-objective prefix experiment. No new training, seed selection,
parameter search or controller change is part of this cohort.

Authenticate learned cohort result
a0ac72330a6c34fbae7812a360b22189086f16bd83f586d71e769539ddc2e720
and matched objective prefix result
edf680896e11ef22096cf56323094be2de24b712405466149cdeeb59bab2a6fd,
including their complete source and artifact bindings and upstream model inputs.
Require the prefix launch's original-cohort map to equal the new launch's map
before reusing its full verification. Keep all original scene specifications,
public missions, robot identity, numerical and renderer settings, command limits,
shared outbound/return budget and strict visibility gates.

Each fresh episode must reproduce the first 900 physics samples, all four paired
public observations 0–3, the first three completed requests and all four saved
supervised decisions. Compare the original JEPA decisions and full shared
observed state against the saved comparison. At observation 3 require the actual
intervention JEPA left arc [0.16, 0, 0.45] to supervised right turn [0, 0, -0.45].
Require the one paired full forecast bank. Never compare later physical outcomes
as if the changed commands shared a trajectory. Later outcomes come from this
fresh execution and the unchanged evaluator. Partial execution of the changed
command is recorded honestly and does not invalidate its preceding prefix.

Use a fresh assigned model/controller/history for each case and a separate fresh
model for its complete raw replay. Retain collection, audit and prefix artifacts
as each completes, even when subsequent verification fails. Publish paired
outcomes only after all three cases and final source/artifact verification finish.

Run one native scene at a time, with one OpenCV/BLAS thread and one case per child
process. Refresh CPU topology/affinity/load, RAM, GPU/VRAM, storage and competing
jobs before execution and monitor during it. Admission uses 32 GiB available RAM,
40 GiB storage reserve and 11 GiB per remaining case; these are capacity checks,
not enforced quotas. CPU-only preflight may overlap an existing native job when
resources permit. Native execution follows the residual maze-2 and tracking
maze-1 pilots; do not overlap native scenes or restart their attempts.

Output: go2_supervised_rollout_mazes_v1_attempt_001 under the established external
development artifact root. Preflight creates no output or native scene. This
experiment alone establishes neither statistical reliability nor isolated JEPA,
memory, real-time, navigation or hardware qualification. Preserve sealed custody.
