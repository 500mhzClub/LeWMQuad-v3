# Causal recorded-observation prefix for planning-map persistence ablation

Use the completed, fully raw-audited eleventh maze0 episode as the actual
observation/command source. Its first64observations precede the floor transport
intervention at1904. The current learned native launch is read only for its
frozen source/runtime/model identities; do not consume that running attempt's
observations or outcomes. All predecessor artifacts and source bindings must
verify before and after replay. Preserve every original failure.

Exclusive output:go2_current_observation_planning_prefix_v1_attempt_001.
Run scripts/replay_go2_current_observation_planning_prefix_v1.py first with
--preflight-only, after assessing current hardware and competing jobs. Use one
CPU replay/OpenCV/BLAS thread, the exact assigned full-JEPA checkpoint and
training-only correction. Require8GiBavailableRAM and256MiBabove the40GiB
artifact reserve. Enforce a bounded compressed stream and refresh storage during
replay. This job creates no native scene, trains no model and executes no command.

Replay at most64actual paired public observations in order. Fingerprint public
inputs before/after each controller call. Compare raw/admitted visual evidence,
accumulated map receipt, auxiliary partition, coordinate mission/settling and
executed residual history exactly, allowing only the validated predecessor
floor-reference wording and explicit new variant metadata. If both policies
produce forecasts, compare the entire raw six-action forecast bank exactly.
Planning proposals, masks, scores, selector scan state and action choices may
differ because the available planning cells differ. A pending current forecast
may differ when the intervention changes terminal state; executed past residuals
may not. Record the complete new decisions and their current-map witnesses.

Compare each old request with its completed actual command. Stop immediately
at the first changed request or terminal state. Do not read the next recorded
decision or public packet, and do not use the next old observation to label the
new command. A64frame replay with no difference is a bounded equivalence result,
not a physical intervention or evidence of a memory advantage.

Require unchanged model weights and absent parameter gradients after replay.
Record source/artifact identities, shared-state checks, compared forecast count,
first command/terminal difference, final old/new requests and exact changed
selection. No unexecuted outcomes are inferred. A later fresh native experiment
needs its own source/protocol freeze, admission, physical/public/prospective
prefix comparison and full raw audit. Queued learned/reactive cohorts stay fixed.
