# Four-observation raw replay for ordinary commitment contact

Use the fixed saved boundary result
`f20100955ca2e4bb39c91a9375cb270ff48731d5e721aacf8d5b314821381808`.
The case is the already assigned full supervised-rollout expanded-model maze-02
case, second in the original six-case adapter batch. Model state is
`755c074325af96d53649aba4927937113d8fab561ea05341a2f3c328598b2bb5`.
No model, layout, coefficient or horizon is chosen from a sweep.

Runner `scripts/replay_go2_commitment_contact_anchored_prefix_v1.py` uses
exclusive root `go2_commitment_contact_anchored_raw_prefix_v1_attempt_001`.
It requires the exact original completed worker terminal SHA, all collection,
audit, readout and adapter-startup identities, unchanged sources and full
original input verification before and after replay. The entire six-case parent
need not finish for this CPU-only replay. A partial collection or provisional
readout cannot satisfy original worker admission. Source-only preflight verifies
preparation and resources without reading a nonexistent terminal or launching
inference. Retain any failure with no automatic retry or replacement.

Run fresh original ResidualAnchoredContinuationController and candidate
CommitmentContactAnchoredController with separately loaded, independently stored
identical models. Use only actual original public packets 0–3 and their complete
recorded command endpoints. Check input fingerprints after each controller call.
Reconstruct every complete original decision and require the complete candidate
decision to match only the already declared ordinary contact-score transformation.
Require the saved selection identities, three unchanged warmups, one complete
six-action forecast bank and the first changed request at observation 3:
left turn `[0,0,0.45]` to forward `[0.2,0,0]`. Stop before observation 4.

At every one of the four observations, compare complete retained contact memory,
floor/occupied cells, model input history and all executed residual fields except
the new pending forecast. Preceding pending forecasts must match before reading
each shared packet. Independently reconstruct each new pending record from its
actual requested action, raw first-step prediction, current observed pose and
clock. Warmups must have no pending forecast. At the changed request, pending
predictions appropriately refer to different selected actions; no unobserved
successor target is admitted. Check both unchanged model states and absent
gradients after replay. This is a complete four-frame check, not a whole-episode
equivalence or navigation result.

Require 48 GiB available RAM and 41 GiB free artifact space before admission and
again before output creation. Use one numerical/OpenCV thread, deterministic
inference and no native scene. Existing batch/frontier/hold native ordering is
unchanged. The earlier old-model contact-horizon pilot's visual-evidence failure
remains a relevant negative result. Even a passing replay proves neither
physical clearance, useful exploration, arrival/backtracking, realistic timing,
independent-layout reliability nor hardware readiness. A native trial requires
separate prospective preparation and complete physical/public prefix and outcome
auditing.
