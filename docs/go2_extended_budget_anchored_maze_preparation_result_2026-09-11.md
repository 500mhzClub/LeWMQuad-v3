# Longer-budget pipeline prepared; execution remains pending

The separate development composition supports 4,000 navigation ticks, 4,013
command intervals and 4,014 observations. It retains the original anchored
controller and native evaluator. The prospective change is the global mission
budget and the recording/resource limits needed to support it.

All 41 focused tests passed in 3.70 seconds (session 7568, exit 0). They include
the actual collector loop with synthetic session/controller dependencies through
the new terminal boundary, all ten zero-command drain intervals and persistence.
The actual command auditor accepted the complete synthetic 201,400-sample trace
and rejected a changed final command sample. The renderer auditor accepted
4,014 synthetic paired witnesses and rejected a changed final pixel binding.
Replay manifest, compressed stream, auxiliary packet and cooperative session
boundary tests cover the previously shorter recording limits. The original
module bounds, function code, closures and undeclared dependencies are unchanged.

These are synthetic boundary and composition tests, not a full raw sensor/model
replay, native simulation or return result. No new episode was started, and the
existing native queue was not changed. A separate launcher, completed original
audit/queue admission and original-prefix comparison are still required.

Preparation checked all 1,908 original launch source bindings and produced a
1,913-path union including the new implementation, test, protocol and existing
physical/budget diagnosis witnesses. Session 61359 exited 0 and rechecked the
source union after writing the exclusive preparation record:

`docs/go2_extended_budget_anchored_maze_preparation_2026-09-11.json`

SHA-256: `82ab7ad505843c75b481dae66a986631ad85f798090a5c69bcca8e580599b962`.

At the 02:41:56 UTC check, original native audit worker 2775051 and packed-index
replay 2786620 were both running. Neither had a result or failure record. The
packed replay had written frame 689 with an exact normalized decision. This
observation is historical progress, not a terminal completion claim.
