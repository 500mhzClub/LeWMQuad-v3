# Corrected JEPA retained floor visibility diagnosis V1

Reconstruct the same complete corrected JEPA recorded run and verify every
decision and model state, under the preceding floor-coverage diagnostic's
single-CPU, one-thread, 256-observation, 8-GiB RAM and 256-MiB output envelope
above the 40-GiB reserve. Launch no scene or optimizer. Freeze the complete
source chain and previous result identities before the exclusive
`go2_training_bias_floor_visibility_v1_attempt_001` output.

At the no-candidate terminal, reconstruct all six front-right-foot centres and
their unchanged 8-by-8 closed-square subdivision. Reproduce original coverage
exactly and require identical centres and covered-tile counts to the preceding
diagnostic. For each tile and every retained observation, calculate the same
outward pixel rectangle, optical-range and image-boundary checks, and count
invalid floor quads with the same retained prefix array. Preserve all visible
frame checks and the current-frame projection. Record the best visible frame
by minimum rejected-quad fraction, then count, then earliest frame; this is an
inspection witness and never a selected action or changed floor rule.

Distinguish covered tiles, tiles never entirely inside a retained camera
frustum, and tiles inside a frustum but lacking a passing measured-floor pixel
rectangle. An in-frustum projection is not proof of unoccluded observation.
Neither rejection class establishes collision or absence of physical support.
Do not infer terrain, waive unknown area, alter a controller or issue any
sensor-placement, real-time, independent-maze or hardware qualification claim.
Reverify all sources, inputs and predecessor diagnostic artifacts afterward.
