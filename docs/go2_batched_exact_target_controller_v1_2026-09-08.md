# Batched map-insertion full-controller benchmark V1

Authenticate both completed exact-mission-target native cases and raw audits.
The original measured-bound insertion and the previously tested batched version
must reproduce every recorded controller decision exactly. Wire the batched
class only into the six initially empty primary/auxiliary all/floor/other
indices. Inherit all sensing, classification, contact, coverage, query, target,
model, planning, arrival and execution behavior unchanged.

Run one fresh original and one fresh batched replay per fixed corrected model.
Use original-then-batched for the first assigned case and batched-then-original
for the second. Require exact complete decision streams, equal model hashes and
unchanged weights. Record uninstrumented controller.observe wall time per frame;
packet reconstruction, JSON comparison/serialization and model loading are
outside that timer. Compare the same recorded selection frames and preserve
all timings. A controller-only ratio establishes no acquisition, whole-loop,
real-time, native execution or navigation improvement.

The combined nine source/index tests passed in 2.99 s, covering exact queries,
counts, order, outward bounds, witness ownership, invalid/capacity failures,
floor/unknown/non-foot contact behavior and inherited controller semantics.
This full replay is required before any native adoption. Keep the running
unoptimized native trial immutable; launch the benchmark only after that trial
and its readout complete. Do not run another substantial job during timing.

Exclusive go2_batched_exact_target_controller_v1_attempt_001 root. One CPU
process/numerical thread, current hardware check, 8 GiB available RAM and
256 MiB output allowance above the 40-GiB reserve. Freeze and reverify complete
source and input bindings. No retry/resume, native commands, fitting or changed
scientific thresholds. Realistic sensing, asynchronous physical timing,
independent mazes, backtracking and baseline comparisons remain outstanding.
