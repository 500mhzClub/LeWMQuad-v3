# Independent population queue admission: implementation result

This goal turn made progress by implementing the missing admission of the
original frontier, hold-reorientation and contact-scoring queue. It did not
select a policy or consume independent-layout sensor data. The navigation goal
remains active with 39 completed audited development episodes and zero verified
round trips.

`scripts/independent_round_trip_queue_completion_development.py` requires the
three original waiter processes to have ended under their exact PID, creation
time, command and boot identities. It verifies each frozen waiter launch and
complete result, the original adapter-batch/frontier/hold/contact dependency
chain and the already completed raw-prefix result identities. It reexecutes
the original native completion verifiers and optionally their full input
admissions, authenticates the complete collector/worker artifact rosters, and
reconstructs the contact/timing readouts from the original saved arrays.

All negative scientific outcomes remain in the ordered admission. Failed or
incomplete evidence cannot become a completed experiment. The returned record
explicitly leaves policy review and population execution false. The final
launcher still needs its separate complete population input admission, the
actual policy review and a frozen verifier joining those records. No helper
can use the new layouts to choose the final policy while retaining an unseen-
layout claim.

Final tests: session 43049 exited 0, 64 passed in 3.11 seconds, comprising 48
new queue tests and 16 original waiter tests. They cover live/reused owners,
incomplete result rosters, changed dependency links/prefixes/children, original
failure files, modified raw/log files, inconsistencies hidden in rebound result
metadata, original-verifier failures, readout reconstruction and refusal to
convert queue admission into policy approval. The tests substitute original
completion and input verifiers and use synthetic native file rosters. They
exercise the existing numerical contact/timing readout but do not execute a
native scene, model inference or the original full native raw audits.

The initial test session 88054 exited 1 with 42 setup errors: the synthetic
fixture constructed only part of the three-result identity map before asking
for the dependency receipt. The fixture was corrected before the final run;
production admission was not weakened.

Source and static verification bound 2,073 files, preserving the previous
2,009-source runtime and all original waiter source bindings. All three exact
completed raw-prefix result hashes were checked against disk. The actual
live-owner gate rejected with `original frontier native waiter is still live;
preserve its queue ownership`. No completed queue admission was attempted.

Verification record:
`docs/go2_independent_round_trip_queue_completion_verification_2026-09-10.json`,
SHA-256 `2bca5d360aa409cb4a8e552f8934d044495c7eca5c7f4e0d3fd222418e13442e`.
The implementation, tests and source protocol are now frozen by that record;
make later changes under new prospective names.

At the last native check, direct-model worker 2709978 retained creation time
1789053472.31 and was running with 14,252.5 cumulative CPU seconds. Its raw
audit, readout and worker terminal files were absent. The original ordered
batch and queued owners were not changed or restarted. This remains an active
audit rather than a completed additional episode.

Next, retain the original live queue and authenticate each completed outcome
when available. The final policy review and input/queue/policy verifier still
depend on those outcomes. Reliable independent navigation, physical backtracking,
matched attribution, realistic sensing/timing and bounded hardware evidence
remain unproven requirements of the full goal.
