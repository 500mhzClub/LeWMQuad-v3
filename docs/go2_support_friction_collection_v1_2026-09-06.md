# Fresh matched support-friction collection V1

Execute once each nominal1.0 and lower-friction0.15 condition after the successful
zero-step preflight. Reuse its exact source, specifications, common seed2026090641,
appearance seed2026090643, unchanged learned gait and225-command schedule.
The zero-step builds generated no motion/outcomes. A full condition is12000
physics samples over24s, including1.5ssettling,226RGB-D captures and1126support
predictions. Both contact hypotheses remain frozen shadow consumers.

Before physics, verify all27robot+1ground coefficients and28ratios. Recheck
after settling, before every command tick and at termination. Read native
solver arrays and cached values. Report the intervention on both bodies,
including non-foot robot geometries; wall coefficients remain unchanged.
Require10GiB disk reserve before each condition. CPU-only simulation, software
offscreen rendering; no hardware or training operation.

Install transducer identities before recorded settling. Acquire all native foot
loads live, without ground-role filtering, retaining stop rows. Verify startup
using the existing evaluator-only bounded setup checks before the command tape;
that region is not a navigation map or a future-motion clearance prior. Retain
the existing native body/contact checks and post-admission0.3m/s/domain checks.
No physics after a PhysicalStop, including no synthetic zero-command tail.
Prediction faults latch and are retained; shadow predictions never select or
modify commands. No retry, resume, overwrite or adaptive friction/timing change.

Persist raw contacts, joint/IMU/RGB-D acquisition, live foot samples, all support
predictions, friction checks, commands, gains, initial/terminal geometry and
terminal reasons. Save all predictions before any native performance scoring.
The collection's terminal status means data are present, not that all raw
acquisition or model behavior has been audited.

The next separately bound audit must reconstruct sensor forces/predictions and
command/clock/guard/friction evidence, then score every available segment and
unavailable interval. Derive contact-point motion from saved native kinematics
and contact positions only for evaluation. Report whether changed friction
actually causes tangential contact motion, prediction error or a physical stop.
Do not infer slip from friction alone, or reuse this outcome to retune V1 and
call it fresh validation. No statistical generalization claim from one paired
geometry/seed. Original camera visibility limitations and ideal-vs-hardware
sensor gap remain. Full sensor-only navigation and JEPA science are unfinished.
