# Moving-action switch input check V1

Only after the complete 144-cell scientific collection passes all raw,
measurement and exact-prefix gates, authenticate every collection artifact and
reproduce its full accounting. Create one exclusive input-check root,
`go2_moving_action_switch_inputs_v1_attempt_001`. No retry or partial-data fit.

Derive model inputs from the actual four public policy packets at frames
10, 11, 12 and 13 and the known selected suffix's exact forty-command plan.
Use the unchanged RGB/body/control tensorization and action normalization.
No geometry identity, native pose, target label or future observation enters
model inference. Recheck exact tensor equality for every matched suffix group.

Only training-role materialization can read actual future RGB/body/control
packets as targets, at audited valid horizons. Contact-censored motion stays
NaN with a false mask; unknown contact stays NaN with a false mask. Future
images require valid contact-free motion. Geometry-transfer materialization
uses only past packets and plans, even during this interface check. The policy
stream opens only bound policy manifests/histories and its explicitly allowed
RGB leaves. Full raw-file integrity hashing occurs outside that policy path.

Check the existing latent-width-32 cumulative-contact model's input/output
shapes, finite active outputs, exact clocks and masks, and finite training
JEPA objective with a private untrained seed-2026091400 model. Use no optimizer,
gradient accumulation, checkpoint read, training or native execution. Confirm
unchanged model state and absent gradients afterwards.

Prepare separate balanced training schedules for optimization seeds
2026091001, 2026091401 and 2026091402. Each has 1,200 updates with six samples
per batch: all six suffix siblings from one train cluster/prefix context.
Shuffle the twelve contexts per cycle and suffix order per batch using the
given seed. All 72 prospective training cells must be available; each gets
exactly 100 draws. Transfer-role data never enters a schedule. These are twelve
matched contexts in only two training geometry clusters, not twelve independent
mazes. A missing training branch prevents this fixed learning scope from launching.

Persist the complete ordered tensor identity index, schedules, source/input
bindings and role-specific target-read accounting. Run one CPU thread after
checking at least 8 GiB available RAM and 256 MiB output allowance above the
40-GiB reserve. A separate matched fitting protocol and hardware benchmark are
still required before any optimization. No navigation or deployment claim follows.
