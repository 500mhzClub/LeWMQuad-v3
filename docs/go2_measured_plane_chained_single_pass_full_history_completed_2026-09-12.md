# Chained single-pass full-history comparison completed

The original comparison finished with status
`MEASURED_PLANE_CHAINED_SINGLE_PASS_FULL_HISTORY_V1_COMPLETE` and result SHA-256
`a6bde0f6f9a9c98e11246e55c7b531895fd6edd98ec646a79e78a8b2a3d4b566`.
Its exact owner, PID 3015121 with creation time 1789209876.83, was confirmed
ended. Original tool session 70177 returned the completion message and actual
result hash. No `failure.json` was present. This supersedes earlier pending
verification snapshots for this comparison.

The output is
`go2_measured_plane_chained_single_pass_full_history_v1_attempt_001` under
`/home/andrewknowles/RecoveryStorage/LeWMQuad-v3/.generated/navigation_development_artifacts_v1`.
Launch SHA-256 remains
`b8a67fc80aebb2f39968092067cea00b94304c449bd7c86fdff30458c58d6260`.
The input is the completed chained native result
`163e92d630d92f9f8633cd3823c7cbf282a92c81bde5d3db998e3a5a8c8b1849`.
That original episode reached the outbound goal but failed to complete its
physical return before the 4,000-step budget. Its negative outcome is unchanged.

## Completed comparison evidence

All 4,014 original decisions equal the baseline replay, and all complete
candidate decisions equal that baseline after the specified implementation
normalization. All public input packets remained unchanged. Each arm made
3,988 actual model forwards. Complete retained states matched at frames
0, 3, 61, 122, 255, 511, 1023, 2047, 3071 and 4013.

Both model states remained unchanged at the assigned direct/no-RGB identity
`56799c99e2bb1fd5e5a591009b931c5b2e04741d3a3744e2346ce9896a2160dd`.
The completed launcher binds full closed-output/public-packet verification,
original raw-input reauthentication and original physical-prefix reconstruction
before and after replay. Its recorded wall time is 13887.458231824916 seconds.

An independent read-only verification, session 24862, exited 0. It authenticated
the exact launch and ended owner, required the complete result scope, rehashed
all 2,639 source bindings and all five output artifacts, reconstructed the
whole scalar report from all decision rows and state checkpoints, and required
exact equality with both saved report copies. It rechecked the result and all
artifact hashes afterward. This check took 0.290676438016817 seconds and did not
rerun a controller, public sensor reconstruction or native simulation.

The full reconstructed report, five output identities and verification metadata
are retained in
`go2_measured_plane_chained_single_pass_full_history_completed_verification_2026-09-12.json`.
An earlier preliminary read-only summary command, session 91114, failed because
it referenced a nonexistent module attribute `job`. Using the documented fixed
model SHA corrected that summary command. No experiment or source was changed
or restarted to address that error.

## Measured controller timing

| Measure | Chained baseline | Chained single-pass |
| --- | ---: | ---: |
| Total controller time, seconds | 5762.480048449 | 2563.720217736 |
| Median controller time, seconds | 1.3993996115 | 0.609352254 |
| 95th percentile, seconds | 3.15003547445 | 1.0112979921 |
| Observations over 100 ms | 4004 / 4014 | 4004 / 4014 |

The candidate used 55.51012417950083% less total measured controller time.
This was a non-isolated development measurement: short focused tests and
read-only inspection overlapped portions of the original run. Sensor acquisition
was excluded. Alternating paired execution order and every recorded row remain
in the result. These measurements do not establish a 100 ms sensing/control
loop, native adoption, a completed return, independent-maze reliability or
hardware qualification.

## Next prospective replay invoked

After completion and independent verification, the prepared launcher
`scripts/replay_go2_extended_return_budget_controller_prefix_v1.py` was invoked
with `--completed-chained-timing-result-sha256` set to the actual result above.
The original deterministic single-thread CPU environment and disabled OpenCL
were retained. This is a new exclusive experiment, not a retry or resumption
of either predecessor.

Original tool session: 67080. Owner PID: 3043682. Creation time:
1789224352.44. The first direct process check confirmed it running during
input admission with about 1.27 GiB RSS and 77.38 GiB RAM available. Its output
`go2_extended_return_budget_controller_prefix_v1_attempt_001` did not yet exist
at that snapshot. Invocation is not a completed admission or replay result.

The hardware review found 16 physical / 32 logical CPUs, affinity across all
32 logical CPUs, 3.4% sampled CPU utilization and idle integrated/discrete GPUs.
The discrete GPU reported 34,208,743,424 bytes total VRAM and 1,843,892,224 bytes
used. Artifact space was approximately 529.2 GiB; workspace space was 19.75 GiB.
After the original verifier ended, available RAM recovered to 78.32 GiB.
The new launcher independently enforces its own idle and resource admission.
It uses the fixed CPU numerical setup; this review does not change a frozen
device or concurrency definition.

The prefix experiment compares fresh 4,000- and 8,000-step controllers on the
same recorded public observations, stopping at the first normalized decision
difference or the original deadline. A positive completed prefix is required
before invoking the prepared fresh 8,000-step native launcher. No longer native
episode, independent-layout population or hardware motion was started here.
The existing independent-study roster still needs a prospective update for the
current controller and model/control assignments before execution; the newly
tested comparator components alone do not update that roster.

### Subsequent confirmed prefix launch

The same original process completed input admission and printed
`EXTENDED_RETURN_PREFIX_LAUNCHED` with actual launch SHA-256
`49f6c5cd36fb67adc0532dd3d0971a507faa435169238e96b27dfef47c150089`.
Tool session 67080 then printed `EXTENDED_RETURN_PREFIX_FRAME 0` and remained
running. PID 3043682 and creation time 1789224352.44 were checked directly.
This supersedes the earlier not-yet-created output snapshot above.

An independent read-only check authenticated the launch and rehashed all 2,656
bound source paths, with protected-path and resolved-path checks before reading.
Every binding matched. The launch binds the actual completed timing result,
the ended predecessor owner, reconstructed complete timing accounting, and
fresh 4,000/8,000-step controllers with at most 4,004 prefix observations.
The planned state frames are -1, 0, 3, 61, 122, 255, 511, 1023, 2047, 3071,
4002 and 4003, subject to the original first-difference stop rule.

At the first recorded resource sample, frame 0, RSS was 1,403,678,720 bytes,
available RAM 82,788,003,840 bytes and artifact space 568,214,880,256 bytes.
Neither `result.json` nor `failure.json` existed at this live snapshot. No
positive budget-only prefix result or longer native episode is claimed yet.
