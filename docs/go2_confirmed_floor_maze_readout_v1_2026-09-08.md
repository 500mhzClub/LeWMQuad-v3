# Completed confirmed-floor maze readout V1

Read only the completed hash-bound confirmed-floor native result, requiring its
raw audit and exact physical/public predecessor prefix comparison. Verify every
bound output, native input and frozen source before and after readout. No new
scene, model loading, training, policy change or outcome relabelling occurs.

Retain the original collection, raw reconstruction, strict visibility and
native mission evaluation. Report full decision/native traces, route and arrival
evidence, terminal constraints, executed waypoint and view-recovery intervals,
pose error and wall-clock timing. Visibility failure remains explicit even if
the native trajectory reaches the mission goal.

Additionally identify actually selected nonterminal commands for which the
original complete auxiliary contact check blocked and the additional confirmed
floor partition cleared the selected check. Require exact primary witnesses,
no non-foot or unknown exemption, matching selected/requested/tape action, and
actual 100 ms endpoint before measuring motion and forecast error. Report partial
intervals as censored with no inferred endpoint. These intervals are not an
estimate of the predecessor controller's counterfactual trajectory: changes to
the eligible set can also affect ranking when the selected action's original
surface was clear. Do not attribute all policy changes to this interval count.

Exclusive output: `go2_confirmed_floor_maze_readout_v1_attempt_001`.
One CPU readout, 8 GiB available RAM and 128 MiB above the unchanged 40 GiB
storage reserve. Preserve terminal failures. Matched comparisons, independent
layouts, real-time control and hardware evidence remain required by the goal.
