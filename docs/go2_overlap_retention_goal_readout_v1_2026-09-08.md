# Qualified-overlap native goal readout V1

Authenticate the completed one-case overlap-retention native result and its
retained-patch predecessor, all artifacts and recursive source/input identities.
Report actual goal, distance, stop/contact, visual, raw replay, strict measurement,
phase/action, map/contact/patch, model state, pose error and full iteration timing
outcomes. Preserve all stopped cases and the exact observer retention receipts.

Compare native/public array, RGB and model forecast prefixes only through the
first changed command or terminal decision. Report the first changed numeric
observed pose separately: the new observer deliberately changes estimation and
adds retention evidence, so whole-evidence identity is not required. Prefix
equality ends before execution of the differing command; no later trajectory
counterfactual is inferred. One known affected case does not establish a matched
cohort, independent-maze navigation, physical backtracking or hardware evidence.
No new physics, commands, training, promotion, retry or old-result changes.
