# Independent development maze session, collection and evaluation

Add native integration for every layout in the completed eight-layout source
inventory. Keep the original four-layout implementation and all live experiment
sources unchanged. This stage implements and checks source; it launches no
native scene and makes no physical geometry, navigation or final-test claim.

The new physical initializer uses the exact new specification and scene pack.
Its initialization body retains the predecessor's CPU backend, frozen Go2 gait,
physics settings, raster ordering, friction installation, contact topology and
runtime accounting. Multiple inheritance inserts this initializer before the
old initializer while retaining the existing contact guards, body and gyro
logging, primary and auxiliary RGBD acquisition, renderer witnesses and
persistence. Do not substitute an old layout number for a new layout number.

The separate collector and raw auditor retain the same residual-anchored
continuation controller, public coordinate-only mission, sensor inputs, timing,
3000-tick shared outbound/return allowance, warmup, terminal drain, storage
reserve, contact checks and complete raw model/command replay. The changes are
the fixed inventory imports, new session, separate collection status names and
independent-layout reporting. No controller receives the scene specification,
graph, route, native pose or layout index. Direct prediction remains predictive;
model RGB removal leaves controller RGBD sensing intact.

The separate numerical evaluator binds graph edges and goals to the new
inventory. It preserves the original 2ms clock, bounded trajectory, observed
arrival identity, native one-second dwell, zero-command, reverse-route,
contact-free and terminal-drain requirements. Its candidate result always
requires the separate raw sensor, command and strict visibility audit.
Synthetic route traces exercise acceptance and rejection; they are test inputs,
not native executions or evidence of controller performance.

Check the full inherited method ordering, exercise the real constructor chain
up to a substituted scene-builder boundary, verify the exact pack and scene
arguments for all eight layouts, and reject predecessor or altered specs before
building a scene. Compare the shared implementation bodies structurally to
ensure the integration has not changed controller inputs or dropped raw checks.
Exercise valid outbound/return traces, wrong-layout traces, missing arrivals,
closed-wall crossings, teleports, contact, nonzero dwell commands, speed, clocks
and incomplete terminal drain. Verify the live queue's complete source bindings
before and after the checks.

Before physical use, a separate population protocol and launcher must freeze
all layout/controller/model assignments, authenticate the original completed
inventory and model evidence, enforce one scene and available resources, record
native geometry/setup/renderer identities, and audit every assigned outcome.
The original queued six-case maze2 comparison owns the next native launch.
No independent scene, alternate waiter, implicit retry, population subset or
outcome-based layout replacement is started by this source integration.

All eight layouts must remain available in their original declared order.
Reliable independent navigation, matched nonpredictive/reactive and memory
controls, realistic sensing, timing feasibility and bounded hardware evidence
remain requirements of the full goal.
