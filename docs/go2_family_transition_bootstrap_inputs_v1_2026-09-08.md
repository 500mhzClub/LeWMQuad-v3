# New transition-prediction bootstrap: scope and input validation V1

The old family experiment's four-second action-necessity design failed. Its
readiness flag remains false and its original policy-stream checker remains
unlaunched. This new experiment has the objective of fitting motion/contact
transitions for the longer candidate plans used by the actual goal controller.
The historical short-pulse model's turn-only behavior motivates this explicit
new training scope. Valid measured transitions can support a predictor even
though the original local-progress assay cannot demonstrate scene-dependent
navigation. This distinction does not change either scientific result.

Use the complete96-episode collection at result
`376b2eeacfd5e741ba6b7a0e1b5e04399f782d16943d66ffdff92eada93ef0fb`
and the complete768-window causal derivation at result
`37bd88d43fd0ebdcee80282d38695f82a41b83313d08158d076f5ad4ce7145a7`.
Authenticate their full artifact/source bindings. Require both measurement
roles to pass, zero exclusions, unchanged native accounting, and exact preserved
false design/readiness flags. Preserve all84 missing contexts and all contact
censoring. No outcome-based subset, role changes or transfer-to-training access.
The train role has336 actual windows from48 episodes/four layouts/two parameter
clusters; geometry transfer has348 from48 episodes/four layouts/two other
clusters. Mirrored siblings and overlapping windows remain dependent.

This first stage validates every training window with its segregated future
targets and every transfer window with past-only inference. Use the existing
receipt-checked policy stream; compare every input fingerprint with the frozen
causal index. Future images remain training targets. Validate actual timed
plans, censoring and target contracts using the fixed cumulative-event model
and training loss under inference mode. No optimizer, gradients, model save or
checkpoint loading occurs at this stage. Preserve model-state identity.

Write one fixed1200-update, batch6 schedule, seed2026091001, with the existing
balanced layout/action/episode/available-offset algorithm. Each of the48
training episodes must appear150 times; every eligible training window remains
in scope. This schedule is prospective input evidence, not optimizer execution.

The intended subsequent bootstrap comparison is one fixed initialization seed
2026091001, input variants full/no_rgb, and conditions direct/supervised_rollout/
jepa: six fresh fits of1200 updates, batch6, latent32, AdamW learning-rate0.001,
EMA0.99 and unchanged losses. These are development fits with no selection on
transfer scores. Full JEPA at that fixed seed is the predetermined first native
bootstrap candidate. A separately frozen fit/benchmark runner must bind this
scope, input check, schedule, resource benchmark and exact settings before any
optimizer runs. The old readiness flag is never changed or passed off as true.
Matched scientific scoring and fresh native missions need their own receipts.
No JEPA/planning/memory contribution or independent-maze claim follows from fit
loss or these dependent local layouts. Moving suffix observations do not cover
all new candidate switches at moving states; that domain gap remains explicit.

Before this input stage inspect hardware and competing processes; require8GiB
available RAM and256MiB output plus40GiB free storage. Use one CPU process with
one Torch/OpenCV/BLAS thread. Replay input materialization is not training and
does not need a training concurrency benchmark. Freeze new sources before
execution; retain all original files and failures. Write only to exclusive
`go2_family_transition_bootstrap_inputs_v1_attempt_001`, with no retry/resume.
