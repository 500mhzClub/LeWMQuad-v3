# Completed supervised commitment-contact raw replay

The original raw replay and its owning waiter completed successfully. The
scheduled independent recorded-evidence verification also completed, with
session 47687 exiting zero. This completes the replay prerequisite for the
already queued contact-scoring native trial. It does not add a navigation
episode: the verified development count remains 39 audited episodes and zero
verified round trips.

Two fresh, independently stored copies of the assigned supervised model
reconstructed four original public observations, indexed 0 through 3. The
original controller reproduced every complete recorded decision. All three
warmup command requests remained unchanged; at observation 3, the candidate changed the
request from left turn `[0.0, 0.0, 0.45]` to forward `[0.2, 0.0, 0.0]`.
One raw model forecast comparison passed. The complete candidate selection
matched the previously recorded scoring intervention.

Both controllers retained equal complete observed memory, map, residual and
model-history state at every consumed observation. Their preceding pending
forecasts matched; each selected pending forecast was separately checked
against its actual selected action, raw prediction and observed pose. Both
model states remained unchanged and gradients were absent. Public sensor input
arrays remained unchanged. No observation after the changed request was
consumed, and the forward command was not physically executed by this replay.

The intervention charges ordinary waypoint choices for the existing 100-ms
commitment contact score, matching the executed command interval and pose
benefit interval. It retains the original 800-ms forecasts and feasibility
gates. This contact score is uncalibrated; the replay establishes a reproducible
change of choice, not safer traversal or successful navigation.

The original replay verified its complete input admission before and after
execution. It binds 1,927 source files and two output artifacts. Recorded wall
time after initial admission was 960.876761 seconds, including the final full
input verification; this is not controller latency. The completed waiter binds
1,941 source files and five output artifacts.

The independent check authenticated both completed results and their original
launches, verified all 18,123 original worker artifact bindings before and after
inspection, reconstructed all four saved original/candidate comparisons and
their completed original command endpoints, and reconstructed the original
public raw sensor packet hashes. It verified its 1,988 source bindings
before and after the check. It did not rerun neural inference, independently
reconstruct hidden controller state, or rerun the full training input verifier;
those computations remain evidence from the original completed replay.

Exact identities:

- Assigned supervised model:
  `755c074325af96d53649aba4927937113d8fab561ea05341a2f3c328598b2bb5`.
- Original supervised worker terminal:
  `60fdf8e04413f26cf689490b92d9797ef6a94b3b4161d6d2ed46c8b32bd7b8e2`.
- Raw replay root: `go2_commitment_contact_anchored_raw_prefix_v1_attempt_001`.
- Raw launch:
  `be3abf0cfeb879535e38d5ac7a104fb5fbaba7e313244f87a4a56dbdbb9f48ac`.
- Raw result:
  `1ff3729edab3da028fa5ea4ecc4b91b76b49bd572885ede73cdf9bcd67715743`.
- Saved candidate decision stream:
  `532d332df7c0b71f90900fcdad88fba18a5e4f26486e84b6a5ddb0a3e49691e1`.
- Raw waiter result:
  `cbcd31ee0ae1b1f598984cef16fa94524029b4e0e611bfde3dd684b0b029ad5a`.
- Independent verification:
  `go2_commitment_contact_anchored_raw_prefix_verification_2026-09-10.json`,
  `f9c373cf57d456c2fbfac3456de11133fc7cf093754de973682074af298273dc`.

At verification, original replay PID 2709917, raw waiter PID 2700744 and
independent checker PID 2711311 had ended. The original contact native waiter
PID 2703476 remained live and reported that its raw-replay owner had ended while
the prior hold-native waiter remained live. It retains the existing order:
complete six-case adapter batch, frontier trial, hold-reorientation trial, then
contact-scoring trial. The direct-model third adapter case remained live under
PID 2709978 and original batch parent PID 2659758. No job was restarted and no
frozen source or prior evidence was changed.

The next behavioral evidence is the prospective native trial and its complete
raw and physical audit. Reliable unseen-maze navigation, prediction-ranking
attribution, realistic timing and real-platform evidence remain unestablished.
