# Auxiliary raw sensor prefix readout V1

Verify the completed integrity-V2 sensor characterization and original corrected
JEPA probe, then quantify every primary RGB/depth difference over all 20 captured
frames. Report changed RGB pixels, maximum and mean absolute channel differences,
exact native-depth equality and all first complete auxiliary floor-patch witnesses.
Retain the exact physical/public prefix hashes, robot pixel counts and measured
auxiliary capture time including diagnostic RGB/segmentation and file output.

Freeze this reader, protocol and complete preceding source closure before
`go2_auxiliary_tilted_depth_prefix_readout_v1_attempt_001`. Reverify all inputs and
sources afterward. This bounded read-only array calculation launches no scene or
model. Preserve all changed pixels; small differences are not exact input identity
or evidence of observer/model compatibility. Do not identify the lighting or
rendering mechanism without further evidence.

The floor witnesses use actual new depth and retrospective original observed
poses/terminal targets. They are sensor-development evidence, not a new online
observer, coverage-aware controller or navigation result. Keep simultaneous
camera timing, physical mounting, hardware calibration and independent-maze
qualification false. Preserve every prior acquisition failure.
