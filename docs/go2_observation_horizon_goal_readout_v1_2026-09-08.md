# Trained observation-horizon native readout V1

Authenticate the complete two-case short-horizon native probe and its bound
sources/artifacts. Report both fixed first-seed model outcomes, native goal
gates, every terminal failure, measured-pose error, action selections,
observer retention and complete iteration timing. Neither model is selected
or omitted based on its result.

Compare each new case with its same-condition 500-ms-model observation-
replanning predecessor, and compare the two new cases with each other.
Compare raw physics, public histories, RGB and observer/memory evidence only
through the observation before the first different command or terminal.
The models and their temporal contract changed; later divergent trajectories
are separately executed outcomes and do not supply counterfactual labels.

Use the exclusive `go2_observation_horizon_goal_readout_v1_attempt_001` root.
No execution, fitting or prior artifact changes occur. Both new cases use
concurrent processes with paused native physics, so timing does not establish
uncontended or real-time deployment. The integration layout supplies zero
independent novel mazes; the broader navigation and hardware goal remains open.
