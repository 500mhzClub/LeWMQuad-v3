# Prospective native measured-settling pilot on development maze0

One fresh episode uses SettledBoundaryRoundTripController with the existing
JEPA model and original RGB-D/gyro observer, floor registration, later-floor
contact evidence, waypoint selector, residual memory and command interface.
The intervention is measured settling: first establish a quiet visual-motion
boundary, then count ten complete subsequent quiet intervals inside the4cm
observed goal region under zero previous requests. Both observed interval
motion estimates must be at most0.05m/s. This is not a bound on continuous
speed; unchanged independent native2ms dwell verification remains mandatory.

Require the completed full settled-boundary controller prefix with exact
unchanged actual requests through1866 and the first mission behavior change
at1866. Require the completed ninth native pilot/result and execution readout
at their exact recorded hashes. Revalidate input/model/source identities.
The collector and full raw audit are exact source derivatives of the ninth
versions, changing only the declared controller import/class and status names.
No frozen predecessor source is edited. No subpixel detector change is included.

Same reused development maze0, physics seed, numerical backend/thread setup,
robot, gains, friction, RGB/auxiliary cameras, public mission, shared3000tick
budget,100ms commands/observations and2ms physics. Native state is evaluator
only. Collect ten zero terminal-drain commands. No automatic retry, reset,
resume, counterfactual trajectory, sensor-mask adjustment or gate relaxation.
Preserve the original nine outcomes, including strict visibility failure909.

Compare new and ninth physics/public observations through frame1866 exactly,
including the observation at the first changed mission state. Every command
in that prefix, including the transition hold, must remain exact. Every new
complete decision must equal its bound prospective replay; all fields outside
declared mission settling must match the ninth decision. The first changed
mission phase is not falsely reported as a changed command. Later execution
is governed by the new controller and requires its own full raw audit.

Completion of this attempt requires collection, complete sensor/model/command
audit and physical/public/prospective prefix comparison. Strict visibility,
native one-second6cm/0.05m/s arrival windows, physical route retracing and quiet
return termination remain unchanged. Reaching the goal alone is insufficient.
The known unchanged-prefix visibility failure prevents qualifying the complete
run even if later navigation improves; do not relabel or remove it.

Before native launch inspect hardware topology/affinity, CPU/GPU/VRAM activity,
RAM, live competition and output storage. Use one CPU native scene and one
numerical thread, no parallel native scene. Require32GiB available RAM and the
existing10GiB collection allowance plus1GiB persistence above40GiB reserve.
Record and monitor actual resources; these are admission allowances, not OS
limits. Physics pauses during computation; no real-time/hardware claim.
Exclusive output go2_settled_boundary_maze_pilot_v1_attempt_001. Require the
actual completed prefix result hash at launch; prepared source is not a run.
