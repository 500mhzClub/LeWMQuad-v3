# Frozen-footprint late-history profile V1

The full original JEPA episode shows substantially larger controller cost after
its first 402 navigation decisions. Rebuild exactly the first 1,428 original
observations, from frame zero through 1427, using the existing frozen-footprint
controller and original assigned expanded JEPA model. Do not construct a scene,
change a command, select a model or consume observation 1428.

The completed original worker, complete artifact roster, actual adapter startup,
model assignment and full original input admission must authenticate before
execution and again after replay. Bind the completed original optimized-controller
profile and independently verified full-episode timing diagnosis in the source
closure. Keep the exact live native sources unchanged.

Every replayed complete decision, command endpoint and raw forecast must match
the original after only the existing declared frozen-footprint controller
metadata normalization. Public packet arrays are fingerprinted before and after
each observation. The assigned model state must remain unchanged with no
gradients. Rebuild the entire preceding state; do not initialize a late window
from receipts, guessed state or a different controller history.

Profile only `controller.observe` in three fixed ten-observation windows:
3–12 (early navigation), 395–404 (original discretionary repeated holds), and
1418–1427 (the first ten observations of the original final 100 nonterminal
navigation decisions). Normalization, hashing, serialization, sensor packet
reconstruction and bounded-depth state-size snapshots stay outside profiling.
Keep all intermediate decisions in the causal replay and save their comparison
hashes. Persist all three raw profiles and function/module summaries. Container
sizes are diagnostic counts, not a proof of complete retained-state identity.

The source tape has an original strict visibility failure at observation 1173,
inside this history. Its strict visibility result is false and its verified
round-trip result is false. Preserve both facts explicitly. This is development
performance diagnosis on known imperfect observations, not a qualified sensing
prefix or navigation result. Full decision equality cannot repair that failure.

One CPU replay is permitted alongside the already owned native queue. Require
48 GiB available RAM, 40+1 GiB artifact headroom and four physical CPU cores;
refresh these measurements before execution. Fix OpenCV, Torch and BLAS to one
thread and retain deterministic Torch behavior and the original hash seed.
Do not add a second native scene or alter the queue. Available-memory admission
is not an OS memory limit. The shared host and profiler overhead preclude an
isolated speedup or real-time qualification claim.
