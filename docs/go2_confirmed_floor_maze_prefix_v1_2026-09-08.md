# Current-primary confirmed auxiliary floor: causal prefix V1

Replay the completed view-recovery native result
`0f40eb01e5d5feaf004d0c0e98a9b6d712791dcd676b6013ac965fbb1603ffb8`
and readout `7f3105a1864b21f99f24726350d7ffa1636f2ed8ef0f8ce72d849fc29c13fe76`
under their full artifact, launch and recursive source bindings. Its navigation
failure and strict visibility failure at primary frame 909 remain unchanged.
This replay creates no scene and grants no qualified navigation evidence.

Use one fresh ConfirmedFloorRoundTripController with the same frozen full JEPA
model, causal history, residual scoring, waypoint/view-recovery selectors and
public primary/auxiliary RGB-D and body packets. Observer, raw floor/occupied
map, original primary/auxiliary point indices and partitions, original geometry
checks, mission, 3,000-command shared budget and latched stops are retained.

The new memory keeps an additional complete per-return auxiliary partition.
Each frame fits a plane only from primary patches accepted by the original
classifier. Additional auxiliary floor classifications require their own
measured patch geometry and validity and the explicit candidate plane gates in
`docs/go2_current_primary_floor_confirmation_candidate_2026-09-08.md`.
Original points, original classifications and original complete footprint
receipts remain stored. No native pose, segmentation, command-integrated pose,
model training, pixel repair or unobserved floor inference is used.

Only the four nominal auxiliary foot-sphere contact queries use the additional
partition. Primary checks and all non-foot auxiliary checks remain exact.
Every still-unconfirmed auxiliary return remains in the blocking other/unknown
partition. Current joint posture, first 100 ms articulated geometry, all eight
nominal path segments, original radius and other candidate/mission gates remain
unchanged. Original and revised full surface receipts are both reported.

At every frame require exact original evidence, map receipt after removing only
the added confirmation receipt, mission, residual inputs and learned forecasts.
Require all original full surface receipts embedded in new checks to match the
recorded predecessor exactly. Re-run the complete frozen predecessor selector
with those original checks and current causal history, using its own sequential
view state and a private selection-time residual copy; require its entire output
to match the recorded old selection. The diagnostic copy clears only its
current pending, not-yet-executed forecast; original controller state is untouched.
All nominal paths, primary checks and raw goal/route inputs must remain exact.
All other pre-intervention decision fields remain identical except explicit
controller/policy identity, revised selection and added map receipt.

Stop at the first requested-command or terminal difference, before consuming
that changed command's outcome. Do not assume the first difference occurs at the
previous final failure: earlier classifications may affect earlier actions.
Verify model parameters/gradients, sources and all inputs after replay. Retain
partial failures; no retry, counterfactual outcome or native success is implied.

Six focused memory/controller checks and three plane/patch checks passed. Require
12 GiB available RAM and 256 MiB artifact headroom above the 40 GiB reserve. One
ordered CPU replay and diagnostic selector pass; no additional scene, checkpoint
or training. Exclusive output: `go2_confirmed_floor_maze_prefix_v1_attempt_001`.
The original visibility discrepancy needs its own prospective treatment before
qualified successor claims. A passing prefix alone never establishes arrival,
return, independent-maze reliability or hardware readiness.
