# Observation-horizon causal input admission V1

Authenticate both original development populations and the complete new
short-horizon derivation. Construct the new view without changing any original
context metadata, availability, role or schedule. For every available sample,
use the existing source-specific past-only stream to reconstruct its four
history packets and verify their recorded tensor identities. The new eight
one-command blocks must exactly equal the first eight known commands of that
admitted original plan, with the same unknown padding and new actual clocks.

Materialize all 408 training contexts and their explicitly bounded actual
100–800-ms future packets; cache them privately. Inference for all 420 transfer
contexts reads only the original past packets, with no future-target reader.
Record a complete 912-slot tensor index and all unchanged schedules. Require
exact history tensors across all 24 six-suffix branch-prefix groups. Preserve
all target-side motion/contact/future masks and unavailable contexts.

Create one private untrained width-32 observation-horizon model with seed
2026091600. Check finite outputs, exact clocks/masks and all six objective/
input combinations on the first fixed-seed scheduled training batch. No
optimizer or gradient update occurs; verify unchanged state and absent
gradients. These private weights are neither saved nor eligible for training,
checkpoint selection or native execution.

Use the exclusive `go2_observation_horizon_inputs_v1_attempt_001` root. Freeze
source/input identities before materialization and reverify afterward. Use
one numerical thread and one bounded 408-context cache; monitor resources.
Require 8 GiB available RAM and 256 MiB allowance above the unchanged 40-GiB
artifact reserve. Preserve any failure without retry. A passing check permits
preparation of the separately frozen matched fitting benchmark, with no
navigation, real-time, calibration or hardware qualification claim.
