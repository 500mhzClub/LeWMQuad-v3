# Density-routed floor-registration replay

Compare two fresh floor-registration objects on original tracking observations
0 through 853. Use the original recorded public primary and auxiliary depth,
proprioception and visual pose evidence. Change only the floor-index function
inside measured-candidate extraction through private function bindings. Preserve
all original registration and transport function bodies, failure gates,
reference and anchor updates, and scientific thresholds.

A fixed 16-pixel stride samples valid low-height pixels solely to route work:
above 50% estimated occupancy selects the original dense kernel; otherwise use
the eligible-cell kernel with original dense fallback at ambiguous numerical
boundaries. Both kernels validate and classify the complete original arrays.
No camera identity, future input, retained history or private geometry selects
outputs. The threshold was developed from the preserved negative predecessor
and its fixed recorded workload probe; this is development timing evidence.

The predecessor registration replay was byte/receipt/state exact but 8.39%
slower overall. Its recorded-workload diagnosis found all auxiliary cells
eligible, with gathering overhead, while sparse primary inputs benefited.
The routed probe on frames 0, 100, 400 and 800 completed 12 alternating pairs
per camera with identical output bytes: primary 33.94–41.28% faster; auxiliary
within 0.61% of the original. These shared-host component measurements do not
establish whole-registration or whole-controller speed.

Execute original and candidate in alternating frame order. Compare every whole
receipt to the recorded original and every complete registration state between
objects. Require public input fingerprints unchanged. Record each paired timing
excluding packet reconstruction and comparisons. Admit original raw worker
artifact bindings and source closure before and after execution. Require the
previous full replay owners ended; preserve any failure, with no automatic retry.

This exclusive attempt consumes exactly 854 observations, loads no model,
reexecutes no visual observer, and runs no controller or physics. It grants no
navigation or real-time claim and does not modify queued experiments. Complete
controller integration, including mapping, and prospective navigation validation
remain necessary before adopting the candidate.
