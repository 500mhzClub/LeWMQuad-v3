# Fitted-checkpoint bridge replay: fixed integration check

Replay all144 completed successive-choice streams and every recorded selection
through the new observed-memory/local-controller bridge. Use all original fixed
three-seed ensembles and heads; do not fit or select a checkpoint. Verify the
completed physical result and corrected full-audit/dependency-witness identities.
Use the source stream's actually ingested packet indices, including the four
actual packets ending at teacher initialization. No historical packet may be
reconstructed from a later sensor buffer.

For this integration check only, supply a symbolic conditioning place and an
exit bearing atan2(y,x) from the source task's existing .8-m direction cue.
These are declared replay fixtures, not RGB place/exit detections. Reconstructing
that cue with sin/cos can introduce floating-point roundoff: allow at most1e-12
absolute difference for initial/current direction vectors and candidate costs.
Require exact input hashes, images, model identities, member predictions, mean
predictions, selected actions and command tapes; exclude execution timing fields
from equality. No source decision may be omitted or new decision introduced.

Do not submit a fabricated arrival. The old directional task has no qualified
place arrival observation. Each replay therefore ends with one outstanding
symbolic attempt, zero qualified traversals and no invented destination nodes.
Passing this check verifies the fitted inference path, not discovery, memory
efficacy, place association or new physical navigation.

Fixed fresh output:
`.generated/go2_exploration_bridge_replay_development_v1_attempt_001`.
Freeze the new bridge/memory/replay/protocol/tests and predecessor source/model
identities before execution; verify bindings afterward. Retain a terminal
failure without retry, tolerance change or source substitution. The ongoing
384-trial physical collection is separate and remains unchanged.
