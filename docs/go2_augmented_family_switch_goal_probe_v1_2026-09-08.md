# Fixed augmented-model native goal probe V1

Execute exactly two fresh cases on the existing `family_episode_039`
integration layout, using final seed-2026091001 full-JEPA and full-direct
models from the completed augmented eighteen-fit study. These assignments
precede fit results and remain fixed despite mixed predictive results.
Authenticate the full eighteen-fit admission and readout, including all
snapshots, update ledgers, source bindings and reconstructed scores.

Use the unchanged parameterized overlap-retention collector and independent
raw auditor. The controller, public sensing, model input contract, observer,
floor memory, route search, six candidate actions, utility, contact and
physical stops, 240-tick navigation allowance and terminal drain are exactly
those of the prior original-model probe. No fitting or checkpoint selection
occurs. Bind the prior run and readout as identity and comparison evidence.

The policy gate remains 4 cm plus ten quiet observations. Verified native
arrival still requires the original 6-cm actual-position gate, one second of
quiet motion and zero commands, an observed-goal terminal, completed drain,
and the original contact, acquisition and measurement requirements. Preserve
every model, observer, physical or budget failure as an outcome. Infrastructure
or raw-audit failures terminate this attempt without retry or replacement.

The two fresh processes each own one scene and one case, with one OpenCV,
PyTorch and BLAS thread. Shuffle the two assignments with seed 2026091501.
Two workers are bounded by the already passing four-process native benchmark
(3.460x speedup and all four paired execution/pixel signatures exact) and
four-process fitting benchmark (3.677x with exact paired model identities).
The conservative inherited four-worker preflight requires 32 GiB available
RAM and 8 GiB output allowance above the unchanged 40-GiB artifact reserve;
record actual two-worker use explicitly and monitor resources every 15 seconds.
There is no claim that two workers are optimal for this combined workload.

Freeze the recursive source closure and all input/native identities before
creating the exclusive `go2_augmented_family_switch_goal_probe_v1_attempt_001`
root. Check the exact original four-packet and first-900-physics-sample warmup
prefix after each execution, before using results for the matched comparison.
This native prefix is evaluator-only evidence; no native state enters policy
or model inputs. Replay every decision and command from raw public packets
using a freshly reloaded unchanged assigned model and audit all physical
visibility gates. Reverify all bound sources and inputs after execution.

Report both cases, including terminal reason, actual goal distances, verified
arrival, measurement gates and measured compute timing. The layout has been
used for development; independent-maze count is zero. Physics remains paused
during compute. Even two arrivals would establish only this integration probe,
not reliable novel-maze navigation, a JEPA advantage, real-time operation or
hardware qualification. The broader active goal remains open.
