# Ninth maze return-transition correspondence diagnosis

This separate read-only diagnosis binds the ninth launch and saved collection
result before inspecting public RGB-D frames 1853 through 1870 inclusive.
Collection has ended; the full native sensor/command audit remains pending.
It preserves the original terminal failure and does not resume that episode.

Reconstruct the unchanged CornerSupportFeatureFrame for all 18 frames. Count
the exact mutual SIFT ratio, duplicate-location, LK status, forward/backward,
descriptor-location and depth-lifting stages. Assert every final correspondence
array equals the original matched_points function. Evaluate consecutive pairs
1853->1854 through 1869->1870 and the eight retained references
1866,1861,1860,1859,1857,1856,1854,1853 against 1870. Save all pairs, not only
favorable results. Counts alone grant no pose or navigation qualification.

Bind all consumed collection manifest, history and RGB-D file bytes before and
after processing. Bind the native launch's inherited source identities plus
the explicitly discovered diagnosis sources before and after. No model load,
training, simulator, additional sensor acquisition, alternative threshold,
feature detector change or native-state controller input. One CPU process,
one OpenCV/numerical thread, 2 GiB available RAM and 64 MiB output allowance
above the standing 40 GiB storage reserve. Record current hardware and competing
jobs. Resource allowances are admission checks, not OS-enforced limits.

The output is go2_return_transition_match_diagnosis_v1_attempt_001 in the
existing authorized development artifact root. Failure outputs are preserved.
