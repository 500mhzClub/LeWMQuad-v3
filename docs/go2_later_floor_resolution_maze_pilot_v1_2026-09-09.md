# Prospective later-floor-resolution maze pilot V1

One fresh CPU simulator scene on reused development maze0 tests
LaterFloorResolutionRoundTripController. The declared intervention records
complete floor coverage from actual paired observations and resolves only
ambiguous nominal-foot enclosures covered by one strictly later camera view.
Original bounds, samples and classifications remain available. Non-foot and
unresolved contacts remain blocking. The static-floor/pose assumptions remain
conditional; no calibrated uncertainty or physical support is established.

Exclusive output: go2_later_floor_resolution_maze_pilot_v1_attempt_001.
The launcher requires the exact completed later-floor-resolution prefix result
hash, with a changed nonterminal command and no consumed unexecuted outcome.
It verifies the original pose/map/mission receipts, forecasts, original contact
queries and full nominal path checks were preserved through that observation.
The completed predecessor is the eighth native attempt, result
f7a6ef564d6e2bfac8b259e2ddc1988e5b357d8be7f180512f8b00393bbdfe41.
That attempt failed both navigation and strict physical visibility.

Verify all prefix, predecessor native/readout, cache, diagnosis, model input
and source bindings. Run --preflight-only before launch and inspect actual
CPU topology/affinity/utilization, RAM, GPU/VRAM, competing jobs and storage.
Require32GiB available RAM and unchanged10GiB collection plus1GiB persistence
allowances above the40GiB reserve. Use one scene worker and one numerical
thread on the frozen CPU setup. No parallel scene or GPU switch. Admission
limits are capacity checks; collection monitors storage, not an OS quota.

The collector and raw audit are exact source derivatives of the completed
joint-floor registered collector/audit, changing only controller imports/class
and status names. Retain the learned checkpoint, action set, pose estimator,
full800ms nominal horizon,3000-tick shared outbound/return budget,10-command
terminal drain, physical stops, acquisition checks and strict visibility gates.
The candidate includes the independently verified per-observation floor cache;
the separate receipt copier is not installed. No training or checkpoint choice.

After collection, replay all public packets and complete decisions with a fresh
controller/model and independently audit raw sensors, commands and native
trajectory. Compare physics and public packets with the eighth attempt through
the intervention observation, excluding its successor outcome. Require every
candidate decision in that prefix to match the prospective stream exactly and
the declared pre-intervention observation/forecast/contact invariants to hold.
Strict visibility applies to the entire new trajectory, including any preserved
predecessor failure. No arrival/return, independent-layout advantage, real-time
execution or hardware qualification follows merely from successful execution.

Preserve all failures and prior artifacts. No frozen source edits after launch,
output overwrite, retry or resume. Report actual trajectory and later-evidence
effects after the completed audit without attaching counterfactual outcomes.
