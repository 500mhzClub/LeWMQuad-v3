# Reconstruct the maze-1 frame-504 floor conflict

Require the completed final result of the existing direct-flow maze-1 native
attempt, its full artifact/source/environment bindings, complete raw sensor,
model and command audit, unchanged assigned JEPA model and exact 215-frame
physical prefix. Collection or interim audit alone is insufficient. Accept its
negative scientific outcome; this diagnostic does not qualify navigation.

Scan the saved ordered decisions only through frame 504. Require no earlier
terminal, the exact SENSOR_OR_MODEL_FAILURE with reason `current measured
candidate conflicts with transported floor reference`, a zero command, null
registered evidence and a present original raw visual witness. Frame 503 must
have admitted registered/transported evidence. Do not consume decision 505.

The full raw audit already reconstructed the saved visual witnesses and all
controller decisions. Load only raw public packet 504, including the original
paired RGB/depth and gyro. Validate its current dual-camera pose. Restore the
original floor registration's reference, anchor and preceding frame exclusively
from the immediately preceding admitted floor receipt, then invoke the unchanged
registration method. Require it to raise the exact same exception, latch failure,
and preserve anchor/reference/frame. No model or tracker execution, altered
controller decision, native pose, command integration or history rewriting.

Reconstruct both complete current candidate clouds with the original selection
and fitting functions. Reconstruct the same transported plane with the original
composition. Report candidate counts, joint missingness reason, anchor age and
correction, both camera signed residuals, RMS/max error, counts strictly above
the unchanged 0.003 m gate, worst point and sampling pixel, plus complete-cloud
and mask hashes. Missing candidates supply no residual or agreement. No trimming,
new plane admission, threshold search, error calibration or physical floor
identity claim. Check all input arrays and saved evidence remain unchanged.

One CPU diagnosis, single numerical threads, 8 GiB RAM admission and 128 MiB
output allowance above the standing 40 GiB reserve. Assess CPU/affinity, RAM,
GPU, both storage volumes and competing processes before submission; repeat
hardware admission before output creation. It may overlap existing independent
CPU jobs only with measured memory headroom. Native scene count is zero.
No protected benchmark access, source export, cleanup or real-robot action.

Exclusive output `go2_direct_flow_maze01_floor_conflict_v1_attempt_001` under
the external development artifact root. Runner requires the actual final
`--native-result-sha256`. Preserve any diagnostic failure. No original source
is modified, no stopped execution is resumed, and following physical outcomes
are not inferred. Any remedy requires separate prospective verification.
