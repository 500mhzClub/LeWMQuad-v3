# Independent population: original queue completion admission

The final population launcher must review the existing development outcomes
before using independent-layout sensor data. This library authenticates the
original ordered frontier, hold-reorientation and ordinary contact-scoring
waiters and their native children. It performs no waiting, launching, policy
selection or independent-layout execution.

The exact original waiter PID, creation time, command and boot checks must say
that all three owners have ended. A quiet live process cannot satisfy this
gate. Each completed waiter must match its already frozen launch, complete
five-file artifact roster, assigned child, source manifest and no-retry policy.
Its input receipt must link to the original completed adapter batch, then the
frontier waiter, then the hold waiter in order. The already completed prospective
raw-prefix result identities are fixed; no substitute prefix or attempt is
accepted.

For each child, rerun the original completion authenticator, including its
original input, exact model/worker, raw-audit and physical-prefix receipt checks.
Require the same saved native-completion report. Authenticate all original
collection, worker, log, audit, prefix and readout artifacts. Reconstruct the
compact outcome from the original audit arrays, collection and complete native
physics-contact array. Negative scientific outcomes remain in the ordered
record. An incomplete collection, original failure file or failed evidence
check cannot be treated as a completed reviewed experiment.

Full admission additionally reexecutes each native runner's original full input
admission. Bound verification reconstructs the same complete queue record and
all file identities without repeating the complete native raw audits. These
are separate scopes: the original completion verifier and full input admission
are not another full native sensor/controller audit. The independent population
input helper still owns its complete six-case cohort and training/correction
admission; the supplied adapter-batch hash must match that separate admission.

The resulting receipt explicitly leaves final policy review and population
execution false. A later frozen policy review must inspect these outcomes and
bind its decision and any changed-policy evidence. The final launcher must
authenticate that review as well as this queue receipt and its original input
admission. This source preparation grants no navigation, timing or hardware
qualification and does not change any live native run or queue order.
