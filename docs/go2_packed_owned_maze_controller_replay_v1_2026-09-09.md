# Full recorded controller equivalence for packed owned measured bounds

The completed128-frame component benchmark
e3f603f7baed193e23b53116fe14f46a94bc6a98ad4b5eebca0af604dbe9a872
preserved every grouping, bound byte, count, witness and sampled query.
Original/packed insertion total ratios were3.05779 and2.96297 in the two
opposite-order passes. At20894 cells, bound backing data was1002912 bytes
for original/packed-owned versus42277536 for the old batched candidate.
Those measurements exclude Python object overhead and controller work.

Install only eight initially empty persistent bound indices in a separately
named LaterFloorResolutionRoundTripController derivative: primary all-return,
primary floor/other, auxiliary all-return, original auxiliary floor/other and
confirmed auxiliary floor/other. Keep the current-frame broad voxel index and
every observer/map/registration/planning/contact/mission method unchanged.
Preserve exact original decision labels; record actual implementation class
in the launch. No substitution into the settled-boundary native candidate.

Replay all1881 saved public observations of the completed ninth pilot using
the same admitted JEPA model and initial state. Require every complete decision
to equal its saved original, including the1870 visual failure and terminal
drain. Stop on any mismatch, preserve its diagnostic, and do not modify the
candidate to hide a failed run. Verify all source, input and model bindings
before/after; weights and gradients must remain unchanged. Record uninstrumented
controller.observe durations but do not compare them as a controlled speedup
against a different run or hardware load. This is full behavior equivalence,
not a new native outcome or established real-time operation.

One CPU replay and one numerical/OpenCV/Torch thread. It may overlap the
independent settled-boundary replay or its subsequent native scene; record
current resources/competition at launch. Require8GiB available RAM and1GiB
output above40GiB reserve; allowances are admission checks, not OS limits.
Exclusive outputgo2_packed_owned_maze_controller_replay_v1_attempt_001.
No new model training, camera acquisition, simulator, native adoption, verified
navigation, calibrated uncertainty or overall-goal completion claim.
