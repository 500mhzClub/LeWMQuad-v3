# Prospective dual-camera collection and audit preparation

The controller prefix is still running. No new native attempt is launched by
this preparation. Require its successful V2 result, the tenth completed native
audit and readout, and agreement of every prefix collection binding with the
final audited native artifact map before any future launch.

New DualCameraNovelMazeSession calls the unchanged original paired capture,
then reads its already saved downward RGB through the validated public packet.
It does not add a render or physics step, replace a primary image, alter camera
pose, or expose native pose/segmentation. Primary/body/gyro/auxiliary-depth
packets retain their original objects. The existing physical paired capture,
timestamps, calibration, robot visibility and camera restoration remain fixed.

New dual_camera_settled_maze_episode_development and
dual_camera_settled_maze_audit_development are separate derivatives of the
tenth collector/auditor. They change the controller/session and pass auxiliary
RGB into both live execution and complete raw replay. Storage limits, mission
budget, warmup/drain, gains, friction, physical guards, strict visibility and
independent arrival/return evaluation remain unchanged. The artifact population
is unchanged because downward RGB was already captured.

New native prefix comparison requires exact physical and paired-public input
history through the intervention observation, all earlier commands exact, and
every candidate decision identical to its prospective replay. At the first
auxiliary intervention, the new command must match the prefix result; it need
not match the predecessor's failed-controller command. No later recorded
outcome is inferred. The admission helper requires V2 prefix completion,
current registered pose, active controller, unchanged model, no later recorded
observations, full native raw checks and all matching collection artifact hashes.

Nineteen focused tests passed1.92s. They verify the new session reads existing
RGB while retaining parent packet identities, rejects partial/failed or
counterfactual prefix reports, enforces exact intervention boundaries, and
rejects final audit/collection binding mismatch or relabeled failures. These
tests and source preparation grant no new native execution or navigation claim.
The launcher and readout for the next episode still need preparation and review
around the actual completed prefix and tenth readout results.

## Launcher and readout prepared

Separate scripts/run_go2_dual_camera_settled_maze_pilot_v1.py and
scripts/read_go2_dual_camera_settled_maze_pilot_v1.py are now implemented.
The launcher takes an actual completed V2 prefix result hash, verifies its full
artifact/source chain, and requires all11331 collection bindings to match the
completed tenth native result. Native binary/data identity comes from the
tenth native launch; the prefix launch supplies its unchanged model admission.
The saved first intervention, complete bounded stream and summary must agree,
with an active controller and current raw/registered poses at that exact frame.
The worker rechecks the bound predecessors and prefix before and after its
fresh collection/full raw audit/physical-and-public prefix comparison.

The readout preserves the previous route, arrival/return, pose-error, contact,
waypoint and timing calculations. It adds exact auxiliary attempt/selection
frames, distinguishing retained failure snapshots from new measurements. No
new observation or command is generated by the readout.

Tests24546 passed34 in1.97s (the19 preparation tests plus15 execution-scope
tests). Strengthening the saved-current-pose frame check added one case;
the resulting16 execution-scope tests76324 passed1.82s. Physical-prefix tests
reject changed state at the intervention observation, earlier command changes,
wrong intervention commands, public-input differences, changed full decisions
and truncated streams, while accepting changed subsequent physics under the
new prospective command. Intervention tests reject later/earlier observations,
summary/stream disagreement and stale pose evidence.

Source check68936 verified all1603 sources of live prefix39133 unchanged and
the compatible inherited tenth readout. The combined native/readout preparation
discovers1618 bound source paths; both future output roots remain fresh. The
launcher imports and its CLI pass check77586. Exact collector/auditor/readout
diff review confirms only declared modality plumbing, admission and reporting
changes; physical, sensing, mission, mapping and evaluation gates are retained.
The final added frame-check test/helper are not part of any launched attempt.

Native execution still requires the actual completed prefix and the declared
hardware/resource preflight. At this preparation checkpoint, prefix39133 is
live at frame1300 with no reported mismatch; no new native scene is launched.
