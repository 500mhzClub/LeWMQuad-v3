# Prospective four-observation compact depth check

Check the prepared compact depth archive helper on exactly frames 0, 3062,
4003 and 4013 from the completed chained native development episode. These
represent its initial observation, outbound arrival, original mission-budget
boundary and final recorded observation. This is a bounded storage/packet
check, not a complete-population comparison, controller replay, timing
benchmark, new scene, acquisition optimization or native-format adoption.

The fixed input result SHA-256 is
`163e92d630d92f9f8633cd3823c7cbf282a92c81bde5d3db998e3a5a8c8b1849`.
The case is `no_rgb_direct_measured_plane_chained_maze_02` under
`go2_measured_plane_chained_maze02_v1_attempt_001` in the existing development
artifact volume. The existing native outcome remains an incomplete return.

The checker is `scripts/check_go2_compact_depth_recorded_sample_v1.py`.
Its exclusive output is `go2_compact_depth_recorded_sample_v1_attempt_001`
under the same artifact base. Bind this protocol, checker, tested archive
helper and discovered source/test ancestry in the launch before conversion.
No existing output may be resumed, overwritten or retried automatically.

Authenticate the exact original result and all 26 selected input artifacts
before and after checking. The six common inputs are the policy manifest and
histories, depth manifest, fast-gyro histories, and primary/auxiliary acquisition
audits. Each selected observation adds its primary RGB, raw/derived primary
depth, auxiliary raw/derived depth/segmentation archive, and auxiliary RGB.
Read only explicit bound paths and honor sealed-material custody.

For every selected observation, use the original cached public-packet reader
and auxiliary RGB/depth builder. Verify native depth and segmentation hashes
against the original acquisition audits, then write one new compact primary
archive and one new compact auxiliary archive. Reopen them, require exact raw
array bits and diagnostic segmentation dtype/bits, reconstruct depth through
the unchanged public constructors, and compare the complete five-part public
packet fingerprint. Preserve original input packets unchanged. Retain every
failure without changing the active controller experiment.

Record both legacy three-file depth size and compact two-file depth size per
observation. That comparison excludes common RGB and global metadata. It is
not a measured complete-run or future-population saving. Reopen all eight
compact archives again before terminal completion and bind the complete ten
output artifacts (eight archives, launch and report) in the result.

Use the original deterministic single-thread CPU environment and disabled
OpenCL. This short array/file check may overlap the existing non-timing
controller-prefix replay; it performs no model load, controller observation,
rendering or physics. Require 8 GiB available RAM and a 40 GiB artifact reserve
plus 64 MiB output allowance. Each archive is bounded by the tested helper's
5 MiB limit; leave 1 MiB for the terminal result. All original inputs and active
source files remain unchanged. Larger recorded-population checks, capture/audit
integration, timing and a future prospective native format remain separate work.
