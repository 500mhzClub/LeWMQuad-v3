# Nominal action constraint readout V1

Authenticate the exact completed four-case nominal-action native result, all
raw artifacts and source closure before and after this exclusive readout.
Compare the complete continuous-connector predecessor with exact requested
command prefixes, RGB, observer/map evidence, forecasts and native/public
arrays before the first differing command executes. Retain differences rather
than inferring counterfactual outcomes.

Summarize goals, stops, terminal/minimum distance, visual errors/failures, phases,
actions, complete raw/measurement audits and concurrent timing. Also record each
forecast's chosen action before/after nominal filtering, jointly feasible count,
nominal and surface rejections, and the count of no-candidate terminal choices.
Keep the prior selected-forecast versus one already observed square and measured
executed-endpoint diagnostic, explicitly distinguishing this one-square witness
from the controller's all-square checks. No intermediate motion or uncertainty
bound is implied. All failures are terminal for this attempt; no native execution,
training, old-result mutation, independent-maze or hardware qualification.
