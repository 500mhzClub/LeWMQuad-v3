# Retained depth-patch foot coverage: prospective direct-039 probe V1

Run one fresh native case: the same fixed final full-RGB direct model on known
layout 039. This affected development case is selected prospectively from the
completed coverage diagnostic; it is not a fresh matched-model cohort. The
three unaffected predecessor cases remain historical references, not newly
executed controls. Bind the full preceding four-case native result/readout,
retained-patch diagnostic and six-fit accounting. No old attempt is resumed,
retried, replaced or modified.

Keep the scoped foot-floor contact rule. Extend coverage only when the complete
44-mm enclosing foot square passes every existing pixel-validity, normal,
planarity, height, range and image-bound test in one retained past/current depth
patch. Preserve original grid positives and all unknown/non-floor and non-foot
vetoes. Retain every prefix and its original observation/pose identity; no
eviction, future observation, changed floor-grid cell or plane-only coverage.
Query only vetoed feet that a patch could resolve; batch simultaneous queries.
Save original grid-rule checks and the complete retained witness for any change.
The pixel-prefix capacity is 4,096 frames; no support/uncertainty certification.

All other science stays fixed: model, utility, nominal radius 0.45 m, observer,
floor and occupied maps, command vocabulary/commitment, native scene/sensing,
actuator, 1.2-m goal, 240-tick budget, quiet arrival, zero drain, physical stops
and native evaluator. Three new integration, five patch and 43 inherited tests
must pass. The collector and fresh-model raw replay use identical controller
source, and native-goal/actuator audit functions remain identical.

Use one fresh single-thread CPU worker because this single chronological native
case has no independent episode units; do not duplicate unaffected simulations
to fill cores. Inspect resources, require 32 GiB available RAM and 8 GiB planned
output plus 40 GiB reserve, and monitor approximately every 15 seconds. Prior
scaling remains an identity witness rather than the rationale for this one-case
configuration. Report actual timing as a single-worker measurement with paused
compute physics, not real-time execution or a causal speed comparison.
All failures are terminal. Report verified goals, contacts, distances, stalls,
nominal/surface/coverage decisions and complete raw/measurement checks. This one
known development layout cannot establish novel-maze navigation, JEPA benefit,
physical backtracking or hardware qualification.
