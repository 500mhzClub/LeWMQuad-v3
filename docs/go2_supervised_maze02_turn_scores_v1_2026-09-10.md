# Complete saved supervised maze2 action-score decomposition

Analyze the authenticated second worker of the original supervised cohort.
Pin its launch, worker terminal, audit, physical prefix, progress-after02 and
preceding case1 terminal to the exact SHA-256 identities in the runner. Verify
all original source and second-worker artifact bindings before and after the
analysis. Require the completed raw audit and original four-observation,
900-sample physical prefix. Preserve maze2's strict physical visibility failure,
zero arrivals/crossings and negative round-trip outcome. Neither those failures
nor the still-running maze3 may be removed or redefined by this analysis.

Reuse summarize and reconstruct from the frozen maze1 readout, source SHA
8c7119b3711bd27541275d58d746f48c8763073d187e04dc2c7e91b1f3941dc0.
The full population is3014 observations,3013 completed command requests and3000
actual selections, matching the saved audit action counts. Reconstruct every
complete saved score output using the original score_waypoint_execution and
strictly causal saved residual receipt. Tabulate all candidates' utilities,
distance/alignment progress,800ms contact scores, phase/surface/eight-segment
feasibility and margins against the selected action. Classify every selected
turn by whether translations were infeasible, or had greater geometric progress
but lower utility after contact cost, or neither. Include all frames and retain
the original tie order and arithmetic. Do not fit, change a horizon/coefficient,
rerun a model/controller, alter a command, or infer an unexecuted physical outcome.
Saved geometry-check flags are reconciled, not independently reexecuted.

This read-only analysis can identify why the saved decisions selected turns;
it cannot establish that an alternative command would succeed. Contact scores
remain uncalibrated. The already prepared contact-horizon native test and all
other queued experiments stay frozen. No full-cohort training-objective advantage,
reliable navigation, timing or hardware qualification follows from this readout.

Use one CPU process with8GiB RAM admission and16MiB exclusive output allowance
above the original40GiB free-space reserve. Recheck hardware topology/affinity,
CPU, RAM, GPU/VRAM, competing processes and both volumes before launch, monitor
the readout alongside the single original native worker, and record hardware
again afterward. The frozen maze1 analysis supplies the representative completed
workload; its ordered full population is processed sequentially. No GPU inference
or new native scene is needed. Admissions are not OS limits. An optional
--preflight-only verifies inputs/sources and resources without creating output.

Write exclusively to go2_supervised_maze02_turn_scores_v1_attempt_001. Preserve
any failure without retry or replacement. This adds no deletion authority. The
original worker's transitive runtime verification is inherited honestly; this
readout independently verifies source/output bindings and score reconstruction,
not that entire runtime verifier or the full raw sensor/controller audit.
