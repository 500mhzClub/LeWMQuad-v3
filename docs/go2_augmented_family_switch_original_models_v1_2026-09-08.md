# Original models on the augmented populations V1

After the complete new native collection and causal input check pass, evaluate
all six unchanged original seed-2026091001 family-transition fits on the old
and new populations. Fully authenticate the original six-fit result, all 7,200
update ledger entries, raw original scores, snapshots and both data sources.
No fitting, model selection or native execution occurs.

Use the source-specific past-only policy stream and actual known action plans.
For each model, save complete train and geometry-transfer prediction arrays.
Require every prediction field on the original family population to match its
original saved arrays exactly. The new branch contexts receive predictions from
the same unchanged evaluation-only model. Assert unchanged model state through
the common prediction helper. Preserve the input/target separation: source
accounting supplies eligibility and plans, while target values never enter the
prediction function.

Save all twelve model/role arrays and a prediction-completion receipt before
scoring any model in this comparison. Score both sources using the same
cluster/initial/continuing/repeat/switch and first-500-ms strata planned for the
new augmented fits. Report complete denominators, contact censoring and
undefined-yaw cases. These old one-seed scores are a fixed predictive reference;
they cannot determine new hyperparameters, schedules, checkpoints or winners.

Use one CPU thread after checking hardware and competing work, at least 8 GiB
available RAM and 256 MiB output allowance above the 40-GiB artifact reserve.
Write source/input/snapshot and array identities, per-model resources and
unchanged-original-output witnesses in the exclusive
`go2_augmented_family_switch_original_models_v1_attempt_001` root. No retry or
resume. Recheck all identities at completion. Neither these reused transfer
clusters nor favorable prediction error establish novel-maze navigation,
calibrated contact probabilities, realistic timing or deployment.
