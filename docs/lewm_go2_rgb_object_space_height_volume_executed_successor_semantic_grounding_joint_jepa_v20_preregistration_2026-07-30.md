# V20 accounting-isolation successor joint-JEPA preregistration

Date: 2026-07-30

Status: preregistered fresh, science-identical accounting-plumbing successor
only. No V20 source root, output root, reservation, generated-input access,
GPU work, training, checkpoint, calibration, G2, navigation, held-out, or
sealed access has occurred.

## Trigger and terminal predecessor

- V19 integrity replacement V1 is consumed and terminal. It may not be
  retried, resumed, recovered, or extended.
- Its committed failure audit is
  `docs/lewm_go2_rgb_object_space_height_volume_executed_successor_semantic_grounding_joint_jepa_v19_integrity_replacement_v1_terminal_failure_result_2026-07-30.json`,
  commit `7105e2d9ed6e724f364c837e84177b6b4c4cd163`, file SHA-256
  `1b155248194ffd6d7943f84d88c25e29843fb9c977fc5b9bd8053e381c49b886`,
  byte count `9497`, and canonical content SHA-256
  `fb794750c9efcc1430235478c3f4da02dcaf5211c131ca4e15084950a8cbd4e3`.
- The attempt initialized the exact reviewed model, passed initial structural
  integrity, and published its update-0 observation. Its first training update
  returned after 16 presentations, twelve gradient calls, one optimizer step,
  and one EMA step, but its post-update integrity validator failed before an
  optimizer/EMA trace row or learned observation was published. No checkpoint
  or registered scientific gate was reached.
- The terminal exception-message SHA-256
  `7c734580a5729589bf3ef5171d0bb5776ab2c2926ee2aa78b88f7f14ff9a224d`
  maps exactly to `V13 per-update accounting changed`. V19 replaced the
  inherited engine-global accounting registry with the extended V19 schema,
  then supplied the intentionally projected V13-shaped receipt to the
  inherited V18-to-V13 validator. That validator dynamically resolved the
  replaced global registry and rejected the projection.
- This is a deterministic receipt-adapter defect, not a model, data, optimizer,
  loss, gradient, or metric failure. The attempt supplies no scientific
  evidence for or against executed-successor semantic grounding.

V20 is a distinct fresh one-shot successor from exact initialization, not a
retry or resume and not a continuation from V19/V1's mutated in-memory state.

## Sole permitted executable correction

- Delete or isolate only the assignment that replaces the inherited engine's
  `ACCOUNTING_MULTIPLIERS` registry with `ACCOUNTING_MULTIPLIERS_V19`.
- The inherited registry must remain exactly the frozen V13 mapping: eight
  backward calls, four predictor objectives, and no factual-successor fields
  per update. The extended V19 mapping remains unchanged and local to the full
  V20 update and terminal-accounting validators: twelve backward calls, eight
  predictor objectives, four factual-successor gradient calls, and four
  factual-successor objectives per update.
- The inherited receipt projection remains V13-shaped. The published V20
  receipt remains the full V19-shaped receipt. No accounting value or training
  operation changes.
- A fail-closed assertion may require the inherited registry to equal the exact
  frozen V13 mapping. Focused CPU-only tests must exercise the real inherited
  validator boundary without monkeypatching `_original_validate_update_integrity`,
  prove the projected receipt crosses that boundary, prove the full update-1
  receipt retains exact V19 accounting, and prove global-schema leakage is
  rejected.
- Change only fresh V20 lifecycle identities and bindings needed for the new
  preregistration, failure witness, schema/evidence prefix, experiment arm,
  source manifest/review/certification/authority paths, clean source root,
  output root, and corresponding focused tests. The existing V19-named source
  adapters may be reused; filename renaming or an additional scientific wrapper
  is not required.

No model source, model parameter, tensor operation, architecture, input, role,
row, label, RGB, camera metadata, seed, schedule element, optimizer setting,
learning rate, AdamW setting, EMA operation, loss, coefficient, Q route,
gradient cap, observation, evaluator, metric definition, threshold, stopping
rule, or accounting multiplier may change. Dynamic receipt rewriting beyond
the existing V13 projection is forbidden.

## Frozen scientific identity

V20 binds and preserves the V19/V1 preregistration commit
`691ed5d39f0b8d1b40071045dc181b9a4b215573`, V19/V1 frozen
source-and-review commit `b1430e51bb428376bdbe00ef81ce65d971c8b436`,
original V19 preregistration commit
`6255a9a2cccffde4e777169eacf95105a828cf7e`, original V19 frozen
source-and-review commit `0aa8b5670c0f7de22020d4cb290b2013d57bede0`,
and controlling V18 result commit
`f2e290ce42f7b0cd142131f3272d1119b7b5d3d1`, including:

- the unchanged learned RGB encoder, eight-height object-space volume,
  semantic head, survival head, action-conditioned predictor, and exact model
  initialization;
- joint online encoder, representation, and predictor learning against one
  stop-gradient EMA target in every completed update;
- the factual executed-action next-semantic objective `Q_succ`, routed only to
  the registered 13-tensor, 259008-parameter latent-transition predictor
  subset, with its independent unit-norm cap and no semantic-head,
  representation, encoder, evidence-head, survival-head, or target gradient;
- all inherited `S/P/U/R/O/C` losses, the unchanged `Q` loss, coefficients,
  route clipping, parameter groups, float32 AdamW settings, one optimizer step,
  and one EMA step per completed update;
- the exact N320 initialization, constructor and projection initialization
  seeds, experiment/schedule/bootstrap seeds, train and checkpoint-selection
  roles and files, 4262-pair schedule starting at presentation zero, and four
  microbatches of four per update;
- the exact inherited eight-family registry ending in
  `visual_sensor_stress`, all observations, metrics, causal controls,
  thresholds, and stopping rules; and
- maximum 1000 updates and 16000 ordered presentations.

V20 must start in a new process from the exact frozen initialization. No V19/V1
model tensor, optimizer state, EMA state, RNG state, schedule position, trace,
metric, receipt, or runtime artifact may be reused. V19/V1's executed first
update is neither warm-up nor training credit for V20.

## Source acceptance before authority

- The inherited engine accounting registry equals the frozen V13 mapping and
  the extended V19/V20 mapping remains unchanged.
- A real unmocked inherited-boundary regression passes after one actual CPU
  synthetic V19/V20 training update.
- Its full update-1 receipt reports exactly: one update, 16 presentations,
  four microbatch graphs, twelve backward calls, four camera-route calls, four
  joint-route calls, four factual-successor calls, 32 camera-frame objectives,
  one optimizer step, one EMA step, four predictor forwards, eight predictor
  objectives, and four factual-successor objectives.
- Existing model, training, evaluator, comparison, control, and gate tests pass.
- Independent review confirms there is no executable difference beyond
  accounting-registry isolation, fail-closed validation, focused tests, and
  lifecycle identity plumbing.

Passing this plumbing regression is not a scientific pass and grants no new
data or downstream authority.

## Unchanged gates

- Update 100 remains informational with mandatory structural, accounting,
  finite-gradient, target-isolation, access, and custody integrity.
- Update 400 passes only with structural integrity, all twelve causal-control
  checks, physical margin count strictly greater than `72`, total physical
  shortfall strictly below `68.96954700805838`, and rough depth p95 strictly
  below `1.8582415819168085` m.
- Any update-400 failure is terminal and publishes no checkpoint. Passing
  continues in the same process to update 1000.
- Update 1000 passes only with V12 full arm `24/24`, at least `112/189`
  physical margins, total shortfall strictly below `33.05143763708337`, at
  least one complete physical scope, rough pixel balanced accuracy strictly
  above `0.8198594673963917`, rough ground balanced accuracy strictly above
  `0.647134926562893`, rough depth p95 strictly below `0.9777327477931971` m,
  and structural integrity.
- Only a complete passing update-1000 result may publish a development
  checkpoint. It does not directly authorize calibration, G2, navigation,
  held-out, sealed, promotion, production, or deployment access.

## One-shot identity and authority boundary

- Schema/evidence prefix:
  `lewm_go2_rgb_object_space_height_volume_executed_successor_semantic_grounding_joint_jepa_v20`.
- Fresh output root:
  `.generated/go2_rgb_object_space_height_volume_executed_successor_semantic_grounding_joint_jepa_v20/attempt_v1`.
- Fresh clean source root:
  `/home/andrewknowles/Workspace/LeWMQuad-v3-v20-accounting-isolation-source`.
- Both fresh roots must initially be absent. There is exactly one attempt;
  retry, resume, recovery, extension, coefficient search, and replacement
  attempts are false.
- The V19/V1 terminal result may enter only as committed source-identity
  evidence. All predecessor runtime artifacts and mutable state remain closed.
- Recursive source closure, independent review, narrow clean-export
  certification, and separate one-shot authority must be committed before
  reservation. Authority covers only the exact frozen train and
  checkpoint-selection inputs on the reviewed runtime and hardware.
- Until a complete passing update-1000 result, probability calibration, G2,
  navigation, held-out, sealed, promotion, production, deployment, retry,
  resume, recovery, and extension remain forbidden. No further plumbing
  successor or integrity replacement is preregistered.
