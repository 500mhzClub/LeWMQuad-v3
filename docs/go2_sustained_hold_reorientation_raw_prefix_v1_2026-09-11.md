# Raw prospective sustained-reorientation prefix

One exclusive CPU attempt reconstructs exactly observations 0 through 406 from
the completed original hold-reorientation native run. Two fresh copies of its
original assigned full-JEPA model drive the complete original and candidate
controllers. The model loader verifies the actual checkpoint and correction
bindings; final model states and absent gradients are checked again.

The original native completion verifier is rerun before and after replay,
including its source, raw-artifact, audit, prefix and original bound-input checks.
Full training-ancestry reconstruction is not repeated: this read-only comparison
uses the original authenticated model bytes and development sensor recordings.
This scope grants no new training or independent-evaluation claim.

Every complete original decision must reconstruct exactly. Every candidate
selection must match its frozen saved-prefix hash. Complete normalized decisions,
retained contact memory, accumulated floor/occupied cells, preceding pending
forecasts and unchanged public input arrays must agree. The only request change
is original hold to continued left turn at observation 406; added recovery
metadata at observation 405 changes no request. Stop before reading observation
407. Neither the changed command nor any successor outcome is executed here.

Require 32 GiB available RAM, 41 GiB artifact space, the original boot, completion
of the identified preceding CPU replay and source native waiter, one-thread
OpenCV/BLAS/PyTorch, deterministic inference, fixed Python hash seed and disabled
OpenCL. The fixed output is
go2_sustained_hold_reorientation_maze02_prefix_v1_attempt_001 under the existing
navigation development artifact root. No retry, resume, source mutation or
original-native restart is permitted. An exclusive failure artifact preserves
any post-launch exception.

The runner is scripts/replay_go2_sustained_hold_reorientation_maze02_prefix_v1.py.
Its --source-preflight-only mode verifies source/resources/ended original owners
without loading models, creating output or running full input admission. Focused
tests are lewm/tests/test_sustained_hold_reorientation_raw_prefix_development.py.
Success supports only a prospective first-command comparison; fresh native
navigation and its unchanged raw audit remain necessary.
