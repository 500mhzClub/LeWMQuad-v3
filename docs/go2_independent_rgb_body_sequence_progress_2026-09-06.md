# First complete layout collection and remaining-stage supervision

The scientific goal remains active and unachieved. This record concerns fixed
development data collection and its audit, not trained-model or navigation success.

## l00 collection and terminal audit completed

Stage 92647 subsequently terminates **exit 0**. The final audit confirms all
120 cases fully raw-audited and RGB/body eligible, 115 completed schedules and
five physical-terminal acquisitions. All **100/100 nonreference sibling prefixes
match exactly**. There are 21 positive contact horizons, 20 eligible examples of
each of the six action-duration cells, eight retained strict-visibility failed
trials and no hard measurement failures. All 127 audit-output bindings are present.
The original PID 2055172 is absent after the observed successful stage exit.

Terminal audit SHA-256:
`c4781dcb66b5e630dafaa8c4dfa5a0d05b0797488f479b421099aebf0fc429a5`.
This is one training layout, not an independent-layout model comparison or a
navigation result. Actual receipt-loader/supervisor preflight 76590 completes
exit 0: all 120 eligible windows join successfully, with no exclusions, the
100 exact prefix comparisons, 21 positive horizons and full 115/5 population
accounting preserved. All 771 prospective source bindings validate.

The single fresh supervisor sequence is then launched under handle **25963**,
with exact parent PID **2063013** observed live. It prints the fixed remaining
l01–l11 sequence and writes its exclusive launch and verified l00 receipt.
Supervisor launch SHA-256:
`e47240aeacd1b3af711d2c9d8ba1698b62ad1020982bcd099b47f8ef8c873a26`.
Initial verified-receipt file SHA-256:
`50e1e5d39eabf598892746fc614c8b4b20f9182ed87b26ff420038361bc9acfb`.
No result or failure exists at this initial live check. All 771 supervisor-bound
sources and the original 765 child collection sources are now immutable while
the sequence runs. Do not launch another supervisor or a competing manual stage.

The supervisor starts l01 in child PID **2063119**, with parent PID 2063013
independently verified. Read-only 84481 completes exit 0: the supervisor's 771
bindings remain unchanged, the child has exactly the original 765 collection
bindings, and its preceding-audit identity is the completed l00 audit above.
l01 is the fixed 120-case training-role batch, with launch SHA-256:
`9a97b95f8cad80bbd4f4bcb34cfcaa24cd869a3773d63f241abe737297f957eb`.
Subsequent native output confirms the first **three cases committed and
raw-prechecked**, all eligible, completing their schedules with no strict or hard
measurement failures. There is no l01 collection or audit terminal record yet.
This is live acquisition on layout two, not a second completed layout or model
generalization result.

### Earlier collection-completion observation

The original stage 92647 completes all 120 prescribed collection cases and
enters its automatic terminal audit, without a restart or competing audit.
Read-only collection check 4d566f observes:

- `RGB_BODY_LAYOUT_COLLECTION_COMPLETE`, 120 commits and 120 raw prechecks;
- 5,796,293,753 committed bytes;
- 274,775 physics samples and 3,813 paired camera frames;
- 120 prospectively RGB/body-eligible cases;
- eight retained strict boundary-depth failed frames and zero hard measurement
  failed frames;
- five physical-terminal cases and 21 positive-contact target horizons.

The physical-terminal cases are near-wall quiet/nominal action1 and near-wall
recent-forward actions0/1 under both nominal and lower friction. Horizon counts
are correlated outcomes, not independent collisions. Full acquisition does not
mean all command schedules completed or all navigation actions succeeded.

Collection result SHA-256:
`bdc9197ebb5298492571305b6b741387716c019b68e5b63ba48858a1f9513690`.
The fixed launch SHA remains
`cc0d583329b3b6713d84e4f8911d089f90160858401bdbb9dc5f384bda75f9c3`.
At that check there is no final audit or audit-failure record. PID 2055172 is
observed live at 3,468 s elapsed, and subsequent native audit output reaches
corner-turn/recent-forward/lower-friction action3. These are running-audit
observations, not a claim that the terminal audit or receipt loader has passed.

## Implemented sequential supervisor

The [prospective supervisor protocol](go2_independent_rgb_body_remaining_stages_v1_2026-09-06.md)
and `scripts/run_go2_independent_rgb_body_remaining_stages_v1.py` automate only
the exact remaining l01–l11 stages after an observed successful l00 stage exit
and completed receipt validation. Every child uses a fresh interpreter, the
unchanged original collection sources and the same declared environment. Each
retains its own per-case safeguards, storage checks and automatic terminal audit.

The supervisor rejects existing remaining outputs, records requests and child
exits, freezes candidate terminal receipts, authenticates them with the full
loader, and advances only after verification. It stops on failure and does not
retry, resume, substitute cases or fit models. Before final completion it repeats
all twelve receipt-loader checks and requires unchanged summaries. All new
supervisor metadata lives in one fresh bounded output, without source export or
modification of a predecessor run.

Focused 71233 completes exit 0: **99 passed in 5.56 s**, including 20 new
supervisor tests plus the 35 loader and 44 collection tests. New tests cover the
exact child command/environment, fixed stage order, full final recheck, existing
outputs, wrong/missing first audit, byte/source/loader/resource failures, nonzero
child exits and preserved failure accounting with no retry.

Source-only check 7073 completes exit 0: **771 recursive source bindings**,
comprising the unchanged original 765 plus this new supervisor, its protocol/test,
the receipt loader and the two existing independent-evaluation/baseline sources
that loader imports. This check neither creates a supervisor output nor launches
a child. The full 236-file regression 37871 completes exit 0: **3,111 passed in
241.43 s**, including all 20 new supervisor tests.

Supervisor source SHA-256:
`a066ddadc662db297cc2a83b919a4d39c6fb1aff50689427790521e3c50ba3b9`.
Test SHA-256:
`c2ff14f6bcb635ba9782940a9545178009c457c779b10213747323d9f3ff14fe`.
Protocol SHA-256:
`d963a06f55a8c5fe340898e7d434f5e501c8b873b0fdd0f57d1cb9f34f32881d`.

## Required next action

Track supervisor 25963 and its own stage children through the fixed sequence;
the completed actual preflight must not be reported as still running. Preserve
its live process/session identity and launch hash. It will not train models
automatically, retry failures or resume existing outputs.

The matched scientific experiment still needs a frozen executable, receipts and
coverage gates, input ablations, checkpoint persistence/reload checks and matched
baseline/seed/exposure accounting. Joint contact/motion/future-image availability
must be reported: some early collisions have positive contact labels but no
available latent-future image target. Reliable local physical execution, online
rollout and memory/backtracking benefit, unfamiliar-maze missions, real-time and
deployment-valid sensing and bounded hardware evidence remain outstanding.
