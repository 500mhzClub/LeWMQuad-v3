# Single-pass queries in the completed body-projected controller

The candidate composes the existing `SinglePassMeasuredSampleBoundsIndex` into
the eight empty packed-owned indices of `BodyProjectedTiledController`. Packed
insertion, independent witness ownership, scalar sphere norms, closed-boundary
semantics, unknown-space handling, model, costs, actions, sensing, floor history
and all recovery branches are unchanged. Existing native jobs keep their
original controllers. This is a timing/equivalence diagnostic, not a navigation
experiment or a change to the independent-layout study.

The completed cProfile witness
`1c3351b8647566f2d9745502aa8d43bb050101d27dfb2991536e25189cd522cc`
identifies repeated original bounds queries in the current controller. Earlier
single-pass evidence is documented in
`go2_single_pass_maze_queries_result_2026-09-09.md` and
`go2_single_pass_receipt_copied_benchmark_result_2026-09-09.md`; it does not prove
an incremental benefit in the present composition. No failed external profile
is used to choose favorable sample subsets or support timing percentages.

Run one complete paired replay at
`go2_single_pass_body_projected_late_history_v1_attempt_001`, using the unchanged
original paired-loop code with private controller/state/normalization bindings.
The baseline is `BodyProjectedTiledController`; the candidate is
`SinglePassBodyProjectedController`. Require all 1428 observations, 1425
forecasts, alternating execution order, separate unchanged model storage,
every original input and decision hash, command endpoints, and the seven
retained-state witnesses. Normalize only the declared controller metadata and
the existing ten exact state type paths. Retain original sensing failure 1173.

The original completed body-projection checker must reconstruct its entire
witness through a private in-memory capture before and after replay, including
actual raw/model admission. Original files are never overwritten. Source and
artifact identities are checked before final output. The independent completion
invocation requires the actual result hash, the original replay owner ended on
its recorded boot, another completed-input admission, all 1428 timing rows,
the complete expected report and preserved negative sensing scope.

One full CPU replay may share the host with the existing native queue. Require
64 GiB available RAM, 41 GiB artifact free space and four physical CPUs before
launch, with one numerical thread, deterministic Torch, assertions and OpenCL
disabled. The failed V2 profiler's four owners must be ended and its complete
failure audit preserved. No automatic retry, resume or another external profile.

Report all-navigation and all three fixed early/hold/late windows, including
regressions and every 100-ms miss. Acquisition, normalization, output writing,
comparison and physics remain outside controller timing. This shared-host
replay cannot establish full-loop real-time performance, navigation success,
independent generalization, JEPA benefit or hardware readiness.
