# Connected-layout pulse-context constructor/pairing pilot V1

Twelve bounded **training-role simulation** episodes: all six existing pulse
actions under nominal1.0 and lower0.15 floor friction, one new fixed connected
metric layout, one recent-forward near-wall context. This is a constructor,
counterfactual-prefix and data-coverage pilot, not an independent-layout split,
learned navigation experiment, hazard qualification or hardware instruction.

Freeze the eight source-declared passage edges on a1.2m cell grid; they form
one connected layout with a loop, branches and dead ends. Generate unique closed
cell-boundary boxes,0.08m thick and1.4m high. Spawn body at(0.08,0,0.375),yaw0.
The east wall of the starting cell is intentionally near the forward body/camera.
Retain full articulated native setup checks: no initial penetration is accepted
to manufacture contact labels. Layout_id includes exact geometry/topology; all
appearances/support/action variants remain train role. General graph-isomorphism
checking and independent-layout role assignment are later inventory work.

Pre-launch source review found that the inherited room-specific±0.8m empty
setup box necessarily intersects this layout's east wall. The distinct pilot
setup check constructs one initial-body-axis envelope from the extrema of every
actual articulated URDF collision primitive, padded by0.040001m (the original
4cm requirement plus1micrometre strict-interior slack). The envelope depends
only on recorded setup joints and geometry, never on wall placement or whether
the result passes. Retain the complete native wall separating-axis test,
all27primitive containment, native foot matching,0.02m/s velocity check and
ground-support witness. Derive and verify it once at1.5s, evaluator-only; do not
supply the envelope or native joints-as-pose to the selector. It is neither a
future swept-body certificate nor a source of observed free space.

For every action sibling, use physics seed2026090801 and appearance seed2026090802,
same frozen gait/gains, body-hidden RGB-D camera and ideal timestamped body/gyro
sensing. Record15settling ticks (750native samples). Then record8common0.12m/s
forward command ticks, with an actual image each tick. At2.3s (departure tick8,
native index1149), issue the assigned forward0.2m/s or yaw±0.45rad/s pulse for2/5
ticks, followed by exactly20zero ticks. Maximum post-setup command count33 and
image count34 per episode. No post-stop physics or automatic zero drain.

The selector takes only action index, tick and a validated current causal RGB/body
packet. Visual-led RGB-D/gyro tracking is logged in shadow and never gates the
schedule. Preserve native disallowed-contact/stability stops plus nonfoot ground,
0.3m/s speed and±8m domain guards. Packet-contract or storage stops are recorded
separately from physical contact. A stopped episode does not cancel the other
planned sibling attempts. Infrastructure failure terminates this attempt; no
overwrite, implicit retry, resume or alternate spawn is allowed within it.

Write exclusively to the owned RecoveryStorage root
`go2_independent_pulse_context_pilot_v1_attempt_001`, preserve40GiB free and require
2GiB additional capacity before starting. Freeze source/test/protocol/native/gait
and construction identities before any physics. Bind all expected artifacts by
explicit paths. No whole-tree export, sealed access, GPU training, model update,
runtime promotion or hardware action.

## Predeclared audits and interpretation

Reconstruct every available raw body/fast-gyro/depth packet and contact event.
The native optical-depth comparator uses surface-interior rays in(0.055,4.98)m,
including near walls below the public depth sensor's0.2m cutoff. Public depth
validity and calibration are unchanged; invalid depth is not filled or declared
safe. Report native near-surface ray counts, public valid-pixel counts and1mm
raw-render discrepancies. Retain failed/incomplete setup contexts explicitly;
do not fabricate histories or endpoints for them.

Within each support context, compare all completed prefixes through departure:
1150native samples and9co-timed RGB/body/command/depth/fast-gyro packets. Matching
seeds alone do not establish matching inputs. Report missing or unequal prefixes;
do not treat unverified siblings as same-context action alternatives.

Derive one pulse window only when its actual departure decision exists. Use
exact requested-command prefixes and2.2/2.5s target times. Native contact before
an interrupted endpoint remains positive if it occurred under the matched
candidate prefix; missing endpoints or noncontact stops remain censored, not
negative contacts. Actual future RGB availability stays separate from native
motion/contact validity. Materialize available windows through the existing
pulse dataset interface as an integration check only, without fitting.

Report all12attempts, setup/warm-up/departure/terminal counts, six action cells by
support, contact positives, missing targets, actual motion and tracking availability.
No positive-contact quota is assumed to succeed. If none occur, keep that coverage
gap. If warm-up hits the wall before departure, preserve that failed context and
redesign prospectively rather than moving the wall silently. Next work is a new
independent-layout inventory, quiet/recent-history contexts, balanced hazards,
sensor/action ablations, reliable execution and real maze/memory studies.
