# Go2 scene-diversity Genesis ROCm V3 integrity replacement V1

Date frozen: 2026-08-04

Status before execution: `PREREGISTERED_NOT_RUN`

Scope: one fresh, development-only, science-identical integrity replacement
for the consumed V3 qualification invocation. This document grants no
qualification, scientific, sealed, held-out, navigation, rollout, planner,
promotion, deployment, retry, resume, refill, overwrite, partial-reuse, or
further-replacement authority. Independent source review and separate exact
one-shot authorities remain mandatory.

## Consumed V3 and eligibility

V3's qualification command failed before reservation with
`ROCm qualification source binding changed`. The final review is
`docs/lewm_go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v3_qualification_terminal_review_2026-08-04.json`,
SHA-256
`139c7365236eb76b56c8354de9100d51dce56964f15541980f102c9f33804cd5`,
8,551 bytes, schema
`lewm_go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v3_qualification_terminal_review_v1`,
and status
`PASS_FAIL_CLOSED_PRE_RESERVATION_SOURCE_IMPORT_ORDER_TERMINAL_REVIEW`.
It records no reservation, GPU query, Genesis import or initialization, scene,
RGB, result, runtime terminal, backend decision, or scientific decision.

That review permits source development and independent review of exactly one
fresh V3 integrity replacement V1. It grants no execution authority and
forbids reuse of the V3 authority, command metadata, payload, reservation,
terminal, cache, worker, scene, RGB, result, or process evidence. The review
document is the sole V3 failure witness admitted here; its transitively bound
diagnostic inputs are not independently admitted or reused.

## Single integrity intervention

The consumed V3 authority was constructed with the correct 220-key source
closure. In a fresh qualifier-first process the V3 runner was first imported
inside a scoped predecessor overlay. The vulnerable expression
`predecessor.PREREGISTRATION` then captured the temporarily rebound V3
preregistration at key `v2_rocm_preregistration_source`, instead of the
immutable V2 preregistration. The other 219 bindings were exact.

Replacement V1 owns the literal path
`docs/lewm_go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v2_preregistration_2026-08-04.md`,
SHA-256
`f4d2b46ddb7a0ac97f95160f55c8aadd58f22ae1e63b7ab85e500c083a86b334`,
4,007 bytes. It copies the complete reviewed V3 source mapping before any
predecessor overlay, forcibly replaces that one key with the literal path,
freezes the mapping, and supplies that pre-overlay closure to all inherited
qualification and scientific validation.

No eager-import cache state is trusted. Focused tests must use fresh isolated
`python -I -B` processes to:

1. reproduce the consumed V3's exact 220-key, one-key mismatch;
2. prove runner-first and qualifier-first replacement imports return identical
   complete mappings and the exact V2 binding; and
3. deliberately poison the cached V3 predecessor closure, then prove the
   replacement still returns the exact literal V2 path and binding.

No other source-control, launcher, backend, environment, qualification,
collection, model, or scientific behavior may change.

## Exact scientific and runtime identity

Apart from fresh replacement identities, roots, role-local Quadrants caches,
source witnesses, and successor-contract metadata, the canonical V3-to-
replacement plan delta must be empty. Both plans retain exactly:

- the 64-scene panel, 256 states, role allocation, targets, histories, and nine
  candidate actions;
- all data, runtime, render, texture, model, optimizer, seed, evaluation,
  bootstrap, metric, and decision-gate fields;
- the lexical ROCm virtual-environment Python interpreter;
- the unresolved `ld.lld` Unix-driver entrypoint and separately bound regular
  target, with direct target invocation forbidden;
- Genesis 0.4.6 on `gs.amdgpu`, exact ROCm/HIP/EGL expectations, and the
  selected R9700/gfx1201 identity;
- the exact 17-key role environment, including the source literal
  `HOME=/home/andrewknowles`, with `USER`, `LOGNAME`, and `LANG` absent;
- qualification scene order `[12, 0]`, watchdog, timing formula, and complete
  qualification evidence contract; and
- all storage, frame, simulation, selected-device memory, and 7,200-second
  wall caps.

The physical and scientific gates are unchanged. Qualification remains a
separate non-scientific two-scene attempt and may not contribute data or
evidence to science. Scientific execution remains conditional on exact
qualification PASS and a separate scientific authority.

## Fresh custody

The only prospective roots are:

- `.generated/dev/go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v3_integrity_replacement_v1_qualification/attempt_v1`
- `.generated/dev/go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v3_integrity_replacement_v1/attempt_v1`

Both must be absent before their respective one-shot authority. Replacement
plans, source review, authorities, reservations, results, terminals, and roots
must use fresh replacement identities. V3 plans and reviewed source files may
serve only as immutable source premises; no V3 generated runtime path or
payload may enter the replacement closure.

## Stop rule

Any source binding mismatch other than the preregistered one-key import-order
bug, any plan delta outside fresh identity/root/cache/witness metadata, or any
environment, backend, data, model, seed, panel, cap, evaluation, or gate delta
fails source review. Any later infrastructure failure consumes the relevant
fresh attempt and creates no scientific decision, retry, resume, or automatic
successor authority.
