# Training-only translation correction native readout V1

Read both fixed corrected-model outcomes and their uncorrected eight-step
predecessors. Verify every source and artifact binding before creating the
exclusive `go2_training_bias_goal_readout_v1_attempt_001` root. Preserve all
arrival, terminal, raw reconstruction, model replay, visibility and timing
outcomes, and the original candidate-veto counts.

Compare each model with its predecessor through the observation before the
first changed command or terminal. Compare raw physics, policy, gyro, RGB and
observer/memory prefixes. For every available six-action eight-horizon forecast
bank in that prefix, require corrected values to equal the recorded predecessor
bank minus the launch-bound training-only XY coefficients, using float32
subtraction. Require yaw and contact entries to remain exactly equal. Do not
attempt to invert subtraction by adding the bias, since rounding is not
invertible. Require at least one forecast witness per same-base-model pair.

Compare the new JEPA/direct pair over the same causal boundary without requiring
different base models or corrections to agree. Exclude affected future frames;
infer no counterfactual executed outcomes. Reverify all bindings after analysis.
No model fitting, native execution, coefficient adjustment, independent-maze,
real-time, hardware or overall-goal qualification is performed.
