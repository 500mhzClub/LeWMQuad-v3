# Live maze renderer provenance diagnosis V1

The earlier empty-context probe reported llvmpipe. That does not bind the
renderer used by later maze captures: the current ninth worker has an amdgpu
DRM client at0000:7b:00.0 and accumulated graphics-engine work. Establish the
scope before extending the llvmpipe source-based raster hypothesis.

Exclusive output go2_live_maze_renderer_provenance_v1_attempt_001. Bind the
ninth launch c3d035abcc69b3b42ecb160021203e7d6d685a176e860e5c3044afa2035cefa4,
its source/native identities, this diagnosis source and the earlier context
probe result fc2d51f3011294573247cfb1782f9c0631dca8a1daa1a8ae390af2e70db60819.
Observe only worker2398270 with parent2398198, verifying process creation time
and parent command identity before/after. Read only its public kernel process
and DRM accounting metadata, grouping duplicate descriptors by client/device.
Record engine-counter deltas and completed decision-timing row counts across
a three-second interval. Do not attach a debugger, read process memory or change
the worker environment, code, graphics state or outputs.

In one separate process, initialize the same CPU Genesis lifecycle and default
EGL OffscreenRenderer, create one640x480 framebuffer target, then read its GL
vendor/renderer/version/profile and EGL device name. No simulator scene,
physics step, draw call, camera image or model loading. Hash the loaded EGL,
Gallium and LLVM library files and the exact EGL selection source before/after.
Bind queries to this fresh context only: matching hardware identifiers do not
retroactively prove the active or historical camera-context GL strings.

The reusable camera-context identity helper first requires the actual camera
depth framebuffer and only calls state-reading GL functions. It releases the
context on every exit; seven synthetic tests verify this behavior. It is not
installed into any frozen collector. Future capture provenance must query the
actual camera context, not infer its identity from CPU physics or another probe.

Inspect hardware/resources before the bounded diagnosis. One small query
process alongside the existing single native scene; one numerical thread,
minimum4GiB RAM and32MiB output above40GiB reserve. Native context query is
explicit, with zero new scenes, rendered frames or physics steps. No speedup,
sensor calibration, visibility-score revision, training or qualification claim.
Preserve every failure and original strict visibility outcome.
