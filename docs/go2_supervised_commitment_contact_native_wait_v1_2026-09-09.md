# One supervised commitment-contact native episode after the current queue

This separate one-shot waiter preserves the existing queue unchanged. It waits
for original queue PID2551088, process start tick130406252 on boot
1264d80f-6e46-4fcd-b2fd-2a5d7b964c73, and authenticates completion of queue launch
651a5815275ecfdd20c5ff6ff7f4d8cdbe4b5c52bbcaf3ad124a6ac97ffd2fb7.
The queued order stays supervised mazes1–3, direct-flow maze3, anchored-continuation
maze2, recent qualified-reference maze1. No source, command or job is added inside
that frozen scheduler. Zero native scenes are created while waiting.

After that exact owner exits, require its completed result, all four original
ordered audit/prefix completion receipts and result/artifact/source bindings.
Scientific negatives do not block progression; process, source, audit or prefix
failures stop this waiter without any native launch. A missing/reused owner PID
alone is never success. A live process is not restarted after an observation
timeout. The prelaunch wait is bounded to48h; expiry records failure with no
new child. No retry, resume, skip or replacement is defined.

Then invoke exactly the frozen
scripts/run_go2_supervised_commitment_contact_maze01_pilot_v1.py,
SHA9cf3a4d103dce745d1909548e99316ba7ea95c4c6aa8ed989df83f1a7de75439,
with the original Genesis interpreter and explicit unchanged numerical env,
--prefix-result-sha256
37b29828635e88fab77f81447f6a05b890911426fc3478d8aac451426a229de0,
and --queue-result-sha256 set to the authenticated completed queue result.
No optimization flags. The child authenticates its inputs and completed queue
again, runs one fresh native episode and performs its complete original raw
audit. After it exits, this waiter verifies all output/source bindings, original
input verifier, completed worker, raw audit and four-observation/900-sample
physical prefix, including completion of the changed command. It records both
positive and negative navigation outcomes; it never interprets exit0 as a
verified round trip by itself.

The waiter filename starts with await_go2 because it is an observer, not a native
runner. The existing conservative same-user run_go2/spawn-worker detector must
ignore it while recognizing its eventual native child. This prevents two waiting
supervisors from blocking one another. Tests verify this distinction. Immediately
before launching require no remaining native owner and the child's exact source
bindings. This is process observation, not a universal interprocess scene lock.

Runner scripts/await_go2_supervised_commitment_contact_native_v1.py; exclusive
root go2_supervised_commitment_contact_native_wait_v1_attempt_001. Freeze its
own protocol/tests/source and the union of all native/prefix/queue sources before
submission. Only explicit guarded artifact roots and source paths are read;
sealed custody remains unchanged. Read-only preflight creates no output.

Assess hardware at preparation and launch. Admission:8GiB waiting/verification
RAM plus32GiB concurrent native allowance,51GiB artifact free plus64MiB waiter
output allowance. Before the child starts recheck its32GiB RAM/51GiB free
requirements. These are capacity checks, not OS-enforced resource limits.
Poll the existing queue every30s; once the child runs,30s observation timeouts
only record liveness and never terminate/restart it. The child's original monitor
records native throughput and resources. Waiter event/child-stdout artifacts and
all failures are retained. No model/controller/scientific parameter differs from
the already tested native pilot, and no simulation or deployment goal is claimed
complete by scheduling or successful verification alone.
