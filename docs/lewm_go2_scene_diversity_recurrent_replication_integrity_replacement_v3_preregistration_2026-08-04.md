# Go2 scene-diversity recurrent replication integrity replacement V3

Date frozen: 2026-08-04

Status before execution: `PREREGISTERED_NOT_RUN`

Scope: one final development-only, science-identical, identity-only
infrastructure replacement after V2's audited transient AMDGPU/native-runtime
failure. This document grants no sealed, held-out, navigation, rollout,
planner, promotion, deployment, retry, resume, refill, overwrite, partial-
reuse, adaptive-fallback, alternate-backend, or further-Vulkan-replacement
authority. A separate independent source review and exact one-shot authority
are required before the fresh V3 attempt root may be created.

## Consumed predecessors and absence of science

The original V1, integrity replacement V1, and integrity replacement V2
attempts are consumed and immutable. Their generated runtime artifacts are
ineligible for reuse. V3 may bind their terminal and terminal-review metadata
but may not copy, open, join, resume, refill, or admit any predecessor scene
unit, receipt, RGB frame, render, mesh, reservation, process result, or other
runtime artifact.

V2's terminal is exactly SHA-256
`ebb520c596ae69c19e3be255c0f661fe55286883e03994a43a0e506936120465`
and 442 bytes. It has status
`FAIL_INFRASTRUCTURE_NO_SCIENTIFIC_DECISION`, a null result binding, and
authorizes no retry or resume. V2's independent terminal review is exactly
SHA-256
`7878dbedf21ed3bdb13927e0404925edc376e512c00a5ba4bf56e0091e3204c6`
and 20,561 bytes, with status
`PASS_FAIL_CLOSED_INFRASTRUCTURE_TERMINAL_REVIEW`.

That audit found 12 contiguous one-scene workers completed infrastructure
collection. Worker 12 then failed during native GPU execution: the kernel
named its process at an AMDGPU `gfx_0.0.0` ring timeout and recoverable device
wedge/reset, and the retained launch tail showed a GsTaichi stream-semaphore
assertion before the parent rejected the worker completion contract. The
deeper driver-versus-Taichi cause was not established. The failure is
classified as GPU driver or native render-runtime infrastructure, not a model,
metric, gate, or data result.

V2 published no combined `physics_result.json` and never entered its complete
64-worker join, pre-DINO closure, DINO extraction, training, checkpoint,
evaluation, metric, or gate stages. It therefore produced no scientific
decision. This preregistration does not reinterpret the 12 partial scene
outputs as scientific evidence and does not authorize opening them.

The original V1 and replacement V1 terminals/reviews remain transitively bound
by the reviewed V2 source closure. V3 binds all three fail-closed predecessor
histories through the V2 runner plus the exact V2 terminal and review above.

## Identity-only final replacement

V3 makes no scientific, runtime, collection-policy, worker-policy, batch,
process-lifetime, resource, data, render, model, metric, threshold, or gate
change. The exact reviewed V2 collector remains the implementation. A minimal
scoped wrapper changes only output schemas, `attempt_id`, fresh roots, and the
worker-script entry point required for each new process to receive that same
V3 identity.

The absent root is fixed as
`.generated/dev/go2_scene_diversity_recurrent_replication_integrity_replacement_v3/attempt_v1`.
It may be reserved only once by the separately authorized runner. There is no
V2 resume and no V2 artifact reuse.

Exactly 64 sequential fresh Genesis processes remain required, one per scene
in frozen plan order. Worker `i` may produce only scene `i`, must inherit and
prove the outer process group, and must exit before worker `i + 1` begins. The
fresh per-worker selected-device baseline, 20 ms global device monitor,
matching post-wait release barrier, result-load ordering, process-group
evidence, plan-first Genesis seed, scene-local mesh cache, final join, and
pre-DINO full filesystem/input rehash are unchanged from V2.

In particular, each worker initializes Genesis from the full plan's first
physics seed `14102849992353107924` (effective signed-31 seed `315871188`). The
independently reviewed V2 process-reset equivalence audit is retained exactly.
V3 neither revises that audit nor introduces another RNG, scheduler, texture,
physics, policy, or render route.

After every worker wait, including a failed worker and the final worker, the
matching selected-device release barrier must return before completion is
accepted or rejected. A scene result may be loaded only after zero exit and a
successful barrier. The standard combined result may be published only after
all 64 workers, all 64 barriers, all receipts, all scene-local mesh bindings,
and the final join validate exactly. No partial join or recovery path exists.

## Exact scientific identity

The V3 exact plan differs from the original frozen V1 exact plan only in
`attempt_id` and `output_root`. Every other field remains byte-canonically
equal: 256 ordered states, 64 independent scene identities, 32 train and 32
evaluation scenes, four states per scene, 2,304 action branches, 3,072 stored
frames, runtime/render and texture bindings, seeds, caps, model declarations,
metrics, and gates.

The frozen recurrent scientific route remains unchanged: frozen DINOv2
context features; visual and exact-zero no-vision arms; three model seeds
`411`, `412`, `413`; sampler seed `414`; 800 updates; live task/action ridge;
train-only PCA/statistics; durable checkpoint close and reopen before any
evaluation receipt or RGB; zero train/evaluation successor-RGB opens; and two
exactly equal evaluations from the disk-reopened checkpoint.

The five fixed gates remain unchanged: integrity/custody pass; visual
normalized rank regret at most `0.13`; visual minus live task/action regret at
most `-0.02` with paired 95% upper bound below zero; visual minus matched
no-vision regret at most `-0.01` with paired 95% upper bound below zero; and
visual beats uniform-random expectation. No threshold may move after any data
or metric is observed. A scientific pass supports only the preregistered
incremental visual hypothesis, not navigation or planning utility.

The global selected-device ceiling remains `16,977,405,952` bytes, total wall
ceiling remains 7,200 seconds, stored RGB remains capped at 512 MiB and 3,072
frames, and every simulation/render call cap remains unchanged. No cap increase
or timeout extension is authorized.

## Final transient-infrastructure stop rule

This is the last authorized Vulkan/Genesis identity-only replacement. Any V3
AMDGPU ring timeout or device reset, GsTaichi stream-semaphore abort, or
otherwise unexplained GPU-worker nonzero exit yields
`FAIL_INFRASTRUCTURE_NO_SCIENTIFIC_DECISION` and a hard stop: there is no V4
Vulkan retry, replacement, resume, refill, or reuse. Further work after such a
failure requires a materially different backend with its own qualification,
preregistration, source review, and authority; this document grants none.

An exact graphics/device/source preflight and absence of concurrent GPU work
may be checked before issuance/execution as ordinary read-only operational
conditions. They do not authorize a code, backend, cap, schedule, data, or
science change. The prior kernel reset succeeded; a host reboot is not a V3
prerequisite and is not asserted to improve the experiment.

Any other infrastructure failure also consumes V3 and produces no scientific
decision, retry, or resume. A scientific gate failure terminates this frozen
recurrent-DINO development recipe as already preregistered. Only a complete
and exact V3 scientific result may be interpreted; partial progress is never
evidence for or against the hypothesis.
