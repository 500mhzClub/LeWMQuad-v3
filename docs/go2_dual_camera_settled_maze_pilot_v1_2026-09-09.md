# Prospective dual-camera settled maze pilot

Execute one fresh development maze0 episode only after the completed V2
controller prefix passes its admission checks. Bind the tenth native result
a7a02db120b4b662cd66efee01f10784edb6f2ed6984bdc420007773a1b1b6fb
and readouta5523b729310d39a81baf9ccf9cf94fa6fb8a82077e322d4fbe394a45748a70d.
Require every closed-collection binding from the prefix to match that final
audited native artifact map. The prefix must stop at the first auxiliary
intervention with current registered pose and an active controller, unchanged
model, exact prior decisions/commands and no later recorded observations.
Require its saved intervention and stream to agree with its summary.

Change only the observer input path to explicitly admitted, already captured
downward RGB alongside the original front RGB/depth and public body/gyro data.
The controller tries the primary tracker first and uses the validated auxiliary
measurement path only for missingness or exhausted measured bridging. Preserve
all scene/gait/friction, sensor/rendering/calibration, motion acceptance,
visibility, map/contact/selector, learned model/residual, mission/settling and
shared3000-tick outbound/return rules. No mapping-performance candidate, detector
threshold change, renderer-identity integration, retraining or prior-state
continuation is included.

Use one new CPU physics scene, one numerical/OpenCV thread, a fresh assigned
model and controller, and exclusive go2_dual_camera_settled_maze_pilot_v1_attempt_001.
The session reads the existing auxiliary capture; no extra render or physical
step is added. Preserve the actual requested/applied command tape, every raw
sensor capture, full compressed decisions, timing and all failures. Retain
the existing storage, physical stop and ten-zero-command drain rules.

After collection, reconstruct all raw sensors, replay the full model/controller
and actual command path, audit the unchanged strict visibility and physical
arrival/return criteria, and verify model/source/input identities. Require
physical and paired-public prefix identity through the intervention observation,
all earlier commands exact, and every new candidate decision equal to the
prospective prefix, including its first intervention command. No later old
trajectory is inferred or replayed as if the new commands had been executed.

The tenth physically settled arrival and its failed return/visibility outcome
remain preserved. A known predecessor visibility failure lies in the unchanged
prefix, so this episode cannot establish full navigation qualification even if
it returns physically. Record physical route/arrival evidence and all failed
gates separately. This reused development maze is not independent-layout or
matched JEPA/planning/memory evidence; uncertainty, realistic timing/sensing and
bounded hardware requirements remain unfinished.

Run scripts/run_go2_dual_camera_settled_maze_pilot_v1.py with the actual
--prefix-result-sha256 and --preflight-only before launch. Assess CPU/GPU,
memory/storage and competing jobs. Require32GiB available RAM and the existing
10GiB collection plus1GiB persistence allowance above40GiB reserve. These are
admission checks, not OS limits. No concurrent native scene. Physics pauses
during computation; record receipt-inclusive deadline misses without claiming
real-time operation. No retry/resume or mutation of any frozen attempt.
