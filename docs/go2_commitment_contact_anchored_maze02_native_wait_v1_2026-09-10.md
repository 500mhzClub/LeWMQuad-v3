# One native contact-score trial after the exact existing predecessors

Waiter `scripts/await_go2_commitment_contact_anchored_maze02_native_v1.py` owns
exclusive root `go2_commitment_contact_anchored_maze02_native_wait_v1_attempt_001`.
It can launch the separately frozen native child only after both original
predecessor owners finish with authenticated complete results:

- Contact-score raw replay waiter: PID 2700744, creation 1789047270.48,
  launch `ca3027a13c67b92e2e181d92a55377ae102fcfd0bd007544ba87cf5dea61c880`.
- Previously scheduled hold-reorientation native waiter: PID 2671835,
  creation 1789036731.41, launch
  `e27675df102b072b62f4351483363ab9ee80e9e0244e1193c398392f716a47f6`.

Check exact commands and boot/creation identity. Poll both same owners every
30 seconds, up to 48 hours. A quiet or long-running original is not a failed
attempt. Missing completion after an owner ends, changed owner identity, changed
already completed result, timeout or child failure retains a terminal failure
without restart, retry or replacement. Preserve the original six-case adapter,
frontier and hold-reorientation order before this new native child.

Freeze the waiter, child, collector/auditor, prefix/completion verifiers, tests
and protocols in one source closure. The child launcher's independently
discovered sources must be contained unchanged. Before launch require the same
32-GiB RAM and 51-GiB artifact resource gate as the child. Pass both completed
result hashes explicitly; the child performs full prior input admission and
checks native idleness immediately before creating its exclusive output.
While waiting the new waiter owns zero native scenes; afterwards its single
child owns at most one. No training or hardware action is scheduled.

Observe that same child until exit, retain its stdout and then authenticate its
complete result, source/output/launch identities, worker terminal, raw audit,
physical prefix, readout and round-trip count. Reexecute the child's lightweight
input checks and unchanged joint outcome gates. The child itself owns full
transitive input verification and complete raw neural/controller replay; the
waiter does not claim another execution of those calculations.

Scientific failures with complete original collection/audit/physical-prefix
evidence remain valid recorded negative results. They do not permit extra
attempts. Neither scheduling nor successful process completion establishes
navigation, independent-layout reliability, real-time sensing/control or
real-platform readiness.
