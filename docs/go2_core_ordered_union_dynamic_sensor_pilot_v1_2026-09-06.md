# Science-identical moving pilot with core-profile precision query

The predecessor eight-run moving pilot is terminal after an OpenGL query failure,
before any committed episode. Its audit retains one attempted/uncommitted run,
seven unattempted, and no available sensor-pair or repeatability evidence. Preserve
all predecessor sources and partial artifacts; this is a distinct bounded attempt,
not resume, overwrite or rescue of its failed result.

The zero-scene native query probe reproduces GL_INVALID_ENUM specifically for
glGetIntegerv(GL_DEPTH_BITS) on the actual Mesa4.5 core-profile software context.
The core framebuffer-attachment query succeeds and records 8 subpixel bits,
24 depth bits and four RGB sample locations. GL state is restored and clean.
This proves the metadata-query correction on the native framebuffer targets,
not moving-sensor repeatability or navigation.

All scientific definitions, eight-case order, scene builds, fixed floor-first
ordering, robot/gait/collision geometry, seeds, roles, sensing, commands, native
stops, strict visibility score, separate footprint accounting, full-stream and
prefix comparisons, failure handling, storage budgets and scientific limitations
are exactly those in the frozen
[original moving pilot protocol](go2_ordered_union_dynamic_sensor_pilot_v1_2026-09-06.md).
The only runtime difference is the precision metadata query. A distinct capture
subclass calls the same mounted acquisition and ordering guard, then the tested
core-profile helper. No compatibility fallback, changed framebuffer precision,
rendered-pixel change, extra case, altered threshold or checkpoint is introduced.

The eight-run collector body is AST-identical to the original except its session
class symbol; the terminal audit computations are AST-identical. Original
constructor, command/sampling guard chain, raw auditor and comparison helpers
remain inherited and frozen. The new protocol identity is used for setup-witness
binding only; it does not change the physical setup check.

Before execution, bind original failed launch/failure/terminal audit, successful
core-query probe and ordered static bench, inherited source/native closure and
new capture/collector/auditor/tests/this protocol. Require the native query probe's
expected mechanism result. No old run is reopened or counted as complete.

Exclusive output is
`go2_core_ordered_union_dynamic_sensor_pilot_v1_attempt_001`
under the owned external navigation root. Keep the same1GiB total budget,
128MiB per-episode allowance and40GiB free reserve. Use CPU/software EGL only.
Run the exclusive terminal auditor once after collection stops, including partial
or unattempted cases. No further retry/resume is part of this protocol.

This attempt grants no training eligibility, boundary-pixel certification,
hardware or navigation qualification. Independent-layout predictive training,
RGB/history/action utility, reliable physical execution, matched online rollout
and memory/backtracking studies, real-time sensing/control and hardware evidence
remain required under the unchanged ultimate objective.
