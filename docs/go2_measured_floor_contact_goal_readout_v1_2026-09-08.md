# Measured-floor foot-contact readout V1

Authenticate the completed four-case native result, all artifacts, source/input
bindings and sample-bound predecessor before and after this exclusive readout.
Summarize verified goals, distances, physical/visual failures, raw/measurement
checks, phase/action traces, nominal feasibility and concurrent timing.

Retain whole-voxel/tight-AABB geometry accounting and explicitly distinguish
all-return conflicts from conflicts after the scoped foot-floor contact rule.
For each forecast/candidate record eligible measured-floor overlaps, retained
non-floor/unknown foot hits and feet with incomplete projected floor coverage.
Save final per-return classification accounting. Compare exact RGB, observer/map,
forecast and native/public array prefixes through the first changed command or
terminal decision. Full histories may match for unaffected cases; report actual
equality without assuming it. Keep the measured endpoint diagnostic and its
one-square/discrete/unqualified limitations. No unexecuted outcome inference,
new training, native execution, old-result mutation, retry, promotion or hardware
qualification. The full novel-maze goal is unchanged.
