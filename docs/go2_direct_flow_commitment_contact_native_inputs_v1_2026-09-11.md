# Contact-plus-flow native input admission V1

Require the exact completed contact full-controller replay, original contact
native result and waiter, and completed sustained-turn native waiter. Every
original owner must have ended on the recorded boot. The original tracking,
budget and sustained-turn executions retain their existing order; this
candidate cannot overtake or modify them.

Admit the full raw controller prefix through its checked helper, which
reconstructs all saved comparisons and raw public fingerprints. Reconstruct
its original ended-worker inputs. Bind the source worker to the final contact
native result and final contact waiter, with the exact original full-supervised
model and correction admission. Do not substitute the no-RGB-JEPA tracking
model or full-JEPA sustained-turn model.

Authenticate the sustained waiter against its frozen launch, prerequisite
identities, full artifact roster and actual native completion. Reexecute the
sustained native completion verifier, including physical prefix and native
readout reconstruction. Full admission reexecutes the original sustained input
admission, including its entire five-stage queue and full budget input chain.
Require the same batch and exact contact waiter throughout that chain.
Retain every scientific failure; a successful navigation result is not required
for the predecessor queue to finish.

The returned admission binds the raw prefix, original contact artifacts and
model correction, sustained waiter/native artifacts, original full input
admission and completion report. Later bound-input checks reconstruct current
artifact/completion bindings without claiming to repeat full training ancestry.
They require the stored initial full-admission flag and compare the entire
reconstructed admission, not only selected success fields.

This module prepares no native launcher or scene. A later launcher must still
enforce the exact controller/model definition, resources, exclusive output,
full raw audit and native prefix comparison. The only adopted change is direct-
flow tracking inside the original contact-scoring controller. Other queued
policy changes, independent-study selection and qualification are not adopted.
