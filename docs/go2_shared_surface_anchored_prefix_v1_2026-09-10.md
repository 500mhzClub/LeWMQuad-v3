# Paired raw replay of invocation-local surface receipt sharing

Use exclusive output `go2_shared_surface_anchored_prefix_v1_attempt_001` and
`scripts/replay_go2_shared_surface_anchored_prefix_v1.py`. Bind the exact
completed batched-query result
`650fe137b41c91f8860ce59987c876d1c2591329ee2fc63ad056e63059e47419`
and its completed receipt-copy and profile predecessors. The batched result is
prior evidence; neither the batched query implementation nor the faster
recursive copy implementation is enabled in this new controller.

Replay the same first 405 public observations, 0–404, of the fully audited
adapter JEPA maze-2 episode, with model SHA-256
`35496b6b402013f7a33d9c30110e115b90ded672f0d0da829a07128601507b6a`.
Construct two independent fresh model instances with no shared parameter
storage and two fresh controllers. Keep the original model, action ranking,
geometry, camera processing, mapper, memory, residual and mission calculations.
The candidate substitutes only the private recovery receipt-copy helper
specified in `go2_shared_surface_anchored_v1_2026-09-10.md`.

Use the completed receipt-copy replay's exact function code object in a private
namespace. Change only the candidate constructor, metadata normalizer, progress
label and output root. Retain every other global binding, including the complete
state-tree serializer. Normalize only the explicit top-level controller name
and sharing-enabled flag. No observed-state type or value normalization is
permitted. Do not modify imported module globals.

Verify every completed original command endpoint and every public packet hash.
Reconstruct every complete original saved decision; require the complete
normalized candidate decision to equal it, including predictions and all
receipts. Each controller must leave every public input array unchanged.
Compare complete retained observed memory, floor, occupied cells, residual and
history at frames 3, 12, 395 and 404. Require all 405 observations, 402 forecasts,
unchanged model states and absent gradients; do not consume observation 405.

Alternate controller execution order by frame parity and time each complete
observe call without profiling. Keep fixed windows 3–404, 3–12 and 395–404.
Report medians, totals, ratios and the number over 100 ms. Sensor acquisition
is outside these timings, the workstation is shared, and a single prefix does
not estimate population speed or establish real-time control. Retain regressions
and failures. Require the original full transitive input admission before and
after execution, plus final source and output binding verification.

Assess the current machine immediately before admission and output creation:
at least four physical CPUs, 48 GiB available RAM and 41 GiB free artifact
storage. Use one numerical thread, deterministic algorithms and no native scene.
The original adapter batch and queued native trials keep their existing owners
and source closures. There is no retry or adoption into an active native job.
This experiment establishes no new maze outcome or hardware qualification.
