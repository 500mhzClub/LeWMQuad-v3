# Chained retained-anchor observer prefix V1

Replay the completed no-RGB JEPA direct-flow worker's public sensor packets
from frame zero through the first changed observer evidence, either observer's
terminal failure, or frame 863, whichever comes first. The worker's original
full visual evidence must reproduce exactly at every consumed frame. Compare
fresh `DirectFlowDualCameraVisualMotion` and `ChainedAnchorVisualMotion`
instances. Preserve all original failures and the negative native outcome.

The candidate first runs the original observer. Original anchor success is
returned unchanged. A measured bridge or missing pose permits a second search
using bounded chains of original retained-reference pixels. Only a qualified
retained-anchor fit can replace the original result. Original qualified
measurements remain conflict vetoes. An unsuccessful search restores the
original evidence and spends the bridge allowance once. The original rigid,
gyro, reference-promotion, conflict and bridge rules remain unchanged. Cache
at most 33 co-timed grayscale/depth frame pairs; no pose increments are summed.

Bind the completed worker terminal, all its artifacts, log, and source closure.
Its parent completion is not claimed by this observer study. Bind the separate
first-bridge pair and recorded-pose verification. Check public packet fingerprints
before and after each observer call and validate every admitted pose through
the existing public dual-camera contract. Stop before consuming any observation
following the first changed evidence. This is not a controller replay or a
counterfactual physical continuation.

Use one CPU replay worker, one OpenCV/BLAS thread, a fixed CPU environment,
at most 864 frames and 256 MiB of derived decision receipts. Assess hardware
before launch and again after waiting. Wait for the exact existing visibility
replay PID/create-time/argv/boot owner to end; preserve and bind its terminal
record. That replay is an operational CPU dependency, not a requirement that
its optimization succeed. Never restart either job due to a polling timeout.
The maximum wait is 48 hours; no automatic retry or replacement is allowed.

The observer runner, new component and tests become frozen when launched.
No checkpoint, model inference, floor registration, map, action selector,
command execution, independent layout, or hardware motion is part of this run.
Improved observer evidence alone is not navigation success. A later complete
controller prefix and separately reviewed prospective physics experiment remain
necessary before claiming a useful navigation recovery.
