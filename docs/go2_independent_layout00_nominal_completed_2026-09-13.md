# Independent layout 0 nominal predictor comparison

The nominal requested-twist predictor failed to reach the goal. Collection and
the full audit completed in
`go2_stop_conditioned_independent_00_nominal_seed_2026091001_full_jepa_v1_attempt_001`.
Result SHA-256:
`5fb8376bbf196d6dcaf89e5a078d7f50d56aa2f6dbd7d0d9de6ff4aad827c76c`.

There were 1,532 decisions, 77,300 native physics samples, three distinct open
edges crossed, no invalid crossings and no contacts. The controller stopped
because no candidate satisfied its observed-surface and nominal constraints.
There were no arrivals; the final observed goal distance was 1.536 m.

Raw sensor reconstruction, auxiliary-camera reconstruction, nominal-controller
command replay, command auditing and strict physical visibility passed. The
audit recorded zero learned-model forward calls. Both resource phases completed.
The old learned-model replay field is false for this nominal arm; the applicable
nominal and controller replay fields are true.

Physics paused during high-level computation. Median observation/control time
was 708.59 ms, with all 1,532 samples exceeding 100 ms. This is one negative
independent-layout comparison, not real-time evidence or a general learned-model
advantage. The queue continues with the two contact-horizon treatments, layout 1,
the memory ablation and the matched six-action reactive comparator.
