# Contact-plus-flow native queue handoff V1

Own at most one child invocation of the prepared contact-plus-flow maze 02
physics launcher. Preserve the original tracking, extended-budget and
sustained-turn experiments. Their completion is an input prerequisite, not
permission to adopt their policy changes.

Require the original contact full-controller replay owner PID 2822758,
creation time 1789119437.63, and its exact command to have ended on the recorded
boot. Bind its launch and completed result to the identities already recorded
by the launcher preparation. Require the original sustained-turn native waiter
PID 2817601, creation time 1789117418.98, exact command and frozen launch
`679d0519e3eb5ddacf5cc3708254de12a117551725c43c0136141e43ce64263e`
to finish with its complete result. Never infer completion from elapsed time,
a quiet log or a missing observation handle.

While owners are live, poll their exact process identities every 30 seconds.
For ended owners, check result and launch hashes, source bindings and terminal
status without repeatedly traversing the training artifact ancestry. Preserve
each observed completion identity across polls. Reject missing results, failure
files including dangling symlinks, changed identities, retries or expired waits.
The maximum wait is 48 hours; expiration does not restart any predecessor.

After both owners finish, recheck source bindings, all original owners, resource
capacity, native idleness and exclusive child output. Start exactly one child
with the raw result and sustained waiter result hashes as explicit arguments.
The child performs the complete input admission, including the original model,
raw replay and full original queue, before creating its physics output.

Wait for that child to exit. A nonzero exit preserves failure and causes no
retry. A zero exit requires the complete artifact roster and worker receipts,
original input verification, full raw-audit checks, actual physical readout and
reconstructed physical/public/controller prefix. Retain an audited zero-success
outcome. Do not equate process completion with navigation success.

The waiter stores its launch, progress events, exact input completion identities,
child output and authenticated native completion. No independent-study policy
review, generalization claim, real-time claim or hardware claim is made.
