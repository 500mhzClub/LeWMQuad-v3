# Independent recorded verification of the combined optimization replay

Use `scripts/verify_go2_reused_floor_mesh_prefix_v1.py` only on the completed
`go2_reused_floor_mesh_prefix_v1_attempt_001` result, with explicit final result
and launch SHA-256 arguments. Reject a failure file, incomplete output,
changed source bindings or any claim broader than the prospective comparison.

Bind the original execution preparation
`d3cbb8a236ce5c136bf97a823b73302c4c9e9f67593eaa9ea74768a285869979`.
Verify its source map and extend it only with this checker, protocol and tests.
Source preflight must not inspect incomplete replay outputs, execute models or
write a verification result. Existing live source files remain unchanged.

Authenticate the exact completed optimized profile and preceding frozen-footprint
replay. Read only original observations 0–404 and their completed command tape,
with original artifact hashes checked before and after. Reconstruct all complete
original decision hashes and all expected candidate hashes. The only candidate
differences are the top-level controller identity, frozen-footprint flag and
mesh-reuse flag. Preserve every nested evidence field. Check each public-input
hash against both completed reference streams. Require 402 forecast-bearing
observations and original command endpoints.

Recompute all unprofiled paired timing windows, enforcing positive finite times,
alternating execution order, exact frame counts and the reported 100 ms exceedance
counts. Check all four reported retained-state hashes against the completed
frozen-footprint replay without any type or value normalization. Recheck sources
and artifacts afterward and write the exclusive document
`docs/go2_reused_floor_mesh_prefix_verification_2026-09-10.json`.

This is independent recorded decision/hash/timing verification. It does not
rerun neural inference, reload raw sensor arrays or independently reconstruct
hidden state. The original paired replay owns those execution checks. Timing
compares the original controller with the combined frozen-footprint and mesh
optimization; it does not isolate the incremental mesh contribution. No native
episode, navigation success, real-time qualification or goal completion follows.
