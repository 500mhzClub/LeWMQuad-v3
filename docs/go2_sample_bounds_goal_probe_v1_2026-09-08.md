# All-return sample bounds and exact foot spheres: native probe V1

Run four fresh known-layout cases with the same fixed final full-RGB direct and
JEPA models on 052/039, roster order seed 2026091102. Authenticate the completed
nominal-action native cohort/readout, the measured-sample-bounds diagnostic,
and original six-fit accounting. No predecessor retry, resume or modification.

Replace persistent whole-voxel surface queries with outward-rounded bounds
enclosing every original sampled return in each same 25-mm key. Preserve first
witnesses and total sample counts; later observations only expand enclosures.
Use exact sphere-to-bound-box distance for URDF spheres, preserving tangency;
retain conservative AABB queries for other primitives. Save whole-voxel and
tight-AABB conflict accounting alongside refined checks. No ground label,
sample deletion, contact waiver, uncertainty bound or free-space certificate.
The original current-frame voxel index is retained but cannot be selected by
this explicitly persistent-only variant. Raw replay rebuilds the entire index.

Keep nominal action and connector radius 0.45 m, utility, models, observer,
map floor/occupied construction, command vocabulary/commitment, scene/sensing,
actuator, mission goal/budget, zero drain, physical stops and native evaluator.
The current-posture-only and discrete-prediction limitations remain. Three new
integration tests plus five sample-bound and 27 predecessor tests must pass.
Every fresh case independently replays its complete raw sensor-to-command chain
with a new evaluation-only model reload. Goal/actuator audit functions stay
identical. All failures and all launched sibling outputs are retained.

Inspect hardware; require 32 GiB available RAM and 8 GiB planned output plus
40 GiB reserve. Use four fresh single-thread CPU processes supported by prior
native scaling; monitor approximately every 15 seconds. Record concurrent loop
timings with paused compute physics, without real-time or hardware claims.
Report verified goals, contacts, distances, stop reasons, geometry/nominal
constraint decisions and full audit outcomes. Four cases on two known layouts
and these unqualified models do not establish independent-maze navigation,
physical backtracking, JEPA benefit or deployment readiness.
