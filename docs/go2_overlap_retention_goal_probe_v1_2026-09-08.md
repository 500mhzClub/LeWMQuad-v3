# Qualified-overlap retention native goal probe V1

Run one fresh full-RGB direct-model case on family episode 039 after exact
admission of the completed overlap-retention observer replay. Require all 147
candidate replay poses, the fixed diagnostic accuracy thresholds, exact original
observer reconstruction and all source/artifact identities. This is a known
affected development case, not a new matched-model or independent-maze cohort.

Use OverlapRetentionGoalProbe with the frozen half-feature-overlap retention
observer. Keep the retained-patch contact map, four-foot floor rule, persistent
all-return bounds, complete footprint-specific patch witnesses, nominal
constraints, waypoint/scan selector, unchanged six model forecasts and selected
direct snapshot. The only new behavior is earlier retention after an already
qualified anchor measurement. No bridge-only promotion or weaker acceptance.

Keep the initial-frame [1.2, 0] m goal, three warmup ticks, 240 navigation ticks,
five-tick command commitment, 4-cm observed-arrival candidate and ten quiet
intervals. Preserve the native 6-cm arrival/quiet test, command clipping, 2-ms
physics, 50-Hz low-level actuator, sensor acquisition and strict raw/visibility
checks. Stop on existing failure conditions and retain ten zero-command drain
intervals. No retry/resume, extension or old-attempt change.

Admit all six fixed fit identities, exact source closure, robot geometry and
predecessor readout. Use one fresh CPU process for this single causal case;
the recorded four-worker fixed-tape scaling experiment remains identity
evidence, not authority to invent duplicate cases. Record CPU/RAM/GPU/storage
and competing jobs before execution and periodically. Require the inherited
32-GiB RAM, 8-GiB output allowance and 40-GiB storage reserve.

Retain all images, depth, public histories, physics, contact/setup/geometry
receipts, forecasts, retention evidence, maps, commands and timings. Reload the
assigned snapshot in a fresh model and replay every public sensor-to-command
decision exactly; preserve model state, every stopped/failed outcome and all
artifact hashes. Reauthenticate sources and inputs after execution. New source,
test, collector and auditor names distinguish this attempt from its predecessor.

Physics still pauses during computation; report full iteration latency and
missed 100-ms periods honestly. No real-time, physical support, uncertainty,
independent-maze, backtracking, navigation or hardware qualification is granted.
The full end-to-end goal remains active regardless of this component probe.
