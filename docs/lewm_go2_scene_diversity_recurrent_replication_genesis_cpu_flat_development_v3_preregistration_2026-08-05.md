# Go2 scene-diversity recurrent replication: flat Genesis/CPU development V3

Date frozen: 2026-08-05

Status before execution: `PREREGISTERED_SOURCE_ONLY_NOT_RUN`

Scope: one fresh development-only two-scene infrastructure qualification.
V3 changes one runtime input from consumed CPU-flat V2: it adds exact
`PATH=/usr/bin:/bin` to the child environment while retaining fixed
`HOME=/home/andrewknowles`. Every scene, state, history, action, horizon,
model, seed, gate, threshold, process, resource, timing, rendering, and
36-lane counterfactual field remains frozen.

## Bound V2 closure

CPU-flat V2 is closed by:

- `docs/lewm_go2_scene_diversity_recurrent_replication_genesis_cpu_flat_development_v2_qualification_terminal_review_2026-08-05.json`;
- SHA-256 `60f18cad6ced0f18fd6e4884439fd688d79bef3deef0a62d41ac66f258dead1d`;
- 12,883 bytes; and
- status `PASS_FAIL_CLOSED_PRE_PHYSICS_PATH_ENVIRONMENT_STOP_ITERATION_TERMINAL_REVIEW`.

V2 retained fixed `HOME` and reached the Genesis initialization banner, then
failed during CPU-device identification with `StopIteration` before scene
construction or physics. Under the exact ten-key environment, `PATH` was
absent and `cpuinfo` returned no usable `brand_raw`, `hardware_raw`, or
`vendor_id_raw`. A payload-free diagnostic under the same bound interpreter
showed that adding only `PATH=/usr/bin:/bin` returns the expected AMD Ryzen and
AuthenticAMD identity. The independent reviewer reproduced this without
initializing Genesis, reading qualification payloads, rendering, or running a
model.

V2 produced no CPU-physics, lane-exactness, rendering, model, or scientific
evidence. It is consumed and retired; no V2 authority, reservation, cache,
receipt, result, partial output, or runtime payload may be reused. The review
recommends only fresh V3 with fixed `PATH=/usr/bin:/bin`, retained fixed
`HOME=/home/andrewknowles`, exactly eleven environment keys, and unchanged
backend, render route, probes, four states, 36 lanes, histories, actions,
horizons, model, seeds, gates, and thresholds. It authorizes no V3 execution by
itself and recommends no alternate environment, architecture, tolerance, or
batching change.

## Exact V3 delta

The immutable V2 qualification plan is:

- `docs/lewm_go2_scene_diversity_recurrent_replication_genesis_cpu_flat_development_v2_qualification_exact_plan_2026-08-05.json`;
- SHA-256 `df4516631a646b46ee89ca9be7aa55b47bf5927a9440a26c4c4fee19e0147ce0`;
- 353,031 bytes.

The V3 qualification plan may differ only in:

1. fresh V3 `attempt_id`;
2. fresh V3 `output_root`;
3. V3 qualification-contract identity and predecessor bindings; and
4. `execution_contract.environment.PATH=/usr/bin:/bin`.

Normalizing those fields must reproduce the exact V2 plan canonical JSON.
Recursive comparison outside `successor_contract` must find only `attempt_id`,
`output_root`, and `execution_contract.environment.PATH`.

The exact interpreter remains:

`/home/andrewknowles/Workspace/LeWMQuad-v3/.generated/venvs/genesis_render_vulkan/bin/python`

The child receives exactly eleven environment keys:

```text
EGL_DEVICE_ID=1
GS_BACKEND=cpu
GS_PARA_LEVEL=0
HOME=/home/andrewknowles
MESA_VK_DEVICE_SELECT=1002:7551!
PATH=/usr/bin:/bin
PYOPENGL_PLATFORM=egl
PYTHONDONTWRITEBYTECODE=1
PYTHONHASHSEED=0
PYTHONNOUSERSITE=1
PYTHONSAFEPATH=1
```

Neither `HOME` nor `PATH` is inherited from ambient state. Both are fixed
plan inputs. No other ambient variable is admitted. Physics remains exact
`gs.cpu` with serialized `GS_PARA_LEVEL=0`; policy remains on CPU. Rendering
and graphics preflight remain on the exact bound Vulkan/EGL AMD Radeon AI PRO
R9700 route. There is no backend, architecture, batching, tolerance, or
science change.

The harness remains flat and explicitly owned. It may not patch, replace,
overlay, context-mutate, or communicate configuration through imported project
module attributes, validators, collectors, environments, or graphics
expectations. Static plan, source, input, and historical-parity validation
must complete before root reservation. Runtime configuration is passed
explicitly to each fresh child.

## Fixed two-scene qualification

V3 uses the fresh root:

`.generated/dev/go2_scene_diversity_recurrent_replication_genesis_cpu_flat_development_v3_qualification/attempt_v1`

The deferred V3 science root must remain absent. Exactly two fresh full-scene
worker process groups run in order `[12, 0]`, one process per scene. Each has a
300-second hard watchdog and executes the unchanged:

- four states and both full history blocks;
- nine matched successors per state;
- 36-lane `parallel_lockstep_envs_no_restore` mechanism;
- full physics horizon and invariant checks;
- 12 context frames, 36 targets, and 48 stored RGB frames per scene; and
- auxiliary depth, render-count, nonfinite, mesh, receipt, and binding gates.

All nine lanes within each state group must remain exactly equal throughout
common-history execution. No numerical tolerance, state-local fallback,
snapshot/restore mechanism, adaptive batching, or alternate branch semantics
is admitted. Complete first-step and synchronization-checkpoint component
diagnostics remain mandatory.

Because rasterization still uses the R9700, the parent continuously samples
selected-device memory. Any observation above `16,977,405,952` bytes fails.
After every worker, including the last, the coordinator must wait for process
exit, reject a leaked process group, and complete the release barrier against
that worker's matching prelaunch selected-device baseline before opening its
result or launching another worker. A bracketed kernel-journal gate must find
no new AMDGPU timeout, reset, wedge, or HSA/KFD exception.

Qualification timing passes only when:

`64 * max(worker_elapsed_seconds) + 900 <= 7200`

The 900 seconds remain reserved for joins, feature extraction, fitting,
durable checkpointing, and two evaluations. The watchdog does not replace the
projection gate.

Any environment, initialization, exactness, physics, render, integrity,
resource, release, process, kernel, or timing failure consumes V3. There is no
retry, resume, refill, overwrite, repair, environment expansion, or partial
reuse.

## Output isolation and deferred science

Qualification output is non-scientific and permanently non-reusable. No V1,
V2, or V3 scene, cache, receipt, RGB, mesh, diagnostic payload, timing trace,
result, or partial runtime artifact may be copied, joined, opened by, or reused
in a 64-scene run.

On success, the harness writes the complete non-authorizing result and
terminal before emitting a minimal PASS decision last. Only that exact PASS
decision may later be rehashed as a science-plan release condition. A PASS
means solely that the fixed CPU mechanism completed exactly and projects
within the wall cap; it makes no model, representation, planning, navigation,
or generalization claim.

No V3 scientific plan exists at preregistration, and the plan builder has no
science-plan output path. A future scientific plan requires an independently
validated V3 PASS plus a separate source review establishing complete
qualification-payload isolation.

If released later, the science experiment remains the frozen 64-scene,
256-state, 2,304-branch comparison: recurrent DINO visual, matched exact-zero-
vision, and live task/action control; the same roles, histories, textures,
actions, DINOv2 binding, seeds, optimizer, 800 updates, train-only statistics,
durable checkpoint, two evaluations, scoring, and 10,000-resample paired
family/scene bootstrap.

The fixed scientific gates remain:

1. integrity, role disjointness, context-only custody, repeatability, and the
   privileged physical oracle pass;
2. visual normalized rank regret is at most `0.13`;
3. visual minus live task/action regret is at most `-0.02`, with paired 95%
   upper bound below zero;
4. visual minus matched no-vision regret is at most `-0.01`, with paired 95%
   upper bound below zero; and
5. visual beats uniform-random expectation.

## Custody, authorization, and stop rule

No sealed, `sealed_*`, held-out, production, or other protected material may
be opened, searched, indexed, copied, or used. Both V3 roots must be absent
before their respective one allowed invocations. All predecessor artifacts are
identity evidence only.

The user's instruction authorizes implementation and source verification of
this fixed successor. One qualification invocation becomes eligible only
after the independent V2 terminal review is bound and a separate independent
review passes the exact V3 preregistration, builder, plan, harness, and focused
tests. This document creates no execution authority by itself and does not
authorize science.

Any V3 qualification failure hard-stops this route without tweaking. If exact
lane equality or the two-hour projection fails, do not relax tolerance, change
batching, or alter the model. All outputs remain development-only,
non-promotable, and ineligible for final benchmark, production, deployment,
or sealed-evaluation claims.
