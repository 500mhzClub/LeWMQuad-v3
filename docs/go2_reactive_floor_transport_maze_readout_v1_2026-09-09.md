# Paired learned/reactive physical outcome readout

Run scripts/read_go2_reactive_floor_transport_maze_pilot_v1.py only after the
current reactive native pilot and current learned native/readout have completed.
Supply their actual --native-result-sha256 and --learned-readout-sha256.
Exclusive output:go2_reactive_floor_transport_maze_readout_v1_attempt_001.
This readout loads completed files only, creates no scene and loads no
high-level world model. Assess current hardware and require8GiBavailableRAM
and128MiB above the existing40GiBreserve before execution.

Bind the complete reactive native result/artifacts and launch, the exact learned
native result/artifacts recorded by that launch, and the completed learned
readout result and launch. Require the learned native SHA to agree across
these records. Verify all bound sources and input artifacts before and after.
Admit only the expected current controllers and the two fixed maze0 case names.
Require the full900sample/four-observation physical/public/shared-state prefix
and all complete prospective reactive decisions to have passed the native audit.

Compare exact scene, coordinate mission, URDF, navigation/shared return budget,
worker/numerical thread settings, storage allowances, paused-physics semantics,
renderer environment/witnesses and native/input/runtime bindings. A different
scene, sensor/actuator/runtime binding or mission cannot be silently labelled
matched. Both systems retain persistent observed memory. The method difference
includes prediction/residual scores and predicted versus current-geometry gates;
do not label it an isolated prediction-ranking or memory ablation.

Preserve the original reactive actual-motion readout calculations and add the
current raw/registered/transport pose accuracy, paired-camera use and renderer
audit. Pair the resulting metrics with the already completed current learned
readout. Include full native arrival windows, outbound and return traversals,
physical retracing, terminal quietness, native round-trip candidate result,
strict visibility, hard measurement failures and verified-round-trip result.
Also include path length, actual terminal location and goal distances, simulated
duration, physical/acquisition/controller terminals, action counts and all
receipt-inclusive timing distributions. Keep an absent return traversal null;
do not convert an outbound arrival or a physical candidate into verified success.

The readout must preserve negative and partial outcomes and perform no model or
controller selection. It reports one reused-layout method comparison. It cannot
establish statistical reliability, independent-layout generalization, a JEPA
training advantage, an isolated planning effect, memory advantage, real-time
operation or hardware qualification. All earlier failed outcomes remain intact.
Preparing this code does not launch the reactive native pilot or its readout.
