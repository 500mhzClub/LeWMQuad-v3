# Independent-layout RGB/body collection: implementation and execution record

The full scientific objective remains active and unachieved. This stage acquires
independent-layout prediction evidence; it does not train a model or demonstrate
learned navigation, rollout utility, memory benefit or hardware performance.

## Implemented fresh collection and terminal audit

The [prospective protocol](go2_independent_rgb_body_collection_v1_2026-09-06.md)
preserves all12 layouts and6/3/3 roles,120 fixed cases per layout and the prior
inventory's geometry, seeds, action/history/support/context population. New
output roots cannot resume or relabel the old batch or sensor pilot.

The new collector imports the frozen core-ordered pilot's native `collect`
function directly, without changing its gait, sensing, commands or physical
stops. New batch machinery binds the exact union/raster/native/sensor artifact
roster and byte counts, writes durable commits and raw prechecks, and applies
the prospective modality-specific eligibility contract. Boundary-only strict
depth failures remain reported, with no pixel repair or navigation permission.
Hard stable-interior/near-occlusion errors and infrastructure failures stop the
batch. Physical stops and failed setups remain planned population outcomes.

The terminal auditor verifies terminal-only access, full commit/precheck
accounting and exact reconstructed raw evidence. It preserves all departure
windows/targets separately from eligible RGB/body windows/targets and dataset
role mappings; all120 case statuses and same-context action-prefix comparisons
remain in the denominator. It materializes eligible tensors with the existing
dataset interface, without fitting or selecting a model.

The stage runner collects one fresh batch and immediately audits its terminal
record, including a collection failure. It refuses existing outputs and cannot
restart/resume a batch. Later layouts require the preceding fixed layout's
completed120-case raw audit and no hard measurement failure.

## Verification before native execution

Initial focused88555 passes97 tests in5.53s. Adding full-population terminal-audit
tests yields56878:100 passed in5.91s. Five further stage-orchestration tests
cover success, terminal collection failure, preflight failure, missing terminal
record and existing-output rejection; they are included in the final regression.
The tests also cover exact inventory roles, copied native collector identity,
boundary versus interior/near-occlusion eligibility, legacy-output rejection,
artifact hashes/bytes, failed captures, raw corruption and retained120-case
denominators.

The initial shell preflight command had an unmatched parenthesis before import
or output creation. Corrected52897 passes with764 bindings. After adding the
stage runner, final preflight89003 completes exit0 with765 source bindings and
120 exact l00 training cases. Full231-file regression79082 completes exit0:
2,987 passed in224.16s, including44 new collection/eligibility/audit/stage tests.
Read-only97666 confirms24 cases per context,60 per history/support and20 per
action-duration cell, all in the fixed training role.

## Fresh l00 execution launched

Stage92647 is launched once under the frozen765-source definition, with
CPU/software-EGL rendering and the declared thread/environment settings. It
collects all120 prescribed cases and then runs the terminal audit; physical
failures remain outcomes, infrastructure or hard measurement failures terminate
collection and still receive a terminal audit when a terminal record exists.
No new model fit or navigation result is claimed. Do not restart this stage or
launch a competing audit while its handle is live.

The explicit external output child is
`go2_independent_rgb_body_collection_v1_l00_attempt_001`. Completion, individual
eligibility counts, pairing and final artifact hashes remain to be observed.

Launch SHA-256:
`cc0d583329b3b6713d84e4f8911d089f90160858401bdbb9dc5f384bda75f9c3`.
The exact stage PID2055172 is independently observed live at54s and171s elapsed.
At the latest native progress check, the first four cases (open passage, quiet,
nominal, actions0 through3) are committed and raw-prechecked. All four complete
their prescribed schedules and pass the hard measurement checks, with prospective
RGB/body eligibility. These are training-layout acquisition outcomes, not mission
successes or an independent-layout generalization estimate.

Read-only86855 completes exit0:verifies explicit artifacts and materializes the
first three actual recorded cases through `PulseTimedDataset`. Input histories
have RGB[4,3,96,128],body[4,20,63],control[4,15,7]; each has5 valid motion targets
and0 positive contacts. Both nonreference common-prefix comparisons (actions1
and2 against action0) match exactly. This is a bounded early integration check;
the full120-case collection, all pairing comparisons and terminal audit remain
pending, and the live stage continues without any source change or model fit.

Subsequent native progress completes all six action-duration cells in that first
open/quiet/nominal context:13,950 physics samples and195 RGB-D frames. Read-only
19679 completes exit0, verifies those exact artifact rosters and the frozen
launch hash, materializes the remaining action3/4/5 tensors and confirms all
five nonreference common prefixes match action0 exactly. Together the two early
checks materialize all six actual samples, each with5 valid motion endpoints.
This is still one context on one training layout, with no generalization or
learned-model result. PID2055172 remains live at297s elapsed (RSS2,319,084KiB);
stage92647 continues through the remaining fixed cases and subsequent audit.

## Scientific follow-through

Newer execution and integration evidence is recorded in the
[receipt-bound study-data progress](go2_independent_rgb_body_study_data_integration_2026-09-06.md).
The live stage reaches 69/120 raw-prechecked cases, with no terminal result yet.
A separate read-only check verifies the first 54 raw artifact rosters and all
45 nonreference sibling prefixes exactly. Boundary-only depth failures remain
explicit. The newly added study loader and hazard evaluator are included in a
completed 233-file regression: 3,042 passed. No new model fit has started.

Complete and audit l00, then proceed through the unchanged remaining fixed
layouts as valid acquisition and storage permit. Preserve failures without
selecting replacement cases. The model comparison remains conditional on actual
eligible train/selection/development-evaluation coverage and its frozen study.
Use matched direct/supervised-rollout/JEPA arms,3 paired seeds, action/time and
zero-motion baselines and RGB/history/action ablations. Reliable local execution,
online rollout/memory/backtracking, novel-maze missions, depth-navigation
uncertainty, real-time and deployment-valid/hardware sensing remain later
requirements of the same unchanged goal.
