# Shared JEPA V5 raw-supervision V8 existing-agent identity rebinding amendment

Date: 2026-07-13

Amendment author: `/root`

Status: **source construction and different-agent review only; no exact authority**

## Boundary

This administrative successor binds the V8 implementation roles to two already
allocated, idle agents because the scheduler cannot allocate the two new agent
identities named by the predecessor amendment while Camera V9 construction is
active. It changes no science, source path, schema, API, transaction,
adversarial-test requirement, reviewer-separation rule, authorization row,
resource rule, or execution sequence.

Its predecessor is
`docs/lewm_go2_shared_jepa_v5_raw_supervision_builder_auditor_v8_terminal_quiet_successor_amendment_2026-07-13.md`,
with SHA-256
`054de82d8648cd6be7edff01b82d549ec916700ebffad51698d4c2041edc6c88`.
Every predecessor requirement remains binding except the two implementation
author literals replaced below.

No Builder V8 or Auditor V8 source, CLI, test, handoff, review, authorization,
exact output, dataset, or audit result existed when these bytes were frozen.

## Replacement identities

The fixed Builder V8 implementation author is:

    /root/raw_v7_successor_author/auditor_v7_author

The fixed Auditor V8 implementation author is:

    /root/camera_v5_independent/camera_v7_pre_freeze_review/v7_review_artifact_schema

The agents are distinct from each other and from the amendment author. The
Builder author did not author Builder V7; the Auditor author did not author
Auditor V7. Prior work by either identity grants no V8 authority and may not be
substituted for the required standalone V8 sources and tests.

Each canonical V8 review must bind the corresponding replacement identity in
its `implementation_author` field. The two reviewers must still differ from
`/root`, both implementation authors, and each other. All nine authorization
paths, both review schemas, their authority fields, and the requirement for a
separately human-supplied frozen authorization file hash remain unchanged.

## Explicit non-authority

This amendment permits only Builder V8 and Auditor V8 source construction and
their later different-agent source reviews. It does not authorize review by an
implementation author, authorization publication, exact build or audit,
opening canonical `.generated` payloads, data, RGB, checkpoints, training,
selection, calibration, G2, held-out, runtime, navigation, hardware,
production, promotion, retry, or deployment.
