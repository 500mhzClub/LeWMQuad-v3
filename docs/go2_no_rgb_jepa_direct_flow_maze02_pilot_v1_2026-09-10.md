# One fresh no-RGB JEPA tracking-recovery maze-02 episode

Prospectively test DirectFlowResidualAnchoredController on the same development
maze, robot, appearance, sensing and fixed no-RGB JEPA model as the failed
original episode. The exact model state is
`fb6f1aba8830a53d67cd6c284fb24199966d5f0c63db3b2a107ab833c81c266f`.
This isolates the existing direct-flow observer integration. No queued frontier,
hold-reorientation or contact-policy change is adopted, no checkpoint is chosen,
and no independent-layout population is opened by this experiment.

Before execution require the completed positive full-controller prefix and its
original owner to have ended. Authenticate its complete artifacts and all 860
saved comparisons, the 859 unchanged original decisions, the 856 original
forecasts and the measured frame-859 recovery. Reexecute the original full
model/training/episode input admission. Require exact completion of the original
six-case batch followed by frontier, hold and contact experiments, using their
three waiter result hashes and exact owner identities. Their completion and
artifacts are operational prerequisites; queued policies do not become this
experiment's model/controller inputs. Do not claim their full training-input
admissions were rerun or that an independent-population policy review occurred.

Use one exclusive attempt, one fresh spawned scene worker, one OpenCV/Torch/BLAS
thread, the original native resource envelope for one case, and no retry or
resume. Verify simulator/library/geometry/model/source identities and refresh
resources before collection. The native idle check must pass. Model, observer,
mapper, residual and mission state start fresh; the original 3,000-navigation-tick
budget and terminal drain remain unchanged. The implementation is simulated
physics with rendering and physics paused during computation, not hardware or
real-time qualification.

After collection retain the complete artifact roster before auditing. Use a
second fresh model and controller to reconstruct every raw sensor/model/command
decision and check unchanged weights. Run the original strict visibility,
contact and maze round-trip evaluation. Compare all 43,700 pre-intervention
physics samples, all 860 observations and complete decisions against the
prospective replay, including completed execution of command 859. A recovered
hold is not movement; preserve the distinction and measure all following
physical outcomes independently. Keep all scientific failures in the result.

Retain raw collection artifacts, logs, audit and comparison receipts if any
later stage fails. Finish only after final source/input/artifact verification.
Report completed audited scientific failure separately from worker/audit
failure. The broader navigation goal is not completed by this pilot alone.

Source-only preflight loads no model, admits no generated input population and
creates no runtime output. Full preflight authenticates inputs without creating
a scene. Runtime requires explicit completed-prefix, batch, frontier, hold and
contact result hashes; no automatic waiting launcher or queue change is added.
