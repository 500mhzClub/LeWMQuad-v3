# PHYSICAL_GRAPH_EDGE_HANDOFF_QUALIFICATION_V3

Development-only semantic replacement of V2. V3 preserves the exact V2/V1 physical panel, teacher, candidate bank, target selection, frozen encoder, frozen ranker, metrics, gates, classifications, and next-decision tree.

V3 adds three descriptive snapshot identities: exact artifact-file SHA-256, canonical snapshot_semantic_digest_v1, and the designated trial-0 snapshot_behavioural_digest_v1. Raw artifact equality is descriptive only.

Every freshly captured V3 pool snapshot receives two 750-sample restores, each in its own fresh simulator instance under command [0.2,0,0]. The exact 45-D controller observation before each policy act and raw 12-D policy._last_actions after that act are repeated over its ten 2 ms samples. Pools 0..7 also restore immutable historical V1/V2 snapshots read-only in separate fresh instances.

Before pool 8, the immutable 48-trace material subset and eight-row semantic/behavioural comparison must pass. Failure is the technical terminal `V1_V2_V3_SEMANTIC_OR_BEHAVIOURAL_REPRODUCTION_MISMATCH` with exactly four official leaves and no scientific result. Success authorizes the unchanged inherited pipeline and one final 544-trace official probe NPZ.

No model, threshold, scientific formula, gate, class, target, candidate, split, or tuning rule changes. Historical shards are never copied, hardlinked, or used as V3 teacher/candidate/ranker inputs.
