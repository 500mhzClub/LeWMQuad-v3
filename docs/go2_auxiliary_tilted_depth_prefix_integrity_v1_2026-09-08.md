# Auxiliary depth prefix segmentation-integrity successor V1

Execute the exact scientific and resource scope in
`docs/go2_auxiliary_tilted_depth_prefix_v1_2026-09-08.md` in a distinct fresh
`go2_auxiliary_tilted_depth_prefix_integrity_v1_attempt_001` root. The original
attempt stopped on its first diagnostic segmentation background entry before
any commanded-prefix tick. Its source, initial sensor artifacts and failure
remain immutable; this successor does not retry or resume that root.

The sole acquisition-code correction is explicit validation of native
background `0 -> -1`, separately from nonnegative entity/link tuple entries.
Retain the background in diagnostic metadata, exclude it from robot labels,
and reject malformed labels or a missing robot identity. Segmentation remains
evaluator-only and cannot influence coverage, commands or model input.

Preserve all other acquisition, camera-pose restoration, robot visibility,
renderer ordering, physics, settling, setup, 19 replayed turn/zero commands,
20 dual views, original-sensor comparisons, raw reconstruction and coverage
analysis. Preserve 32-GiB available RAM, 1-GiB planned output above the 40-GiB
reserve, one CPU scene and one numerical thread. Reverify the failed launch
and failure identities as well as the original geometry/probe sources.

Freeze the new helper, background tests, source-scope tests, launch script and
failure report before execution. Preserve every further failure; neither this
integrity correction nor a successful sensor capture constitutes new navigation,
independent-maze, simultaneous-camera, hardware-mount or timing validation.
