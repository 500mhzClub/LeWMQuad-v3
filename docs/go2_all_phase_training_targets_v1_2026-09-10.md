# Expanded training contexts from existing recorded trials

The current observation-horizon models use408 available training contexts, all
at camera indices congruent to3 modulo5. A training-only census of the original
recordings found4010 available contexts over4800 fixed candidate slots.
Materialize this complete target population without collecting new physics,
fitting weights, changing roles or reading future RGB. These are target-side
native labels; they must never enter model policy inputs.

Use exactly the original48 family and72 switch training trials, every offset
0..39 of each trial's original40-command suffix, and the first eight remaining
known commands at100ms cadence. Family departure is3+offset; switch departure
is13+offset. Retain missing/post-contact contexts and all existing target masks.
Preserve every original456 training context slot and its exact target values,
including408 available contexts. Do not reassign geometry-transfer or sealed
roles, add navigation outcomes to training, or infer unrecorded commands.

Original target result:
fe3ab252e6da0ebadba13927c0dad7410d2084145c3b50439f6848ea5b65a775.
Original windows:
c293e454ac391da377a282c61dc30dd6abbdd5274d253c963e4837a78e8f7811.
Original launch:
b7ba49b2c354600108aa9ba0e0e35ba5290f9acf94069b13ab5cea633fd8bddd.
Use its exact training-trial physics/camera bindings. Verify original target
artifacts, sources and consumed training leaves before and after derivation.
The historical raw collection audits are not rerun by this target derivation;
the binding and native target calculations are the explicit verification scope.

Expected complete census:4800 slots,4010 available,3974 first-step motion labels,
3140 complete eight-step motion sequences. There are1443 valid first-step motion
labels with a zero next command. The existing recordings still have command
switches only in phase3, including77 nonzero-to-zero switches. Reindexing adds
context phases, not command-switch phases or independent episodes. Preserve this
limitation in subsequent training and evaluation claims.

Use one CPU derivation process, at most one native scene owned by the existing
queue,8GiB derivation RAM plus32GiB concurrent-native allowance,40GiB artifact
reserve and64MiB new metadata allowance. Keep one training trace resident at a
time. No checkpoint, model forward, optimizer, rendering or future image load.
Output go2_all_phase_training_targets_v1_attempt_001 is exclusive; failures
remain recorded. Source and existing runtime artifacts remain unchanged.

Subsequent work must separately verify causal past-only input materialization
and private training-only future packets for these new indices, fix matched
training schedules and assignments before evaluation, and compare actual
navigation. Targets or loss improvements alone do not establish JEPA benefit,
useful planning, braking reliability, real-time operation or navigation success.
