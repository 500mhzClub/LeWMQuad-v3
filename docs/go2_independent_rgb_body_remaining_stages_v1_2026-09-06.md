# Fixed remaining RGB/body collection stages: sequential supervisor V1

This automates the existing l01–l11 stages only after l00 completes its collection
and automatic terminal audit. The initiating operator must observe l00 stage
92647 terminal exit 0 and supply the exact terminal-audit SHA-256. The supervisor
also verifies the fixed l00 launch SHA
`cc0d583329b3b6713d84e4f8911d089f90160858401bdbb9dc5f384bda75f9c3`
and fully validates the completed batch through the receipt-bound study loader.
It cannot attach to, restart, resume, re-audit or take over a live l00 stage.

The supervisor output is the fresh owned RecoveryStorage child
`go2_independent_rgb_body_remaining_stages_v1_attempt_001`. All eleven remaining
batch roots must initially be absent. Existing outputs are never skipped or
resumed. The supervisor's sources, tests and this protocol are bound alongside
the original 765-source collection definition before its first child starts.
All original collection sources remain unchanged.

Execute each exact existing stage in a fresh interpreter with the same declared
CPU/thread/software-EGL environment as l00, in fixed l01 through l11 order. No
alternative episode list, seed, geometry, role, support condition, action,
sensor schema, native guard, physical stop or collection/audit rule is introduced.
Each child retains the existing 8 GiB batch cap, 256 MiB episode allowance,
40 GiB free-space reserve and preceding-completed-audit check. The supervisor's
own metadata allowance is 32 MiB, with half reserved beyond its launch record.

Each stage request and child return code is recorded durably. The parent waits
on the actual child process; there is no observation deadline, implicit signal,
timeout restart, background retry or alternate command. A nonzero child exit
stops the sequence. The child remains responsible for its own automatic terminal
audit, including collection failures when a terminal record exists. The parent
does not launch a competing audit or repair a failed stage.

After a zero child exit, record candidate launch/audit hashes and authenticate
the complete batch through the receipt-bound loader. Verify exact equality to
the original collection source manifest. Only then record a verified receipt and
start the next fixed batch. Hash/source/metadata/loader/resource failure stops
the sequence with all recorded evidence retained. Interruption or supervisor
failure does not authorize another attempt or resumption. If observation is lost,
inspect the exact supervisor/child process and terminal artifacts before any
further action; a missing observation alone is not a stopped child.

Before its final result the supervisor repeats receipt-loader verification for
all twelve batches and requires unchanged summaries. An eventual result contains
twelve verified receipt identities and all requested stage metadata. This
establishes completed development data acquisition, not a
model result, final benchmark, promotion or goal completion. Failed setups,
physical terminals, exclusions, strict boundary failures, incomplete targets and
missing contrasts remain reported. No fitting, ablation, checkpoint selection,
calibration, navigation or hardware execution occurs in this supervisor.

Before any model fit, the distinct prospective comparison must freeze data and
coverage gates, matched arms/seeds/exposure budgets, baselines, input ablations
and checkpoint persistence/reload checks. Reliable local execution, online
rollout/memory contributions, whole unfamiliar-maze missions, real-time sensing
and bounded real-platform evidence remain requirements of the unchanged goal.
