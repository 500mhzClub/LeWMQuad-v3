# Supervised commitment-contact prefix V1

Prospective development replay, fixed before execution. The completed supervised
maze1 all-selection diagnostic has SHA-256
`e546494d75770c174453663d14cb5d38754a8f994bf89ba847ab425dbd3ddf8f`.
All3000 selections admitted all six actions, yet2999 selected turns and one
selected hold. Forward had greater predicted geometric potential progress and
lower utility at every selection. The inherited intermediate-waypoint scorer
uses100ms pose utility and800ms cumulative contact cost. This diagnostic motivates
one prospective horizon change; it does not invalidate the original definition.

Use the same completed first supervised episode, original launch, first worker
terminal, first cohort progress, all recorded artifacts, training-only correction
admission and supervised state
`171c8576d2c3fcfd0ce698351acf86a05f2ea29bdf829041e98641cdac778a73`.
The original three-case supervised cohort and prepared native queue remain frozen.

Change only intermediate-waypoint contact cost from predicted step8 to step1.
Keep coefficient1.2, first-step pose potential arithmetic, strictly causal observed
residual, all eight nominal path segments, original surface and phase vetoes,
action order and tie behavior. Keep full-plan contact values as explicitly named
diagnostic receipts. These scores are uncalibrated. No observer, registration,
floor transport, map, memory, mission, final-goal or reentry policy changes.
The new selector inherits the actual original ViewReentrySelector, including its
executed-waypoint scorer. The new controller inherits observe and advance from
MeasuredFloorTransportController; only selector installation and result identity
differ. No frozen module globals are patched.

Runner: `scripts/replay_go2_supervised_commitment_contact_prefix_v1.py`.
Exclusive output: `go2_supervised_commitment_contact_prefix_v1_attempt_001` under
the guarded navigation artifact base. Admit the complete3014 observations and
3013 completed original commands, but consume at most3004 sequential decisions,
stopping at the first changed request or either terminal. Never acquire the
following original observation after a changed command. Every prospective
selection must equal the declared pure transformation of its original selection;
every other complete decision field must agree except explicit controller identity
and the selected action/request at the intervention boundary. Original model
forecasts and all feasibility receipts must agree exactly. This verifies a common
causal prefix, not any alternative physical future.

Load a fresh identical supervised model; paired primary/auxiliary public RGBD and
body packets only. Validate current typed raw and measured-floor poses. Check
public input arrays unchanged and model state unchanged with no gradients.
Authenticate original native inputs transitively before/after using the separately
benchmarked scoped digest helper: original conditions, original initial digest,
stable metadata and final fresh hashes, isolated globals, no persistent cache or
atomic-snapshot claim. Bind all new source, tests, protocol, original sources,
diagnostic and benchmark evidence. Existing inherited manifests are not newly
claimed recursive closures. Preserve sealed custody and all failures.

One CPU replay worker, one numerical/OpenCV thread; zero native scene workers.
Assess hardware before verification and again before launch. Admission:8GiB
replay RAM plus32GiB concurrent native allowance,40GiB artifact reserve plus4GiB
replay output allowance. These are checks, not OS-enforced limits. Keep compressed
output below half the allowance for flush/metadata headroom. No model training,
GPU workload or independent native launch. Read-only preflight creates no output.
An exclusive failed replay remains terminal; no automatic retry or resume.

Focused tests cover horizon arithmetic, contact penalties, unchanged original
vetoes, pass-through modes, nonfinite/future/privileged inputs, full-decision
comparison, causal stop, public-array mutation, model changes, original controller
inheritance and terminal latching. Passing replay permits preparing a separately
defined native follow-up after the current fixed queue; it proves no goal arrival,
round trip, predictive advantage, calibration, physical safety or deployment.
