# Frozen footprint receipts paired raw prefix V1

Run one CPU paired replay at the exclusive artifact root
`go2_frozen_footprint_anchored_prefix_v1_attempt_001`. Admit the completed
surface-sharing result
`97624a042d388cf1ce988c3007e1886ed25db001e834de8a7a80359f4fe1a385`
and its complete predecessor chain. Use the original completed full JEPA
adapter case on maze 2 and its assigned model
`35496b6b402013f7a33d9c30110e115b90ded672f0d0da829a07128601507b6a`.

Retain the completed receipt-copy replay function's exact code object. Replace
only its candidate constructor, two-field metadata normalizer, exclusive
output path and progress label in private globals. Do not normalize state
types or values. Use fresh independent original and candidate models, the same
405 raw observations 0 through 404, and alternating execution order. Require
all original and normalized candidate decisions to match, all 402 forecast
comparisons, unchanged public arrays and model states, absent gradients, and
exact complete retained observed states at observations 3, 12, 395 and 404.
Do not consume observation 405 or later observations after any differing
decision. A mismatch is a terminal failure, not an opportunity to change the
normalizer or retry the fixed attempt.

Verify the original completed worker and full input chain before and after
replay. Freeze and verify the recursive source closure. Recheck the existing
RAM/disk envelope before admission and output creation. Retain the original
early, whole-prefix and repeated-hold timing windows and all failures. Timing
is controller-only, with no profiler; acquisition is excluded and the shared
workstation is not an isolated benchmark. Compare measured results, not the
synthetic copying workload's speedup.

This run constructs no native scene, trains no model, changes no running native
job, and consumes no independent-layout sensor data. The original native queue
retains its order. Any later native use requires a separately frozen protocol
and the completed raw replay evidence. This replay does not establish reliable
navigation, a real-time controller or hardware qualification.
