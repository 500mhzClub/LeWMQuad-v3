# V27 physical-builder facade integrity replacement V2 preregistration

## Purpose

The V27 integrity replacement V1 passed source custody, GPU visibility, runtime
composition, model initialization, and the complete update-0 physical and H6
validation observation. It then stopped before constructing the first training
microbatch because the runtime-selected V27 training module omitted 19 names
from the 22-name compatibility facade consumed by the frozen V25 physical
builder chain. It completed zero training updates, zero training presentations,
zero optimizer steps, and zero EMA steps, and produced no checkpoint. The
explicit-plan discounted-successor-state hypothesis therefore remains
scientifically untested.

This document authorizes exactly one science-identical schema-integrity
replacement under fresh source and output roots. It is not a retry or resume of
either consumed V27 attempt.

## Frozen scientific identity

The V27 model, dataset helper, evaluator, and every scientific function in the
tensor training core remain unchanged. The replacement preserves the RGB crop
and normalization, H6 V2 indexes and first 6,400 training rows, physical
schedule and labels, seed, initialization, four ordered plan actions, gamma 0.9
discounted EMA target, losses, optimizer groups and learning rates, gradient
clipping, EMA, observation updates, bootstrap, donor controls, hard thresholds,
one joint optimizer/EMA step per update, 400-update cap, and
12,800-presentation cap.

The physical and H6 routes remain 16 presentations each per update. The sealed
and held-out benchmarks remain unopened. Navigation and probability
calibration remain unauthorized.

## Sole source correction

The V27 tensor training module must expose the complete frozen V25 physical
builder facade by direct alias from `v25`. These are the only permitted
training-module changes:

- Base/V13: `CURRENT_RGB_KEY`, `REQUIRED_BATCH_KEYS`,
  `CURRENT_CAMERA_ORIGIN_KEY`, `NEXT_CAMERA_ORIGIN_KEY`,
  `CURRENT_CAMERA_BASIS_KEY`, `NEXT_CAMERA_BASIS_KEY`,
  `CURRENT_GROUND_PLANE_Z_KEY`, `NEXT_GROUND_PLANE_Z_KEY`,
  `CURRENT_PIXEL_HIT_KEY`, `NEXT_PIXEL_HIT_KEY`,
  `CURRENT_PIXEL_DISTANCE_KEY`, `NEXT_PIXEL_DISTANCE_KEY`,
  `CURRENT_GROUND_IN_FRUSTUM_KEY`, `NEXT_GROUND_IN_FRUSTUM_KEY`,
  `CURRENT_GROUND_CLEAR_KEY`, and `NEXT_GROUND_CLEAR_KEY`.
- V21: `SCENE_INNOVATION_NEGATIVE_ROW_KEY_V21` and
  `REQUIRED_BATCH_KEYS_V21`.
- V23: `ACTION_PRIOR_M_KEY_V23` and `REQUIRED_BATCH_KEYS_V23`.
- V24: `REQUIRED_BATCH_KEYS_V24`.
- V25: `REQUIRED_BATCH_KEYS_V25`.

All 22 values must equal the same-named values on the frozen imported `v25`
module. The exact schema relations must hold:

- V21 equals base plus `SCENE_INNOVATION_NEGATIVE_ROW_KEY_V21`.
- V23 equals V21 plus `ACTION_PRIOR_M_KEY_V23`.
- V24 equals V23.
- V25 equals V24.

No model, loss, gradient route, parameter partition, optimizer, EMA, dataset,
evaluator, threshold, or schedule code may change.

## Lifecycle identity

Only the denied-by-default launcher, executor lifecycle identity, and focused
lifecycle tests may otherwise change:

- Use schema prefix
  `lewm_go2_rgb_object_space_explicit_plan_discounted_successor_state_joint_jepa_v27_integrity_replacement_v2`.
- Use clean source root
  `/home/andrewknowles/Workspace/LeWMQuad-v3-v27-explicit-plan-successor-integrity-replacement-v2-source`.
- Use output root
  `.generated/go2_rgb_object_space_explicit_plan_discounted_successor_state_joint_jepa_v27_integrity_replacement_v2/attempt_v1`.
- Preserve the exact pre-reservation `HIP_VISIBLE_DEVICES=0` guard and rejection
  of all eight conflicting GPU selectors.
- Preserve the post-reservation check for exactly one
  `AMD Radeon AI PRO R9700` with 34,208,743,424 bytes.
- Preserve attempt-root mode `0700` and immutable receipt modes `0444`.

## Required pre-execution test

A CPU/source-only test must import the actual runtime-selected V27 training
module, compare all 22 facade names directly with `v25`, and assert the four
schema relations above. It must also assert that `REQUIRED_BATCH_KEYS_V27`
equals `REQUIRED_BATCH_KEYS_V25` and retain the existing frozen V25 builder-hook
identity assertion. The test must not use fake ready-made batches as a
substitute for this interface check. The complete focused V27 suite must pass
with warnings treated as errors before source review or authority.

## Lifecycle and terminal control

The V2 replacement is one new attempt under fresh source and output roots.
Retry and resume are false. Source closure, independent review, narrow-export
certification, and a separately committed exact authority are required before
launch.

On a scientific gate failure, stop with no checkpoint. On an exception,
terminalize once with truthful partial-checkpoint accounting and quarantine.
On a pass, publish only the update-400 development scale seed; its reuse still
requires separate authority. A pass does not authorize G2, navigation,
held-out, sealed, production, or promotion access. There is no third V27
infrastructure replacement.
