# Go2 scene-diversity recurrent replication: flat Genesis/CPU development V2

Date frozen: 2026-08-05

Status before execution: `PREREGISTERED_SOURCE_ONLY_NOT_RUN`

Scope: one fresh development-only two-scene infrastructure qualification.
V2 changes one runtime input from consumed CPU-flat V1: it adds exact
`HOME=/home/andrewknowles` to the child environment. Every scene, state,
history, action, horizon, model, seed, gate, threshold, process, resource,
timing, rendering, and 36-lane counterfactual field remains frozen.

## Bound V1 closure and causal evidence

CPU-flat V1 is closed by:

- `docs/lewm_go2_scene_diversity_recurrent_replication_genesis_cpu_flat_development_v1_qualification_terminal_review_2026-08-05.json`;
- SHA-256 `590abd021b33781d7786fc7bdc9f6286173ecd218b53428ae751bad4cd832a9c`;
- 9,730 bytes; and
- status `PASS_FAIL_CLOSED_PRE_GENESIS_HOME_ENVIRONMENT_ABORT_TERMINAL_REVIEW`.

V1 reserved its fresh root and launched scene 12 in a fresh process group. The
child exited by native `SIGABRT` after 0.684 seconds. The reported native
assertion was `core.cpp get_repo_dir assertion failed: home != nullptr`.
The exact nine-key environment deliberately removed ambient `HOME`; Genesis
repository-path initialization requires it. The failure occurred before scene
construction, physics, the initial 36-lane clone audit, common histories,
counterfactual branches, rendering, RGB, DINOv2, training, evaluation, or the
timing gate. It is not CPU-physics, model, representation, planning, or
scientific evidence.

The V1 release barrier passed against its matching prelaunch R9700 baseline;
there was no VRAM breach or leaked process group. V1 is nevertheless consumed
and retired. It may not be retried, resumed, refilled, overwritten, repaired
in place, or used as a runtime/cache/payload source.

The independent review recommends exactly one fresh V2 whose only material
environment delta is fixed `HOME=/home/andrewknowles`. It explicitly preserves
the CPU backend, Vulkan/EGL R9700 render route, probe order `[12, 0]`, four
states and 36 lanes per worker, histories, actions, horizons, render contract,
model, seeds, gates, and thresholds. It recommends no alternative environment,
architecture, tolerance, or batching change and creates no V2 execution
authority by itself.

## Exact V2 delta

The immutable V1 qualification plan is:

- `docs/lewm_go2_scene_diversity_recurrent_replication_genesis_cpu_flat_development_v1_qualification_exact_plan_2026-08-05.json`;
- SHA-256 `397c1932f69bcbf29239fd6b86188f177222f9e350e97775cbe2968e97e868a9`;
- 350,749 bytes.

The V2 qualification plan differs only in:

1. fresh V2 `attempt_id`;
2. fresh V2 `output_root`;
3. V2 qualification-contract identity and predecessor bindings; and
4. `execution_contract.environment.HOME=/home/andrewknowles`.

Normalizing those fields must reproduce the exact V1 plan byte-for-canonical-
JSON value. Recursive comparison outside `successor_contract` must find only
`attempt_id`, `output_root`, and `execution_contract.environment.HOME`.

The exact interpreter remains:

`/home/andrewknowles/Workspace/LeWMQuad-v3/.generated/venvs/genesis_render_vulkan/bin/python`

The child receives exactly ten environment keys:

```text
EGL_DEVICE_ID=1
GS_BACKEND=cpu
GS_PARA_LEVEL=0
HOME=/home/andrewknowles
MESA_VK_DEVICE_SELECT=1002:7551!
PYOPENGL_PLATFORM=egl
PYTHONDONTWRITEBYTECODE=1
PYTHONHASHSEED=0
PYTHONNOUSERSITE=1
PYTHONSAFEPATH=1
```

No ambient variable is admitted. `HOME` is fixed plan input, not inherited
ambient state. Physics remains exact `gs.cpu` with serialized
`GS_PARA_LEVEL=0`; policy remains on CPU. Rendering and graphics preflight
remain on the exact bound Vulkan/EGL AMD Radeon AI PRO R9700 route. There is
no ROCm/HIP physics-backend substitution and no cross-backend equivalence
claim.

The harness remains flat and explicitly owned. It may not patch, replace,
overlay, context-mutate, or communicate configuration through imported project
module attributes, validators, collectors, environments, or graphics
expectations. Static plan, source, input, and historical-parity validation
must complete before root reservation. Runtime configuration is passed
explicitly to each fresh child.

## Fixed two-scene qualification

V2 uses the fresh root:

`.generated/dev/go2_scene_diversity_recurrent_replication_genesis_cpu_flat_development_v2_qualification/attempt_v1`

The deferred V2 science root must remain absent. Exactly two fresh full-scene
worker process groups run in order `[12, 0]`, one process per scene. Each has a
300-second hard watchdog and executes the unchanged:

- four states and both full history blocks;
- nine matched successors per state;
- 36-lane `parallel_lockstep_envs_no_restore` mechanism;
- full physics horizon and invariant checks;
- 12 context frames, 36 targets, and 48 stored RGB frames per scene; and
- auxiliary depth, render-count, nonfinite, mesh, receipt, and binding gates.

All nine lanes within each state group must remain exactly equal throughout
common-history execution. No numerical tolerance, state-local fallback,
snapshot/restore mechanism, adaptive batching, or alternate branch semantics
is admitted. Complete first-step and synchronization-checkpoint component
diagnostics remain mandatory.

Because rasterization still uses the R9700, the parent continuously samples
selected-device memory. Any observation above `16,977,405,952` bytes fails.
After every worker, including the last, the coordinator must wait for process
exit, reject a leaked process group, and complete the release barrier against
that worker's matching prelaunch selected-device baseline before opening its
result or launching another worker. A bracketed kernel-journal gate must find
no new AMDGPU timeout, reset, wedge, or HSA/KFD exception.

Qualification timing passes only when:

`64 * max(worker_elapsed_seconds) + 900 <= 7200`

The 900 seconds remain reserved for joins, feature extraction, fitting,
durable checkpointing, and two evaluations. The watchdog does not replace the
projection gate.

Any environment, initialization, exactness, physics, render, integrity,
resource, release, process, kernel, or timing failure consumes V2. There is no
retry, resume, refill, overwrite, repair, environment expansion, or partial
reuse.

## Output isolation and deferred science

Qualification output is non-scientific and permanently non-reusable. No V1 or
V2 scene, cache, receipt, RGB, mesh, diagnostic payload, timing trace, result,
or partial runtime artifact may be copied, joined, opened by, or reused in a
64-scene run.

On success, the harness writes the complete non-authorizing result and
terminal before emitting a minimal PASS decision last. Only that exact PASS
decision may later be rehashed as a science-plan release condition. A PASS
means solely that the fixed CPU mechanism completed exactly and projects
within the wall cap; it makes no model, representation, planning, navigation,
or generalization claim.

No V2 scientific plan exists at preregistration, and the plan builder has no
science-plan output path. A future scientific plan requires an independently
validated V2 PASS plus a separate source review establishing complete
qualification-payload isolation.

If released later, the science experiment remains the frozen 64-scene,
256-state, 2,304-branch comparison: recurrent DINO visual, matched exact-zero-
vision, and live task/action control; the same data roles, histories, textures,
actions, DINOv2 binding, seeds, optimizer, 800 updates, train-only statistics,
durable checkpoint, two evaluations, scoring, and 10,000-resample paired
family/scene bootstrap.

The fixed scientific gates remain:

1. integrity, role disjointness, context-only custody, repeatability, and the
   privileged physical oracle pass;
2. visual normalized rank regret is at most `0.13`;
3. visual minus live task/action regret is at most `-0.02`, with paired 95%
   upper bound below zero;
4. visual minus matched no-vision regret is at most `-0.01`, with paired 95%
   upper bound below zero; and
5. visual beats uniform-random expectation.

## Custody and authority

No sealed, `sealed_*`, held-out, production, or other protected material may
be opened, searched, indexed, copied, or used. Both V2 roots must be absent
before their respective one allowed invocations. All predecessor artifacts are
identity evidence only.

The user's instruction authorizes implementation and source verification of
this fixed successor. One qualification invocation becomes eligible only
after an independent review passes the exact preregistration, builder, plan,
harness, and focused tests. This document and the predecessor terminal review
create no execution authority by themselves and do not authorize science.

Any V2 qualification failure hard-stops this V2 route without tweaking. If
exact lane equality or the two-hour projection fails, do not relax tolerance,
change batching, or alter the model. All outputs remain development-only,
non-promotable, and ineligible for final benchmark, production, deployment,
or sealed-evaluation claims.
