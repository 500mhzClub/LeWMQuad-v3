# Go2 RGB multiresolution perception V3 operational-recovery decision

Date: 2026-07-24

Author: `/root/runtime_reuse_audit`

Status: **AUTHORIZED SOURCE-ONLY SCIENCE-IDENTICAL V3 PREPARATION AND
INDEPENDENT REVIEW, PLUS ONE LATER INDEPENDENTLY REVIEWED SYNTHETIC R9700
COMPATIBILITY RUN; NO V3 RESERVATION, GENERATED INPUT, CHECKPOINT, DATASET,
MODEL TRAINING, PROBE EXECUTION, QUALIFICATION, JEPA, G2, NAVIGATION,
HELD-OUT, PRODUCTION, PROMOTION, OR DEPLOYMENT AUTHORITY**

## Exact user authorization and context

The assistant proposed the following exact scope:

> Authorize source-only preparation and independent review of exactly one separately versioned, science-identical V3 integrity replacement. In V3 only: fix canonical receipt normalization; add the real producer-to-runner regression; add post-reservation pre-ledger terminalization and injected failure tests; and prepare one no-payload strict-determinism compatibility checker for scatter_add and grid_sample. Preserve the model, data, seed, schedule, optimizer, losses, thresholds, initialization, evaluation checkpoints, and 16,000-presentation cap; keep V1 and V2 source and runtime outputs frozen. Also authorize exactly one independently reviewed R9700 compatibility run using synthetic tensors only, with no dataset, checkpoint, model training, generated input, or V3 attempt reservation. This grants no V3 training, qualification, JEPA, G2, navigation, held-out, production, promotion, or deployment authority.

The exact UTF-8 scope text above, excluding Markdown quote syntax and without a
trailing newline, is 915 bytes and has SHA-256
`5e2103a5774b5c1593f8b6a7657984453aa2195f287e887b5d734f0141a3dcb5`.

The user's verbatim authorization responses, in order, were:

1. `approved`
2. `yes`

Those responses authorize only the exact quoted scope in its immediately
preceding assistant context. Neither response is treated as a free-standing
general retry, source-mutation, hardware, training, or downstream grant.

## Bound terminal predicate

V2 executed from source commit
`8f871ecc1304dcb0c9ba1f10fea288a027e36e7c`. Its independent terminal
evidence is:

| Artifact | Commit | File SHA-256 | Canonical content SHA-256 | Bytes |
|---|---|---|---|---:|
| `docs/lewm_go2_rgb_multiresolution_perception_v2_terminal_audit_2026-07-24.json` | `ea31fb2b9781e5776f8a79647d35ced3e588be33` | `de43da71bf92526d4d8314bbdd1c186af608aa73eb03b4246dffa16cdeace90b` | `872a942e4a16b112d934ddc5063289d963ee1873e43e8adb4242d66065da11f7` | 8,841 |
| `docs/lewm_go2_rgb_multiresolution_perception_v2_terminal_root_cause_2026-07-24.json` | `0b8df6e3116cd649c12f16a549767558465e296a` | `feb88c8fe42ca02da4a10669755539f7a1aa1560b3bc54355b1e7b9cce2c1883` | `58fe292f810f38549f10e7c46fdf83715cdc66b64b4b8c82977a2161e77af434` | 9,414 |
| `docs/lewm_go2_rgb_multiresolution_perception_v2_terminal_independent_review_2026-07-24.json` | `0b8df6e3116cd649c12f16a549767558465e296a` | `7c19df3af038a80d510dc6247dea69d6a32091352dcf056b24ceb903c8bd0d7c` | `d3c342167f9ae73bae20f9a9f049b09c6cb79777dd649d123afb0221dd15798d` | 7,956 |

These records establish that V2 reserved and consumed its sole attempt, failed
closed during model-preparation receipt validation, entered no training,
produced no scientific result, qualified no checkpoint, and has no retry,
resume, recovery, second seed, extension, observer rerun, or replacement
authority. V2 may not be patched and rerun. V3 is a separately versioned
successor, not a reinterpretation or continuation of V2.

V1 and V2 source and runtime outputs remain frozen. This decision grants no
authority to open, list, copy, unseal, rename, delete, or mutate either runtime
root.

## Decision

Authorize source-only preparation and independent review of one
science-identical V3 operational successor. The planned V3 attempt index is
one and its maximum attempt count is one, but no V3 output-root check,
reservation, preflight, generated-input access, checkpoint access, GPU probe,
training, or probe execution is authorized by this decision or its
preregistration.

If a later distinct execution authorization is ever issued, it must bind a
new V3 attempt identity and a fresh absent V3 output root. That later
reservation would consume the sole V3 attempt. No such execution authorization
exists now.

## Exact scientific identity

The immutable science source is the `/experiment` object in
`docs/lewm_go2_rgb_multiresolution_perception_v2_execution_authorization_2026-07-24.json`
at executed source commit
`8f871ecc1304dcb0c9ba1f10fea288a027e36e7c`:

- file SHA-256:
  `5e1ac702b7a17e0dc05a40a67b4c7700870abe8f5f772d01b609f0388607f0fb`;
- declared canonical content SHA-256:
  `fe1f75a9f184d37450d39c9d5f20e97acc23cb8fefcba639f84a5f4c30f7455c`;
- byte count: `8,194`.

Canonical JSON encoding of that exact `/experiment` object with sorted keys,
separators `,` and `:`, ASCII escaping, and non-finite numbers forbidden has
SHA-256:

`e181381c00585fa5df41a71fff918b5599acc955d59283ce397ba6dd530dc23f`

V3 must be deeply equal to that object. The model and parameterization, N320
initialization, data roles and order, seed, presentation schedule, optimizer,
learning-rate function, losses and coefficients, thresholds, evaluation
checkpoints, operation counts, and 16,000-presentation cap are unchanged.
There is no new scientific mechanism.

## The only authorized V3 source and lifecycle deltas

### 1. Canonical initialization-receipt normalization

In separately versioned V3 source only, prefer the actual
`MultiresInitializationReceiptV1.to_dict()` canonical representation before
generic dataclass `asdict` normalization. The canonical method converts
`copied_state_keys` from the producer's internal tuple to the list required by
the frozen runner validator.

Add the missing real producer-to-runner regression. It must construct an
actual `MultiresInitializationReceiptV1`, pass it through the V3 normalizer and
full receipt validator, and prove that the exact 84 sorted permitted entries
are accepted. Negative regressions must prove that the change does not relax
schema, field set, ordering, uniqueness, count, hash, model-state, hard-sync,
frozen-state, dense-decoder exclusion, or arbitrary/non-dict normalizer
checks.

### 2. Post-reservation pre-ledger terminalization

The V3 lifecycle must terminalize every failure after successful reservation,
including a failure before the partial-access-ledger header exists. A
before-header failure receipt must bind the reservation, record that no ledger
header or runtime-input open occurred, publish exact zero operation counts and
all downstream denials, and seal the terminal root. An after-header failure
must bind the reservation and the durable ledger prefix, publish exact
operation counts and denials, and seal the terminal root.

Add injected failures immediately before ledger-header publication and
immediately after durable header publication. Both tests must establish
complete fail-closed terminal custody without opening any runtime payload.

### 3. Synthetic strict-determinism compatibility checker

Prepare source and tests for one no-payload checker that exercises only
synthetic tensors through the exact current-stack `scatter_add` and
`grid_sample` forward/backward paths needed by the candidate. It must request
and verify strict deterministic algorithms, fail closed on an unsupported or
nondeterministic operation, bind the exact Python, Torch, HIP, device, source,
operation, shape, dtype, and deterministic-state identities, and perform no
model training.

This checker is compatibility evidence only. It may not alter the model,
science contract, runtime algorithm, precision, loss, schedule, or operation
cap.

No fourth source or lifecycle delta is authorized. Cleanup, refactoring,
dependency changes, alternate algorithms, `warn_only=True`, disabling strict
determinism, model or data changes, tuning, and science changes are outside
scope.

## Prior strict-determinism evidence

The committed
`docs/lewm_go2_shared_jepa_v5_matched_training_v3_terminal_failure_audit_2026-07-15.json`
at commit `ffef9830926840067d73638646381cbd9a863373` has:

- file SHA-256:
  `2f94d6ddaf076bc011eaac46408261aea3b8ac030386c9d2185463fe87a08e4a`;
- canonical content SHA-256:
  `b93146f00c79a6b2d151a07fb33696c673a1d45677ee6b948e20acadef9c9899`;
- byte count: `12,883`; and
- verdict:
  `PASS_CONFIRMED_FIRST_B4_FORWARD_LOSS_THEN_ROCM_DETERMINISM_INFRASTRUCTURE_FAILURE_ZERO_LEARNED_UPDATE`.

It records one B=4 forward/loss followed by strict
`torch.use_deterministic_algorithms(True)` rejecting
`grid_sampler_2d_backward_cuda`, with zero completed backward, optimizer step,
EMA update, checkpoint, or persistent learned-state mutation.

The authorized synthetic R9700 check is therefore current-stack confirmation,
not discovery of a new issue and not scientific evidence. If either required
`scatter_add` or `grid_sample` path fails strict compatibility, V3 probe
training cannot be authorized under the frozen strict mode. The failure may
not be bypassed by `warn_only=True`, disabling strict mode, changing the
operation, or silently changing the science contract.

## One conditional synthetic R9700 compatibility run

After the checker source, tests, launcher, output contract, and recursive
source closure are independently reviewed and exact-bound, exactly one R9700
compatibility run is authorized. It may:

- validate exactly one visible R9700;
- import the reviewed current Torch/HIP stack;
- allocate only the checker-defined synthetic tensors;
- execute the exact reviewed `scatter_add` and `grid_sample`
  forward/backward compatibility cases under strict deterministic mode; and
- publish only the checker-defined compatibility receipt.

It may not open a generated input, dataset, RGB payload, checkpoint, model
state, V1/V2 runtime output, navigation artifact, or held-out material. It may
not instantiate or train the candidate, reserve or inspect the planned V3
probe root, or consume the planned V3 attempt. It has one run, no retry,
resume, extension, alternate device, alternate operation, or warning-only
fallback. Its result authorizes no V3 execution; it is one required input to a
future independent V3 source/lifecycle review.

## Required custody sequence

1. Commit this decision and the canonical V3 preregistration together.
2. Obtain an independent preregistration and recovery-exception review.
3. Implement only the three authorized deltas in separately versioned V3
   source and add only their required tests.
4. Produce and review a recursive V3 source manifest.
5. Obtain an explicit narrow custody exception before any V3 clean export;
   the current repository instructions authorize allowlist-only export for V1
   and V2, not V3.
6. Obtain an independent V3 source and lifecycle review.
7. Independently review and exact-bind the synthetic checker, then perform its
   one authorized R9700 run.
8. If and only if strict compatibility passes, a different authorizer may
   consider a distinct V3 probe execution authorization that binds every
   preceding artifact and verifies a fresh absent V3 root without opening
   V1/V2 outputs.

This sequence grants no authority to skip a step. The implementation author,
independent reviewer, synthetic-run authorizer, and any future probe execution
authorizer must remain appropriately distinct.

## Authority boundary

This decision authorizes V3 documents, separately versioned source,
source-only tests, source closure, and independent review within the exact
three-delta surface. It conditionally authorizes the one reviewed synthetic
R9700 compatibility run described above.

It does not authorize V3 root inspection or reservation, generated input,
checkpoint, tensor checkpoint deserialization, dataset or RGB access, model
training, a V3 probe attempt, checkpoint qualification, perception
qualification, JEPA training, G2, navigation, held-out access, production,
promotion, or deployment. A future V3 execution authorization remains
separate and currently nonexistent.
