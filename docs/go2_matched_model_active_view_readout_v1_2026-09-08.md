# Matched-model active-view readout V1

After the complete twelve-case native result is frozen, accept its exact result
SHA-256 and authenticate all source/input/native/URDF and recorded artifact
bindings. Require the exact prospective case roster and complete cohort status.
Use an exclusive derived root `go2_matched_model_active_view_readout_v1_attempt_001`.

For each actual model/layout case retain the original terminal/native goal and
measurement audits, phase transitions, model forecast and selected action counts,
completed/nonzero active command intervals by phase, waypoint/scan-target trace,
initial/final map receipts, measured heading range, maximum accepted-pose XY
error and complete-loop timing. Nonzero waypoint commands may be turns; they
are not counted as forward travel. Report two-case verified-goal counts per
model, separately retaining measurement-clean goal counts and physical stops.

This readout never changes original commands, models, controller decisions or
native outcomes. It executes no simulator, inference or training. Model-specific
trajectories are actual observations, not matched state/action counterfactuals.
Two known mirrored layouts and one optimization seed cannot establish unseen-
maze reliability or statistical superiority. Reauthenticate before final receipt.

Retain the first sensor-failure tick and its anchor/increment continuity reasons.
For the two full-RGB JEPA repeats, separately authenticate the entire original
active-view result and compare command tapes, RGB frame hash sequences, and
array identities for physics traces, native contacts, public body/control
histories and fast-gyro histories. Compare complete decision records after
removing only the changed controller label and added model-condition/input-
variant metadata. Preserve any first differing decision and both array/RGB
witnesses; numerical nonidentity is a reported result, not grounds to relabel,
retry or omit a case. ZIP container timestamps and wall timings are excluded
from this explicit numerical/decision equivalence comparison.
