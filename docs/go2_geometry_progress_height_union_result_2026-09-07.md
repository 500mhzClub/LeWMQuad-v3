# Geometry-progress pilot: prospective sensing and action-design gates pass

The fresh variable-height union collection and its prospective raw audit both
completed. All 24 episodes and 906 frames pass the declared RGB/body measurement
contract; there are also no strict physical-depth failures. The native task
retains the required mirrored action-success reversal. This resolves the pilot's
acquisition problem and supports building a broader learning dataset. It is not
learned navigation, independent-maze evaluation, or completion of the long-term
goal. No model was fitted and no high-level action was selected by a fitted model.

Numbers and exact science-receipt identities are in
[the compact readout](go2_geometry_progress_height_union_scientific_readout_2026-09-07.json).
All original attempts and handoff files remain unchanged.

## Preserved constructor failure and explicit correction

The first new 5mm-camera attempt,
`go2_geometry_progress_near_field_v1_attempt_001`, failed before scene allocation
or physics: `ValueError('one common wall base and top required')`. Its visual
union provider supported equal-height walls, while the original physical task
has a 0.7m panel and 1.4m room walls. Zero episodes completed. That attempt remains
terminal; its source, protocol, launch and failure are preserved.

The new `lewm_genesis.variable_height_union_surface_development` constructs the
boundary of a grounded axis-aligned box union using three-dimensional coordinate
compression. It supports unequal heights, removes internal faces, checks exact
volume and outward face orientation, and bounds the compressed grid at 65,536
cells. The associated native scene builder retains the prior physical box,
robot and camera construction. Physical dimensions were not changed to satisfy
the old renderer. Appearance generation is explicitly new.

A separately frozen static native bench rendered 20 previously authenticated
camera poses across four geometry/appearance strata, including all old near-wall
failure poses. All 20 passed strict and stable-footprint checks. It used zero
physics steps and wrote 77 bound artifacts totaling 41,416,356 bytes. Only after
that completed bench did the separately named dynamic successor launch.

## Complete dynamic evidence

The successor retains the two mirrored panels, two appearance seeds, six-action
bank, randomized episode assignment, fixed checkpoint gait, nominal friction,
three quiet history ticks and 40 candidate ticks. The camera uses a 5mm render
near plane and an explicit floor-first variable-height wall-union raster order.
The public depth interval remains 0.2–5m. Native state supplies evaluation and
physical stops; it does not choose commands.

| Action | Full horizons / 4 | Successful local progress / 4 | Contact stops / 4 |
| --- | ---: | ---: | ---: |
| Hold | 4 | 0 | 0 |
| Forward | 0 | 0 | 4 |
| Left arc | 2 | 2 | 2 |
| Right arc | 2 | 2 | 2 |
| Left pure turn | 4 | 0 | 0 |
| Right pure turn | 4 | 0 | 0 |

The successful left and right arcs reduced actual goal distance by 313.25mm and
351.08mm respectively. Both appearance seeds show the required reversal. Holding
and pure turns fail the 150mm progress threshold; no constant action succeeds in
all four strata. These four successful candidate episodes are not a navigation
policy success rate. The robot has not yet demonstrated passage traversal or
maze arrival.

Collection: 24 episodes, 16 full horizons, eight contact stops, 62,194 native
2ms samples, and 906 camera/decision packets. All 4,320 expected artifacts are
present, totaling 1,382,954,124 bytes before later audit products. Partial command
ticks and stopped candidates remain in the population.

The frozen prospective audit verified raw body/control/depth/RGB packet replay,
exact float64 requested commands and float32 actuator clipping, native contacts,
setup, friction/gains, stops, camera mount/clock, raster order and native precision.
Every frame also received physical-first-surface and stable-footprint checks:

- 906/906 stable-interior metric passes; minimum 2,536 compared stable rays.
- Maximum stable-interior depth residual 0.36545mm, below the fixed 1mm threshold.
- Zero stable or boundary bad rays, clipped opaque rays, falsely public-valid
  near-surface rays, or strict physical-visibility failed frames.
- The complete native design and prospective measurement gate is true.

These are sampled simulated geometry checks, not full-image/self-occlusion,
depth-navigation or hardware qualification. No old failure was relabeled.

All 24 model contexts materialized through the existing JEPA tensor interface.
There are four RGB histories but one body history and one control history.
All 24 prefix comparisons, including four references, are exactly equal within
their geometry/appearance strata. Independent-episode provenance remains explicit.

All 192 half-second target slots remain accounted for: 160 valid native motion
targets, 160 actual future-image packets, 192 known contact labels and 32 positive
contact labels. All 32 positive contacts retain missing future motion and images;
no post-stop future is synthesized. The 160 available image targets now pass the
prospective measurement contract.

## Resources, tests and prepared next implementation

Launch observed 16 physical/32 logical CPUs, 82,133,983,232 bytes available RAM,
104,722,014,208 free artifact bytes and no competing heavy Python workload.
The frozen run used one CPU scene and one OpenCV/BLAS thread, with a 3GiB planned
storage allowance and 40GiB reserve. During collection, sampled CPU utilization
was 3.3%, available RAM about 80.5GB, and collector RSS about 2.46GB. Capacity was
adequate, but the serial job did not saturate the machine.

Median observation/control time was 126.00ms; 836/906 decisions exceeded 100ms.
This includes acquisition and shadow tracking, excludes the physics tick, and
pauses physics during computation. Real-time execution remains unresolved.

51 distinct focused tests passed in this continuation: near-field contracts 16,
variable-height geometry 11, successor equivalence 4, science aggregation 5,
predictive selection 10, and new layout family 5. No optimizer steps occurred.

New source-only components prepared alongside the native work:

- `lewm/geometry_progress_predictive_selection_development.py` runs the actual
  cumulative-event model on all six known plans and ranks predicted terminal
  distance reduction minus an explicit contact-score cost. It retains every
  forecast and measures selection time. Tests demonstrate forecast-sensitive
  action reversal, exact requested commands, missing/invalid prediction rejection,
  input/target separation and unchanged weights under real untrained inference.
  It does not admit a checkpoint or establish calibrated risk. Quiet-start data
  alone does not validate repeated replanning while moving.
- `lewm/geometry_progress_layout_family_development.py` defines eight new panel
  layouts in four parameter clusters, two matched appearances and six actions:
  96 prospective episodes. Training and geometry-transfer roles each have two
  disjoint parameter clusters, their four mirrored layouts, and 48 episodes.
  Mirrored siblings are explicitly dependent. The actual eight meshes construct
  in synthetic tests; physics, the collector, transfer evaluation and training
  for this family have not been launched. These remain local obstruction tasks,
  not eight independent mazes.

The dynamic collector is `scripts/run_go2_geometry_progress_height_union_v1.py`,
its session is `scripts/geometry_progress_height_union_session_development.py`,
its raw auditor is `scripts/audit_go2_geometry_progress_height_union_v1.py`, and
its complete readout is `scripts/read_go2_geometry_progress_height_union_science_v1.py`.
Original frozen helpers and attempts were not monkeypatched or edited.

## Exact terminal identities

All roots below are under
`/home/andrewknowles/RecoveryStorage/LeWMQuad-v3/.generated/navigation_development_artifacts_v1`.
Every launched process is terminal. Do not rerun, resume or overwrite these roots.

| Root | File | SHA-256 |
| --- | --- | --- |
| `go2_geometry_progress_near_field_v1_attempt_001` | `launch.json` | `4fd1caf21cfa7cf5417693fab6607fe4b44a4404442cc3e81488b80494e4ae42` |
| same | `failure.json` | `f851d7496765993925a9f04dad1c3a528aa8dd983cdea2f0ab7fe609250958ab` |
| `go2_geometry_progress_variable_height_render_bench_v1_attempt_001` | `launch.json` | `3d2a15124095c632e7b5d64c5e7663377c0d2f1258d01dbf9946a3e69d6849f4` |
| same | `result.json` | `8cfb8b30f67514a4f64490eea32eb3f188e57a32f57894d29dc29f0b20231738` |
| `go2_geometry_progress_height_union_v1_attempt_001` | `launch.json` | `321eecd390175d99fd8da4535c401102e8101013e2a04fff037356da34a2f50d` |
| same | `result.json` | `46be655f7af866b024367945089907d11ddc0f06f8fef9766565d69ea2c4bb54` |
| same | `near_field_audit_launch.json` | `a54b3b1c293c45405653524166e1c2122954d7fe24c13c3fdc11d22795ae9e8f` |
| same | `near_field_audit.json` | `1cabe4b09590d5c409ba107ece18df17672e686f2f107feff304e30ea7182112` |
| `go2_geometry_progress_height_union_science_v1_attempt_001` | `launch.json` | `2ac462e0464f9e7d815c08b89e1156133975a2b3dd668fd42212f22ff6ae007d` |
| same | `result.json` | `5cfcac9d65d21daa40334b74f0aaa49796602a0eafb3cdb3f10762e12d489b7e` |

Failed constructor launch binds 808 source files, the static bench 813, the
completed collection/audit 818 and science readout 820. The selector and new
layout-family modules were prepared later and are not part of those frozen
execution bindings. `AGENTS.md`, `.ignore`, and all eleven section-12 handoff
hashes were verified unchanged. All new source/results remain local/uncommitted.

## Ordered continuation

1. Preserve this passing pilot and all predecessor failures. The renderer problem
   is resolved for this cohort; do not rerun another unchanged camera pilot.
2. Implement the new layout-family collector, full-denominator raw auditor and
   generalized causal tensor adapter in new files. Reuse the verified physical
   construction, exact command semantics and fixed action/target clocks explicitly.
   The old adapter intentionally rejects new assignments; do not monkeypatch it.
3. Before the 96-episode job, assess fresh hardware and benchmark independent
   process concurrency/throughput with a separately recorded, bounded comparison.
   Freeze resource/concurrency choices and the full new dataset contract before
   collection. Preserve all failures and role/cluster denominators.
4. Fit matched RGB/no-RGB and direct/rollout/JEPA models on the training role;
   evaluate the geometry-transfer role without checkpoint selection or exclusion
   based on native success. Report action selection as well as prediction losses.
5. Use an authenticated fitted model to choose commands in new physical episodes.
   Expand the training distribution to moving causal contexts before claiming
   repeated predictive replanning. Then integrate the joint observer, memory,
   physical returns and matched reactive/nonpredictive baselines on new mazes.
6. Keep the long-term goal active. Independent closed-loop maze success, measured
   JEPA/planning/memory contributions, timing, realistic sensing and bounded
   hardware evidence remain outstanding.
