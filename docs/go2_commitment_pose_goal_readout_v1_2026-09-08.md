# Commitment-pose native readout V1

After the four-case native result is complete, authenticate its exact SHA-256,
complete artifact/source/input/native/model bindings and fixed case roster.
Authenticate the original twelve-case matched-model result and its complete
artifact map as the explicitly named comparison input. Use the exclusive derived
root `go2_commitment_pose_goal_readout_v1_attempt_001`.

Retain every original terminal/native goal, physical stop, measurement result,
first visual-failure reason, model/action/phase trace, initial/final map receipt,
heading range, accepted-pose error and complete-loop timing. Report two-case
verified-goal and physical-stop counts for each model. No numerical success
threshold changes. Current timings were measured with concurrent workers and
must not be reported as uncontended speedups over the serial predecessor.

For each assigned model/layout pair, locate the first differing requested command
or a tape-length difference. Compare RGB hashes, complete observer/map evidence,
all model forecasts, physical trace arrays and public body/control/fast-gyro
history arrays through the observation immediately before that differing command
executes. Preserve both array witnesses and any nonidentity. If no command differs,
compare the complete available observation prefix and report full tape equality.
This exposes whether a command difference followed identical measured state and
predictions; it does not infer outcomes of unexecuted alternatives. All raw audits
remain those of the actual respective trajectories.

Reauthenticate all inputs and sources before final receipt. This readout performs
no simulator execution, training, model inference, action reselection or outcome
modification. It does not establish independent-maze, real-time or hardware
qualification. All predecessor and current failures remain retained.
