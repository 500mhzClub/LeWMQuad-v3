# Go2 scene-diversity recurrent replication: flat Genesis/CPU development V1

Date frozen: 2026-08-05

Status before execution: `PREREGISTERED_SOURCE_ONLY_NOT_RUN`

Scope: one fresh development-only two-scene infrastructure qualification.
The 64-scene intervention, 36-lane counterfactual mechanism, recurrent-DINO
model comparison, seeds, training, evaluation, and fixed scientific gates do
not change. This is not a new representation, architecture, data, or threshold
hypothesis.

## Why this is the next narrow experiment

The flat Genesis/ROCm development V1 qualification is closed by:

- `docs/lewm_go2_scene_diversity_recurrent_replication_genesis_rocm_flat_development_v1_qualification_terminal_review_2026-08-05.json`;
- SHA-256 `dffcc637258d5b17b1239b603b72e9a376d421a9783c2b89e1e8691db11dc206`;
- 8,519 bytes; and
- status `PASS_FAIL_CLOSED_POST_GENESIS_FIRST_STEP_DIVERGENCE_TERMINAL_REVIEW`.

That attempt reached real Genesis 0.4.6 `gs.amdgpu` physics. Scene 12 built and
the initial 36-lane clone was exactly equal, but all four nine-lane state
groups diverged after their first 20 ms physics step despite identical
within-group commands, policy inputs, and policy outputs. No rendering, RGB,
scene result, DINO, training, evaluation, metric, or scientific gate followed.
The review classifies this as backend/infrastructure evidence and retires flat
ROCm V1. It expressly forbids tolerance relaxation and recommends one fresh
flat CPU qualification before redesigning counterfactual branching.

The earlier CPU-backend V1 qualification is also consumed. It never entered
Genesis: a layered validator context rejected its exact plan before the first
scene. Its terminal review is SHA-256
`9b0c31c05b4fb6064c67116a456d34a6f7e49cfe85ec55ed081599acb18502f0`
and 20,536 bytes. Consequently it produced no CPU-physics or render evidence.
The CPU scientific and qualification plans themselves remain useful immutable
science-identity witnesses, and their independent source review established
the exact CPU backend delta. No authority, reservation, runtime output, cache,
receipt, partial scene, or RGB from that attempt is reusable.

This successor combines those two surviving facts only: the already reviewed
CPU backend contract and the independently reviewed flat, non-monkeypatched
harness discipline. It does not retry either consumed implementation.

## Exact backend and environment

The qualification payload is copied from the bound CPU qualification plan.
Every data, scene, state, history, action, horizon, render, model, seed, gate,
and access-order field is unchanged. It receives only a fresh attempt identity,
fresh output root, and explicit qualification-only flat-harness metadata.

The exact interpreter remains:

`/home/andrewknowles/Workspace/LeWMQuad-v3/.generated/venvs/genesis_render_vulkan/bin/python`

The physics backend is exact `gs.cpu`. The plan environment is exactly these
nine keys:

```text
EGL_DEVICE_ID=1
GS_BACKEND=cpu
GS_PARA_LEVEL=0
MESA_VK_DEVICE_SELECT=1002:7551!
PYOPENGL_PLATFORM=egl
PYTHONDONTWRITEBYTECODE=1
PYTHONHASHSEED=0
PYTHONNOUSERSITE=1
PYTHONSAFEPATH=1
```

Genesis CPU physics remains serialized under `GS_PARA_LEVEL=0`. Rendering
remains on the bound EGL/AMD Radeon AI PRO R9700 route with the unchanged
Vulkan/EGL identity preflight. The policy device remains CPU. CPU numerics may
differ from ROCm or Vulkan; no cross-backend bit-equivalence claim is made.

The harness is flat and explicitly owned. It may not enter a historical
adapter context or replace, patch, overlay, context-mutate, or communicate
runtime configuration through imported project module attributes, functions,
dictionaries, validators, collectors, execution environments, or graphics
expectations. The parent validates the exact bound plans and inputs before
root reservation. Each child receives its backend and environment explicitly
and constructs only process-local runtime state.

## Fixed two-scene qualification

Qualification uses the fresh root:

`.generated/dev/go2_scene_diversity_recurrent_replication_genesis_cpu_flat_development_v1_qualification/attempt_v1`

The deferred science root must remain absent. Exactly two fresh full-scene
worker process groups run in order `[12, 0]`, one process per scene. Each
worker has a 300-second hard watchdog and executes the unchanged:

- four states and both full history blocks;
- nine matched action successors per state;
- 36-lane `parallel_lockstep_envs_no_restore` mechanism;
- full horizon and physics invariants;
- 12 context frames, 36 target frames, and 48 stored RGB frames; and
- auxiliary depth, render-count, nonfinite, mesh, receipt, and binding checks.

Within every state group, all nine lanes must remain exactly equal throughout
common-history execution. No numerical tolerance may be introduced or
relaxed. The terminal diagnostics must preserve complete first-step and
preregistered synchronization-checkpoint component audits so a failure is
localizable without another run.

Because raster rendering still uses the R9700, the parent continuously samples
selected-device memory. Any observation above `16,977,405,952` bytes fails.
After each worker, including the last, the coordinator must wait for process
exit, reject a leaked process group, and complete the inherited release barrier
against that worker's matching prelaunch selected-device baseline before it
opens the scene result or starts another worker. The bracketed kernel-journal
gate must find no new AMDGPU timeout, reset, wedge, or HSA/KFD exception.

Qualification timing passes only when:

`64 * max(worker_elapsed_seconds) + 900 <= 7200`

The 900 seconds are reserved for joins, feature extraction, model fitting,
durable checkpointing, and two evaluations. The 300-second watchdog is a
safety ceiling, not a substitute for this projection gate.

Any initialization, exactness, physics, render, integrity, count, resource,
release, kernel, process, or timing failure fails qualification. There is no
adaptive batching, state-local fallback, snapshot/restore substitution,
tolerance change, retry, resume, refill, overwrite, or repair in place.

## Qualification output and science release

Qualification output is non-scientific and permanently non-reusable. No probe
scene, state receipt, render receipt, RGB frame, mesh, cache, timing trace,
diagnostic payload, partial result, or runtime artifact may be copied, joined,
opened by, or reused in a 64-scene run.

On success, the harness writes its complete non-authorizing result and terminal
before emitting a minimal PASS decision last. Only that exact decision may be
rehash-validated as a later science-plan release condition. A qualification
PASS supports only that CPU execution is exact, complete, and fast enough; it
makes no world-model, representation, planning, or navigation claim.

No successor scientific plan exists at preregistration. The plan builder has
no science-plan emission path. A scientific plan may be created only after an
independent audit validates the exact qualification PASS and a separate source
review confirms that qualification payloads cannot enter science.

## Frozen scientific experiment

The immutable CPU scientific identity witness is:

- `docs/lewm_go2_scene_diversity_recurrent_replication_cpu_backend_v1_exact_plan_2026-08-04.json`;
- SHA-256 `258d6bf004fa3618d492b583c56ea7fbc15b127ade36299fcba11295b147745e`;
- 346,045 bytes.

If later released, science starts independently at scene 0 under the fresh
root:

`.generated/dev/go2_scene_diversity_recurrent_replication_genesis_cpu_flat_development_v1/attempt_v1`

It retains exactly 64 scenes, 256 states, 2,304 matched counterfactual
branches, 768 context frames, 2,304 targets, nine actions, fixed role
allocation, histories, textures, DINOv2 binding, recurrent visual and exact
zero-vision arms, task/action control, seeds, optimizer, 800 updates,
train-only statistics, durable checkpoint, two evaluations, scoring, and
10,000-resample paired family/scene bootstrap.

The fixed gates remain:

1. integrity, role disjointness, context-only custody, repeatability, and the
   privileged physical oracle pass;
2. visual normalized rank regret is at most `0.13`;
3. visual minus live task/action regret is at most `-0.02`, with paired 95%
   upper bound below zero;
4. visual minus matched no-vision regret is at most `-0.01`, with paired 95%
   upper bound below zero; and
5. visual beats uniform-random expectation.

No threshold, seed, model, update count, data volume, or decision rule changes
after observation. A pass would support only the preregistered incremental
visual-information hypothesis; it would not by itself establish planning
utility.

## Custody, authorization, and stop rule

No sealed, `sealed_*`, held-out, production, or other protected material may
be opened, searched, indexed, copied, or used. Both fresh roots must be absent
before their respective one allowed invocations. Historical artifacts are
identity evidence only and grant no runtime reuse.

The user's instruction to iterate the next experiment authorizes source
implementation and verification. One qualification invocation becomes
eligible only after an independent review passes the exact builder, plan,
preregistration, harness, and focused tests. This document and the predecessor
reviews create no authority by themselves and do not authorize science before
the exact PASS decision.

Any qualification invocation failure consumes and terminates CPU-flat V1.
There is no retry or infrastructure tweak. If exact lane equality or the fixed
two-hour projection fails, stop without relaxing tolerance and consider only a
separately preregistered state-local ROCm diagnostic. If qualification passes
but the eventual frozen scientific gates fail, stop this recurrent-DINO recipe
on the exposed development route rather than adding scenes, seeds, updates, or
threshold changes.

All outputs remain development-only, non-promotable, and ineligible for final
benchmark, deployment, production, or sealed evaluation claims.
