# Go2 scene-diversity recurrent replication: flat Genesis/ROCm development V1

Date frozen: 2026-08-05

Status before execution: `PREREGISTERED_NOT_RUN`

Scope: one development-only infrastructure successor. The scientific question,
64-scene data intervention, three-arm comparison, and fixed gates are unchanged.
This is not a new model, data, threshold, or representation hypothesis.

## Why this successor exists

The consumed backend-V3 integrity-replacement-V2 qualification is closed by:

- `docs/lewm_go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v3_integrity_replacement_v2_qualification_terminal_review_2026-08-05.json`
- SHA-256 `b699e2f632be089a41d3ee88d30b4164809335003cba8e93e0cde33e096fc000`
- 15,335 bytes
- status `PASS_FAIL_CLOSED_POST_RESERVATION_SHARED_ENVIRONMENT_TERMINAL_REVIEW`

That review records a consumed one-shot qualification, successful in-memory
platform preflight by control-flow evidence, and no scene execution or RGB. The
first worker failed because the layered ROCm adapter changed shared pilot
environment state before lazy revalidation of the immutable historical Vulkan
parity contract. This is infrastructure evidence only: it says nothing about
the data intervention, recurrent model, thresholds, planning, or navigation.

The review retires the layered-adapter line. It permits only a separately
scoped source study of a flat, explicitly owned, non-monkeypatched harness and
forbids retry, resume, payload reuse, another alias, another context overlay,
or another integrity replacement. This preregistration takes that one route.

## Single infrastructure intervention

The V1 harness must pass one explicit immutable run specification through
validation, coordinator, and worker calls. Historical textured-V03 parity,
source, plan, and input bindings must be validated before root reservation and
before any GPU, Genesis, HIP, ROCm, or EGL call. The historical Vulkan parity
contract is validated as historical evidence; it is never recomputed under a
ROCm-mutated module global.

Runtime state is local to each fresh child process. The harness must not
replace, patch, overlay, or context-mutate imported module attributes,
functions, dictionaries, execution environments, graphics expectations, plan
builders, collectors, or validators. In particular, no shared module global
may communicate the backend or environment. Runtime dependencies are explicit
arguments or child-local values.

The lexical worker interpreter remains
`/home/andrewknowles/Workspace/LeWMQuad-v3/.generated/venvs/genesis_rocm_0_4_6_v1/bin/python`.
Each worker starts from an empty environment with exactly these 17 keys, except
that `GS_CACHE_FILE_PATH` is its fresh role-local cache path:

```text
HOME=/home/andrewknowles
EGL_DEVICE_ID=1
GS_BACKEND=amdgpu
GS_CACHE_FILE_PATH=<fresh role-local cache>
GS_ENABLE_FASTCACHE=1
GS_ENABLE_NDARRAY=1
GS_ENABLE_ZEROCOPY=1
GS_PARA_LEVEL=0
HIP_VISIBLE_DEVICES=0
PATH=/opt/rocm-7.1.1/lib/llvm/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
PYOPENGL_PLATFORM=egl
PYTHONDONTWRITEBYTECODE=1
PYTHONHASHSEED=0
PYTHONNOUSERSITE=1
PYTHONSAFEPATH=1
ROCM_PATH=/opt/rocm-7.1.1
ROCR_VISIBLE_DEVICES=0
```

Ambient `USER`, `LOGNAME`, `LANG`, `HSA_OVERRIDE_GFX_VERSION`, and
`MESA_VK_DEVICE_SELECT` are not admitted. Genesis remains 0.4.6 on exact
`gs.amdgpu`; the R9700/gfx1201, EGL, `ld.lld`, runtime identity, render,
physics, contact-force prohibition, and input-binding contracts are unchanged.

## Unchanged qualification

Qualification has its own fresh root and cache:

`.generated/dev/go2_scene_diversity_recurrent_replication_genesis_rocm_flat_development_v1_qualification/attempt_v1`

It runs exactly two fresh full-scene worker process groups in order `[12, 0]`.
Each has a 300-second hard watchdog and executes the unchanged four states,
histories, nine action successors, full horizon, render counts, transient depth
checks, and receipt/integrity checks. The parent continuously samples total
selected-device memory; any observation above `16,977,405,952` bytes fails.

After each worker, including the last, the coordinator must wait for process
exit and complete the inherited release barrier against that worker's matching
prelaunch selected-device baseline before it opens the scene result or starts
another worker. A bracketed kernel-journal gate must find no new AMDGPU ring
timeout, reset, device wedge, or HSA exception. A nonzero exit, leaked process
group, failed release barrier, render/physics/integrity failure, or fallback
also fails qualification.

Qualification timing passes only when:

`64 * max(worker_elapsed_seconds) + 900 <= 7200`

The 900 seconds are reserved for joins, model fitting, durable checkpointing,
and evaluation. The 300-second watchdog is not a substitute for this gate.

Qualification output is non-scientific. The exact PASS decision may be
rehashed solely as the science release condition; no qualification scene,
receipt, RGB, cache, runtime payload, timing sample, or partial result may be
copied, joined, opened by, or reused in the 64-scene run. Qualification and
science use distinct roots, reservations, identities, caches, receipts,
results, and terminals.

## Unchanged scientific experiment

Only an exact qualification PASS makes one fresh scientific invocation
eligible at:

`.generated/dev/go2_scene_diversity_recurrent_replication_genesis_rocm_flat_development_v1/attempt_v1`

Science begins independently at scene 0. Relative to the consumed V2
scientific plan, the only admissible differences are fresh identity, roots,
role-local caches, the flat-harness description, and the exact qualification
PASS decision reference (never its payload). The experiment retains exactly
64 scenes, 256 states,
2,304 matched counterfactual branches, 768 context frames, 2,304 targets, nine
candidate actions, fixed role allocation, histories, textures, seeds, model,
optimizer, 800 updates, scoring, 10,000-resample paired family/scene bootstrap,
resource caps, and access order.

The three comparisons remain the frozen recurrent-DINO visual arm, its matched
exact-zero-vision arm, and the live task/action ridge control. No successor
RGB, semantic or depth target, reward, planner, rollout, encoder update, JEPA
target, or exposed-role tuning is admitted.

The fixed gates remain:

1. integrity, role disjointness, context-only custody, repeatability, and the
   privileged physical oracle pass;
2. visual normalized rank regret is at most `0.13`;
3. visual minus live task/action regret is at most `-0.02`, with paired 95%
   upper bound below zero;
4. visual minus matched no-vision regret is at most `-0.01`, with paired 95%
   upper bound below zero; and
5. visual beats uniform-random expectation.

No threshold is relaxed after observation. A pass supports only the original
developmental incremental-visual hypothesis; it is not evidence that the
model is useful for planning.

## Custody, authorization, and stop rules

No sealed, `sealed_*`, held-out, production, or other protected material may
be opened, searched, indexed, copied, or used. Both prospective roots must be
absent before their one allowed invocation. No predecessor runtime artifact,
reservation, cache, partial scene, RGB, result, or process evidence is an
input; the bound terminal review above is source-study evidence only.

The user's explicit instruction to iterate the next experiment is the
operational authorization to implement and verify this fixed harness, execute
one fresh qualification, and, only after exact qualification PASS, execute
one fresh scientific run. No separate authority-builder stack or authority
JSON is required for this development route. That authorization does not
relax any preregistered custody, freshness, resource, qualification, or
scientific gate.

All source, plans, receipts, qualification output, scientific output, and
conclusions in this route remain development-only, non-citable as final
benchmark evidence, non-promotable, and ineligible for deployment or sealed
evaluation regardless of outcome.

The stop rules are fixed:

- Source review must reject any shared module overlay, hidden ambient runtime
  input, post-reservation historical-parity validation, or science/gate delta;
  no root or runtime may be contacted until those properties pass.
- Any qualification invocation failure consumes and terminates flat
  development V1. There is no retry, resume, refill, overwrite, partial reuse,
  repair-in-place, alias patch, adapter successor, or second V1 attempt.
- Science cannot start without the exact complete qualification PASS, and it
  cannot consume qualification payloads.
- Any scientific infrastructure or integrity failure consumes the scientific
  attempt and yields no scientific decision, retry, resume, refill, or
  overwrite.
- Any scientific gate failure stops this frozen recurrent-DINO recipe on the
  exposed development route. Point-estimate improvement below a gate is
  reported but does not authorize more scenes, seeds, updates, threshold
  changes, or another implementation tweak.
