# Floor-index input equality diagnosis V1

Replay the first101 observations of the completed seventh maze-0 attempt using
its unchanged controller and model. At fixed frames20,60,100, Python's profiling
callback reads the arguments to observed_floor_cell_index without replacing or
modifying the function. Group calls using exact depth/validity/up bytes, shapes
and dtypes; hashes in the report identify the groups but are not the equality
test. Preserve callers to locate repeated work. Every complete controller
decision must equal its recorded raw-audited decision. No caching is installed.

Exclusive root go2_floor_index_reuse_diagnosis_v1_attempt_001. Bind original
native result and all recorded artifacts/sources before and after, plus this
script and protocol. One CPU process, one numerical thread, minimum4GiB RAM,
128MiB output allowance above40GiB reserve. The immutable predecessor replay
may run beside the existing single native scene. Record resources and preserve
failures; do not change a running scientific attempt. Callback-instrumented
timings are not a speed estimate, navigation outcome or real-time certificate.
