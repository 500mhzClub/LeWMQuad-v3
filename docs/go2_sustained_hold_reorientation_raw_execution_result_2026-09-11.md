# Sustained-reorientation raw replay started

The paired raw-controller replay passed input admission and reconstructed its
first complete original and normalized candidate decision. It is running toward
the fixed 407-observation boundary; no completion or successor outcome is claimed.

The raw-loop/comparator and candidate suite passed 47 tests in 2.72 seconds,
session 72541, exit zero. Source-only preflight passed in session 88809 for 1,972
paths with 79,450,722,304 bytes available RAM and 610,514,673,664 artifact bytes
free. The preceding CPU replay and source native waiter had ended, and a process
scan found no competing full replay before launch.

The new process is PID 2813368, creation time 1789115323.91, on boot
1264d80f-6e46-4fcd-b2fd-2a5d7b964c73. Its exact command is recorded in
`docs/go2_sustained_hold_reorientation_raw_process_start_2026-09-11.json`.
Tool session 87754 owns the ongoing execution. Poll this original process/session;
do not restart it after a quiet poll or an observation timeout.

The launch SHA-256 is
`6f68017be0f68198ab08af97fb4d90f33ebbdc2b1b56f8da123e8b34b4dd89df`.
The output root is
`/home/andrewknowles/RecoveryStorage/LeWMQuad-v3/.generated/navigation_development_artifacts_v1/go2_sustained_hold_reorientation_maze02_prefix_v1_attempt_001`.
The checked live execution record is
`docs/go2_sustained_hold_reorientation_raw_execution_2026-09-11.json`.

The first recorded raw frame was checked against the exact launch and live
process identity. The original completion verifier has run successfully for input
admission. Two fresh model copies and the complete original/candidate controllers
are now consuming the bound original sensor packets. No native scene or changed
command is executed by this replay. It stops before observation 407, then checks
model state, source and input identities again before emitting a result.

On completion, authenticate every recorded comparison, unchanged observed state,
model identity and the exact first changed command at observation 406. Only then
prepare the fresh native successor and its unchanged raw audit. The original
contact experiment remains in its own input admission; tracking and extended
budget remain queued behind it. The goal remains active.
