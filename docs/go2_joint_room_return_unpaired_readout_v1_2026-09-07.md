# Distinct unpaired readout of the completed joint room-return collection

The fresh collection is terminal and immutable: result SHA-256
`a62a1de6837a758709d386eaa3aacfa8d49b043a20e477c867058cc49174a2ca`.
Both nominal controllers report seven completed stages; lower friction reports
zero and stops on observed excursion at tick 521. Native holds remain unscored.

The original audit is terminal failed, SHA-256
`09ce73a3d590004db18ff53c95e6e52ce6f7b8d436fb1f04401e2a8276bb8042`.
Its exact initial-RGB pairing assertion failed for nominal-left after raw sensor
reconstruction returned and the six 750-sample native setup comparisons passed.
No per-condition audit report was saved. Do not restart or overwrite that audit,
change its source/criteria, or describe the original paired audit as passed.

A bound first-frame inspection found all three initial RGB images differ from
the historical predecessor: 25,955 / 24,012 / 25,846 pixels, respectively, with
maximum channel differences 149 / 148 / 149. Mean absolute channel differences
are 5.25644 / 4.39229 / 5.29909. Every checked native setup channel, scene
specification, environment identity and all fifteen saved visual mesh files
match. Native depth arrays differ by at most 27.657 / 23.365 / 24.557 micrometres;
their validity masks match. Camera metadata differs only in RGB identity and
capture duration. The cause of the render difference is unresolved. In
particular, small depth differences do not make the much larger RGB differences
scientifically negligible. No pixel tolerance replaces the failed equality gate.

Perform one distinct post-hoc analysis under
`go2_joint_room_return_unpaired_readout_v1_attempt_001`, reading only the exact
completed collection, its failed audit witnesses and the previously bound
historical predecessor. This is not a new native collection, controller retry,
matched-assay replacement or source export. The scientific question is whether
the actual newly executed, unpaired development trajectories satisfy the
unchanged raw sensor and native hold requirements.

Authenticate all input artifacts and sources before and after analysis.
Reconstruct raw sensors/contact/command histories and replay every decision on
all three complete conditions. The prior failed audit left no durable raw-pass
receipt, so its partial in-memory work cannot substitute for a complete bound
readout. Preserve each completed condition if a later condition fails. Record
initial native equality, initial RGB inequality and pixel/depth differences as
separate facts, never as a passing combined pairing assertion.

Keep every native hold, winding, home, depth, contact, actuator, scene, clock,
command and timing check from the frozen auditor. Define unpaired native full
return success using the same seven-stage, all-completed-holds, home-hold,
no-physical-stop and existing raw-depth tolerance conjunction. Report accepted
pose errors and unavailable frames separately. Do not change arrival, yaw,
settling, tracking or motion limits. No complete readout may be published from
a partial condition population.

Use one CPU process, require 16 GiB available RAM, retain the existing 40 GiB
artifact reserve, and bound new metadata to 64 MiB total and 16 MiB per file.
No raw images, depth arrays, checkpoints or native traces are copied. This
analysis grants no original paired pass, isolated observer treatment effect,
maze-navigation qualification, learned planning benefit or hardware claim.
Rendering reproducibility remains a separate required diagnosis for future
matched fresh-collection experiments.
