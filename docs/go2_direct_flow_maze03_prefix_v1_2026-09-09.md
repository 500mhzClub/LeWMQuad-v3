# Unchanged direct-flow tracking on the maze-3 failure prefix

Replay the original completed learned maze3 through its first visual failure at
observation264, consuming exactly observations0–264. Use the unchanged direct-
corner-flow observer and complete controller already checked on maze1. This is
another development failure case, not new held-out generalization evidence.
Do not tune association, rigid geometry, temporal continuity, bridge budget,
reference history, mapping, mission, model, action selection or sensor thresholds.

Authenticate the completed original three-layout cohort result
a0ac72330a6c34fbae7812a360b22189086f16bd83f586d71e769539ddc2e720,
its complete artifacts and sources, original maze3 raw audit and assigned
corrected JEPA model. Also authenticate completed maze1 tracking prefix result
345b54f1b3c647be516040f2b8bf03b4ab3c709b9737dc0bdf7c78bedcacdbe3
with its artifacts and frozen source identities. Preserve the earlier maze1 V1
runner-validation failure bindings as historical evidence; do not restart it.

Use a fresh model/controller/history. Before264, require every complete original
decision and actual command to match after only the already declared controller
label normalization. At264, require the exact original failure evidence and
unchanged-threshold fallback receipts. Check current raw and registered poses on
the live decision, before tuple identities are converted for JSON storage.
Record and preserve either full controller recovery or a terminal negative.
Stop at264 regardless of whether the requested command changes. Never consume
observation265 or infer a following command's physical outcome. Preserve candidate
decisions even if boundary comparison or live validation fails.

The comparator differs from the frozen maze1 comparator only in its fixed
boundary264 rather than214. The replay calculations are identical apart from
the assigned scene, output labels and explicit layout identifier. Tests verify
those source relationships, complete prefix counting, early mismatch rejection,
state/input integrity, retained negative outcomes and exclusion of later data.

One CPU replay with one numerical thread,8GiB available RAM,40GiB storage reserve
plus256MiB output allowance. Refresh hardware before launch; this job may overlap
the sole native scene and paired controller benchmark when aggregate headroom
permits. No native execution, training, source mutation or automatic retry.
Output:go2_direct_flow_maze03_prefix_v1_attempt_001 under the established external
development artifact root. No new arrival, backtracking, navigation, real-time or
hardware qualification follows from a successful recorded-prefix replay.
