# Fixed augmented-model native goal readout V1

Authenticate the complete two-case native probe and all bound artifacts and
sources. Report both assigned first-seed models without checkpoint or outcome
selection. Preserve verified native goal gates, sensor/model command replay,
physical and observer failures, actual goal distances, observed-pose error,
selected actions, observer retention and measured iteration timing.

Compare each new case with the original full-direct probe and compare the two
new cases with each other. Compare raw physics, policy and gyro histories,
RGB and observer/memory evidence only through the observation immediately
before the first different command or terminal. Models can make different
predictions at identical inputs. Later divergent trajectories are distinct
executed outcomes, not matched-context counterfactual evidence.

Use the exclusive `go2_augmented_family_switch_goal_readout_v1_attempt_001`
root. There is no new execution, optimization or change to prior artifacts.
Both new cases ran concurrently: their timings cannot establish uncontended
model latency. This reused integration layout supplies zero independent novel
mazes. Preserve the broader navigation, timing and hardware limitations even
if either native goal gate passes.
