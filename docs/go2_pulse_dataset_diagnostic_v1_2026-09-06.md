# Pulse-timed dataset and schedule integration diagnostic V1

Materialize every eligible window in the already bound185-window coupled-room
index, using its completed native-label derivation and full raw collection.
All three trajectories share one four-wall layout despite different starts,
appearance and friction; assign them all to one train-role development group.
There are no selection/evaluation layouts, no held-out/sealed access, no fitting,
no optimizer step, no physics and no navigation/generalization claim.

Use PulseTimedDataset to validate exact one-to-one pulse identities, command-
duration cells, target clocks and independent image/motion/contact masks.
Keep missing-history windows in the index and report exclusions rather than
fabricating past images. Keep incomplete future outcomes censored. Sampling
must depend only on declared layout/role, action and input-history availability,
not target values, future visibility, contacts or mission success.

Construct an identical deterministic12-update,6-sample schedule with seed
2026090711 for direct, supervised-rollout and JEPA arms. Cycle shuffled layouts,
then action cells, then examples within cells; require all six supported cells
per layout. Return the full schedule and pulse/role-bound SHA identity. Do not
confuse repeated rare-cell sampling with additional independent examples.
This schedule is checked, not executed as training.

Load all185 eligible observation/native-target pairs one at a time, using copied
past/current and target-side future tensors. Report actual materialized counts,
motion/contact/image coverage, positives, exclusions and per-cell populations.
No native state/future image enters the inference-input dictionary. The caller
binds all raw artifacts and publishers' layout roles; the interface itself
cannot establish geometric independence from arbitrary layout-name strings.

Exclusive output `.generated/go2_pulse_dataset_diagnostic_v1_attempt_001`.
Freeze this protocol, diagnostic, dataset source and tests at launch; validate
inherited source/input and exact native-label identities plus complete raw
artifacts before/after. Preserve partial failures. No source export, relocation,
old-output mutation or sealed access. The active physical collector is separate
and unchanged. Full objective requirements remain in the execution-to-JEPA plan.
