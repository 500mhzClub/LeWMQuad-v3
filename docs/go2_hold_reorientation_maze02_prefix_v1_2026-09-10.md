# Raw full-controller hold-reorientation prefix

Use the first completed and raw-audited worker of the existing adapter batch:
`all_phase_full_jepa_residual_maze_02`, launch
`97c307ce8d2178494e7484f14b3b0cdcd8d7d0ceaab2f94d94d59da44e39b17a`.
The caller must supply its exact completed worker-terminal SHA-256. Verify all
bound worker artifacts, the original model assignment, raw audit and actual
adapter startup boundary. Scientific failure is retained and does not prevent
diagnosis. The other five cases may remain active; parent cohort completion is
not required for this offline replay of the completed first case.

Reexecute the full original input verifier before and after replay, including
its original dataset and model admission. Authenticate the completed saved
selection check result
`ea832425482ec3f82e3a91407b5a9aa0029f1bad466213e64b8c83cce18f651c`,
all its artifacts and inherited/new sources. Do not mutate any frozen source.

Instantiate two fresh identical expanded full-JEPA adapter models, state
`35496b6b402013f7a33d9c30110e115b90ded672f0d0da829a07128601507b6a`.
Run the exact original ResidualAnchoredContinuationController and the separate
HoldReorientationController from raw original public RGB/depth/body/control
packets and auxiliary camera evidence, with the original public mission.
Require every full original decision to reconstruct, public arrays to remain
unchanged, and identical accumulated planning cells, complete contact memory
and executed residual history. Pending forecasts must also agree before each
new observation. Reconstruct the actually completed original command endpoints.

Consume exactly frames 0–405. Match each original row hash to the fixed saved
check, and each newly computed candidate selection to its expectation. Complete
normalized decisions must be identical before 405. At 405 permit only the
declared hold-to-left-turn selection/request and controller identity metadata
to differ; all forecasts, scores, vetoes and observed state remain exact. Do not
read packet 406. Record full candidate decisions, comparison receipts, public
packet hashes and contact-state fingerprints. No native scene, changed command
execution, later counterfactual trajectory or navigation success is produced.

Use one CPU process with one OpenCV/Torch/BLAS thread, deterministic algorithms,
32 GiB available memory and a 40 GiB artifact reserve plus one GiB allowance.
Exclusive output `go2_hold_reorientation_maze02_prefix_v1_attempt_001`; preserve
failures without silent retries or replacement. Source-only preflight requires
no completed native worker and creates no runtime output. Native execution of
this candidate remains a separate experiment after the already queued frontier
experiment, whose priority and frozen sources remain unchanged.
