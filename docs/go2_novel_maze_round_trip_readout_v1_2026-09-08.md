# Completed first-maze readout V1

After the first native maze pilot completes collection and its full raw audit,
run `scripts/read_go2_novel_maze_round_trip_pilot_v1.py --native-result-sha256 HASH`
at its exclusive `go2_novel_maze_round_trip_readout_v1_attempt_001` root. An
infrastructure/audit failure is not accepted as a completed input. Authenticate
the result, launch, all native artifacts, and frozen recursive source/native
dependencies before and after readout.

Read the complete bounded gzip decision stream, original native pose trace and
completed audit. Preserve the existing verified-round-trip outcome. Report both
native dwell/path evidence and measurement gates, actual initial-frame path
length and outbound-goal distances, observed phase/target/action/map counts,
first sensor/model failure or valid infeasibility, and original acquisition,
control, command and receipt-write timing. No learned forecast is substituted
for an unexecuted outcome. Native coordinates remain evaluator-only readout.

Minimum distance over the complete native recording is descriptive; it does not
complete an observed dwell or substitute for a return. A physical return would
not establish a causal memory or learned-planning benefit. No matched baseline
exists in this first native pilot. No checkpoint/controller selection, model
training, new simulation, hardware execution or promotion occurs.

Require 8 GiB available RAM and 128 MiB above the unchanged 40 GiB storage reserve.
Retain the original paused-physics and ideal sensor assumptions; measured cycle
times are not real-time qualification. Preserve any readout failure independently
of the immutable native attempt.
