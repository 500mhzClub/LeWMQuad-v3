# Causal residual diagnostic: JSON-admission integrity replacement V1

Use the exact native/readout inputs and eight-tick prequential science defined
in `docs/go2_causal_executed_residual_diagnosis_v1_2026-09-08.md`. The original
attempt failed before a result because JSON serialized the identity tuple as
a list. Authenticate its launch/failure and preserve every original binding.

Change only the recorded-pose adapter: require exactly three nonnegative JSON
integers, restore their tuple encoding, then invoke the original complete live
pose/clock/identity/witness admission. Do not change identity values, poses,
timestamps, gates, algorithm, window, native labels or metric populations.

Exclusive `go2_causal_executed_residual_diagnosis_integrity_v1_attempt_001`;
one process/numerical thread, 8-GiB RAM, 256-MiB output allowance above 40-GiB
reserve. Freeze/reverify all original and new source/input/failure evidence.
No original retry/resume, runtime correction, fitting, native execution,
navigation or hardware qualification.

Before launch, six JSON round-trip/identity/time/witness tests passed in 1.80 s.
The adapter also admitted all 582 recorded non-null poses across the two
JEPA/direct input pairs (244+47+244+47), with original episode/frame/clock
checks active. Analysis and summary function ASTs match the failed script
exactly; only their explicitly imported pose adapter changes.
