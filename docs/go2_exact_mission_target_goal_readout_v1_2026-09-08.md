# Exact final-target native readout V1

Authenticate both completed native missions and their raw audits, plus the
observed-floor-contact predecessor. Preserve both goal, contact, sensor, stop,
timing and incomplete outcomes. Record exact-goal selection/connector evidence
alongside original candidate constraints, active waits and executed actions.
Count an arrival only when the unchanged controller dwell, native quiet/contact
goal gate and both sensor audits all pass.

Compare each method with its predecessor only through the observation before
the first command or terminal difference. Compare the new pair similarly,
including actual physics, policy/gyro histories, primary RGB, auxiliary
depth/masks, observer/map and learned forecasts. A changed target may change
costs and commands without changing the learned forecast on a shared prefix;
do not misattribute target correction to JEPA training.

Exclusive go2_exact_mission_target_goal_readout_v1_attempt_001 root after full
source/input admission; reverify afterward. No fitting, controller change or
native execution. A reused-layout arrival would remain an integration milestone,
not independent-maze, backtracking, baseline, real-time or hardware qualification.
