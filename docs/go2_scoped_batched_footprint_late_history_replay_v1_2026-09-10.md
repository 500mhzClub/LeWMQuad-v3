# Incremental late-history batching after scoped reuse

Prepare the harness while the scoped-only replay is live, but do not execute
the raw comparison until that exact owner has ended and its completed result,
timings and invariants have been reviewed. Require the completed result SHA on
the command line and the fixed original launch identity. Preserve any original
failure; never retry or resume either attempt.

Use the existing paired loop's exact code in an isolated function namespace.
Substitute only the scoped-only baseline, combined scoped/batched candidate,
their declared complete-decision normalization, the previously checked two
patch-store type normalizations, output path and progress label. Do not mutate
imported modules. Use fresh independent copies of the assigned model.

Reconstruct observations 0–1427 and all 1,425 forecasts. Require original command
endpoints and complete decisions, unchanged public inputs after each controller,
and baseline decision hashes equal to the completed scoped candidate. Compare
retained state between the pair and to all seven original scoped checkpoints.
The only permitted state type normalizations are the primary and auxiliary
patch-store tags; no state values or witness arrays are normalized. Stop before
observation 1428. Verify unchanged model state and absent gradients.

Alternate execution order, time complete controller observation calls without
profiling, and retain every sample and regression. Recompute the predecessor's
full timing report from its rows before admission. Report an incremental batching
comparison with scoped reuse in both controllers. The original negative sensing
result and zero-round-trip scope remain explicit. No real-time, acquisition,
isolated-host or navigation qualification follows from a faster diagnostic.

Repeat complete original worker/input admission before and after replay. Require
64 GiB available RAM, 41 GiB artifact headroom and four physical CPUs, refreshing
admission before replay. These are checks, not OS reservations. Keep the original
single simulator scene and queue intact. The source-only preflight loads no
model, creates no artifact root and requires no predecessor completion.
