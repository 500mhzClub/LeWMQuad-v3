# Augmented family/switch full-fit readout V1

Only after all eighteen fixed scientific fits finish, admit every one of the
21,600 update-ledger entries, all raw role scores, final snapshots and paired
initializations through the frozen all-model admission checker. Reproduce both
source schedules and verify every artifact/source identity. Partial or selected
studies cannot enter this readout or native control.

Admit the complete unchanged original-model reference, using the same new
branch input result, and require the new seed-2026091001 models to match the
original seed's initial state exactly. Aggregate already authenticated scores
by source, role and stratum, weighting position/yaw by valid motion counts and
contact Brier by valid contact counts. Preserve all target denominators and
undefined-yaw counts; any undefined valid yaw makes that aggregate yaw metric
unknown, rather than removing the case.

Report all eighteen models, paired first-seed augmented-minus-original
differences, and means/standard deviations across the three optimization seeds.
Label those standard deviations as optimization-seed variation, with no
independent-maze confidence interval. Keep old initial/continuing windows, new
repeat/switch branches and first-half-second horizons separate. All raw cluster
scores remain available in their original bound files.

No score selects a checkpoint, model, seed, schedule or future hyperparameter.
The fixed first full-JEPA model remains the primary native candidate and the
matched full-direct comparison remains required. The root is the exclusive
`go2_augmented_family_switch_fit_readout_v1_attempt_001`. Use one readout after
hardware assessment, at least 8 GiB available RAM and 256 MiB output allowance
above the 40-GiB reserve. Preserve source/input/model and descriptive metric
receipts; recheck integrity at completion. No fitting, native navigation,
verified arrival, final evaluation or deployment qualification occurs here.
