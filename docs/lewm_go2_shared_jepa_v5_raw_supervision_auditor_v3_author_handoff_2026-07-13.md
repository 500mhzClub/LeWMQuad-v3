# Shared JEPA V5 raw-supervision auditor V3 author handoff

Date: 2026-07-13

Implementation author: `/root/raw_auditor_author`

Status: **READY FOR DIFFERENT-AGENT INDEPENDENT REVIEW; NOT AUTHORIZED**

This additive V3 is the mechanical authorization successor to the frozen
Auditor V2 security base. It preserves V2's dataset, population, integer,
hard-link, replay, publication, and authorization-before-manifest controls but
binds only the Builder/Auditor V3 nine-role closure preregistered by the frozen
successor amendment. V1, V2, and their review evidence were not edited.

No exact audit, exact build, canonical output, failure receipt, or machine PASS
review was created. No canonical dataset, development source, RGB, legacy
label, G2, checkpoint, held-out, runtime, hardware, navigation, production, or
promotion payload was opened. Verification used synthetic temporary fixtures,
one native thread per math library, and no accelerator.

## Frozen authority

The pre-implementation successor amendment is frozen at:

| Artifact | SHA-256 |
| --- | --- |
| `docs/lewm_go2_shared_jepa_v5_raw_supervision_builder_auditor_v3_authorization_successor_amendment_2026-07-13.md` | `501062e2eba625cf4d7ab28810f2a629652c327c770366c07f3b788f3f6f8b2b` |

The retained Auditor V2 implementation base remains:

| Artifact | SHA-256 |
| --- | --- |
| `lewm/datasets/go2_shared_jepa_v5_raw_supervision_auditor_v2.py` | `d57aacd4849ea3e79468618b73925418ad2035d47de636dc991afda777314b2a` |
| `scripts/audit_go2_shared_jepa_v5_raw_supervision_v2.py` | `4502ac44a451841af18e9f9eb545ef961bc81324ea84ce713e434c434e000ae9` |
| `lewm/tests/test_go2_shared_jepa_v5_raw_supervision_auditor_v2.py` | `45d60db1f1a7385b7941f8f52e01a923f056bb3f52cc85b7fec4097d54fa9399` |
| V2 author handoff | `6a338b7c15c1fe23ab3680e80c4a30781369e29eebb33331e7ccff723cd4b7ab` |

## Additive candidate

| Artifact | SHA-256 |
| --- | --- |
| `lewm/datasets/go2_shared_jepa_v5_raw_supervision_auditor_v3.py` | `08cbbc8b7ae197ee100e3327adcd2c3921c90ba834d433f0fdf0a9ce348a9606` |
| `scripts/audit_go2_shared_jepa_v5_raw_supervision_v3.py` | `f1258680802be18ad77ca4cf0fa1aacef5e941d9aca40fa68a6d7d8105892445` |
| `lewm/tests/test_go2_shared_jepa_v5_raw_supervision_auditor_v3.py` | `4e111e961ed3e8a7250f6c0cfbff4033c5cb6487c67cbbb9d65d389081e9fd19` |

These three hashes are the Auditor V3 candidate freeze. This explanatory
handoff is intentionally not an authorization role: the amendment fixes the
auditor candidate to source, CLI, and test, and the complete source map to
exactly nine rows.

## V3 authorization closure

V3 accepts only authorization schema
`lewm_go2_shared_jepa_v5_raw_supervision_build_authorization_v3`, review-binding
schema
`lewm_go2_shared_jepa_v5_raw_supervision_implementation_review_binding_v3`, and
the amendment's exact ordered V3 paths. The builder review must use
`lewm_go2_shared_jepa_v5_raw_supervision_builder_v3_independent_review_v1`; the
auditor review must use
`lewm_go2_shared_jepa_v5_raw_supervision_auditor_v3_independent_review_v1`.

All four Builder roles now name Builder V3, and all three Auditor candidate
roles name this V3 candidate. The builder handoff remains the intentional ninth
lineage input; this auditor handoff cannot become a tenth row.

The fixed authorization file remains the amendment-compatible canonical path
`docs/lewm_go2_shared_jepa_v5_raw_supervision_build_authorization_2026-07-13.json`.
The caller must supply its independently frozen file SHA-256. Before opening a
manifest, metadata, inventory, or development source, V3:

1. opens only that fixed authorization file;
2. duplicate-key parses and canonical-checks the exact six-field object;
3. validates its self-hash, exact ordered nine-role V3 map, paths, candidates,
   distinct authors/reviewers, and PASS bindings without opening a target;
4. reparses and exactly revalidates the immutable phase-one capsule;
5. descriptor-reads exactly nine fixed targets and validates both machine
   reviews; and
6. only then reads and binds the dataset manifest.

A new two-case regression rewrites an otherwise valid synthetic V3 authority
with either all V2 paths or both V2 review schemas. Both are rejected in phase
one with zero target opens. A complete V2 closure therefore cannot authorize
V3 by changing only the top-level schema or self-hash.

## Retained audit contract

The synthetic callback API has no exact switch. The sealed exact API has no
sample-recomputation, loader, input-plan, source-inventory, or replay callback.
It retains fixed repository, dataset, authorization, report, and replay paths.
Workers remain exact integers in `[1,6]`.

V3 retains V2's independent derivation of 5,172 pairs, 10,344 endpoint
references, 9,460 unique endpoints, 88 scene shards, three roles, and eight
families per role before comparing frozen declarations. Booleans and floats
remain invalid at every cardinality and shape boundary. The manifest and every
declared dataset leaf remain descriptor-bound, singly linked regular files;
the complete dataset and source population is rechecked before publication.

The canonical audit leaves are additive and fixed:

- `.generated/go2_shared_observable_camera_ray_jepa_v5/development_raw_supervision_v1.audit_v3.json`;
- `.generated/go2_shared_observable_camera_ray_jepa_v5/development_raw_supervision_v1.audit_v3.failed.json`.

Exact success schema is
`lewm_go2_shared_jepa_v5_raw_supervision_audit_v3`. Callback-driven fixture
audits emit only
`lewm_go2_shared_jepa_v5_raw_supervision_synthetic_audit_v3` with synthetic,
non-authoritative scope. Neither was published during authorship.

## Verification

All tests disabled external pytest plugins, fixed OMP, OpenBLAS, MKL, and
NumExpr threads to one, and hid CUDA, HIP, ROCr, and GPU ordinal visibility.

```text
Focused Auditor V3 adversarial/synthetic suite:    28 passed in 0.38s
Auditor V2 + V3 retained closure:                  53 passed in 0.62s
py_compile (V3 source, CLI, test):                 PASS
git diff --check (V3 source, CLI, test):           PASS
ASCII-only check:                                  PASS
exact execution or output publication:             NOT RUN
GPU opened:                                        NO
```

The focused suite includes every retained V2 author test plus the frozen V2
base/amendment bindings and two V2-to-V3 authorization rejection cases.

## Required independent review

A reviewer other than `/root/raw_auditor_author` must rehash this candidate and
the frozen amendment, rerun the retained V2 security/science matrix and the V3
suite, independently attack V2 path/review substitution and all nine V3
bindings, and verify zero pre-authorization manifest/source opens. The reviewer
must publish PASS or BLOCK at the amendment-fixed path:

`docs/lewm_go2_shared_jepa_v5_raw_supervision_auditor_v3_independent_review_2026-07-13.json`

Even a PASS grants no exact run. Builder V3 must independently pass review, a
separate canonical nine-role authorization must bind both reviews, and a human
must separately supply its frozen file SHA-256. Dataset use, training,
selection, calibration, G2, held-out, navigation, runtime, hardware,
production, and promotion remain unauthorized.
