# Profile the completed footprint receipt-copy controller

The completed paired replay preserved all 1,428 decisions, 1,425 forecasts and
seven retained-state checks, but every planning observation still exceeded
100 ms. Its measured 2.4% incremental reduction does not resolve real-time
execution. Profile this exact candidate to locate the remaining costs before
choosing another optimization.

Reconstruct the original 1,428-observation causal history using one fresh
assigned model and the exact ReceiptCopiedFootprintController. Profile only
controller.observe in the unchanged ten-frame windows 3–12, 395–404 and
1418–1427. Preserve the original profile body's packet order, command endpoint
checks, full decision and model-state comparisons, public-array mutation
checks, hold-window checks and scope statements. No following observation is
consumed. Compare every profiled replay row against the completed receipt-copy
replay's public input, original decision and candidate decision identities.

Bind the completed paired replay to result
`3e82c140151b4e0976b65bd4b439c519f87a89e4f4df3cb9b2975df7324fe56d`
and its independently checked completion record. Reexecute the original
completion verifier with a private output-capture function; preserve its code,
all validations and original module globals. Compare the complete regenerated
receipt except its timestamp with the original. Recheck actual raw/model
bindings before and after profiling. Do not overwrite the original receipt or
claim to repeat full training ancestry.

Use the original closure-free profiling function with only the output path,
controller class, normalization function and progress label substituted in
private globals. Preserve all saved pstats and complete JSON summaries. Do not
infer speedup from profiled timing or add percentages from different runs.
State-size snapshots are descriptive, not independently verified state hashes.

Assess hardware and competing jobs before launch. Use one full CPU replay slot,
one BLAS/OpenCV/Torch thread and disabled OpenCL, with at least 48 GiB available
RAM, 41 GiB artifact space and four physical CPUs. The preceding replay owner
must have ended on its original boot. No frozen native job is changed, and no
physics scene, training run or hardware command is launched by this profile.
Create one exclusive output; retain failures with no automatic retry or resume.

Retain the original visibility failure at frame 1173 and failed round trip.
Profiling does not establish navigation, valid sensing, independent-layout
benefit, real-time execution or hardware qualification.
