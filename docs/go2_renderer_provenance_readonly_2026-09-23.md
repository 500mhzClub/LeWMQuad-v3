# Bounded renderer-restoration provenance review

This review concerns exposure to the demonstrated **scene-time rewind / stale visual-cache mechanism**. “Ruled out by verified path” below is specific to that mechanism; it is not a blanket certification of an image population, checkpoint or scientific claim. No historical simulation, dataset regeneration, model recomputation, sealed-material access or full historical inventory was performed. Original reports and failures remain unchanged; this document adds qualification notes.

| Population / use | State/time restored before rendering? | Relevant visual reset / rendering path | Exposure assessment and dependent evidence |
|---|---|---|---|
| September matched short-pulse branch assays | No. Every episode starts a fresh scene and advances a fixed command tape monotonically. | `collect_go2_short_pulse_learning_development` binds `moving_action_switch_episode_development.collect`; new `GeometryProgressFamilySession`, settle, sensor capture, command ticks. No rewind needing a visual reset. | **Ruled out by verified collection path** for the rewind mechanism. All 36 frame-13 contexts (18 training-role / 18 exposed geometry-transfer) feed the JEPA/supervised/untrained assay, frozen historical predictor transfer assay, and native-adaptation branch assay. Their results remain development evidence. |
| Actual-future near-goal diagnosis | No snapshot rewind. Each of 12 alternatives creates a new scene and replays the full prefix to frame 35, then its five-command suffix. | `diagnose_go2_dense_visual_goal_overshoot_development.branch`; native sensor capture during forward replay. Exact context PNGs at frames 25/30/35 and exact original forward successor required. | **Ruled out by verified path**, additionally supported by retained exact-prefix/successor checks. Two post-hoc failure states; downstream raw-feature and learned-goal-metric actual-future diagnoses depend on these images. No checkpoint was fitted on these 12 diagnostic outcomes. |
| Stalled-turn local branch diagnostic | No rewind. Fresh initialization for each hold/left-turn/right-turn branch, then 2,220 original 20-ms commands. | `collect_go2_dense_stall_turn_branches_development`; exact source context RGB at 434/439/444 and pose agreement required before suffix. | **Ruled out by verified path**. Three branches at one exposed stall; downstream predicted-versus-actual-future motion readout and action retrieval evidence remains qualified by its post-hoc single-state design. No fitting. |
| September local-control factorial | No. A new session per trial; monotonic physical time across its two edges. | `run_go2_local_control_factorial_development_v1.collect_trial`; initial/edge/final captures, no restore. Retained audit explicitly reports continuous 2-ms timestamps and no reset between edges. | Rewind exposure **ruled out by inspected path and retained audit**; full transitive runtime source identity not re-established here. Eight geometries × four arms; RGB is boundary evidence, not a fitted-model control input. Preserve the separately documented 100/10 versus 20/0.5 actuator-gain finding; renderer analysis does not erase it. |
| Native short-pulse branch-generated training | No, same fresh-scene monotonic collection as the first row. | Collector, bound episode function, geometry session and target derivation match hashes in the retained launch. The prose protocol has since changed and does not match its old launch hash; that discrepancy is recorded, not substituted for original evidence. | Rewind exposure **ruled out by verified path** for this component. Training-role short-pulse windows enter the native JEPA/supervised predictor and readout schedules, native frozen-V-JEPA adaptation, balanced continuation and horizon-dense continuation. This is not a complete requalification of all their other inherited inputs. |
| Balanced-start and balanced-start-horizon training | No. Forty-eight cases each, fresh worker/session, fixed zero prefix then candidate suffix; no snapshot restoration. | `collect_go2_balanced_start_actions_development` and `collect_go2_balanced_start_horizon_actions_development`; RGB-only native capture. Horizon extension requires equality of the retained original RGB prefix. Both collectors match their pre-execution plan hashes. | Rewind exposure **ruled out by verified path**. Balanced mixed/control predictors and the 100–800-ms predictor depend on these populations. The current action predictor therefore inherits these native images, as well as earlier training/checkpoint ancestry not fully inventoried here. |
| Legacy V1.2 branch corpus / historical frozen-feature predictor ancestry | Yes in the physical branch scene: `_execute_and_render` → V1 `restore_branch_state`, which restores solver and scene `_t` without resetting that scene's visualizer. | Inspected corpus source renders captured poses through **a separate static `TexturedV03Renderer` scene**, setting its camera pose for every frame. It does not render the rewound physical scene. A visual reset in the physical scene therefore would not fix or change this separate rendering path. | The specific shared-scene rewind mechanism is ruled out **for the inspected separate-renderer call path**. **Historical population-wide exposure remains unresolved**: this bounded review did not bind every historical corpus execution to that source version or certify legacy renderer content. Historical factorial RGB one-step/rollout initializers feed later native adaptations. Do not infer contamination, or full validity, for every ancestor checkpoint from the current pilot failure. |
| Current pilot original source decision images | No rewind before collection: source rollout and original sensor persistence complete before branch execution. | `run_go2_decision_headroom_source_development` uses native paired-camera acquisition with monotonic time. Restored branch capture occurs afterward through a separate audit adapter. | Rewind exposure **ruled out by verified source call ordering**. Original source RGB/causal decision inputs are not invalidated by the branch cache failure. Full packet/geometry/target validity still requires its own checks; physical pose agreement alone cannot establish input validity. |
| Original pilot branch futures | Yes, same native scene after source rollout; old audit restore rewound physics but did not reset visual-cache clocks. | Camera transforms agreed while visual state did not update correctly. Original evidence retains 0/384 bitwise source-replay image matches. | **Demonstrated** RGB restoration failure consistent with this call path. All original branch future images remain unqualified for R3 / readout and forecast decomposition. No training or comparative audit used them. Physical/source-input evidence has separate qualification and is not automatically discarded. |
| Amended two-state recheck | Yes, with the approved visualizer reset immediately after physical restoration and before RNG restoration. | Both visualizer and rasterizer cache clocks are invalidated. Same capture/save path; four source replays occur after candidate branches. | **Qualified on the two tested states:** 96/96 exact source RGB, physical checks and candidate repeat agreement. This does not retrospectively repair old images or certify all source/layout combinations. |

The same word “branch” covers fresh-scene forward replay, native snapshot restoration and rendering physical poses in a separate static scene. Those have different cache exposure. In particular, the old floor-only waypoint-rendering defect documented in `go2_active_interface_specification_2026-09-04.md` is a distinct manifest-schema problem. That report already limits the finding to its verified caller; it must not be generalized to every earlier predictive dataset.

Source identities and scope: [machine-readable bindings](go2_renderer_provenance_readonly_2026-09-23.json). Exact top-level historical collector hashes matched for near-goal, stalled-turn and both balanced collections. The short-pulse launch also matches its collector, episode function, geometry session and target-label source. This establishes the relevant acquisition route without reopening all transitive historical software. Sources not historically hash-bound in this review are explicitly weaker evidence; their current hashes identify what was inspected.

Checkpoint dependencies are retained in `go2_frozen_vjepa_native_adaptation_plan_2026-09-17.json`, `go2_balanced_start_predictor_plan_2026-09-17.json` and `go2_horizon_dense_predictor_plan_2026-09-18.json`. The current horizon action checkpoint is `5d39753fbdc7714c60bf8c24c1757fd0ffbbdfe9485d776d65b5c3caa9fbb7ca`. The matched old-data and maze-data Stage A readouts are respectively `97b3972cf6e1283d8825e7e900c0d504ef88ce3f495690d8b8ac284cee2fb19f` and `aa853c6f16d7f78e01ade2bb91c1dfceebda2b1a12b0657a223b184fe2005fb8`. All use the frozen pretrained encoder; no evidence in this review shows that the current pilot defect affected encoder pretraining.

The named September assays and diagnostic reports retain their original numerical results. This review neither reranks them nor upgrades them into held-out decision-quality evidence. Remaining ancestry uncertainty is recorded rather than resolved by unauthorized reruns or a full inventory rebuild.
