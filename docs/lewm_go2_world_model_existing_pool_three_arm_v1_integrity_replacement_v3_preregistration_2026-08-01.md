# Existing-pool three-arm world-model V1 integrity replacement V3

Date: 2026-08-01

Status: **PREREGISTERED SOURCE-INTEGRITY REPLACEMENT; NOT EXECUTION AUTHORITY**

## 1. Purpose and closed predecessors

This is one science-identical integrity replacement for the consumed
`world_model_existing_pool_three_arm_v1_integrity_replacement_v2/attempt_v1`.
It corrects a third pre-training source defect reached only after V2 passed
fresh packing, the predecessor-loader correction, complete arm allocation,
trainability partitioning, and construction of all three optimizers. It is not
a retry, resume, refill, continuation, or runtime-artifact reuse.

The full scientific experiment remains the original three-arm contract in:

- `docs/lewm_go2_world_model_existing_pool_three_arm_v1_preregistration_2026-08-01.md`;
- `docs/lewm_go2_world_model_existing_pool_three_arm_v1_plan_2026-08-01.json`.

Integrity replacement V2 and its prior failure chain are closed by:

- `docs/lewm_go2_world_model_existing_pool_three_arm_v1_integrity_replacement_v2_preregistration_2026-08-01.md`;
- `docs/lewm_go2_world_model_existing_pool_three_arm_v1_integrity_replacement_v2_terminal_pretraining_source_failure_result_2026-08-01.json`;
- expected SHA-256
  `8cb652fb8e88d725b187f45d3b2988de440b9feabe253d372de90fb6134a1902`;
- expected byte count: 8,487;
- status: `PASS_COMPLETE_TERMINAL_PRETRAINING_SOURCE_FAILURE_AUDIT`.

The V3 plan, independent review, and authority must bind that exact audit.
The original, V1, and V2 attempt roots and every artifact beneath them are
quarantined and ineligible as V3 inputs.

## 2. Established V2 replacement failure

V2 rebuilt both packs, passed the corrected 187-tensor predecessor loader,
constructed the real temporal substrate and three complete independently
trainable arms, and constructed all three optimizers. It then failed the
aggregate payload-free parity guard inside
`build_frozen_substrate_and_arms`, before update-zero evaluation. It
completed no training update, optimizer step, result, checker, or scientific
verdict.

The frozen substrate was recursively in evaluation mode. V2 put each copied
arm recursively in training mode before a no-gradient, zero-tensor contract
probe compared:

- the conditioned copied-arm helper with the frozen reference route;
- the blind arm across candidate-action substitutions; and
- online and target persistence routes.

All gates require exact `0.0`. Although all relevant dropout probabilities
are zero, train/eval mode can select different Transformer or attention
execution paths and yield nonzero floating-point differences for the
conditioned reference comparison. The V2 terminal receipt records only the
aggregate guard failure; exact subprobe attribution comes from separate
source-only synthetic reproduction, not from the runtime receipt.

This is not evidence about data, objective, architecture learnability, or
generalization. It validates only that both earlier integrity corrections and
all model/optimizer construction crossed their prior boundaries.

## 3. Sole new source correction

For the unchanged payload-free probe, V3 must:

1. recursively place every copied `ArmCore` tree in evaluation mode;
2. run the unchanged no-gradient, zero-tensor probe with all three unchanged
   exact-zero assertions; and
3. after every assertion passes, recursively restore every arm to training
   mode before return, update zero, or any training computation.

The correction must not:

- relax an exact-zero assertion or change any probe input;
- change parameter values, identities, names, shapes, dtypes, counts, buffers,
  gradients, or `requires_grad` flags;
- change optimizer membership, groups, hyperparameters, state, or learning
  rates;
- consume or advance CPU or CUDA RNG state;
- change actual evaluation or training forward modes;
- change data, packs, arm treatments, model initialization, losses, schedule,
  metrics, thresholds, caps, or accounting.

The exact `ArmCore` has no buffers or BatchNorm. Predictor and attention
dropout are zero, and the GRU is one layer with zero dropout. Thus the mode
bracketing changes only the numerical dispatch used by the payload-free
diagnostic. The shared substrate remains frozen and recursively in evaluation
mode; every arm must be recursively in training mode on return.

Before authority, a source-only regression under the exact bound ROCm runtime
must use a fully synthetic 187-entry predecessor state and prove:

- conditioned-helper, blind-treatment, and persistence errors are exactly
  zero during the aligned probe;
- all arm parameter hashes, identities, inventories, and trainability flags
  are unchanged;
- optimizer groups are unchanged and optimizer state remains empty;
- CPU and CUDA RNG state are unchanged;
- every arm and child module is in training mode on return;
- the substrate and every child module remain frozen and in evaluation mode;
- no real checkpoint, pack, RGB, held-out, sealed, or protected payload is
  opened.

The prior loader correction is retained unchanged: the exact bound spatial-V1
checkpoint must contain 186 finite strided float32 tensors plus scalar strided
`torch.long ema_update_count=1000`; the temporal constructor enforces its
exact migration boundary, hard-syncs a fresh target, and resets temporal EMA
to zero.

The prior arm correction is retained unchanged: after independent copies,
`ArmCore.requires_grad_(True)` makes all 36 parameter tensors trainable
without changing their values or the exact 30-predictor/6-memory partition.

## 4. Science-identical contract

Everything outside the three enumerated integrity corrections is frozen
unchanged from the original preregistration and V2, including:

- all four input hashes and visible positions;
- all arm semantics and order;
- shared frozen encoder/target and independently allocated heads;
- seeds, hashed schedule, presentations, batches, and 700 updates;
- optimizer groups, learning rates, warmup, cosine schedule, weight decay, and
  gradient clipping;
- validation/train-fit panels, metrics, bootstrap, thresholds, and gate order;
- requested-action factual-only claim scope;
- wall/GPU/free-space caps and exact 57-file success inventory;
- receipt-only checker custody; and
- development-only, non-citable, no-network, no-navigation, no-held-out, no
  promotion status.

No data, model value, loss, threshold, schedule, objective, or scientific
accounting change is permitted. A normalized comparison with V2 must contain
zero scientific differences after removing only lifecycle identity,
direct-predecessor evidence, and the new diagnostic correction.

## 5. Fresh V3 lifecycle

The sole V3 identity is:

- attempt ID:
  `world_model_existing_pool_three_arm_v1_integrity_replacement_v3/attempt_v1`;
- output root:
  `/home/andrewknowles/Workspace/LeWMQuad-v3/.generated/dev/world_model_existing_pool_three_arm_v1_integrity_replacement_v3/attempt_v1`;
- maximum attempts: one;
- fresh absent root required;
- reservation consumes the attempt;
- retry, resume, overwrite, refill, partial reuse, and automatic extension:
  false.

V3 must rebuild its pack from the original bound inputs. It may not read or
link any consumed attempt's pack, audit payloads, snapshots, RNG state, or
partials. Their terminal JSON documents are identity evidence only.

V3 authority/plan/review/reservation/result/check/worker-failure/terminal
schemas must be V3-specific. The supervisor and checker must reject the
original, V1, and V2 roots, IDs, plans, authorities, and schemas.

Before reservation V3 requires a committed V2 failure audit, frozen V3
source/plan, independent review with no findings and no authority, separately
committed caller-bound authority, exact live binding equality, absent root,
idle authorized device, at least 16 GiB free, and the exact-runtime synthetic
ROCm regression above.

If V3 fails after reservation, it is terminally consumed. This document grants
no further replacement.

## 6. Custody and result boundary

Sealed, held-out, G2-G8, navigation, production, promotion, and network access
remain forbidden. The checker may open only the result, two audit JSON
receipts, and 24 measurement JSON receipts. Packs, RGB, checkpoints, and
snapshots remain identity-only to the checker.

A successful V3 produces the original experiment's terminal metrics under a
V3 lifecycle. It still requires an independent terminal audit and durable
handoff and does not itself authorize promotion or protected evaluation.
