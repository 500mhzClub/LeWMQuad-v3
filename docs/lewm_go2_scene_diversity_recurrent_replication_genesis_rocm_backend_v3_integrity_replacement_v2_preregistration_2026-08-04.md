# Go2 scene-diversity Genesis ROCm V3 integrity replacement V2

Date frozen: 2026-08-04

Status before execution: `PREREGISTERED_NOT_RUN`

Scope: at most one fresh, development-only, science-identical integrity
replacement for the consumed replacement-V1 qualification invocation. This
document grants no qualification, scientific, sealed, held-out, navigation,
rollout, planner, promotion, deployment, retry, resume, refill, overwrite,
partial-reuse, or further-replacement authority. Independent source review
and separate exact one-shot authorities remain mandatory.

## Consumed replacement V1 and eligibility

Replacement V1 failed before reservation because the substituted collector
did not export `_require_v1_review_binding`, which an inherited qualifier
called during exact authority validation. The final review is
`docs/lewm_go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v3_integrity_replacement_v1_qualification_terminal_review_2026-08-04.json`,
SHA-256
`45bcd329d778dc9cba71882b398aa25943600dc160436bdba6b919868418f6fa`,
10,267 bytes, schema
`lewm_go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v3_integrity_replacement_v1_qualification_terminal_review_v1`,
and status
`PASS_FAIL_CLOSED_PRE_RESERVATION_LAYERED_INTERFACE_TERMINAL_REVIEW`.

The review records that replacement V1's authority and invocation were
consumed, but no attempt root, reservation, GPU query, Genesis import or
initialization, scene, RGB, result, runtime terminal, qualification decision,
or scientific decision was produced. It permits source development and
independent review of at most one comprehensive replacement V2 hypothesis.
It grants no execution authority and forbids reuse of replacement V1's
authority, command, receipt, runtime metadata, payload, reservation, cache,
worker, scene, RGB, result, or process evidence. The final terminal review is
the sole new replacement-V1 witness admitted here.

## Single comprehensive integrity intervention

The failure is classified as a layered-adapter interface-closure defect, not
a backend, data, model, or scientific failure. Adding only the immediately
missing alias is forbidden. Replacement V2 owns a closed compatibility
facade and an exact override-key matrix for every inherited collector,
qualifier, and runner layer used by:

- qualification-authority validation;
- pre-reservation qualification execution;
- worker argument and child-environment construction;
- qualification-result replay;
- scientific-authority validation; and
- pre-reservation scientific execution.

The collector facade owns immutable, rehashed validators for the exact V1,
V2, V3, and replacement-V1 terminal reviews. Authority validation invokes
the lowest immutable authority validator and then those four replacement-
owned validators. It does not delegate review validation through mutable
predecessor plan-builder APIs.

The runner owns a flat, immutable, rehashed 12-entry predecessor-failure
evidence set: the 11 already admitted historical witnesses plus only the
final replacement-V1 qualification terminal review. No inherited failure-
evidence helper is called at runtime. No replacement-V1 authority, command,
receipt, reservation, terminal, cache, or runtime payload is admitted.

The exact authority closures add one field only:
`predecessor_replacement_v1_qualification_terminal_review_binding`.
Qualification and scientific authorities therefore contain 19 fields. The
qualification-result closure remains the inherited exact 20 fields.

Focused tests must run in clean `python -I -B` interpreters and must:

1. validate the complete collector, qualifier, and runner compatibility
   matrices;
2. build a realistically bound temporary qualification plan, source review,
   and 19-field authority and call the exact runtime
   `validate_qualification_authority`;
3. traverse qualification execution to a deliberate reservation seam before
   any attempt root is written;
4. bomb inherited failure-evidence chains and prove the flat replacement
   evidence remains valid; and
5. prove every temporary module overlay is restored after the sentinel
   exception.

Any missing attribute, unexpected override key, import-order mutation,
unrestored identity, field-closure delta, or reservation before the sentinel
fails source review.

## Exact scientific and runtime identity

Apart from fresh V2 identities, roots, role-local Quadrants caches, the final
replacement-V1 review witness, closed-interface audit metadata, and direct
source witnesses, the replacement-V1-to-V2 canonical plan delta must be
empty. Both plans retain exactly:

- the 64-scene panel, 256 states, role allocation, targets, histories, and
  nine candidate actions;
- all data, runtime, render, texture, model, optimizer, seed, evaluation,
  bootstrap, metric, and decision-gate fields;
- the lexical ROCm virtual-environment Python interpreter;
- the unresolved `ld.lld` Unix-driver entrypoint and separately bound regular
  target, with direct target invocation forbidden;
- Genesis 0.4.6 on `gs.amdgpu`, exact ROCm/HIP/EGL expectations, and the
  selected R9700/gfx1201 identity;
- the exact 17-key role environment, including literal
  `HOME=/home/andrewknowles`, with `USER`, `LOGNAME`, and `LANG` absent;
- qualification scene order `[12, 0]`, watchdog, timing formula, and complete
  qualification evidence contract; and
- all storage, frame, simulation, selected-device memory, and 7,200-second
  wall caps.

The physical and scientific gates are unchanged. Qualification remains a
separate non-scientific two-scene attempt and may not contribute data or
evidence to science. Scientific execution remains conditional on exact
qualification PASS and a separate scientific authority.

## Fresh custody

The only prospective roots are:

- `.generated/dev/go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v3_integrity_replacement_v2_qualification/attempt_v1`
- `.generated/dev/go2_scene_diversity_recurrent_replication_genesis_rocm_backend_v3_integrity_replacement_v2/attempt_v1`

Both must remain absent until their respective separately reviewed one-shot
authority. Replacement V2 plans, source review, authorities, reservations,
results, terminals, and roots must use fresh identities. Predecessor plans and
reviewed source may serve only as immutable source premises.

## Stop rule

Any pre-reservation source-binding or interface failure consumes this
hypothesis: no replacement V3, extra alias patch, retry, resume, or additional
layered successor is eligible. The layered adapter chain must instead be
retired in favor of a flat, non-monkeypatched harness under a separately
reviewed future scope. Any plan delta outside fresh
identity/root/cache/witness/interface-audit metadata, or any environment,
backend, data, model, seed, panel, cap, evaluation, or gate delta, fails
source review. Any later infrastructure failure consumes the relevant fresh
attempt and creates no scientific decision or automatic successor authority.
