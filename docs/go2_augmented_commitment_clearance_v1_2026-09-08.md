# Recorded augmented commitment clearance diagnostic V1

Authenticate the complete augmented two-case probe, its readout and the
original full-direct overlap-retention probe. Analyze every completed selected
five-command commitment; retain absent or interrupted selections explicitly.
Use saved, already raw-replayed forecasts, with no new model inference or fit.

Native poses are evaluator-only labels. Report selected first-500-ms XY and
yaw errors and the sampled native path against a finite set of occupied-cell
witnesses that were already present in that decision's recorded nominal
checks. Compare the observed endpoint as well. Do not add later geometry.
A sampled intersection proves a conflict with the nominal radius around that
previously observed square, not an articulated physical collision. A miss
certifies neither other squares, unobserved space nor intermediate motion.
Report whether the next selection's closest cell was already a prior witness.

The two augmented trajectories coincide; they are not independent samples.
The old trajectory diverges at tick 28. Score each model only for the action
it actually executed. Do not infer the result of its unexecuted alternatives.
No threshold, model, controller, prior result or terminal status changes.

Use one process for three small recorded trajectories and the exclusive
`go2_augmented_commitment_clearance_v1_attempt_001` root. Check hardware and
reserve 256 MiB above the unchanged 40-GiB artifact reserve. Bind the recursive
source closure and all input receipts before analysis, and reverify afterward.
