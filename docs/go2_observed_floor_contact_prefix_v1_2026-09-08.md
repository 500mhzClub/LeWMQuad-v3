# Observed floor-contact policy prefix V1

The completed 45-degree native pair and exact floor-coverage diagnosis show
measured floor-only foot vetoes despite accepting no-intersection candidates
whose support is also unobserved. Define a declared contact-policy intervention:
four exact 22-mm nominal foot spheres may contact individually classified
measured floor returns irrespective of complete projected support coverage.
This is an intentional change to collision-contact semantics. It neither
observes missing support nor certifies support, free space, terrain or pose
uncertainty. Preserve all original coverage and collision evidence.

Keep the original per-return floor classifier, every retained floor/other point,
both depth streams, explicit calibration, primary observer, model inputs,
weights and correction, six action tapes, phase/view/route logic, all nominal
first/eight-step constraints, bounded waits, arrival, budgets and native stops.
Query the existing other/unknown partition for foot obstacles; all non-foot
checks remain unchanged. Never waive an unknown/non-floor return from either
sensor, including a return sharing a floor voxel.

Require the focused tests to demonstrate the declared floor-only behavior,
unknown support, retained returns, unknown returns in either stream, non-foot
floor collisions and stale-evidence rejection. On the completed native pair,
run two fresh replays per model. Require exact causal observations, map receipts,
learned forecasts and nominal checks; require every changed surface check to
equal precisely the declared contact-policy transformation of the old check.

Stop each replay at its first requested-command or terminal-policy difference,
including that observation. If neither differs, stop at the recorded tape end.
No changed command executes and no later counterfactual trajectory is inferred.
Keep both methods and unchanged model-state identities, including a negative
result if this does not alter their actions.

Exclusive output go2_observed_floor_contact_prefix_v1_attempt_001; freeze and
reverify full source and input bindings. One CPU process/thread, 8 GiB RAM and
256 MiB output allowance above the 40-GiB reserve, with a current hardware
check. No native execution, fitting, independent-maze or hardware qualification.
