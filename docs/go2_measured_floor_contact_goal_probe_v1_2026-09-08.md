# Measured-floor foot-contact native probe V1

Run four fresh cases with the fixed final full-RGB direct/JEPA models on known
layouts 052/039, order seed 2026091102. Authenticate the complete sample-bound
native cohort/readout, all-return floor-partition diagnostic and six-fit records.
No predecessor retry, resume, replacement or result mutation.

This is an explicit NEW experimental contact rule. Partition every measured
stride-four return using the existing four-quad/nine-height floor-patch test.
Retain all floor and non-floor/unknown bounds, counts and first witnesses; later
unknown returns are never exempted because of an earlier floor classification.
For the four exact 22-mm URDF foot spheres ONLY, permit measured-floor overlap
when every grid cell touched by the entire nominal projected foot disk is in
the existing measured-floor map. Retain all non-floor/unknown foot conflicts.
When projection coverage is incomplete, retain the original all-return foot
query. Every non-foot primitive keeps its original all-return collision query.
Save both original and revised checks and per-frame classification accounting.

This permits scoped nominal foot-floor contact; it does not certify support,
friction, penetration dynamics, continuous terrain, pose/model uncertainty or
hardware safety. Unobserved-space limitations from the predecessor remain.
No floor height or body-Z/yaw lift correction is applied. Retain the 0.45-m
nominal action/connector geometry, utility, snapshots, observer, raw floor/map
construction, command vocabulary and commitment, scenes/sensing, actuator,
mission goal/budget, zero drain, physical stops and evaluator. Four new tests,
four partition tests and all 35 predecessor tests must pass before launch.

Use four fresh single-thread CPU processes with the bound native scaling
evidence. Inspect resources, require 32 GiB available RAM and 8 GiB output plus
40 GiB reserve, and monitor every approximately 15 seconds. Each case collects
then independently reconstructs every sensor-to-command decision with a fresh
evaluation-only snapshot. Original actuator and native-goal audits are identical.
Retain every sibling and failure; no automatic replacement. Report goals,
contacts, distances, failures, both geometry decisions and concurrent timings.
Physics pauses during compute. No independent-maze, real-time, backtracking,
JEPA-advantage or hardware qualification follows from these development cases.
