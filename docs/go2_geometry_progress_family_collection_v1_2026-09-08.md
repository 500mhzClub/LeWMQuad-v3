# Prospective geometry-transfer family collection V1

One complete fresh96episode collection with immediate per-episode raw audit at
`go2_geometry_progress_family_v1_attempt_001`. Require the exact complete passing
height-union pilot and the completed serial/four-process scaling bench. Admit
only the worker count selected by the frozen benchmark rule, with exact source
identity shared between benchmark and collection. Every episode runs once in a
fresh spawned CPU process. No retry, resume, outcome-conditioned exclusion or
replacement of a failed episode. The eight benchmark episodes are ineligible
for this dataset; all96 collection episodes are fresh.

`lewm.geometry_progress_layout_family_development` fixes eight panel layouts in
four parameter clusters, two matched appearance seeds and all six original
actions. Clusters00/01 are training; clusters02/03 are geometry-transfer. Each
role has two parameter clusters, four mirrored layouts and48 episodes. Mirrored
siblings are dependent. No complete independent maze evaluation is claimed.
Role, cluster, layout, action labels, native poses and future observations do not
enter the learned encoder. The prospective command plan is an explicit model
input. Physical initial state, checkpoint gait and physics seed remain common.

Panel x/length/inner-edge/height in millimetres:

| Cluster | Role | x | length | inner edge | height |
| --- | --- | ---: | ---: | ---: | ---: |
| 00 | train |630|1020|-60|760|
| 01 | train |690|940|-20|680|
| 02 | geometry transfer |660|1100|-90|820|
| 03 | geometry transfer |720|1060|-40|640|

Retain 1.5s settling, three quiet100ms ticks, forty candidate ticks and the
original hold/forward/left-arc/right-arc/left-turn/right-turn bank. Evaluate local
distance reduction toward (1.2,0)m in the actual departure body frame. Success
requires a full horizon, no contact or other stop, and at least150mm reduction.
This is local progress, not passage traversal or maze arrival. Native state is
evaluation/stop evidence only and does not select the fixed excitation action.

Keep the variable-height grounded-box union renderer, floor-first raster order,
5mm camera near plane and unchanged public0.2–5m depth interval. Replay every raw
sensor packet, exact float64 request/float32-clipped actuator value, contact,
setup, stop, friction/gain, camera clock/mount and raster witness. The complete
prospective measurement gate requires every role's candidate acquisitions to
finish or reach a measured contact stop, with complete initial histories and no
hard stable-interior/near-occlusion failures. Preserve strict depth failures
separately. Report the mirrored action-success reversals and nonprogress
controls for every cluster/appearance; never remove an unfavorable layout.

Record all eight half-second future slots per episode with independent native
motion, actual future image and cumulative contact masks. A positive contact
survives a missing future endpoint. Do not substitute post-stop observations or
old benchmark episodes. The generalized adapter must retain exactly the existing
causal tensor interface while enforcing the new assignment contract.

Infrastructure/audit exceptions stop future batches after running siblings
finish and retain their receipts; missing episodes remain explicit. A completed
raw audit may record physical measurement failure without discarding the case.
Such cases prevent the complete measurement gate. Freeze all source before
collection; verify full inherited sources/inputs/native dependencies before and
after every episode and again before the final cohort receipt.

Require at least32GiB available RAM,12GiB planned storage plus40GiB reserve,
and current hardware inspection. Record actual per-worker and cohort throughput,
peak RSS and resource observations. Use one OpenCV/BLAS thread per process and no
competing heavy job. Physics pauses during processing. No real-time, depth
navigation, hardware, learned-selection, JEPA-benefit or maze-success claim.
Training is a later separately frozen matched experiment; no optimizer runs here.
