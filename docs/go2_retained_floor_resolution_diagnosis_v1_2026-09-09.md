# Later observed floor coverage of retained ambiguous returns

Read-only provisional diagnosis of the completed eighth collection while its
native audit runs. The controller stopped1057, after which10zero commands
drained. Bind its collection result, complete collection artifacts, native
launch and source bindings. Do not claim that the final native audit passed.

Extract every first-cell enclosure from the terminal action checks' primary and
auxiliary possible-intersection lists. Require one intersecting voxel per hit
so no other unknown hit is silently omitted. These hits concern nominal feet;
this diagnostic does not alter their classification or contact policy.
Transform all eight enclosure corners into the saved initial gravity-aligned
map. Report the full height interval relative to the unchanged floor hypothesis
and all5cm floor squares touched by its XY enclosure. This is descriptive
floating-point geometry, not an outward-rounded calibrated uncertainty bound.

For each such enclosure, inspect the executed collection's paired observations
strictly later than its last contributing sample and no later than1057. Use
each observation's original saved registered pose, fixed map transform and
floor height. Apply the unchanged floor_coverage calculation to all queried
squares. Record the first later single-camera observation that covers the entire
set, and require the enclosure heights to lie within the existing10mm band.
Stop when every enclosure has a witness or the terminal observation is reached.
No floor-plane-only classification, missing-ray interpolation or unexecuted
outcome is allowed. A positive result establishes only that this conservative
coverage query passes under the existing pose/floor hypotheses. It does not
prove ground support, bound pose/sensor error, clear strict visibility failures,
authorize a new command, or establish an adopted reclassification policy.

Exclusive root go2_retained_floor_resolution_diagnosis_v1_attempt_001. One CPU
process/thread, minimum4GiB RAM,128MiB output allowance above40GiB reserve.
It may run beside the native audit and immutable cache replay; inspect current
resources and record competition. No model loading/training or native scene.
Preserve failures, verify all bindings before/after, and freeze source at launch.
