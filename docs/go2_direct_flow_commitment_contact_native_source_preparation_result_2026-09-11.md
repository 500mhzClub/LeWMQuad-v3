# Contact-plus-flow native collector and auditor prepared

The new collector and raw auditor use DirectFlowCommitmentContactController.
Their entire ASTs match the frozen original contact-scoring modules after only
the declared controller import/class, status strings and direct-flow flag are
normalized. The original budget, packet interfaces, drain, renderer evidence,
raw decision/model replay and physical/visibility criteria remain unchanged.

Nine source-equivalence and mutation-rejection tests passed in 2.56 seconds,
session 98264, exit 0. Source verification session 89800 exited 0 with 2053
bound paths, extending the currently running controller replay's fixed source
closure. It did not construct a model, admit a new native run or create a new
native output.

Preparation record:
`docs/go2_direct_flow_commitment_contact_native_source_preparation_2026-09-11.json`,
SHA-256 `e24b460d11792b8058ca2473b1767b596d9f287f583c913344fe7288d01220d4`.
These source files and the protocol are now frozen for this preparation.

The full controller replay remains PID 2822758, creation time 1789119437.63,
session 35432, launch
`6c577445f7bff2e58aad960c6d26908c683a20b0d7a4fbb2344967b5072da9ad`.
Latest observed progress was frame 150 of the 562-observation prefix. No full
controller recovery outcome is yet available. The contact native parent still
had no final result at the check; its original queue remains unchanged.

Next prepare the native prefix comparator and completed-input admission,
then the launcher. Require the actual full controller result, exact original
model and all original native queue completions. At observation 561, a
recovered hold is an explicit unchanged-command outcome, not a fictional
command intervention. Define any native prefix around the recovered observation
and use only fresh measured physics afterward. No pending or frozen run may
be changed to adopt this candidate.
