# Fresh recent-qualified-reference maze1 development pilot

This single-attempt experiment executes RecentQualifiedAnchorController from
observation zero in the original development maze1. It follows the supervised
layouts1,2,3 cohort, direct-flow maze3, and anchored-continuation maze2 in the
single-native-scene queue. Check live ownership before launch; queue metadata
is a schedule, not an interprocess lock. No existing attempt is restarted.

Admit the exact completed result from go2_recent_qualified_anchor_prefix_v1_attempt_001
at the launcher's frozen PREFIX_SHA:
6e25c6c561473b60966a1a47388a01b48ab3e547da0c8ab13aa1e267b9fad302.
The original native height result must be
32edbb748e04e18816e0b0fb265f465706ac8c8684849d73a091321f79a3b07f.
Authenticate all source/artifact/environment bindings and reconstruct every saved
prospective comparison against that original episode before native execution.

The fixed replay must have646 observations,635 exact original decisions before
reference intervention,642 raw forecast-bank comparisons, two extra-reference
attempts and one qualified extra reference. The first attempt, qualified use and
decision difference are635. The first changed command is645, original terminal
zero to candidate left turn[0,0,.45], with no candidate terminal/failure. The
original terminal is SENSOR_OR_MODEL_FAILURE. No following recorded observation
may have been consumed. Different counts or outcomes require a separate reviewed
experiment definition; this attempt cannot silently accept them.

The new observer retains one immediately preceding already anchor-qualified
visual view and tries it only after the original reference bank fails without
conflict. Its frozen replay protocol documents the causal eligibility and the
single dispatch change under the original joint witness wrapper. Preserve all
rigid registration/support/envelope gates, original conflict vetoes, camera
priority, ten-frame bridge budget, height registration, mapping, residual,
predictive planner and mission criteria. Never promote a bridge or floor-derived
pose into the extra reference. Relative-pose chains remain uncalibrated.

Use the original assigned full/JEPA model seed_2026091001_full_jepa with state
4f796afdf32c2c6dbcd1d5981834a7b17be86e2efdafd9427b2d80240def0ca6.
Load fresh identical assigned models separately for physical collection and full
raw replay. Use the original URDF, scene, cameras, renderer, numerical settings,
observed exploration and shared3000-tick outbound/return budget. The separate
collector and auditor must preserve original height-run physical/evaluation
calculations except for the explicit controller, result labels and flag.

After collection and the complete raw audit, compare the first33000 physics
samples,646 paired public observations,645 preceding requests and642 emitted
paired forecast banks. Every new native decision must equal the prospective
replay decision through645. Original visual/downstream decisions may differ from
635 onward while commands and physical inputs still match; do not claim their
state is unchanged. Compare every causal reference receipt again. Do not compare
physical outcomes following changed command645 as shared predecessor evidence.
Record partial execution of the changed command as a physical outcome.

Preserve raw files, command tape, timing, audit, prefix comparison, worker logs
and terminal evidence, including scientific negatives. Integrity/raw-audit/prefix
failure terminates this attempt without automatic retry/resume/replacement.
Report actual observed and native arrivals, return traversal, contacts, terminal
quiet and pose error through the original evaluator. Reused development layout
execution is not independent-layout reliability or evidence of hardware readiness.

Use the admitted scoped digest helper with fresh per-call hashes, retaining all
original replay/native verification conditions and fixed identities. No retained
cache, imported-global mutation, whole-tree source export or sealed access.
Keep hard-linked training artifacts immutable.

Before substantial work assess CPU topology/affinity/load, RAM, GPU/VRAM, disk
and competing jobs. Native admission uses32GiB RAM and the unchanged reserve,
collection and persistence storage allowances. One8GiB read-only preflight may
overlap one native worker with40GiB total available RAM. Admission is not an
OS-enforced memory limit. Run one fresh spawned native worker, one OpenCV/BLAS
thread, deterministic Torch, one task per process and15-second monitoring.
Physics pauses during compute; no real-time or physical clearance claim.

Run scripts/run_go2_recent_qualified_anchor_maze01_pilot_v1.py with
--prefix-result-sha256 followed by the frozen completed replay SHA; add
--preflight-only for admission without output or scene creation. Output is
go2_recent_qualified_anchor_maze01_pilot_v1_attempt_001 in the guarded external
navigation development artifact root. The overall navigation goal stays active
until the full independent-navigation requirements are actually demonstrated.
