# Actual maze-camera recorder integration after the current native episode

This is a source-prepared, separately named sensor integration probe. It must
not execute alongside the current native scene. Require the actual completed
go2_dual_camera_settled_maze_pilot_v1_attempt_001 result hash, full raw audit
and prospective physical/public/controller prefix agreement before preflight.
Preserve whatever navigation/visibility outcome that completed episode records.
Also bind the completed actual-Camera helper integration result
c1636416aa33295641358bec601a6bb6202e664410104f6afb9dbcd35103a8f5.

RendererWitnessDualCameraMazeSession inherits the original primary/auxiliary
captures and returns the original public packet tuple. It records actual
camera-context identity, framebuffer sampling and precision readbacks after
the primary RGB/depth capture and after the paired capture finishes and restores
the primary pose. The auxiliary path ends with evaluator-only segmentation;
the inspected installed renderer uses the single-sample target and disables
multisampling for that path, like depth-only rendering. This is source evidence
for the query point, not yet proof that the new wrapper executed successfully.

The endpoint record after the paired capture does not claim that framebuffer
contents depict the restored primary pose. It records context properties at
that point. No per-draw shader/arithmetic reconstruction or rasterization error
bound is established. No historical maze context is inferred from a new query.
The policy receives none of these evaluator-only fields. The raw image hashes,
sample/clock and camera pose bind the witnesses to their actual acquisitions.

Queries add no render or physics call. Require unchanged pose, physical sample
count and simulation clock across each query, identical context/readback
properties across the paired endpoints and episode, exact primary-pixel
identity, and complete witnesses for every captured frame. Latch failures;
preserve partial witnesses even when another persistence operation fails.
Audit the saved camera poses using the existing OpenGL-to-optical convention
against the separately audited primary capture poses. Retain all old visibility
scores and the unresolved uncertainty limitation.

Execute exactly the first three original controller warmup observations with
its unchanged assigned learned model and unoptimized map. Require each actual
requested command to remain[0,0,0]. Keep the original750-step settling phase and
all three50-step command intervals:900 physical samples total. The collector
ends with the explicit DECLARED_THREE_OBSERVATION_PROBE_BOUNDARY acquisition
cutoff, no artificial mission terminal or terminal drain. It is not a new
navigation episode, a backtracking experiment, or independent-layout evidence.

Run the unchanged full dual-camera raw sensor/model/command audit on this
bounded collection, then compare all900 physical samples, all three complete
decisions, all public packets, full primary native depth and auxiliary native
depth/diagnostic segmentation with the completed current native startup prefix.
Read only that prefix; do not infer later outcomes from it. Require all six
renderer endpoint records to match their separately audited raw acquisitions.

Use scripts/probe_go2_maze_renderer_witness_v1.py with the actual completed
--native-result-sha256 and --preflight-only first. Exclusive output root
go2_maze_renderer_witness_probe_v1_attempt_001. One CPU scene/process and one
numerical/OpenCV thread, no parallel native scene. Inspect and record CPU
topology/affinity/utilization, GPU/VRAM, available RAM, competition and both
storage volumes. Require32GiB RAM and256MiB probe output plus1GiB persistence
headroom above40GiB reserve; these are admission checks, not OS limits. Verify
sources, native/model inputs, actual installed camera/graphics runtime bindings
and all output identities before completion. Preserve every failure; no retry,
resume or modification of a frozen attempt.

Even a passing integration only establishes the new recorder's actual startup
API and noninterference on this bounded population. A later full acquisition
would need its own recorded context history and independent raw audit. The
known visibility failure, realistic uncertainty/timing and bounded hardware
requirements remain outstanding; no qualification gate is relaxed here.
