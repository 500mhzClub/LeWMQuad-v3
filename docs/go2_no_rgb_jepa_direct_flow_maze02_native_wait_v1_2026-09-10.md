# Ordered handoff for the tracking-recovery native test

Append the prepared single no-RGB JEPA tracking-recovery maze-02 test after the
existing six-case batch → frontier → hold → contact native sequence. Start no
new scene while waiting. Bind the exact original five process identities,
including the full-controller replay, to the existing machine boot. A reused
PID or changed command is an error, not evidence of completion.

The reviewed intermediate controller boundary selected right turn at frame 859,
after 859 unchanged earlier decisions. Its launch, closed decision stream and
report hashes are fixed in the waiter. Require the same original replay owner
to finish with the successful terminal result and those exact artifacts. A
positive intermediate report alone does not release the native test.

Poll every 30 seconds for at most 48 hours. While waiting, check only completed
result/launch identities, source bindings and the reviewed prefix report. Lock
each first-seen completion identity and reject changes. Full episode/model and
queue artifact admission remains the native launcher's job once every original
owner has ended. This avoids repeatedly hashing large recorded datasets while
another prerequisite is still running. Missing completion, terminal failure,
identity changes and timeout stop the handoff without restart or replacement.

After all five completions, refresh source, owner, native-idle and one-case
resource checks. Own exactly one child invocation of the prepared launcher with
the five exact result hashes. The child still performs its full admission and
single fresh scene/audit. Keep its stdout, exit status and failure artifacts.
Authenticate its complete result, original prerequisite links, source identity,
collection/worker artifact roster, raw audit, physical-prefix receipt and
recomputed contact/timing readout. Scientific failure remains a completed audited
outcome; incomplete execution or evidence is a terminal handoff failure.

The waiter introduces no controller, model, sensing, geometry or policy change.
No new independent-layout population or final policy review is authorized by
queue completion. Before scheduling that population or another native job,
account for this appended tracking-recovery owner and review its outcome.
The broader navigation goal, realistic timing and hardware evidence remain
unproven. No automatic retry or scientific resume is performed.
