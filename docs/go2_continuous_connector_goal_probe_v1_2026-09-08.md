# Continuous connector native probe V1

Run four fresh known-layout development cases: the same final full-RGB direct
and JEPA snapshots on family layouts 052 and 039, order seed 2026091102. Bind
the completed commitment-pose native cohort, its readout, and the observed
geometry refinement result before and after. No retry, resume or replacement
of those attempts. Admit the same six-fit ledger and fixed final snapshots.

Change only entry-connector geometry: exact closed segment-to-observed-square
distance with the original 0.45-m radius and 1e-12-m tangency allowance. Retain
the original conservatively inflated observed-floor candidate cells, ordering,
BFS, frontier objective and explicit unknown connector cells. No map mutation
from proposals. Retain the measured visual observer, all persistent articulated
surface vetoes, commitment-pose waypoint utility, scan utility, all six forecast
plans, five-tick commitment, native scenes/sensors/actuator, 1.2-m goal, 240-tick
budget and zero drain. The 1.784-mm nominal diagnostic connector margin is not
an uncertainty allowance or a safety claim. Floor classifications do not alter
this trial's surface filter.

Run five new integration/route tests plus nine frozen geometry tests and the
eight frozen commitment-utility tests before launch. Each of four fresh single-
threaded CPU processes collects raw data then independently replays every
sensor-to-command decision using a fresh assigned evaluation-only model. Keep
native goal and actuator audit functions identical. Retain all four siblings
even if one fails. All infrastructure failures are terminal for this attempt.

Inspect hardware, competing work and storage; require 32 GiB available RAM,
8 GiB planned output and the existing 40 GiB reserve. Authenticate the existing
four-worker native throughput benchmark (3.34x, exact fixed-tape arrays/pixels)
and monitor resources approximately every 15 seconds. Current concurrent loop
timing is not uncontended or real-time timing; simulation pauses during compute.

Report verified native goals, contacts, terminal/minimum distance, visual and
measurement failures, phases/actions and timings. These two known layouts do
not establish novel-maze performance, JEPA advantage, backtracking, calibrated
safety, realistic timing or hardware readiness. The full goal remains active.
