# Shared JEPA V5 development raw-supervision Builder V8 author handoff

Date: 2026-07-13

Implementation author: `/root/raw_v7_successor_author/auditor_v7_author`

Status: **FROZEN AUTHOR CANDIDATE; INDEPENDENT REVIEW REQUIRED; NO EXACT OR DATA AUTHORITY**

## Contract

This candidate implements only the Builder V8 role established by these
frozen pre-implementation amendments:

| Artifact | SHA-256 |
| --- | --- |
| `docs/lewm_go2_shared_jepa_v5_raw_supervision_builder_auditor_v8_terminal_quiet_successor_amendment_2026-07-13.md` | `054de82d8648cd6be7edff01b82d549ec916700ebffad51698d4c2041edc6c88` |
| `docs/lewm_go2_shared_jepa_v5_raw_supervision_v8_existing_agent_identity_rebinding_amendment_2026-07-13.md` | `392745c80ca2c6e7a103cca4a55c3614cd2c988de9a379fba950b0087df41698` |

The rebinding fixes the Builder V8 implementation author to the identity shown
above. It changes no science, transaction, source path, schema, API, test, or
review-separation requirement.

This handoff is not an authorization. No canonical authorization, `.generated`
payload, source data, RGB, checkpoint, dataset, audit output, G2, held-out,
runtime, hardware, production path, or accelerator was opened or changed while
authoring or testing this candidate.

## Frozen candidate

| Role | Artifact | SHA-256 |
| --- | --- | --- |
| `builder_source` | `lewm/datasets/go2_shared_jepa_v5_raw_supervision_builder_v8.py` | `f45533354c8b45b88f8eadb2126ec5eaf96fe1f57c21a9bfcd95a8855bfaaa35` |
| `builder_cli` | `scripts/build_go2_shared_jepa_v5_development_raw_supervision_v8.py` | `f6471f1fa0ca7a13976f752a41ee9ddacbc76636e4d5fb0eee1ebf75bdaee72d` |
| `builder_test` | `lewm/tests/test_go2_shared_jepa_v5_raw_supervision_builder_v8.py` | `fc1f0cf3fc18bdbd1393be6a514bc04459f943f39b438ced78ebee30e7c57d9a` |

The fourth Builder V8 candidate role is this handoff. Its file SHA-256 is
computed externally after freeze and must be included verbatim in the
different-agent review and any later nine-row authorization source map.

No non-role V8 test helper was added. All production-ineligible synthetic
authority support is contained in the reviewed Builder V8 test role.

## Mechanical successor

Builder V8 is a standalone additive promotion of the independently passing
Builder V7 source. It does not import or call Builder V7, Auditor V8, a legacy
builder/auditor, a test module, or a legacy exact entry.

The V8 author test normalizes only `V7`/`v7` identifiers and compares the AST
of every top-level class and function in Builder V7 and Builder V8. All 80
definition names are present. Every definition is identical except
`_review_binding`, whose V8-only change enforces the amendment's stricter
reviewer identity boundary. In particular, the complete construction science,
source validation, closed publication transaction, two terminal drains, final
retained-ancestry validation, canonical destination identity check, cleanup,
and worker bodies are unchanged.

The V8 authority-only changes are:

- the fixed nine V8 role paths, V8 schemas, capsule types, provenance labels,
  exact entry name, and CLI import;
- Builder author `/root/raw_v7_successor_author/auditor_v7_author` and Auditor
  author `/root/camera_v5_independent/camera_v7_pre_freeze_review/v7_review_artifact_schema`;
- reviewers must start with `/root/`, differ from `/root`, both implementation
  authors, and each other;
- both review authorities require `retry_authorized=false` alongside all
  retained false authority fields; and
- a 69-row predecessor map consisting of Builder V7's 55 rows, both V8
  amendments, and the 12 additional V7 PASS/BLOCK evidence rows frozen by the
  terminal-quiet amendment.

The canonical sorted compact JSON SHA-256 of `FROZEN_PARENT_HASHES` is
`79fe832122ed335188357a59bad8a031cc235449ef17e6e19ac78de9d5aff669`.
All 69 predecessor files and all nine reviewed V4 science sources reproduce
their declared hashes.

## Preserved behavior

The fixed exact entry remains keyword-only:

```text
execute_exact_build_v8(*, authorization_sha256: str, workers: int)
```

Workers remain exact non-boolean integers in `[1,6]`, use `spawn`, validate
the fixed V8 authority before task receipt and again before protected source
use, force one native math thread, and hide CUDA, HIP, ROCr, and GPU ordinal
visibility. The canonical output, dataset schemas, population, camera/raycast
semantics, array layouts, direct joins, 354-record source access ledger,
one/six-worker determinism, private staging, retained descriptors, no-replace
rename, fsync, event characterization, inode-owned cleanup, and downstream
license denials are unchanged.

## Author verification

All commands used one native CPU thread and empty accelerator visibility.
Filesystem transaction tests were serialized.

```text
pytest -q lewm/tests/test_go2_shared_jepa_v5_raw_supervision_builder_v8.py
69 passed in 0.75s

pytest -q lewm/tests/test_go2_shared_jepa_v5_raw_supervision_builder_v7.py
65 passed in 0.68s
```

The V8 source, CLI, and test pass `py_compile` and `git diff --check`. Static
inspection finds no Builder V7 import or exact entry, auditor/test import,
dynamic import/eval/exec, alternate authority registry, visible accelerator
assignment, mutable root/path injection, or unrestricted production opener.

## Independent review request

A reviewer distinct from `/root`, both V8 implementation authors, and the
other V8 reviewer must independently bind the exact frozen source, CLI, test,
and this handoff. Review must rerun the 69 V8 tests and all 65 retained Builder
V7 tests, rehash the full predecessor/science closure, reproduce the normalized
AST comparison, inspect the V8 reviewer/retry authority constraints, and verify
that all construction and transaction definitions are unchanged.

The only eligible machine review path and schema are:

```text
docs/lewm_go2_shared_jepa_v5_development_raw_supervision_builder_v8_independent_review_2026-07-13.json
lewm_go2_shared_jepa_v5_raw_supervision_builder_v8_independent_review_v1
```

A `PASS` may grant only `builder_source_approved=true`. Every exact-build,
exact-audit, dataset-use, training, selection, calibration, G2, held-out,
navigation, runtime, hardware, production, promotion, and retry field remains
false. Changed bytes or any finding require another additive successor.
