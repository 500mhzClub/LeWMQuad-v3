# Tracking prerequisite updated to the parallel learning study

The pending tracking launcher now requires the distinct parallel-execution
study, preserving the original scientific 36-fit factorial and all-twelve-layout
collection gate. No model experiment, native challenge, memory/OOM probe or
robot action was launched by this amendment. The original collector continues.

## What changed

- Learning definition is
  `8d8c3456054a284aa83031ea417d8c433beddbcc04a8b47d3164f120bc0ae5d8`,
  with output `go2_independent_pulse_parallel_study_v1_attempt_001`.
- `completed_learning` uses the distinct reader's terminal authentication:
  complete 36 fits/43,200 updates, all 416 preterminal artifacts, source/config,
  original sequence, fit/snapshot bindings and per-update schedule accounting.
  This gate does not aggregate scientific scores, deserialize checkpoints,
  materialize images/native arrays, or rerun training.
- Completed-learning evidence names the execution revision, definition and
  output root. A sequential-result label is not interchangeable with this
  parallel result. A known original collector failure rejects before study
  artifact access.
- The process-exclusion list includes **both** learning launchers and the
  original collector/stage/audit jobs. Actual owned `/proc` command metadata,
  not lock-file presence, remains the live-job check.
- The source-bound native resource review was amended for the changed launcher
  and its new test evidence. Its prior SHA and prior launcher SHA remain in
  amendment metadata; the earlier review-result document is historical evidence,
  not a claim that its old hash is the current review file.

The eight native specifications, physical seeds, ideal sensors, fixed command
tapes, raw scoring, stress interventions, predecessor comparisons, artifact
budgets and process-memory containment implementation did **not** change.
The final tracking configuration remains a candidate, not an execution receipt
or scientific qualification.

## Verification

83 focused tests passed in 25.37 seconds across launcher and outside-supervision
tests. One integration test passes a complete generated synthetic parallel-study
artifact set through the actual reader and then the tracking gate, and verifies
that a retained worker failure blocks it. Other tests check original collection
failure, sequential-result rejection, incomplete/promoted results and process
exclusions. Synthetic fixtures use reduced training budgets only inside tests;
production remains 36 * 1,200 updates.

JUnit:
`.generated/navigation-development-staging.m6MDz1/independent_tracking_parallel_gate_initial_v1.xml`,
SHA `04f74748ff87b8309d384ff6823466be3906720f292c414644696e0b45406fcd`.
Independent XML inspection found 83 tests, zero errors/failures/skips.

Actual read-only source/resource checks also completed successfully:

- Before editing, all four edit targets were outside the frozen 790-source
  learning definition and both 792-source tiny-probe definitions.
- The amended resource review authenticated its exact ten implementation
  bindings, seven native bindings, four evidence-document hashes and both
  complete actual tiny-probe receipts, including their unchanged source lists.
  The nine other reviewed implementation hashes did not change. No probe rerun.
- The parallel definition remained exactly
  `8d8c3456054a284aa83031ea417d8c433beddbcc04a8b47d3164f120bc0ae5d8`.
- Actual process inspection identified the original supervisor PID 2063013 and
  l09 stage PID 2128968; both tracking output roots remained absent.

Current changed bindings:

| Artifact | SHA-256 |
|---|---|
| Tracking launcher | `b0a3af7b871a26885082147633a65b076cecb5e76a29b1b8fd6e0badc9bdbf06` |
| Launcher tests | `bf1e3422e5b1ff347170293871bf57ec2874f5c032640809b2cdbf3450931599` |
| Tracking protocol | `5de548f483b819e51c6b95c0001208073da2f570f356099c8b5e02db45825a02` |
| Amended native resource review | `b7ddde6755291b17619b28ea8840bd2307db3d81476de193ae1dc8b78f545553` |

Read-only tracking candidate: **825 source paths**, definition
`223056ac7ddcb47b9d1a4b1b188028761fb15f56443f02fda28ecfa668326744`.
It inherits the parallel study and binds the additional reader/tracking sources.
This supersedes the earlier 818-source candidate, not a launched experiment.

## Next actions

Keep collection and the frozen parallel learning study first. Launch learning
once its complete-cohort and fresh hardware/storage gates pass; do not wait for
unrelated tracking preparation or rerun the sequential comparison. Use the
parallel scientific reader after the complete study and report all contrasts,
including negative JEPA findings.

For tracking, finish the complete-result independent verifier and final
source/config review before adoption. Existing numerical coverage/pose and
continuity checkers are implemented; the remaining integration must authenticate
the complete sensor phase, actual stress exposure, raw audit and predecessor
comparisons. Preserve the missing incremental-rotation witness and common-gyro
dependence limitations rather than claiming independent heading evidence.

Recheck storage before the later tracking launch: its 52 GiB allowance plus
40 GiB reserve is stricter than learning's 8 GiB plus 40 GiB. The optional old
environment cleanup still needs explicit approval. Nothing has been deleted.
