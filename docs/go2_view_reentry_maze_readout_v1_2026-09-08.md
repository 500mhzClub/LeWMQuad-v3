# Translating view-recovery completed-native readout V1

Only a completed, hash-bound `VIEW_REENTRY_MAZE_PILOT_COMPLETE` result with
successful native/public prefix comparison is admitted. The readout verifies
all native artifacts and frozen launch sources before and after reading, then
preserves the audited mission outcome. Neither a small native goal distance nor
an improving prediction can relabel a failed mission as a verified arrival.

The predecessor readout remains unchanged. The new readout retains its actual
selected waypoint and nominal-reentry forecast/execution measurements, observed
nominal-gate restoration events, complete and censored command intervals, native
traversals, quiet arrival windows, stops, timing and sensor/visibility gates.
It additionally lists the actual translating view-recovery intervals using the
explicit selected-candidate phase-exception receipts. Every listed interval is
validated against its executed command tape by the existing recovery readout;
an incomplete interval retains a null endpoint/error and is never extrapolated.
The original phase allowance must exclude each such translation, while the
explicit recovery allowance must admit it. Native pose is evaluator-only.

Output is exclusively `go2_view_reentry_maze_readout_v1_attempt_001`, requiring
at least 8 GiB available RAM and 128 MiB artifact headroom above the unchanged
40 GiB reserve. No physics, training, model selection or cache deletion occurs.
All failures and input identities remain preserved. This reused development
maze provides no new independent layout evidence; this readout establishes no
matched baseline advantage, memory advantage, real-time or hardware claim.
# Final auxiliary contact registration diagnosis

Before this readout was launched, completed collection exposed a final auxiliary
foot conflict despite clear nominal paths. Add a descriptive public-only
diagnosis bound to the same completed native result. Reconstruct the latest
contributing frames of the first conflicting voxels, including exact whole-frame
floor/other classification counts, original four-quad ground checks and original
nine-pixel 10 mm height checks. Preserve the limited witness scope explicitly.

Fit descriptive planes independently to the initial primary and auxiliary
measured ground candidates within the original initial height band, and to the
cited contemporary primary and auxiliary ground candidates without height-band
filtering. Report two-dimensional support, residuals and the cited samples'
distance to those planes. Plane fits do not relabel samples, permit contacts,
change commands, certify physical floor or infer alternative execution. The
primary contemporary fits use no auxiliary fit samples. Verify sources and
complete native inputs again after diagnosis. Focused tests cover known tilted
planes, nonplanar residuals, degenerate support and nonfinite inputs.
