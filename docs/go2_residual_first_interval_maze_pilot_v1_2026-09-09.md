# First-interval residual feasibility: fresh maze2 pilot V1

Run one fresh development maze2 simulation with the separately implemented
ResidualFirstIntervalController. Use the same assigned full-JEPA rollout model,
training-only correction, camera/body/control input contracts, public coordinate
mission, gait, physical scene, geometry, map, settling and evaluation rules as
the completed original independent maze2. The only policy intervention is the
causal first100ms feasibility fallback described in the frozen component and
prefix documents. This reuses an inspected development layout; it is not an
independent-layout generalization test or sealed benchmark evaluation.

Required completed actual-observation prefix SHA-256:
`45b9b10e0dc89d4ba477499f817a08df02cc79392c3bf3df9b0363394f7b714b`.
Original learned cohort SHA-256:
`a0ac72330a6c34fbae7812a360b22189086f16bd83f586d71e769539ddc2e720`.
Original case `full_jepa_novel_maze_02`; new case
`full_jepa_residual_first_interval_maze_02`.
Model `seed_2026091001_full_jepa`, corrected state
`4f796afdf32c2c6dbcd1d5981834a7b17be86e2efdafd9427b2d80240def0ca6`.
Load an independently authenticated fresh instance for collection and another
identical fresh instance for complete raw controller replay. No training,
checkpoint selection or model change is permitted within this attempt.

Before execution, authenticate all464saved prefix decisions and the exact
completed report. The first intervention is463: original zero request becomes
right_arc `[0.16,0,-0.45]`. All461available raw forecast banks, original
selections, observed/mission/residual state and prior requests match. The native
run must reproduce the original23900physics samples and464paired public
observation endpoints through463 exactly, plus every complete candidate decision
from the prospective replay. Record whether the changed command completed.
The prefix comparison must not compare later physical outcomes with the old
episode. Those belong to the new execution and are evaluated independently.

Keep the original shared3000navigation-tick outbound/return budget,3warmup
ticks,100ms command commitment,800ms forecast horizon,50physics steps per
command and10terminal-zero drain ticks. Follow actual sensor-driven replanning;
do not replay a precomputed command tape. Original raw forecast targets and
surface vetoes remain preserved. The correction changes only first-stepXY for
separate surface and all-eight-segment nominal checks. It gives no calibrated
model error, physical clearance or terminal viability bound.

Retain the full raw sensor, model/command, command-dispatch, renderer and strict
physical visibility audits. Native goal/quiet arrival and physical return/retrace
requirements remain unchanged. A negative navigation or visibility result is a
negative result; audit/infrastructure failure terminates with saved evidence.
Never delete, overwrite or automatically retry the attempt. Raw collection
bindings must be retained if a later audit, prefix or verification stage fails.

Exclusive output: `go2_residual_first_interval_maze_pilot_v1_attempt_001` under
the existing owned navigation-development artifact root. Bind the full
inherited source/input identities and explicitly discovered new source/protocol/
test paths. No source export or whole-tree copy is used. Before creation, require
32GiBavailableRAM and40GiB artifact reserve plus10GiB collection allowance and
1GiB persistence headroom. Recheck after lengthy validation. Resource capacities
are admissions, not OS-enforced quotas. One native scene/process, fresh worker,
one OpenCV/BLAS thread; physics pauses during computation. The native launch
must wait for the active fixed reactive cohort and queued planning-memory pilot.
A CPU-only `--preflight-only` check may run alongside those jobs and must not
create output or start native execution.

Report collection termination, physical/acquisition stops, actual goal and
return outcomes, all failed audits, full-prefix equality, unchanged model state
and measured timing. Completion of this pilot does not by itself establish
reliability across layouts, isolated JEPA/planning/memory advantage, real-time
operation or hardware deployment validity.
