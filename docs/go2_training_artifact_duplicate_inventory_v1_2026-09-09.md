# Byte-verified training-artifact duplicate inventory V1

The user asked whether deterministically recreatable training data can be
removed to clear the supervised comparison's disk-space blocker. Current
training admission still requires original raw collection and fitting artifacts.
This read-only investigation therefore proposes sharing identical stored bytes
while preserving every original artifact path and content, instead of relying
on an untested regeneration claim or removing a required input.

Inspect only the artifact bindings in these exact completed results:

- go2_geometry_progress_family_v1_attempt_001:
  376b2eeacfd5e741ba6b7a0e1b5e04399f782d16943d66ffdff92eada93ef0fb
- go2_moving_action_switch_family_v1_attempt_001:
  e89ea05cb4590164bcdc48e45136f022cdc941a7b63ea13bedcd4cfc1e6dffa1
- go2_observation_horizon_fits_v1_attempt_001:
  45b4680b85c87bd69dcaed6a0058f091105632909661dba319d6c05f5b533418

No recursive discovery, protected-path access, predecessor rerun, model loading,
simulation or input mutation. Authenticate result and source identities. Group
by recorded hash, byte count, device, mode and ownership; require single-linked
ordinary files. Freshly SHA-256 every proposed member against its completed
binding. Verify device/inode/mode/ownership/size/link-count/timestamp/allocation
metadata before and after each hash, then recheck the whole candidate population.
Recheck result and source identities before publishing the exclusive proposal.
This guards ordinary concurrent changes but does not claim an atomic snapshot.

Metadata-only preliminary inspection98458 found potential7,219,273,728 allocated
bytes across5,523 groups in53,856 bindings. That preliminary number is not yet a
byte-verified cleanup plan. The original native/control/raw-audit files remain
immutable and all failure evidence must remain available.

Output: go2_training_artifact_duplicate_inventory_v1_attempt_001 under the
established artifact volume. One CPU process,4 GiB admission allowance plus16 GiB
for the live independent replay,128 MiB output and40 GiB artifact reserve.
Check current CPU/affinity/RAM/GPU/VRAM/storage/processes before running.
Use the existing Genesis interpreter and one numerical-library thread.
The script writes only fresh inventory artifacts. No deletion or hard-link
replacement is authorized or performed by it. Any later operation requires an
exact reviewed scope, fresh content/metadata and active-consumer checks, an
exclusive transaction journal, and explicit user approval. Shared files would
remain immutable; no active verifier or dataset reader may overlap conversion.
