# Factored configuration and trajectory interface check

This is a new development consumer and a wiring check, not a rescore of the
original configuration consumer or physical trial. Keep their bound bytes and
results unchanged. Bind the original three trial identities, inherited source,
input, artifact and native files; also require the exact preceding attribution
result and its 14 source identities. Add only the six explicit new source,
test, probe and protocol paths, giving 20 development paths checked before/after.

Replay all 15 startup/tail frames into the same continuous owner; reproduce all
12 startup decisions and 15 relative observations. Compare the factored consumer
at the already diagnosed 0.75/1.00-m tangent configurations against its independent
reference backend. Report original counts alongside new component facts; never
interpret component non-floor clearance as ground support or an approved action.
Keep missing coverage, penetration and incompatible covered plane observations.

At the final 2.9-s anchor exercise three fixed four-tick nominal command plans:
all zero, two forward 0.08-m/s targets then two zero, and two yaw 0.35-rad/s
targets then two zero. Predict the ideal slew-limited SE(2) response in the
sensor-derived gravity tangent plus measured joint-velocity persistence. These
are non-learned, unvalidated baselines; zero target does not prove physical rest.

For the trajectory API check supply endpoint point-error assumptions
[0, 0.05, 0.05, 0.05, 0.05] m and per-interval physical-point speed assumptions
[2, 2, 2, 2] m/s. These values are deliberately EXPLICIT HYPOTHESES, neither fitted
nor validated by this recording, and may not become controller acceptance limits.
Check each nearest-endpoint-expanded configuration through 3.3 s with the
unchanged setup expiry 3.5 s, and require full compiled/reference equality.
All trajectory nodes, not just the final node, are evaluated. The recording has
no actual future samples after this anchor; report that limitation explicitly.

No simulator, command, training, revised old prior, calibrated motion bound,
validated stopping model, learned policy or navigation permission is produced.
The next empirical step must be fresh bounded motion/error/contact/braking
validation and then a complete mission, not tuning these hypothetical errors
until this interface check passes.
