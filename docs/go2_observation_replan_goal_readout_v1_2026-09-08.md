# Observation-cadence replanning native readout V1

Authenticate the complete two-case observation-replanning probe and every
source and artifact binding. Report both fixed augmented first-seed models,
native goal gates, terminal failures, observed-pose errors, retained observer
evidence, selected actions and measured complete-iteration timing.

Compare each new case with its same-model five-command predecessor, and
compare the two new cases with each other. Use the established comparison
boundary: through the observation before the first different command or
terminal, never including its affected future. Predictions may differ when
the receding selector runs on observations where its predecessor retained a
commitment. A 500-ms candidate forecast is not a label for the one command
actually consumed. Do not score interrupted plans as executed endpoints.

Use the exclusive `go2_observation_replan_goal_readout_v1_attempt_001` root.
There is no new execution or fitting. Keep both outcomes regardless of
success, with zero independent novel mazes and no uncontended or real-time
timing claim. All prior results and qualification limits remain unchanged.
