# Separate readout of the completed bootstrap goal probe

Authenticate exact probe launch and terminal result and every recorded artifact
before and after reading. Summarize both complete episodes, all online choices,
native goal distances, sensor/command audits, measurement gates and timing.
No fitting, checkpoint selection, native rerun, score adjustment or promotion.

For the first recorded observer failure in each episode, reconstruct every
reference/current pair listed in its reference-selection attempts from the
actual policy RGB-D packets. Recompute the gyro rotations from the recorded
fast packets. Call the original joint registration; for its composite consensus
rejection only, reproduce its deterministic proposal/ranking/pruning using its
unchanged fit and inlier functions to report which of fraction, image-grid
coverage or displacement failed. This decomposition cannot grant a pose or
change a failed outcome. Focused synthetic tests check preserved acceptance
and grid-only rejection. Freeze the new readout sources before execution.

Write only to the new exclusive source-bound development root
`go2_learned_goal_bootstrap_readout_v1_attempt_001`. Preserve the probe and all
earlier scientific failures unchanged. This is diagnosis, not final evaluation.
