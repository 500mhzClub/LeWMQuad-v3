# Queue the measured-plane native pilot after the original queue

The waiter launches exactly one child described by
`docs/go2_measured_plane_maze02_pilot_v1_2026-09-11.md`, after the exact existing
chained-anchor waiter ends with an authenticated completed result. Its owner is
PID 2845479, creation time 1789129072.88, on the recorded boot. Poll every 30
seconds with no elapsed-time assumption of completion. Preserve failures and
never restart a predecessor or the child. No native worker exists while waiting.

Source-check the launcher, simulator composition, physical-prefix checker,
input admission, focused tests and both protocols before registering the waiter.
Require the verified controller completion and the original queue launch. Bind
all sources, actual owner identity, environment and hardware at registration.
Read-only source preflight does not create an execution root or load a model.

After the original queue ends, admit its actual result SHA-256, check native
idle status and resources, and launch the child with that identity. Require
the child to exit zero, authenticate its complete raw artifact roster and
worker receipts, reconstruct the physical/contact/timing readout and actual
prefix result, and recheck source/input identities. The child's own full raw
model/controller audit is retained; no unrelated training is reexecuted.
Scientific success is not required to preserve a complete negative result.

The exclusive waiter root is
`go2_measured_plane_maze02_native_wait_v1_attempt_001` under the existing
navigation development artifact root. No overwrite, retry, resume, queue
reordering or source changes belong to this attempt. This scheduling mechanism
establishes no navigation, real-time, hardware or final-goal success.
