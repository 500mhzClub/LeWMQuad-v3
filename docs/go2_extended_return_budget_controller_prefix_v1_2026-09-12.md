# Prospective longer-budget controller prefix V1

## Question and scope

Determine whether the fresh 8,000-navigation-step chained single-pass
controller reproduces the original recorded behavior before the 4,000-step
deadline, and identify the first decision intervention. This is a recorded
development-prefix replay, not native execution, successful navigation,
independent-maze evidence, a timing benchmark or a hardware test.

The fixed output is
`go2_extended_return_budget_controller_prefix_v1_attempt_001` under
`/home/andrewknowles/RecoveryStorage/LeWMQuad-v3/.generated/navigation_development_artifacts_v1`.
The launcher is
`scripts/replay_go2_extended_return_budget_controller_prefix_v1.py`.
This protocol and its complete discovered source/test ancestry must be bound
in the actual exclusive launch before execution. An existing output is never
resumed, overwritten or automatically retried.

## Inputs and ordering

Use the completed original chained native result only, SHA-256
`163e92d630d92f9f8633cd3823c7cbf282a92c81bde5d3db998e3a5a8c8b1849`.
It has 4,014 observations and ends at the mission budget without a completed
physical return. Preserve that negative result.

First require the exact original chained/single-pass full-history comparison
to end. Its launch SHA-256 is
`b8a67fc80aebb2f39968092067cea00b94304c449bd7c86fdff30458c58d6260`,
owner PID 3015121, creation time 1789209876.83, boot
`1264d80f-6e46-4fcd-b2fd-2a5d7b964c73`. Supply its actual completed result
SHA to the launcher; there is no placeholder result binding, queued replay
or automatic waiter in this protocol.

Admission authenticates the completed comparison's launch, source roster,
exact five artifacts and result, then reconstructs its entire scalar decision,
forward-call, timing and state-checkpoint accounting from all 4,014 rows.
The original result must bind successful complete public-packet verification
and input authentication. That prior controller execution is not rerun.

The original native admission independently rehashes its complete artifact
roster, validates worker/collection/readout receipts and reconstructs the
original physical prefix. Its receipt must exactly equal the input admission
used by the completed timing comparison. Both sets of inputs and the complete
source union are reauthenticated after the prospective replay. Sealed material
remains inaccessible; no source export, training-data access or model update
is involved.

## Paired execution

Construct two independent copies of the same assigned corrected direct/no-RGB
model, SHA-256
`56799c99e2bb1fd5e5a591009b931c5b2e04741d3a3744e2346ce9896a2160dd`.
Require separate tensor storage, evaluation mode, unchanged state and no
gradients. Instantiate fresh articulated geometry and controller memory for
each arm. The baseline is `MeasuredPlaneChainedSinglePassController` with a
4,000-step budget; the candidate is `ExtendedReturnBudgetChainedController`
with an 8,000-step budget. Neither controller instance is upgraded or resumed.

Read the original public RGB/depth/auxiliary/IMU packet for each observation,
validate its original physical command endpoint and give the same packet to
both arms. Hash the complete packet before and after each controller call.
Compare the baseline with the complete recorded native decision using only
the already tested chained single-pass performance normalization. Compare
the candidate with that baseline after removing its new implementation flag,
restoring the baseline controller identity and normalizing exactly these two
budget fields:

- `shared_navigation_budget_ticks`
- `mission_receipt.global_navigation_ticks`

Every other decision field, including model forecasts, measured tracking,
retained history and command evidence, remains in the comparison. Count actual
model forwards with hooks and preserve complete decisions in an exclusive
bounded gzip stream.

Stop after the first complete normalized decision difference, any matched
terminal, or frame 4003, whichever happens first. Decode and feed no following
public packet. The complete original manifests and bound history archives may
be authenticated/read as inputs, but observations after the intervention are
not evaluated by either controller. No changed command is physically executed.

Compare complete retained controller state initially and at frames
0, 3, 61, 122, 255, 511, 1023, 2047, 3071, 4002 and the actual stopping frame.
The initial state is frame -1. Normalize only the ten explicit implementation
type paths and three budget locations listed by
`scripts/extended_return_budget_prefix_comparison_development.py`.
Model and geometry identities are checked separately. All state checkpoints
before decision intervention must agree. Record both stopping-state hashes;
their equality is not required once the decisions differ.

## Resources and output checks

Use the original deterministic single-thread CPU environment, with OpenCL
disabled, deterministic Torch execution and no concurrent native or named
repository CPU replay. Admission requires at least 64 GiB available RAM and
43 GiB artifact space. Record RSS, available RAM and disk every 100 frames
and at the stopping frame; retain at least 8 GiB RAM and 41 GiB disk.
The complete compressed paired decision stream is limited to 2 GiB and keeps
the existing 32 MiB per-row bound. These are prefix-replay limits, not resource
admission for the future 8,000-step native trial.

The closed-output checker reconstructs every recorded comparison and public
packet through the actual stopping observation. It requires all initial,
fixed and stopping state receipts, unchanged model/geometry identities, complete
resource samples and an exact reconstructed report. It checks for an extra
saved row before decoding any subsequent public packet.

The result binds launch, decision stream, state checks, identities, resource
monitor and report. A completed comparison can report an early negative
intervention. It supports budget-only preboundary decisions only when all
earlier complete decisions match and frame 4003 changes the original
`MISSION_TICK_BUDGET_EXHAUSTED` decision to a nonterminal candidate decision.
Operational, identity or output-check failures receive a terminal failure
receipt, preserving all existing evidence without automatic retry.

## Interpretation and next experiment

The replay cannot establish physical prefix equality for a new native
execution, successful return, collision avoidance after the intervention,
independent-maze reliability, JEPA/planning/memory benefit, real-time operation
or hardware qualification. A fresh longer native trial still requires its
own resource assessment, protocol and launcher, followed by actual physical
prefix agreement and complete outcome audits. This protocol grants no such
result by inference from replay.
