# Reactive nearer-route connector completed native readout V1

Read only the completed, exact-SHA-bound native connector result, its full
artifact set, independent raw audit, physical/public prefix comparison and
frozen source closure. Require one maze-0 case. Preserve the auditor's outcome
and evaluator's arrival/return gates; descriptive distance minima cannot promote
a failed run. Bind the completed executed-waypoint comparison readout and retain
the explicit differences in predictive geometry gates and shared memory.

In addition to predecessor path, crossings, arrival windows, terminal causes,
visibility, observed-pose errors and timing, list each actual nearer-route
selection. Require its original connector to fail, its selected connector and
current clearance to pass the unchanged 0.45 m radius, its current articulated
surface to be clear, and its nonterminal action to match the native command tape.
Retain original and selected targets, indices and measured clearance receipts.
For each fully executed interval, compute actual local-body XY displacement
between native samples 749+50*tick and the endpoint 50 samples later. A partial
interval has a null endpoint rather than an inferred 100 ms outcome. Native
poses are evaluator-only and never enter the policy. No outcome was forecast by
this reactive controller and no alternative command trajectory is inferred.

Use `scripts/read_go2_reactive_connector_maze_pilot_v1.py` with the completed
native result SHA-256. Check sources and artifacts before and after readout.
Require 8 GiB available RAM and 128 MiB above the 40 GiB reserve; no new scene,
training or checkpoint. Exclusive output:
`go2_reactive_connector_maze_readout_v1_attempt_001`. Preserve partial failures.
This remains one reused development maze; no independent-layout, planning,
memory, real-time or hardware advantage follows from this readout alone.
