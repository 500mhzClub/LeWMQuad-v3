# Separate prospective causal suffix derivation V1

One fresh readout at `go2_geometry_progress_family_causal_v1_attempt_001` after
the complete96episode family collection/raw audit. This specification was written
during collection, before its final outcomes or any fit. Preserve the frozen
initial-departure labels; derive additional moving-context labels into this
separate root. No physics, optimizer, checkpoint selection or policy execution.

Enumerate eight planned offsets per episode:0,5,10,15,20,25,30,35 command ticks
after the original departure. Keep all768 rows, including missing actual
contexts. Each available context uses its own four chronological100ms packets,
ending at its actual decision time. No sibling episode, commanded dead-reckoning,
post-contact departure or fabricated future is permitted. Role and parameter
cluster remain exactly those of the parent episode; dependent windows do not
increase the number of independent layouts.

The prospective plan is the remaining suffix of the original requested command
sequence. Normalize exactly as the existing model, preserve every known command,
and pad unknown suffixes with masked zeros. Known half-second future offsets
increase to the remaining plan endpoint; inactive offsets are zero. Unknown
padding is not an assumed brake or a new counterfactual action.

Native target-only poses define current-body XY displacement and projected
relative yaw at actually observed endpoints. Record native motion, actual future
image and cumulative contact validity independently. Observed contact remains a
known positive event through later known plan horizons despite unavailable motion
and images. Without a measured event or endpoint, contact is unknown. Missing
contexts retain no fabricated targets. Initial-context tensor inputs and targets
must exactly match the frozen generalized initial adapter, including NaNs/masks.

Authenticate all cohort artifacts, raw audit receipts and source/native bindings
before deriving and again before finalizing. Aggregate complete role/cluster
counts and prospective measurement gates unchanged. Materialization may diagnose
schema compatibility even if the cohort gate fails, but the final learning-ready
flag requires the complete measurement gate and informative training design.
Transfer outcome quality does not select training examples or checkpoints.

Future fitting must use role/episode/action-balanced draws over available windows
and retain missing-context accounting. It must not oversample long safe episodes
merely because they have more windows. Freeze the fitting schedule and matched
RGB/no-RGB and direct/rollout/JEPA treatments separately before optimization.
Moving suffix observations broaden the recorded distribution but do not prove
that new full-horizon actions can be repeatedly replanned while moving.

Bound output at256MiB with40GiB reserve. Process one episode at a time and retain
only compact window/index records. No tensor checkpoint or raw-image copy is
written by this derivation. Save an evaluator-only native base-centre path figure
covering all eight layouts and both appearances; paths do not represent swept
robot volume or learned-policy execution. Report ideal simulated sensors, paused physics,
measured collection timing and zero complete-maze/hardware qualification honestly.
