# Completed residual first-interval maze-2 outcome readout

After the fixed residual native pilot completes its collection, raw audit,
physical-prefix comparison and final artifact verification, read that completed
result and the original completed learned maze-2 result. This is CPU-only
post-hoc evaluation, with no model loaded, native scene, controller selection,
training or alternative physical trajectory inference.

Authenticate both results, complete artifact maps, frozen sources and native
launch dependencies. Require identical assigned corrected JEPA state,
training-only correction, scene, mission, numerical settings, physics/renderer
settings and navigation budget. Require all original raw audits and the actual
23,900-sample / 464-observation prefix through the first changed command at 463.
Retain original arrival, traversal, strict visibility and round-trip outcomes
without changing their definitions or treating scientific negatives as an
admission failure.

Report both actual path lengths, goal distances, terminal decisions, arrival
and traversal evidence, observed pose error and measured cycle timing. Traverse
each decision stream once. Retain all residual fallback attempts, including
unsuccessful attempts. For selected commands, verify the pre-command residual
receipt, original and corrected surface vetoes, all eight corrected nominal
segments at 0.45 m radius, the phase restriction and dispatched command.
Compare raw and corrected first-interval XY predictions with the actual native
100 ms body-frame displacement of that selected command only. Censor incomplete
commands; do not invent missing endpoints or unexecuted alternative outcomes.

The original waypoint readout requires original nominal feasibility and is
therefore inappropriate for these explicit exceptions. This separate helper
preserves the original raw path receipts and labels its corrected nominal
clearance as observed-map evidence, without a model-error or physical-clearance
bound. One repeated layout cannot establish statistical reliability, independent
generalization, memory advantage or deployment qualification.

Output: go2_residual_first_interval_maze_readout_v1_attempt_001 under the
established external development artifact root. Refresh hardware before launch;
admit at least 8 GiB available RAM and 40 GiB reserve plus 128 MiB output headroom.
This analysis may overlap a later native job when available resources permit.
Preserve its exclusive output, all failures and sealed benchmark custody.
