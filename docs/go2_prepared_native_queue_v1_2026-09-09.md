# Fixed sequential execution of three prepared native pilots

This scheduler waits for original supervised cohort PID2534319, process start
tick129848858 on boot1264d80f-6e46-4fcd-b2fd-2a5d7b964c73. It authenticates the
complete original three-case result against observed launch SHA
49a182da3d795f1b31910fb2b6123732db66349dfc011d768bb72fc9732a09e3,
then executes the unchanged, already prepared runners in this fixed order:

1. Direct-flow tracking maze3.
2. Residual anchored-continuation maze2.
3. Recent qualified reference maze1.

The runner fixes each original source SHA, command argument, output root and
case. It authenticates source bindings inherited from the three exact completed
prefix results and the observed supervised launch, plus every reviewed native
addition. Its own launch freezes that union and explicit import discovery for
the scheduler/test/protocol and three final prefix reports. No source export,
recursive artifact discovery, training, parameter selection or controller change.

Each child is a fresh original Genesis interpreter with the established explicit
PYTHONPATH, hash seed and single-thread numerical settings. Its original source,
input, hardware, model, physical-prefix and raw-audit checks remain in force.
The scheduler separately reexecutes each original input verifier and verifies
all original result/source/output hashes, ordered cases, worker terminals,
raw-audit flags, saved prefix success and unchanged model. Original supervised
paired outcomes are recomputed from the two complete saved cohorts. This is
authentication of the original audit, not a second native collection or a
replacement raw-sensor reconstruction. Scientific negatives advance unchanged;
process, integrity, raw-audit or prefix failures stop the queue without retry.

There is at most one child of this scheduler, and no child starts until the
original supervisor and observed competing same-user run_go2 processes/spawn
workers have exited. PID reuse or reboot is rejected. Same-user preflight-only
commands are excluded from the scene-owner check. The check is conservative
process observation, not a universal lock understood by unrelated launchers.
Do not independently launch another native scene while this scheduler owns the
queue. Owner/competitor waiting is bounded to48h; a live child is observed every
30s without automatic timeout termination or restart. Existing original output
or scheduler output prevents reuse; no resume or outcome-based skipping.

The waiting/imported verifier allowance is8GiB with16MiB scheduler metadata and
40GiB storage reserve. Before each child, inspect CPU affinity/topology/load,
RAM, GPU/VRAM, competing processes and both volumes, and require32GiB available
RAM and51GiB artifact free space. Each original runner additionally enforces its
own exact admission and resource monitoring. These are capacity admissions,
not OS limits or real-time claims. The scheduler records child logs, exit status,
completion identities and its own event stream in the exclusive
go2_prepared_native_queue_v1_attempt_001 root. Every original native artifact is
left at its original exclusive path. No deletion or hardlink mutation.

Prepared pilots and their earlier preflights are documented in
go2_direct_flow_maze03_native_preparation_2026-09-09.md,
go2_residual_anchored_continuation_native_preparation_2026-09-09.md and
go2_recent_qualified_anchor_native_preparation_2026-09-09.md. The overall navigation
goal remains separate from queue completion. Reliable independent-layout round
trips, model/planning/memory advantages,100ms timing and hardware deployment are
not established by scheduling or by positive replay prefixes.
