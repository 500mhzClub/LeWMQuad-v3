# Native input admission prepared

The admission module connects the completed sustained raw replay to the original
full-JEPA model, native hold predecessor and completed five-stage queue. It rejects
live original owners before expensive input access and rejects mixed batch, hold,
model, replay or budget identities. Full fifth-stage input verification is required
on admission; subsequent checks use the existing bound queue verifier. Negative
scientific results remain admissible evidence and are never silently filtered out.

All 18 focused tests passed in 2.39 seconds, session 80429, exit zero. They cover
actual verifier dispatch, full-queue invocation, mixed identities, altered scope,
typed completion flags, the original boot and owner-before-input ordering. Source
preflight verified 2,171 paths in session 20986, exit zero. A call to the real
admission function rejected the still-live original raw replay before input
access. No complete native input admission occurred.

The preparation record is
`docs/go2_sustained_hold_reorientation_native_inputs_preparation_2026-09-11.json`,
SHA-256 `7ff018b8e33994bfd3cc2f8f8343304f46fb433c81cd1771957847607096d138`.
It was written in session 43600, exit zero. Source:
`scripts/sustained_hold_reorientation_native_inputs_development.py`.

The raw replay remains PID 2813368 / creation 1789115323.91 / session 87754 and
has reported observation 150 reconstructed: 151 observations verified so far.
The contact native worker is PID 2813548 / creation 1789115409.4, owned by original
parent 2808232. Both were confirmed live. Tracking and extended budget remain
queued, and no sustained-turn native scene has started.

Next implement the native launcher against this module, using the prepared
collector, auditor and prefix comparator. Bind the original model, scene, runtime,
resource envelope and exclusive output; require completed replay and original
queue before worker creation. Native collection, full raw audit and actual
physical-prefix comparison must all finish before any new outcome is accepted.
Navigation, timing and hardware qualification remain unproven.
