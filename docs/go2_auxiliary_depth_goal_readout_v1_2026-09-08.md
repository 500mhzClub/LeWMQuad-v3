# Auxiliary depth native goal readout V1

Authenticate the completed two-case auxiliary depth native probe, its full raw
audits, its source closure and the fixed corrected-model primary-only predecessor.
Create the exclusive `go2_auxiliary_depth_goal_readout_v1_attempt_001` root only
after input verification. Report both outcomes, native goal criteria, physical
and acquisition stops, exact model replay, primary and auxiliary visibility,
auxiliary robot occlusion, observer errors, floor/other accounting, action choices,
nominal and articulated constraints, and complete-iteration timing.

Compare each model's actual trajectory to its primary-only predecessor through
the observation before the first different requested command or terminal.
Record exact raw physics, policy, gyro, primary RGB, observer/memory and forecast
agreement separately. Primary RGB can differ because the renderer now includes
the robot; auxiliary evidence changes memory. Do not assume either agrees, or
attribute all differences solely to auxiliary depth. The unchanged base models
retain their frozen training-only correction; there is no new coefficient fit.

Compare the new JEPA/direct pair across the same causal boundary, additionally
checking every auxiliary public depth and valid-mask array. A predecessor with
no auxiliary stream has no auxiliary equality result. Exclude affected future
frames from matched-prefix evidence, and infer no counterfactual executed
outcome. Reverify sources and every input artifact after analysis. This readout
performs no model fitting or native execution and adds no independent-maze,
real-time, hardware or overall-goal qualification.
