# Contact scoring with direct-flow tracking: native source preparation V1

Prepare a separate collector and full raw auditor for the original contact-
scoring controller with the existing direct-flow visual observer. This source
preparation follows the completed observer recovery at frame 561 and does not
assume that the full controller replay or a future native execution succeeds.

The original collector SHA-256 is
`77f8f9098160a607f5e346c0a374a921777bb15f036f3b92db7b003b950c32b3`;
the original auditor SHA-256 is
`f8f494d507a584ed9d28450c443fcd44829dceb5697f9ceb267a76e317d3ba3b`.
Require complete AST equivalence to these originals after normalizing only
the declared controller import/class, collection status strings and explicit
direct-flow feature flag/assertion. Both new modules use the same new class;
the originals remain unchanged.

Preserve the public mission, original 3000-navigation-tick budget, warmup and
terminal drain, camera/body/gyro packets, renderer witnesses, gait and geometry,
resource guards, complete raw artifact roster and all strict success criteria.
The raw audit must replay every recorded controller decision and model command
from the actual public sensors with the original assigned model. Native pose
and contact remain evaluator-only. Do not infer postintervention outcomes from
the old terminal drain.

Source preparation, passing tests and a completed observer replay do not
authorize an immediate native child. A future launcher must require the
completed full controller replay with admitted current evidence and a valid
selected command, the
complete original native input chain, an exclusive fresh output and an idle
native scene. It must preserve the already queued tracking, budget and
sustained-turn runs and compare the prospective physical/public prefix before
the intervention. If the recovered controller still requests hold, record
that unchanged command explicitly and define the prospective boundary by
the recovered observation; do not invent a changed request or replay the old
terminal drain as new future evidence. No training, independent-study policy selection, navigation
qualification, real-time qualification or hardware claim follows here.
