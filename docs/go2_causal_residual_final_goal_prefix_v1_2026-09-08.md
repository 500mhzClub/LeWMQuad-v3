# Causal observed-residual final-goal prefix V1

Authenticate the executed-horizon native pair/readout and the completed causal
residual integrity diagnostic. Replay both fixed corrected models twice from
fresh state on their recorded public primary/auxiliary observations. Stop at
the first changed command or terminal, including its preceding observation.

The new residual state admits each current public joint pose before selection.
When a previous requested command had an original 100-ms candidate forecast,
label its displacement only from the next consecutive admitted pose. The
executor and raw audit still own proof that the requested command executed.
Update from original model predictions, never adjusted scores. Retain only
the preceding eight ticks' residuals; gaps receive no imputed forecast/target.
Match online correction values/source/availability ticks exactly to the
completed prequential diagnostic wherever it has an eligible recorded step.

Change only final-goal utility: subtract that shared observed XY residual from
the candidate's first predicted scoring pose. Keep raw model forecasts, yaw,
full 800-ms contact penalty, original first-step articulated and all eight
nominal checks, phase allowances, intermediate/view scores, arrival gates and
mission budget. Record separate scoring poses and residual provenance; their
paths have not been clearance-checked. No model weights change.

Require exact causal observations, maps, forecasts, surface/nominal checks and
the explicit pure score transformation of each old selection. Preserve every
infeasible/terminal outcome. Ten online-state/scoring tests passed in 1.97 s.
No prospective recovery is established by replay or lower prediction error.

Exclusive `go2_causal_residual_final_goal_prefix_v1_attempt_001`; one CPU
process/numerical thread, 8-GiB RAM and 256-MiB output allowance above the
40-GiB reserve. Freeze/reverify all source/input/diagnostic bindings. No native
commands, fitting, checkpoint selection, retry/resume or qualification.
