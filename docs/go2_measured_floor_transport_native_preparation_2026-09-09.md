# Prospective floor-transport experiment prepared

This is preparation, not a completed intervention or navigation result. The
live causal replay90487 remains responsible for proving the current controller
prefix through1904 before native admission. All existing launched sources stay
frozen. The full goal remains active with zero verified round trips.

Prepared scripts/run_go2_measured_floor_transport_maze_pilot_v1.py, its separate
measured_floor_transport_maze_episode and measured_floor_transport_maze_audit,
measured_floor_transport_intervention and measured_floor_transport_native_prefix
helpers, plus scripts/read_go2_measured_floor_transport_maze_pilot_v1.py and
typed JSON/readout helpers. Exclusive future roots:
go2_measured_floor_transport_maze_pilot_v1_attempt_001 and
go2_measured_floor_transport_maze_readout_v1_attempt_001.

The collector is the original full dual-camera episode with only the new
controller, explicit collection labels, and the independently tested camera
context recorder. Full original scene, model, observations, gains/friction,
action execution, warmup/navigation/drain timing, reserve and persistence logic
are retained. The audit replays the entire new controller from raw packets and
retains original physical/sensor/command/visibility/arrival/return evaluation,
adding complete renderer endpoint verification. AST scope comparisons passed
for the collector, artifact inventory and full raw audit.

Native admission binds the completed1905-frame causal stream, exact intervention
and summary, last admitted1903anchor, active current transported pose/RETURN,
unchanged current raw visual evidence and actual completed predecessor result/
readout. It also binds the completed renderer startup result, native/runtime
identities and five explicit renderer environment selectors. One fresh native
worker; no parallel scenes. Preflight must inspect current hardware and require
32GiB RAM and original10GiB collection plus1GiB persistence above40GiB reserve.
The resource allowance is not an enforced OS memory/storage quota.

Native prefix comparison includes95950physical samples through the intervention
observation, all1905paired public observations and all1905complete decisions
from the prospective replay. It checks all1904earlier actual commands and the
bound intervention command. Following new physics is allowed and independently
evaluated; following old observations never score an unexecuted action.

Readout preserves original physical/outcome/timing/contact/camera calculations
and validates both current-floor and transported schemas. Only declared JSON
identity fields are restored. Transport records retain anchor age and full
correction norm/angle, with current-plane tilt/height fields explicitly absent.
Changing native evaluator endpoints changes errors, never the admitted pose.

Tests59046 CLOSED18passed5.25s: complete synthetic1905-row intervention
admission, malformed/truncated/later/earlier/stale/different witness rejection,
actual renderer startup admission and AST execution/audit scope. Tests58117
CLOSED19passed6.11s: final physical sample boundary, post-intervention physics,
actual commands, public packets and complete decisions; transport readout,
nested anchor identities, evaluator endpoints and terminal missingness.
These37tests do not establish native navigation or raw image-fitting accuracy.

CLI5672/22681 CLOSED pass. Source24961 CLOSED pass:1658 combined prepared
native/readout paths; all1626live-prefix,1631renderer and1618eleventh-readout
bindings unchanged and compatible, both future output roots absent. Launcher
docstring was clarified afterward; no executable behavior changed. Installed
renderer/scene startup source inspection found the five bound selectors are
read as configuration; the EGL path temporarily restores DISPLAY, which is
outside those selectors. Native preflight will recheck actual environment.

Next: continue90487; if complete, run the launcher with its actual
--prefix-result-sha256 and --preflight-only, then one fresh episode if admission
passes. After full native audit/prefix completion run the dedicated readout
with the actual native result SHA. Protocols:
docs/go2_measured_floor_transport_maze_pilot_v1_2026-09-09.md and
docs/go2_measured_floor_transport_maze_readout_v1_2026-09-09.md.

Strict visibility failure909 remains preserved. Prospective camera provenance
does not supply a raster-error bound. A reused-maze return would not establish
independent-layout reliability, matched JEPA/planning/memory attribution,
real-time execution or deployment readiness.
