# Prospective independent development layout inventory

Prepare eight new sixteen-cell tree mazes for later navigation work. This is
source-defined construction and scene-pack preparation only; it creates no
native scene, dataset observations or model inputs. It is not sealed or final
evaluation and provides no unseen-maze navigation result. Do not modify the
running maze3 pilot, queued six-case development comparison or their sources.

Use the same1.3m grid pitch,0.08m wall thickness,1.4m wall height, start cell(-1,0),
initial heading0, forced first passage to(0,0), nominal friction, camera and
physical scene-pack settings as the four existing round-trip layouts. Generate
spanning trees with a fixed Python RNG seed2026091027. Accept the first eight
eligible candidates in order, with a10000-candidate hard bound. Eligibility is
structural: farthest-cell route of at least six edges and two direction changes,
no exact abstract-graph or rigid-grid-embedding duplicate within the new set or
the explicit previous source registry. Never filter using prediction scores,
native success, visual tracking, collision outcomes or controller behavior.
Retain every structural rejection and both identities; no runtime resampling.

The explicit registry comprises24 counterfactual source layouts,8 online-choice
source layouts,4 current round-trip layouts and12 connected-layout context
inventory layouts. Read only their pure reviewed source constructors. Do not
open legacy datasets, runtime outputs or protected manifests. Use the existing
bounded exact graph canonicalizer, not only a graph-refinement hash or a rotation/
reflection hash. Record the number of distinct previous abstract groups rather
than assuming all48 previous embeddings are distinct graphs. Novelty is relative
to this explicit registry, not an unsupported claim about every earlier world.
Compare prior grid embeddings at the common new-scene dimensions; do not claim
to reconstruct each legacy layout's actual metric width/height. Record those
comparison dimensions and the Python/NumPy environment used for construction.

Assign accepted layout index0..7 to physics seeds2026092700..2026092707 and
appearance seeds2026092800..2026092807. Use the deterministic farthest-cell goal
and fixed tie-breaking. Preserve the complete graph, route, wall roster and
identities as evaluator-only metadata. The controller-facing mission contains
only goal coordinates in initial-body frame, home coordinates and return intent;
it must not include layout indices, seeds, graph/route identities or topology.

Validate wall boxes against every open and closed interior grid edge, and validate
scene packs and mission boundaries using source-only tests. These checks do not
replace physical setup, sensing/renderer evidence, raw command replay or actual
navigation. The existing native initializer accepts only the old four layouts;
it must remain unchanged. A separately reviewed new initializer, collector and
evaluator integration, frozen controller/model assignment and native execution
protocol are required before this inventory can be used in a physical run.

Preserve the eight-layout population without outcome-based subsets. Future
training/tuning on these layouts would consume their independence for a later
claim and requires a new declared development scope. No current native launch
or model promotion is granted by this source inventory. The full goal still
requires actual reliable round trips and matched planning/memory controls.
