# Appended tracking-recovery completion before independent-study review

The original independent-study queue helper covers the batch followed by the
frontier, hold and contact tests. The subsequently started tracking-recovery
waiter is an additional prerequisite. A new wrapper requires all original
owners to end, authenticates its exact frozen launch and completed result, and
binds its inputs to the same batch and ordered three-stage queue, plus completed
controller replay `152da30ba5c142d8150dacd5865278f81d5db46c7603bd637eb8ec2eee292fdf`.

Accept only an original queue admission that previously performed full input
verification. Revalidate that admission. Reexecute the tracking waiter's native
completion checker, which checks the collection, worker, raw-audit receipt,
prospective physical-prefix comparison and independently recomputed contact
readout. Preserve zero-round-trip outcomes as valid scientific completions.
Infrastructure failures, absent completion, changed identities or reused process
IDs must fail; do not start replacement jobs.

The wrapper's initial `admit(..., full=True)` additionally repeats the original
tracking native input admission. Subsequent `verify_bound` requires that initial
full admission and reconstructs the same evidence with bound input verification.
It does not independently repeat collection or the native raw audit. Neither
entry point selects a policy or permits population execution.

Source preparation verifies the original helper and the running tracking
waiter's launch/source bindings. It may run while those owners are live; actual
completion admission must reject before reading their incomplete output. The
wrapper has no launcher, model construction, waiting loop or native execution.

The future joined population verifier must consume this extended admission,
the complete population input admission and a subsequent explicit policy review.
It has not yet been implemented. The prepared four-arm study separates training
objective and planning-map persistence; its reactive arm changes feasibility
rules and remains a whole-method comparison, not an isolated prediction-ranking
ablation. The final review must preserve that distinction and resolve the
remaining attribution scope before independent-layout execution.
