# Prospective expanded-model adapter physical comparison

Execute six fresh development simulations on existing maze 02, in the original
fixed order: full JEPA, full supervised rollout, full direct, no-RGB JEPA,
no-RGB supervised rollout, no-RGB direct. All use seed 2026091001's originally
assigned, training-corrected saved model states. The only runtime intervention
is AllPhasePlannerModel, the byte-identical evaluation-interface adapter. Keep
ResidualAnchoredContinuationController, collection and full raw replay audit,
scene, public mission, sensing, candidate actions, motion constraints, forecast
horizons, timing, budgets, and fresh persistent memory unchanged. No training,
checkpoint selection, frontier intervention or independent-layout execution.

Output: `go2_all_phase_adapter_maze02_matched_native_v1_attempt_001` under the
existing external navigation artifact root. Existing failed cohort and V1/V2
adapter replays remain immutable. No automatic retry or resume.

Require completed original six-case result
`a08496e1d62ec6e00ffae3d85729cc7cf069c2fddcb60d421910c83d30556e80`, completed
waiter `68b965638210893d5b3cd446eac70d432b7b9d9afd130c2ee64e4d9966b70da2`, and
all-six raw adapter prefix
`0d006a18920ba733f46f79fc27444c72038de8da3a4df08ff5af343c8f965daa`.
Reexecute original complete fit/correction/native input admission before launch
and at completion; verify all bound inputs around each worker. Bind the complete
inherited source union and new explicit import closure before execution.

Require one native scene and one fresh worker process per case; one OpenCV,
PyTorch and BLAS thread; deterministic inference; at least 32 GiB available RAM
and the unchanged six-case disk envelope (40 GiB reserve plus 11 GiB per
remaining case). Check resource availability and competing live processes before
each case. Simulation physics pauses during computation; no real-time claim.

Each worker must preserve all collection outcomes and run complete raw sensor,
model-command, command and visibility audits with a fresh adapted model. Require
the exact original model state before and after collection and audit. Compare
the same case's original and fresh 900 physical samples, four public observations
and three completed zero warmup commands. Require all four complete new decisions
to equal the prospective adapter replay, including every forecast and the first
changed command at observation 3. Require that boundary command to have actually
completed through physical sample 949. Later outcomes are measured only on the
new executed trajectory. A failed startup/audit is retained as an incomplete
experiment; no result is silently discarded or replaced.

Report all six ordered outcomes, including scientific failures, using unchanged
strict native/observed/visibility/contact/return and quiet-terminal criteria.
Controller observation or mapping failures may still end these fresh runs.
Completion of this development comparison is not navigation qualification or
evidence of an independent-layout, JEPA, RGB, planning or memory advantage.
