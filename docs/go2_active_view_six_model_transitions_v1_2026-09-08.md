# Six-model executed-transition diagnostic V1

This is a prospective definition of a post-outcome diagnostic on the two frozen
active-view native failures. It does not run a simulator, train or select a
checkpoint, retune scan utilities, or establish navigation benefit.

Authenticate the entire active-view result
`bdd1b21cec0d02413f5833f5ff2a27bfde9a95af72c5aa8c03056f593c5db3bb`,
its completed readout, and all six final family fits using the existing full
ledger, input/schedule, raw-score and snapshot admission. Bind all inherited
sources and new diagnostic/test sources before creating the exclusive root
`go2_active_view_six_model_transitions_v1_attempt_001`.

Rebuild identical four-packet causal public histories for every one of the 94
recorded actual model-selection contexts. Infer all six existing models on the
same six original candidate plans. Each model uses its trained head and RGB
treatment; all first full-JEPA predictions must exactly reproduce the native
decision record. No native pose, future observation, measured outcome, map or
waypoint enters inference. Predictions are complete before native labels load.
Retain complete predictions, causal tensor hashes, case/tick/action identities,
and every eligible or excluded outcome. No scientific parameters change.

An executed half-second label is eligible only when all five native command
intervals completed and every requested command equals the selected candidate's
first five commands. Verify exact native indices, 500-ms elapsed time and all
250 raw requested-command samples. Otherwise retain the exclusion reason.
Compute XY and yaw relative to that interval's initial native body pose, and
contact over the next 250 samples. Score only the actually executed action.
Report Euclidean XY error, shortest wrapped yaw error (undefined yaw retained),
and cumulative-contact Brier by case and all/hold/nonzero action groups. Include
a declared zero-motion/zero-contact numerical reference, without calling it a
navigation controller. Contact-free cases cannot establish contact sensitivity.
The windows share trajectories; no independent sample counts, significance or
counterfactual outcomes are claimed. This is not a matched native policy trial.

Inspect current CPUs, utilization, RAM, GPU/VRAM, competing jobs and storage.
Reserve 8 GiB available RAM and 40 GiB free artifact space plus a 1 GiB output
envelope. Use single-threaded CPU kernels; benchmark one versus two independent
model threads on identical two-context workloads, twice after warmup. Require
bitwise equality and use the faster mean wall time. Preserve per-model timing,
workload measurements and before/after hardware observations. These capacity
checks are not OS quotas. Reauthenticate every source/input/model afterward.

Three focused synthetic tests exercise current-window rotation, horizon/contact
boundaries, incomplete/mismatched command exclusions, raw-tape disagreement,
wrapped yaw and undefined yaw. Preserve all predecessor failures and custody.
