# Dual-camera controller prefix on the tenth completed collection

The tenth collector has ended with1883 observations,1882 completed commands,
94850 physics samples and10 terminal zero commands. Its case result SHA is
2875fbdee5d5069de2ec3ef872f942e5a52b53d34eafccd88108cab45a3cd914;
native launch eee7ddac0d0a5410806c7e16f6d5abc71ee2cf9ffb3772ed4f910333868de188.
The full native audit is still pending. The frozen worker persists the case,
hashes every collection artifact and then performs a read-only audit. A
parallel controller prefix can bind these exact closed case artifacts without
substituting for that audit. Before launch require the whole explicit case
artifact inventory, matching counts and hashes; verify all bindings again at
completion. Do not read a partial stream or live-written collection.

Gate launch on the completed dual-camera motion/floor-registration replay of
all1881 ninth observations, supplied by its actual result hash. Require complete
raw reproduction and1870 exact original registered-prefix frames. Use a fresh
unchanged assigned model and DualCameraSettledController from the tenth
episode's first observation. All map/planner/mission/geometry settings are
unchanged; no packed-index or query optimization is installed.

Before the first auxiliary attempt, validate the explicit dual-camera and
registered witnesses and compare every complete decision to the collected
original. Remove only the declared added modality metadata at explicit paths:
the new controller label/flag, observer label, auxiliary calibration IDs,
current/historical pose pixel/depth bindings, camera-choice/current flags and
auxiliary feature witness. Require primary camera selection with no attempted
fallback; all commands, mission, model forecasts, map and planner fields remain
exact. Compare original requested commands with the actual collected tape.

Stop after computing the first decision that attempts auxiliary tracking.
Record its complete before/after decisions and sensor witnesses even if it
fails or requests zero. Do not feed a later recorded observation after behavior
can diverge. Save every mismatch/failure, no automatic restart or threshold
change. Verify model state, source and input bytes after execution.

This is prospective-prefix evidence only. Before any new native run, require
the tenth final audited root result, its unchanged qualification failures and
all final artifact bindings to match this prefix's collection bindings. The
prefix neither replaces the native audit nor establishes a physical arrival,
return, independent layout, attribution, sensing/timing or hardware claim.

Exclusive output go2_dual_camera_controller_prefix_v1_attempt_001. Run
scripts/replay_go2_dual_camera_controller_prefix_v1.py with the completed
--registered-result-sha256 and --preflight-only before launch. Assess CPU/GPU,
memory/storage and competing workers. One CPU process/numerical thread,
8GiB available RAM and1GiB output above40GiB reserve beside the existing native
audit. No scene, training, source export, retry or old-attempt mutation.
