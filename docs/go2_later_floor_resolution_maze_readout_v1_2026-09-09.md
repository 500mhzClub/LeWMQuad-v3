# Completed later-floor-resolution maze readout V1

Read only a completed, hash-bound later-floor-resolution native result with
its full raw sensor/model/command audit and exact physical/public prospective
prefix comparison. Exclusive output go2_later_floor_resolution_maze_readout_v1_attempt_001.
Preserve its strict visibility and native arrival/return outcomes unchanged.
Report trajectory distance, maze edges, goal distance, raw/registered pose
accuracy, actual selected actions and full-loop timing. Native poses remain
evaluator-only. No simulator, training, checkpoint selection or policy changes.

For actually selected commands whose original contact check blocked, verify
that the revised check passes, all eight camera/foot queries account for all
original hits, non-foot shapes are unchanged and every clearing witness is
strictly later than its ambiguous samples and already measured by that tick.
Require the tape to contain that exact request. Compare the 100ms forecast
with its measured native endpoint only for completed intervals; retain censored
intervals without invented endpoints. This identifies executed contact changes,
not all causal policy effects or a counterfactual predecessor trajectory.

The original confirmed-floor readout cannot be applied directly to the revised
primary/auxiliary contact queries. This readout explicitly reports the new
later-floor-resolution scope instead. Pose, mission, waypoint and reentry
summaries retain their existing definitions. Missing terminal-drain poses stay
missing. Reused maze0 cannot establish independent-layout, matched-baseline,
real-time or hardware qualification.

Bind all source/model/native artifacts before and after. Inspect actual CPU,
RAM/GPU/competition/storage before the bounded one-process readout. Require8GiB
available RAM and128MiB above the40GiB artifact reserve. Preserve all failures;
no source edits after launch or output overwrite.
