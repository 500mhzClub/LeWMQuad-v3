# Complete actual policy-stream and schedule verification V1

One fresh check at `go2_geometry_progress_family_policy_stream_v1_attempt_001`.
Require the complete passing family collection/measurement gate and separately
bound causal derivation. Preserve every old result. No retry/resume, physics,
optimizer step, fitted checkpoint or scientific learned-benefit claim.

Validate every available training window through the policy-only training path
and every available geometry-transfer window through the past-only inference
path. Compare all history and known-action tensors with the exact published
derivation fingerprints. Bind every consumed policy manifest, body-history
archive and RGB leaf before and after initial materialization. The private cache
contains training samples only; returned batches are new tensor stacks. Verify
all input and derivation bytes again before the terminal receipt.

Inference must not visit future targets or open future images, depth, native
state or command tapes. The metadata scoring view remains separate. Run the
actual cumulative-event model, width32, initialization seed2026090951, in
evaluation/inference mode on all batches, checking every active prediction and
exact actual horizon/mask. Parameters and gradients must remain unchanged.
These are untrained interface checks, not prediction quality or policy evidence.

Generate one fixed training schedule:1,200 updates, batch6, seed2026090950,
balanced over training layout, action and episode, then over available offsets
within that episode. Require exactly150 draws from each of48 training episodes.
No transfer-role draw, success-conditioned weighting or longer-safe-episode
oversampling. Preserve unavailable-context accounting. This schedule is a
concrete prospective input for a later separately frozen matched fitting study;
generating it performs no optimization.

Use one CPU/OpenCV/BLAS thread, at most384 privately cached training windows,
64MiB planned output and40GiB storage reserve. Record wall time and peak RSS.
No tensor or model checkpoint is written. Ideal simulated sensors, paused
collection physics, dependent mirrored layouts and lack of complete-maze,
real-time or hardware qualification remain explicit.
