# Observation-horizon full-fit readout V1

Admit all eighteen short-horizon fits by reconstructing all ledgers, saved
scores and final snapshots. Retain every seed and method. Report weighted
motion/contact denominators and undefined-yaw failures, source/role/stratum,
100-ms and 500-ms metrics, and descriptive variation across all three seeds.
Do not filter undefined results or interpret optimization variation as a
confidence interval over independent mazes.

Authenticate the earlier complete eighteen-model augmented fit and its full
admission readout. Compare only the shared 500-ms target horizon: new output
slot five against old output slot one, with exactly matching source, role,
stratum, seed, context schedule and target denominators. New 100-ms targets
and new all-horizon scores have no such old-horizon comparison. The temporal
architecture, token width and training horizons changed; identical whole-model
initialization or a single-factor loss intervention is not claimed.

Use the exclusive `go2_observation_horizon_fit_readout_v1_attempt_001` root,
with no inference, optimization, native execution or model selection. Preserve
the fixed first-seed full-JEPA native candidate and full-direct comparator.
Bind and reverify all source and input artifacts. Check hardware and require
8 GiB RAM and 256 MiB above the unchanged 40-GiB artifact reserve. Predictive
scores do not establish navigation, real-time operation or hardware deployment.
