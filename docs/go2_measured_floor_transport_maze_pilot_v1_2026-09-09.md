# Prospective measured-floor transport on development maze0

Run scripts/run_go2_measured_floor_transport_maze_pilot_v1.py with the actual
completed --prefix-result-sha256 and --preflight-only, then execute once if
all source/input/hardware admission checks pass. Exclusive output:
go2_measured_floor_transport_maze_pilot_v1_attempt_001. One CPU native scene,
one numerical/OpenCV/BLAS thread, fixed seed/model, original3000navigation-tick
shared outbound/return budget, three warmup observations and ten terminal zero
commands.32GiB available RAM, original10GiB collection plus1GiB persistence
above40GiB reserve; this is admission/accounting, not an enforced OS quota.
Inspect actual competing processes and do not run parallel native scenes.

Require completed causal prefix0..1904 from
docs/go2_measured_floor_transport_prefix_v1_2026-09-09.md. The saved stream,
summary and intervention must agree exactly:1904unchanged earlier commands,
same raw current visual evidence at1904, valid explicit transport from the
recorded1903floor anchor, active RETURN, and no later recorded observations.
Require the completed eleventh native/result/readout and all65diagnosis
bindings matched to its final native artifact map. Fixed predecessors:

- native44710966178a57b31f7da3bec10ad4f21710bcff3701b0a1038750f1ef6d747c;
- readout6fbbf9a63ca770317fc929c49796993ff3cf9b268cb4572d2d39a7bfd660d5db;
- renderer startup integration5107b9a48c9767149b0f1a217875065f6e98489fe97885626cca9a0ace547f88.

The only control change is the explicit current pose hypothesis during floor
count/extent missingness. Current raw visual admission remains mandatory;
available planes and qualified conflicts use the original registration gates.
The model, mapper/contact/selector algorithms, observations, robot geometry,
gait/gains/friction, action library, settling rules and mission budget remain.
The separate map-performance candidate is not adopted. No training, source
export, sealed access, real-robot command or historical-outcome revision.

Additionally use the independently tested RendererWitnessDualCameraMazeSession
to record actual context at each original capture endpoint. This adds no public
controller input, render or physics step. Its source/runtime/environment and
completed three-observation integration are bound. Audit every recorded endpoint
against actual raw acquisition hashes, clock/sample/optical pose and context
consistency. Preserve partial captures and failures; no silent witness retry.
This gives prospective provenance, not historical context or raster-error bounds.

The collector retains complete streamed decisions and all raw artifacts. A
fresh controller/model must reproduce the entire new sensor-to-command history.
Independently audit actual requested/applied command intervals, physical guards,
robot/camera geometry, RGB/depth and both camera footprints, model state and
physical arrival/return criteria. Require the first95950physical samples and
all1905paired public observations through the intervention to match the old
episode, and all1905complete new decisions to match the prospective prefix.
After the intervention, evaluate only the newly executed trajectory.

Strict visibility remains unchanged, including preserved predecessor failure909.
Native physical outbound/return candidates and full qualification remain
separate. There is no calibrated pose uncertainty, real-time or deployment
claim. Even a successful reused-maze return does not establish independent-
layout reliability or JEPA/planning/memory advantage. Preserve every failure.
The broad goal remains active until its full end-to-end requirements are met.
