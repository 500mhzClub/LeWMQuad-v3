# Full Training V3 Camera ladder-topology correction amendment

Date: 2026-07-14

Status: **source-free correction frozen before substantive V3 implementation,
Camera ladder design/execution, payload access, GPU use, or training;
different-agent review required**

Implementation author: `/root/full_training_v3`

## Bound parent

This narrow correction binds and supersedes only one ambiguous sentence in:

`docs/lewm_go2_shared_jepa_v5_full_training_v3_successor_amendment_2026-07-14.md`

Parent file SHA-256:
`93737e1556fc3b523408e0fd01ed632ec8571acb30978ae1f17e1dd653e40278`.

Every other parent clause, loss, reduction, lifecycle, namespace, provenance,
pre-G2 schema, unresolved-hash rule, authority denial, and review requirement
is retained literally.

## Exact correction

The sole authorized Camera V13 seed `20260710`, `N=5` exact attempt is the
first-seed N5 ladder rung. If and only if that existing one-shot attempt later
receives its canonical different-agent source-review PASS, executes once, and
passes all 26 unchanged gates, the future two-seed ladder must bind and reuse
that immutable PASS evidence. It must never launch, reserve, repeat, replace,
or reinterpret another seed `20260710`, `N=5` attempt.

A later source-free ladder preregistration and different-agent review may
authorize exactly seven additional fresh-initialization attempts, in this
fixed order:

```text
seed 20260710: N=16, N=32, N=320
seed 20260711: N=5, N=16, N=32, N=320
```

Together with the already completed seed `20260710`, `N=5` V13 PASS, those
seven attempts form the required two-seed, four-rung evidence table. Each of
the seven uses a fresh initialization; no attempt warm-starts from an earlier
rung or seed. Only seed `20260710`, `N=320` may migrate into Full Training V3.

The future aggregate ladder receipt must contain eight distinct rung
bindings, exactly one per `(seed, N)` pair in
`{20260710,20260711} x {5,16,32,320}`. Its seed-20260710-N5 row must bind the
pre-existing V13 gate/result/completion identities. It must also attest:

```text
additional_attempt_count = 7
seed_20260710_n5_reexecuted = false
all_rungs_fresh_initialization = true
warm_start_used = false
both_seed_ladders_pass = true
```

Any second seed `20260710`, `N=5` reservation or execution is a terminal
structural failure and cannot support Full Training V3.

## Authority boundary

This correction grants only additive V3 source implementation against both
bound amendments. It authorizes no Camera attempt, retry, later-rung design or
execution, `.generated` open, data/RGB/checkpoint open, accelerator use,
preflight, training, selection, calibration, G2, held-out, navigation,
runtime, hardware, production, promotion, or deployment. All future Camera
hash fields remain `null` and unresolved in the V3 source-time manifest.

An independent V3 implementation reviewer must bind both amendment hashes and
verify the exact eight-row/one-existing-plus-seven-new topology before any
later authority is considered.
