# Causal observed-residual final-goal native probe V1

Run the fixed corrected seed-2026091001 full JEPA/direct pair on reused
family_episode_039 in unchanged seed-2026091501 order. Require complete original
correction admission/readout, executed-horizon native pair/readout and two
fresh causal-residual prefix passes per model. Require exact agreement with
the completed prequential diagnostic on eligible prefix frames and at least
one changed command. No checkpoint choice or further fitting.

At each newly admitted public joint pose, observe the immediately preceding
requested command's displacement and retain its original-prediction XY residual
for eight ticks. The next frame must follow actual dispatch/execution in the
unchanged collector; the raw audit verifies every command and timestamp. Never
use native labels, command-integrated pose, future frames or an unexecuted
candidate's outcome. Keep residual updates based on original model forecasts,
not the already adjusted scoring pose.

Adjust only the exact final-goal 100-ms scoring pose by the mean past residual.
Keep raw forecasts and original first-step articulated/all eight 0.45-m nominal
checks intact. Keep yaw and the original 800-ms contact cost with coefficient
1.2. The adjusted scoring pose/path has no new clearance certificate. Preserve
all intermediate/view scoring, phase allowances, maps, sensors, bounded waits,
240 navigation ticks, 4-cm arrival dwell, evaluator 6-cm one-second quiet/contact
gate and ten terminal zero commands. Gait, slew, gains, setup and stops stay
unchanged. Original map indices remain in use.

Exclusive `go2_causal_residual_final_goal_probe_v1_attempt_001`. Freeze/reverify
source and input identities. Use two one-thread CPU scenes after current
hardware inspection and prior scaling evidence; require 32-GiB available RAM
and 8-GiB output allowance above 40-GiB reserve. Monitor resource and complete
loop timings. Require identical first-four public/auxiliary packets and first
900 native samples. Replay every recorded decision from raw sensors using a
fresh assigned model and apply every original audit. Preserve all failures.
No retry/resume or widened gate.

Lower average retrospective prediction error is not closed-loop success. Count
only the complete original arrival gate. Independent maze exploration/return,
matched reactive/nonpredictive and JEPA/RGB/planning/memory comparisons, realistic
timing and hardware validation remain outstanding.
