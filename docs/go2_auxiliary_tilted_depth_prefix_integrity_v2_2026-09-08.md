# Auxiliary depth prefix acquisition-order integrity successor V2

Preserve the full scientific, sensor, physics, renderer, audit and resource scope
of `docs/go2_auxiliary_tilted_depth_prefix_v1_2026-09-08.md`, plus the validated
segmentation background handling in integrity V1. Use the distinct fresh
`go2_auxiliary_tilted_depth_prefix_integrity_v2_attempt_001` root; neither failed
predecessor may be restarted, resumed or edited.

The only collection change is requesting `capture_current()` before checking
the current primary frame and capturing its auxiliary pair. Require returned
index equal to tick, exactly 750+50*tick physical samples and tick+1 primary
manifest rows. Test the lazy primary acquisition for all 20 planned frames,
and reject a wrong primary index or physical clock before auxiliary capture.
Preserve commands 0–18 exactly and stop at observation 19 before translation.

Reverify integrity V1's failure and full frozen launch closure, which also
contains the original failed source and failure report. Freeze the new loop,
acquisition helper, tests and acquisition-order failure report before execution.
Keep the same one-CPU/one-thread, 32-GiB available RAM and 1-GiB output allowance
above the 40-GiB reserve. Preserve every outcome and further failure. This is
fixed-prefix sensor characterization, with no model inference or navigation
controller and no hardware, timing or independent-maze qualification.
