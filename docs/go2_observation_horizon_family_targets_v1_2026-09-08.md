# Observation-horizon family target derivation V1

Authenticate the complete original family input admission and the complete
moving-prefix/suffix input admission, all raw collection artifacts and the
completed observation-cadence native probe readout. Derive new 100–800-ms
targets from the 240 already recorded episodes without new collection.

Retain the exact 912 original context slots, 408 training and 420 transfer
available contexts, 84 unavailable contexts, roles, cluster assignments,
sample identities and all three 1,200-update mixed-context schedules. Check
each available context's actual source command prefix against the native
requested-command trace. Preserve the original context availability and
missing-context reason. Derive no post-contact input context.

Each of eight output slots represents one actual 100-ms command interval.
Check exact 2-ms native clocks and the complete actual camera prefix. Contact
is absorbing; missing labels remain unknown and motion/future observations
are censored at contact. Future indices refer only to actual same-episode
frames. Do not open RGB images, materialize tensors, fit a model or promote a
scope. The shared 500-ms motion/contact target must exactly equal the old
target, with matching future-image identity wherever retained.

Write the exclusive `go2_observation_horizon_family_targets_v1_attempt_001`
root, a complete new windows manifest, unchanged training schedules and role/
source denominators. This does not edit either old target population. Verify
the new source closure and all consumed input bindings before and after the
derivation. Use one process with one trace resident at a time for this small
numeric/metadata pass; no rendering or model workload benefits from GPU use.
Check hardware and require 8 GiB available RAM and 256 MiB above the unchanged
40-GiB artifact reserve. Preserve any terminal failure without retry.
