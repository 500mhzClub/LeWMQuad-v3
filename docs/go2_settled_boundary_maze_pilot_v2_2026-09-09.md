# Prospective settling pilot admitted by the corrected target-reset comparison

This is the same prepared SettledBoundaryRoundTripController native experiment
as go2_settled_boundary_maze_pilot_v1_2026-09-09.md. That native launcher was
never executed: its predecessor replay failed at1866. No native attempt is
restarted. Use the exclusive root go2_settled_boundary_maze_pilot_v2_attempt_001
only after the fresh full V2 controller prefix has completed and its exact
result SHA-256 has passed --preflight-only admission.

The sole preparation revision is admitting the separately named V2 replay and
using its narrowly checked target-reset comparison during native verification.
Require the explicit final target-reset difference, unchanged controller and
preserved failed attempt. The first mission behavior change remains1866; all
requested commands through that observation, including its hold, remain exact.
The native prefix comparator checks the preceding full candidate decision for
the target-reset effect, in addition to original full physics/public-input and
complete prospective-decision equality. No post-transition command is inferred.

Reuse the exact original prepared collection and raw-audit functions. The
worker body is unchanged. Same maze0, model, numeric settings, camera setup,
one CPU physics scene, gains, friction, seed, navigation budget, sensing,
mapping, contact policy and visibility/speed thresholds. Packed-owned mapping
and the failed subpixel feature candidate are excluded. Bind completed ninth
native result3745c0b7c45265a2fcc38caf732b6f3f4b228487f344d23502dc7395077d5755
and readout7635c951522895fc03059a491be4f1539a11f85205e406780bb529ef43b70e5c.
The earlier primary visibility failure909 stays visible and unchanged.

One native scene and one numerical thread; no parallel native scenes. Inspect
CPU topology/affinity, CPU/GPU/VRAM, RAM, competition and storage. Require32GiB
available RAM and10GiB collection plus1GiB persistence above40GiB reserve.
These are capacity admissions, not OS limits. An independent CPU equivalence
replay may continue if resource checks pass. Revalidate all source/input/model
bindings and perform the full raw audit and physical/public prefix comparison
before calling this pilot complete. No automatic retry. Preserve every failure.

Any arrival and return need their own unchanged native2ms-sample qualification;
observed interval-average settling alone is insufficient. Physics remains
paused during computation. This reused development layout does not establish
generalization, matched baseline/ablation benefit, real-time operation, hardware
validity or full-goal achievement.
