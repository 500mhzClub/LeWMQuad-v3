# Wait for the exact learned episode before full-history timing

Observe the current learned native parent, PID 2916106, creation time
1789162140.42, complete command and boot from its fixed launch
`93d0af54af1d07eb1981338e17313f5e08bca00f257a57c3a4bee055ef44aacb`.
Recheck its launch binding while it is live. Do not read a growing decision
stream or a result before the exact parent ends. A transient observation error
causes another observation of the same owner, not restart or inferred completion.

Once the parent ends, require the actual complete raw-audited result. Preserve
an execution/audit failure without dispatch. A complete negative navigation
outcome remains admissible. Authenticate the complete original raw population,
the completed short combined-controller prefix and the ended reactive CPU prefix.
Wait for 64 GiB available RAM and 43 GiB artifact space before dispatching one
full paired replay; reobserve temporary resource shortages every 30 seconds.

The waiting process creates no model, scene or CPU replay. Its source preflight
uses the ordinary 32 GiB available-RAM/55 GiB artifact reserve; full replay
resources are separately required at dispatch. One full CPU replay may overlap
one separately owned native scene. The child is named `replay_go2_...` and is
not classified as another native runner by the existing scene guard.

After the child exits, verify its exact source and artifact roster, actual input
admission, every saved comparison row, all state checks and the entire original
public packet population. Reauthenticate the original raw artifacts without
rerunning the learned controller. Record complete timing distributions and
observed reduction, even when the optimization is ineffective or slower. No
native simulation, navigation advantage, realtime qualification or goal
completion is inferred from this CPU experiment.

Output: `go2_measured_plane_full_history_timing_wait_v1_attempt_001`. Preserve all
failures; no automatic retry, resume or source changes after registration.
