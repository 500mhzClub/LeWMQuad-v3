# Fixed family-JEPA checkpoint and corner observer: fresh native goal probe V1

After all six fixed transition fits complete, admit only full-JEPA seed2026091001
as preregistered before training. Bind the exact complete study-result SHA-256
at launch, verify every fit artifact, reconstruct all7200 ledger steps and all
saved role scores, and reload the selected final snapshot evaluation-only with
its exact experiment/data/schedule/configuration bindings. No checkpoint or
hyperparameter choice based on fit or native outcomes.

Require the passing complete corner-observer replay at result
`97517b61fc33d2cdc484d96f0f47aeef0e730defb866444b08cc0ac41cb97375`
and retain its bound sources. The failed spatial-SIFT candidate and original
goal controller remain frozen. New model and observer are an integration
package; this two-case probe does not isolate a JEPA or observer contribution.

Run fresh family_episode_052 then family_episode_039 physical instances with
their exact original geometry/appearance/native sensor/gait/stop configuration.
These are known training-layout integration panels, not independent mazes.
The original action assignments never enter the new controller. The inherited
goal algorithm remains exactly1.2m in the first observed body frame, three quiet
history ticks, six complete four-second candidate plans, distance-reduction
minus1.2m times uncalibrated contact score, hold-first ties, and five committed
100ms commands before replanning from four actual packets. Current corner
joint-RGB-D pose is required before each nonterminal decision. No map/native
state, future image, label, command-integration fallback or threshold changes.

Keep240 navigation ticks, the.04m observed goal threshold, ten quiet observed
intervals, and ten terminal zero-command drain ticks. Preserve every sensor,
model, physical, contact and acquisition failure. Scientific controller failure
does not skip the second fixed case; an infrastructure/raw audit failure does.
Verify raw sensors, exact float64 requests/float32 applied slew, model forecasts,
observer/command replay, native stops and sampled physical visibility. The
unchanged evaluator requires the observed goal candidate, complete drain, no
contact/stops and501 final native samples within.06m at speed at most.05m/s,
with500 zero requests. Any hard measurement failure invalidates a success claim.

Assess hardware/competing jobs/storage before launch. Use serial fresh native
workers for complete-loop timing, one CPU/OpenCV/Torch/BLAS thread,32GiB
available RAM and4GiB output plus40GiB free storage. Record resources and
acquisition/controller/complete command iteration timing. Physics pauses during
computation; no real-time or hardware qualification follows from this probe.

Freeze all new source, checkpoint and observer identities before native work.
Write only to exclusive `go2_family_transition_goal_probe_v1_attempt_001`;
no retry, resume, model replacement or parameter changes within the attempt.
Actual independent-maze goal-reaching, matched reactive/non-predictive controls,
exploration, memory/backtracking and realistic sensing/timing remain required
by the active goal after this development probe.
