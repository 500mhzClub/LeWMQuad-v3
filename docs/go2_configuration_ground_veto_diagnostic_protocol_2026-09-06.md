# Read-only ground-veto attribution

Before execution, fix the diagnostic to the saved startup/tail trial and its
existing three result identities. Reconstruct the continuous owner, reproduce
all 12 startup decisions and 15 relative observations, and retain every original
configuration-query negative. No simulator, training, command or old-result write.

At the final 2.9-s state query the measured posture translated 0.75 and 1.00 m
along the existing sensor-gravity tangent, through 3.3 s. Use the unchanged
3.5-s setup expiry, 4-cm padding, zero supplied local configuration error and all
existing pose/plane/range allowances. These are static supplied configurations,
not execution predictions, traversals or swept-volume checks.

For all 27 physical primitives and every retained observation plus the latest
frame, independently reconstruct the observation-frame AABB, relative transport
allowance, plane gap interval, coverage and original floor-exemption predicate.
Compare whole-box depth evidence both without and with the independently observed
plane-family mask. Require compiled/reference equality for both branches and
the complete original query. Require exact original per-shape obstacle and
penetration timestamp lists and unchanged original output after diagnosis.

The unconditional plane-mask branch is an attribution intervention ONLY. If it
removes a veto, report that result together with unresolved physical floor
intersection and missing coverage; do not treat it as support, actual collision,
free-space or action permission. Wall conflicts must survive the plane branch in
synthetic counterexamples. Same-frame pose cancellation remains intact; no old
pose allowances are shrunk. Bind all inherited source/input/artifact/native
identities and the 14 explicit diagnostic/development paths before and after.
