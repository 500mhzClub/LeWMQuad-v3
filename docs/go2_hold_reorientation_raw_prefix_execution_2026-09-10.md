# Raw hold-reorientation replay prepared and automatically queued

## Completed and independently admitted

The original child and waiter have exited successfully. Raw result:
`f7e054e78879fd7827761cf36bb3c698c22fe394cd084ed17f165a0f643611e6`;
waiter result:
`62980125c12a4455f355f6c311ccf56d74d4f94bbc6ee0507b7908d53d6a135d`.
Independent session 78707 passed the complete native-prefix admission helper,
including all 406 saved comparisons and raw public packet fingerprints.
Session 83197 verified the waiter completion chain and all its bindings.
The result and verification are recorded in
`docs/go2_hold_reorientation_raw_prefix_result_2026-09-10.md` and
`docs/go2_hold_reorientation_raw_prefix_verification_2026-09-10.json`.
The hold-native waiter reports its raw-prefix owner finished and continues
waiting for the scheduled frontier owner. The following pending-state sections
are historical snapshots, not the current completion state.

## Raw prefix written and independently checked; final admission pending

The original child PID 2672447 has completed its 406-observation comparison
loop and is running final full original input verification. Launch:
`f3cdd9b0d97cb56328adc6f60edac2f5a62161a81bc91bf882117f7d1d5207f2`.
Original worker terminal:
`617056f19ba4928aa9ff7738616947e6e63a387cc6046353e30617ce50afa57e`.
No final replay result or failure existed at the 11:57:12 UTC check.

Independent session 84671 passed. It reverified all 1,943 source bindings,
the exact launch and original worker terminal, and the original bound decision
stream, command tape and auxiliary acquisition metadata. It then streamed all
406 original/candidate comparisons, reconstructing the complete normalization,
matching both selections to the frozen saved-prefix expectations, and
recomputing every original public packet fingerprint. All command timing
endpoints and saved comparison receipts matched. There were 403 raw forecast
comparisons and exactly one changed request: observation 405, from hold
`[0, 0, 0]` to left turn `[0, 0, 0.45]`.

The candidate compressed stream was unchanged across the check, SHA-256:
`98147f2c0604ec6a8379e032a3381d679e79e9d937b764073b52cd4802aac1ea`.
The independent check did not rerun neural inference or independently rebuild
hidden retained contact state; the original raw runner performs those checks.
Final completion still requires its successful terminal result and final
input verification. The existing hold-native waiter owns the later physical
trial after the frontier experiment. This raw change is not an executed
candidate command, observed escape, goal arrival or verified round trip.

The chronology below records the earlier registration state. The completed
episode count subsequently reached 38, with zero verified round trips.

Snapshot 2026-09-10 10:19 UTC. The preceding goal turn completed the saved-prefix
diagnosis and intervention check. This turn implemented and tested the full raw
controller replay and registered an automatic CPU handoff. Navigation remains
unverified: 37 completed audited development episodes, zero verified round trips.

The original first adapter JEPA worker, PID 2662101 (created 1789031235.91), is
still running its audit/finalization after collecting 1,529 observations and
1,528 completed commands. Its collection stopped with no feasible candidate;
the worker audit and terminal have not yet completed. Parent PID 2659758 remains
live. A quiet audit is not a reason to restart it.

The new waiter is PID **2669739**, created **1789035532.54**, tool session
**25488**. Command:
`.generated/venvs/genesis_rocm_0_4_6_v1/bin/python scripts/await_go2_hold_reorientation_raw_prefix_v1.py`.
It uses deterministic hash seed, PYTHONDONTWRITEBYTECODE, PYTHONPATH
`.:lewm_genesis:lewm_worlds`, and one OMP/MKL/OpenBLAS thread. Output:
`go2_hold_reorientation_raw_prefix_wait_v1_attempt_001` under the external
navigation artifact root. Launch SHA-256:
`969c9153d9bebbf7cc7ba24e56ed76a912edf33f0a13aeaacc243cdcd2862e2c`.
Its 1,946 bound sources are frozen. The current event is
`WAITING_FOR_ORIGINAL_ADAPTER_JEPA_WORKER`.

After the exact original worker ends with a valid completed case audit, the
waiter pins its terminal hash and launches one child:
`scripts/replay_go2_hold_reorientation_maze02_prefix_v1.py
--original-worker-terminal-sha256 <that exact completed terminal hash>`.
The child independently verifies the complete original worker artifacts and
reexecutes full original input admission before and after replay. It does not
require the other five native cases to finish. Do not launch a second replay
manually: the waiter owns the sole transition. Missing terminal evidence,
changed process identity, failed admission or child failure is retained without
an automatic retry. The wait is bounded at 48 hours and polls every 30 seconds.

The raw replay will instantiate both complete controllers using fresh identical
expanded JEPA models and reconstruct observations 0–405. It must reproduce every
full original decision and the exact saved candidate selections, preserve raw
forecasts/scores/vetoes, observed map/contact memory and executed residual state,
and finish with the declared first hold-to-left-turn change at 405. It does not
consume packet 406 or execute a candidate command. At this snapshot,
`go2_hold_reorientation_maze02_prefix_v1_attempt_001` does not yet exist: raw
replay completion is not claimed.

Validation completed:

- Full decision comparison: session 56335, 15 tests passed in 1.80 seconds.
- Synthetic replay wiring, command/input/state integrity and stopping at the
  first changed command: session 65780, 12 tests passed in 2.18 seconds.
- Exact-owner wait/termination behavior: session 16709, five tests passed in
  2.09 seconds.
- Final source-only preflight: session 44082, 1,943 source bindings passed and no
  replay output created.
- The new waiter's 1,946 sources, existing batch's 1,908 and frontier waiter's
  1,929 were independently reverified unchanged after registration.

The existing frontier waiter PID 2663938 retains the next native experiment
after all six adapter cases. This new handoff launches only a CPU replay and
does not alter that priority or any frozen experiment. Approximately 606 GiB
remain free on the artifact volume. No independent comparison layout was
rendered or navigated by this work.

Next: inspect this exact waiter/child and original worker completion. If the
raw replay passes, independently verify its complete saved comparisons and
public packet identities, then prepare a separately frozen prospective native
hold-reorientation experiment after the frontier experiment. Retain the original
failure and all later outcomes; neither a prefix pass nor a turn proves escape,
goal-reaching, backtracking, model advantage or deployment readiness.
