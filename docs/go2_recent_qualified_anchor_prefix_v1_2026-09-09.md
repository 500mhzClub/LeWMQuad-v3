# Recent qualified reference: prospective maze1 replay

Replay from observation zero of the completed height-native maze1 result
32edbb748e04e18816e0b0fb265f465706ac8c8684849d73a091321f79a3b07f.
Authenticate all original source, artifact, assigned-model and environment
bindings plus the complete original raw audit and physical-prefix result.
The recorded motivation is in
docs/go2_partial_floor_height_maze01_anchor_loss_readout_2026-09-09.md.

The new observer retains one immediately preceding view only after an accepted
INITIAL_REFERENCE or ANCHOR_MEASUREMENT. It preserves the original bank of up
to eight references during search and tries the additional view only after all
those references fail without a qualified conflict. The extra view must be the
identical previous Reference object, exactly one frame and100ms earlier, and
absent from the original bank. It uses the original per-camera candidate fit,
rigid/support/envelope gates, direct-flow eligibility and witness accounting.
The normal anchor/increment comparison and camera priority remain in effect.
Accepted use triggers the original qualified-recent-reference promotion path.
No view is retained from a bridge, registered floor pose, native pose or command
integration. Any bridge or observer failure clears the extra eligibility.

The original continuity method directly calls its parent selector, bypassing
ordinary subclass dispatch. A separate explicit copy changes only that call to
instance dispatch; a syntax-tree comparison verifies all other calculations.
The class ordering leaves the original joint witness wrapper around that
method. No imported module, original class, frozen source, bridge limit,
overlap threshold or rigid threshold is modified.

This changes reference history and may change subsequent pose, map, residual,
mission and command decisions. Frequent chains of qualified relative fits
remain uncalibrated; no absolute error bound or physical clearance is claimed.
The prefix comparison verifies complete original decisions before the first
extra-reference attempt after removing only explicit retention annotations and
the new controller label/flag. After the first attempt, record actual downstream
differences instead of asserting unchanged visual or mission state. Every extra
pair must have a causal preceding anchor-qualified reference and preserved
original rejection receipts. Compare raw6x8x5 model banks whenever both original
and candidate emit them; reject differences. Validate current raw and registered
poses through the existing public-sensor accessors. Model weights, public input
arrays, model assignment and all original preintervention requests remain exact.

Stop at the first changed requested command, the original terminal, or the
candidate terminal, whichever occurs first. Consume no following recorded
observation. The maximum is646 observations, through original failed observation
645, from the complete656-observation/655-command source. A new failure is a
scientific result; an input/comparator failure retains the failed row and a
terminal failure artifact. Do not retry or overwrite the exclusive attempt.

Use the same full/JEPA seed_2026091001_full_jepa model with state SHA
4f796afdf32c2c6dbcd1d5981834a7b17be86e2efdafd9427b2d80240def0ca6,
the original height registration, mapping, residual accounting, planner,
mission, public maze1 definition and shared3000-tick budget. No anchored-path
feasibility correction, new training or performance extension is combined here.

scripts/replay_go2_recent_qualified_anchor_prefix_v1.py admits the completed
scoped-verification benchmark137867773bfe6c6eb05a125a288012ff6017aa3134f4687d7bccdc7f99c02071
and runs the original height-native verifier context, including that native
launch's fixed benchmark identity. Every per-call cached path receives a fresh
final hash; no cache survives and imported globals remain unchanged.

Before large work assess CPU/affinity/load, RAM, GPU/VRAM, storage and competing
jobs. One CPU replay may overlap one native scene with8GiB replay plus32GiB
native RAM allowances and the40GiB artifact reserve plus2GiB output allowance.
Cap the compressed decision stream at1GiB. These are admission/output gates,
not OS-enforced memory limits. Use deterministic Torch and single OpenCV/BLAS
threads, fresh model/controller from frame zero, no native scene or tensor
training. Source/input preflight creates no output or replay model.

Run with `--native-result-sha256 32edbb748e04e18816e0b0fb265f465706ac8c8684849d73a091321f79a3b07f`;
add `--preflight-only` for read-only admission. The exclusive artifact directory
is go2_recent_qualified_anchor_prefix_v1_attempt_001. Preserve sealed custody,
all historical failures and immutable hard-linked training artifacts.
Positive replay evidence only identifies a prospective intervention for a
separate fresh native experiment; it establishes no navigation success.
