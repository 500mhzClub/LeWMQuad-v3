# Fixed-population diagnosis of the eleventh floor-extent rejection

Use the closed eleventh collection, case result
dc5d54e1372b67ae88c783cfb06034777c3e50eccdba40f2aeb1dc5bb3f4fdc5
and launchafb8485c377a12989004cdc7827476948d5308b3b89387a7327b385c2739cba6.
Its full native audit is separate and may still be running. Bind every selected
input before/after; later require these bindings to match the final native
artifact map before using this diagnosis for a future navigation experiment.

Inspect exactly frames0,1868,1872 and1894 through1904: the initial floor
reference, arrival, first auxiliary intervention, ten observations before the
first floor rejection and that rejection. Check the complete preceding
decision stream to confirm first failure1904. Consume no later recorded
observations to infer outcomes under different commands.

Validate each current saved visual pose through the original paired-camera
accessor, reconstruct every original measured floor candidate from current
public depth and the initial public force reference, and call the unchanged
joint fit/composition/admission functions. Earlier admitted fits must match
the stored receipts exactly; the denied frame must reproduce the original
extent reason with no differing composed moment fields. Save all candidate
points/pixels and original fit statistics; no trimming, threshold change or
new pose/plane admission.

Report the second covariance eigenvalue and its fixed0.05**2m² requirement,
the corresponding spatial standard deviation, and diagnostic residuals to the
algebraic plane even when it is unadmitted. Only after fitting, use native
physical poses for candidate world-height and normal-angle diagnostics and
auxiliary segmentation for robot-candidate counts. These evaluator fields
must not participate in candidate selection, fitting or controller admission.

Run scripts/diagnose_go2_dual_camera_floor_extent_v1.py --preflight-only first.
One CPU/numerical/OpenCV thread alongside the existing native audit, no model
inference, GPU context or new scene. Inspect actual CPU/GPU, RAM/storage and
competition; require8GiB available RAM and32MiB output above40GiB reserve.
Exclusive go2_dual_camera_floor_extent_diagnosis_v1_attempt_001, with final
source/input/model-input identities checked. Preserve all failures. This
diagnosis grants no navigation, uncertainty-bound or gate-relaxation claim.
