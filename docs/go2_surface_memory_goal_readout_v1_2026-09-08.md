# Paired surface-memory goal readout V1

Read the complete four-case native result only after exact SHA-256 admission;
the launch is fixed at
`055f7eafac1e275bc579d7e98ff830da2ada750b20e9b639b3385177d790ecb9`.
Create `go2_surface_memory_goal_readout_v1_attempt_001` exclusively, binding all
new readout sources and original artifacts before and after execution.

Report all actual goal distances/successes, contacts, controller stops, model
selections, changes from unfiltered choices, surface-veto counts, measurement
failures, accepted-pose errors and acquisition/controller/whole-loop timing.
For each fixed layout find the first differing requested command and first
differing selected action or terminal state (which can precede command divergence
when no-action and hold both initially request zero). Require exact
equality of complete public packets and observer evidence through the first
selection/terminal divergence,
and of its model predictions and original utilities. Record the shape conflicts
and actions for both variants. Do not compare later observations as if actions
had remained matched. No shared-prefix difference is not proof of equal full
outcomes when trace lengths differ; preserve each terminal record.

No native execution, training, new parameter choice, outcome relabeling or
independent-maze/statistical benefit claim is made. A stop or fewer contacts alone
does not satisfy the original goal-reaching objective. This is a paired diagnostic
readout of the fixed experiment, not an action-selection or retry stage.
