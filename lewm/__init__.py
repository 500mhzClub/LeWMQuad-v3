"""LeWMQuad-v3 world-model package.

``lewm.models`` holds the paper-faithful base LeWM (ViT-Tiny encoder, AdaLN-zero
transformer predictor, SIGReg). H-JEPA layers and training/eval entrypoints are
added alongside as they are built.

``lewm.actions`` is the source of truth for active-block command encoding.
"""
